# Simple ABC constraints — trades QoR for reliability.
# Used by synthesis retry when default ABC hangs.
# Minimal driving cell + load, no aggressive optimization.
set_driving_cell BUF_X1
set_load 10.0
