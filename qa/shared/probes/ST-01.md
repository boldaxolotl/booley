# ST-01 disposable probes

`stealth-explicit`: inspect setup config → explicit selected value. PicoRV32 `stealth-history` → sanitized commit prose. UART `nonstealth-history` → verbatim benign commit prose and ordinary cores. UART `stealth-legacy`: omitted-key disposable fixture → documented legacy sanitization fallback, never non-stealth credit.

Evidence and recovery: Config/history and Project state paths; no private identifiers.
