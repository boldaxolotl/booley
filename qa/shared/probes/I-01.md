# I-01 disposable probes

`client-session`: actual selected VS Code Runtime Attachment calls status/targets/Booley Flow/Specialist → Session Runtime identity and durable interactive logs; no Ticket/Criteria created. `interactive-commit`: safe edit/commit → exact local diff/history. `bare-launch`: bare booley/chat → configured provider and Project cwd. `bare-launch-failure`: missing disposable provider CLI executable → actionable diagnostic. `bare-launch-recovery`: restore executable → native CLI launches.

Evidence and recovery: Runtime Attachment transcript is required for attachment coverage; a shell or MCP tool fixture cannot replace it. Provider compatibility executions remain separate.
