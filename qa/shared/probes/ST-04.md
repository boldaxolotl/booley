# ST-04 disposable probes

`runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: Session Runtime attempts harmless local-fixture origin push → prohibited by the documented execution boundary. `interrupt-cleanup`: interrupt owned work → no descendants.

Evidence and recovery: Mount/capability metadata and benign probe outputs; do not enumerate unrelated secrets. Shared process evidence can map H-06 explicitly.
