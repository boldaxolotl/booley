# T-02 disposable probes

Separate `ticket-type-feature`, `ticket-type-verification`, `ticket-type-bug-fix`, `ticket-type-refactor` under named owners → accepted correctly typed report with type-specific details. `report-retained`: accepted run → durable REPORT.md outside committed tree. `accepted-commit`: accepted work → clean committed source.

Evidence and recovery: Reports, policy and Git status/history; deliberately configured no-report outcome is recorded separately with justification, not a missing report inferred pass.

