interface:
  display_name: "Add to Booley QA"
  short_description: "Add a behavior or trap to a QA mission"
  default_prompt: "Use $booley-add-to-qa to add this behavior to the QA missions."

policy:
  allow_implicit_invocation: false
