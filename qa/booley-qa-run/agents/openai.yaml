interface:
  display_name: "Booley QA run"
  short_description: "Timeboxed bug hunt on a pinned IP"
  default_prompt: "Use $booley-qa-run to run the named QA mission against this Booley build."

policy:
  allow_implicit_invocation: false
