interface:
  display_name: "Booley Scenario Run"
  short_description: "Execute an evidence-producing public QA run"
  default_prompt: "Use $booley-qa-run to execute a named public QA Configured Scenario."

policy:
  allow_implicit_invocation: false
