format_version: 1
scenario_id: coverage-lifecycle
title: 'Native coverage: lifecycle'
source: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
check_sets:
- id: coverage-lifecycle-core
  checks:
  - setup.artifact
  - setup.ready
  - setup.catalog
  - oracle.positive
  - oracle.negative-hit-count
  - oracle.negative-source-grouping
  - oracle.negative-digest
  - oracle.negative-rational-verdict
  - baseline.native
  - baseline.policy
  - baseline.analysis
  - baseline.waiver
  - baseline.retention
  - publish.manifest-failure
  - publish.manifest-failure.restore
  - publish.simulation-failure
  - publish.simulation-failure.restore
  - publish.acceptance-failure
  - publish.acceptance-failure.restore
  - publish.progress-failure
  - publish.progress-failure.restore
  - publish.shared-abort
  - publish.shared-abort.restore
  - publish.interrupt
  - publish.interrupt.restore
  - publish.restart
  - publish.immutable
  - ticket.author
  - ticket.gap
  - ticket.advice
  - ticket.no-implicit
  - ticket.improve
  - ticket.evidence
  - ticket.stale
  - ticket.stale.restore
  - ticket.regain
  - ticket.advice-no-approval
  - ticket.deliver
  - retain.native
  - retain.native-preserves
  - retain.native-analysis
  - retain.native-retry
  - retain.full
  - retain.full-analysis
  - retain.number-reserved
  - retain.reject-native-ambiguous-target
  - retain.reject-native-ambiguous-target.restore
  - retain.reject-native-unknown-target
  - retain.reject-native-unknown-target.restore
  - retain.reject-native-invalid-number
  - retain.reject-native-invalid-number.restore
  - retain.reject-full-invalid-number
  - retain.reject-full-invalid-number.restore
  - retain.reject-native-unsafe-path
  - retain.reject-native-unsafe-path.restore
  - retain.reject-full-unsafe-path
  - retain.reject-full-unsafe-path.restore
  - retain.reject-native-symlink
  - retain.reject-native-symlink.restore
  - retain.reject-full-symlink
  - retain.reject-full-symlink.restore
  - retain.reject-native-changed-native
  - retain.reject-native-changed-native.restore
  - retain.reject-full-changed-native
  - retain.reject-full-changed-native.restore
  - retain.reject-native-unknown-payload
  - retain.reject-native-unknown-payload.restore
  - retain.reject-full-unknown-payload
  - retain.reject-full-unknown-payload.restore
  - retain.reject-native-unexplained-missing-native
  - retain.reject-native-unexplained-missing-native.restore
  - retain.reject-full-unexplained-missing-native
  - retain.reject-full-unexplained-missing-native.restore
  - retain.reject-native-tampered-points
  - retain.reject-native-tampered-points.restore
  - retain.reject-full-tampered-points
  - retain.reject-full-tampered-points.restore
  - retain.native-interrupted
  - retain.native-interrupted.restore
  - retain.full-interrupted
  - retain.full-interrupted.restore
  - retain.active
  - retain.active.restore
  - retain.interrupted-producer
  - final.regression
  - final.analysis
  - final.archive
  - cleanup.processes
  - cleanup.runtime
  - cleanup.faults
  - cleanup.git
  - cleanup.storage
  - cleanup.records
configured_scenarios:
- id: coverage-lifecycle-ubuntu-codex-cli
  required: true
  parameters:
    host_os: Ubuntu 24.04
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Codex
    interactive_mode_client: native CLI
    ticket_mode_backend: Codex
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-lifecycle-core
  exclusions: []
- id: coverage-lifecycle-ubuntu-claude-cli
  required: true
  parameters:
    host_os: Ubuntu 24.04
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Claude
    interactive_mode_client: native CLI
    ticket_mode_backend: Claude
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-lifecycle-core
  exclusions: []
- id: coverage-lifecycle-windows-codex-cli
  required: true
  parameters:
    host_os: Windows with Docker Desktop WSL2
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Codex
    interactive_mode_client: native CLI
    ticket_mode_backend: Codex
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-lifecycle-core
  exclusions: []
- id: coverage-lifecycle-windows-claude-cli
  required: true
  parameters:
    host_os: Windows with Docker Desktop WSL2
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Claude
    interactive_mode_client: native CLI
    ticket_mode_backend: Claude
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-lifecycle-core
  exclusions: []
inputs:
- id: release
  kind: pre-run
  value: Exact immutable booley-rtl package SHA-256, full source commit, matching docs, runtime image digest and
    selected provider/model identity.
  source: https://github.com/boldaxolotl/booley/blob/main/README.md
  verification: Record identities before setup; pre-run selection cannot alter fixture bytes, suites or thresholds.
- id: verilator
  kind: git
  value: ea338be98e1e838d3518809ce8899f85a009963c
  source: https://github.com/verilator/verilator/tree/ea338be98e1e838d3518809ce8899f85a009963c
  verification: Record exact tool source identity and v5.052; 5.050 rejected.
authority:
- Maintainer approved fixture/policy/waiver inputs only; act inside ledgered run-owned Project/runtime and fault
  roots.
- No product/SDK patch or private evaluator import. Simulated external inputs are labeled boundary evidence; live-provider
  Checks require real selected client.
- Preserve pins, thresholds, exact suites and every original failure. No push, issue submission, remote publication
  or permanent repair.
- Restoration never depends on successful detection. Enter unconditional cleanup by minute 450; preserve borrowed
  resources and secrets.
budget:
  deadline_minutes: 480
  cleanup_minutes: 30
  contingency_minutes: 134
  cleanup_start_minutes: 450
  final_regression_start_minutes: 426
phases:
- id: prepare
  title: Artifact, fixture and independent oracle preparation
  minutes: 52
- id: baseline
  title: Fresh named local baselines
  minutes: 60
- id: exercise
  title: Expanded checks and independent restorations
  minutes: 180
- id: final
  title: Fresh regression, Analyst and evidence archive
  minutes: 24
- id: cleanup
  title: Unconditional resource cleanup
  minutes: 30
steps:
- id: setup.artifact
  phase: prepare
  action: Verify exact immutable installed package/source/docs/image identities and pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c.
    Record actual selected provider/SDK/executable identities; no source-checkout imports.
  checks:
  - id: setup.artifact
    capabilities:
    - PRODUCT-ARTIFACT-INSTALLATION
    - EDA-VERILATOR-SIMULATION
    stimulus: Verify exact immutable installed package/source/docs/image identities and pinned Verilator v5.052
      ea338be98e1e838d3518809ce8899f85a009963c. Record actual selected provider/SDK/executable identities; no source-checkout
      imports.
    expected: Verify exact immutable installed package/source/docs/image identities and pinned Verilator v5.052
      ea338be98e1e838d3518809ce8899f85a009963c. Record actual selected provider/SDK/executable identities; no source-checkout
      imports.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Verify exact immutable installed package/source/docs/image
      identities and pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c. Record actual selected provider/SDK/executable
      identities; no source-checkout imports.'
    capture: evidence/coverage-lifecycle.setup.artifact
  timeout_minutes: 10
- id: setup.ready
  phase: prepare
  action: 'Follow shared coverage/RUNBOOK.md setup: fresh run-owned Project/runtime, copy exact hashed fixture,
    initialize via product, install tests into resolved project-data root, compile scoped fault library and run
    evaluator/fault controls. Freeze owned-root/resource ledger. Use the selected native CLI client and configured
    Specialist model. No artifacts from other Scenario Runs.'
  checks:
  - id: setup.ready
    capabilities:
    - PROJECT-INITIALIZATION
    - SESSION-RUNTIME-LIFECYCLE
    stimulus: 'Follow shared coverage/RUNBOOK.md setup: fresh run-owned Project/runtime, copy exact hashed fixture,
      initialize via product, install tests into resolved project-data root, compile scoped fault library and run
      evaluator/fault controls. Freeze owned-root/resource ledger. Use the selected native CLI client and configured
      Specialist model. No artifacts from other Scenario Runs.'
    expected: 'Follow shared coverage/RUNBOOK.md setup: fresh run-owned Project/runtime, copy exact hashed fixture,
      initialize via product, install tests into resolved project-data root, compile scoped fault library and run
      evaluator/fault controls. Freeze owned-root/resource ledger. Use the selected native CLI client and configured
      Specialist model. No artifacts from other Scenario Runs.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Follow shared coverage/RUNBOOK.md setup: fresh run-owned
      Project/runtime, copy exact hashed fixture, initialize via product, install tests into resolved project-data
      root, compile scoped fault library and run evaluator/fault controls. Freeze owned-root/resource ledger. Use
      the selected native CLI client and configured Specialist model. No artifacts from other Scenario Runs.'
    capture: evidence/coverage-lifecycle.setup.ready
  timeout_minutes: 30
  requires:
  - setup.artifact
  assets:
  - path: coverage/baselines.json
    base: shared
    audience: scenario-operator
    sha256: 8e74bce480e395d5390009106fbf697867faeeb9a2f7118179b66a98635f5c20
  - path: coverage/policies.json
    base: shared
    audience: scenario-operator
    sha256: 82736ce9dc0244a7b833b81bd331699dafc6f11b426703bb7bfcf2da4c319d58
  - path: coverage/expected.json
    base: shared
    audience: scenario-operator
    sha256: f0f5edcf078329bf468fc5418609a644c20952264c6845308ae33725563e7c41
  - path: coverage/RUNBOOK.md
    base: shared
    audience: scenario-operator
    sha256: 216e061687934ee83fcc07166e3ed61b1bebd76ba8fa719792e657b10ec0a84e
  - path: coverage/approval-inputs.json
    base: shared
    audience: scenario-operator
    sha256: 2ed73b45e21afe0104f11bfa533e0f040ac20a55c34bae5ca533145c910306c0
  - path: coverage/project/coverage.core
    base: shared
    audience: scenario-operator
    sha256: 7400301012dc1e66b977488348c94c61c057c0a0ebbdbbce96c5f3dd188366fc
  - path: coverage/evaluator/controls.py
    base: shared
    audience: independent evaluator
    sha256: 8136daced7ac3abe620472d010346fe63b3ddd9662667e377b880b4dea5d9283
  - path: coverage/evaluator/measurements.py
    base: shared
    audience: independent evaluator
    sha256: b9cc311d59850c0acf34cd0364f4a7d1d2d4b0c87492df441473ae9ef3f00dbc
  - path: coverage/proof/parity.ys
    base: shared
    audience: scenario-operator
    sha256: 9efe18a93df8f320fcea6ff24b6dbc47d54a0d6ccb2129bb8a1956b878e8769c
  - path: coverage/tickets/close-test-gap.md
    base: shared
    audience: scenario-operator
    sha256: 3063bda64ae5ca1ff1be01d6226ba2f833edc6200055266de730f5810204012a
  - path: coverage/tickets/tests.toml
    base: shared
    audience: scenario-operator
    sha256: 4fd70c5c36a4710bb1b680c553cf04f2022b49c9617cbded3c3397b67e686c10
  - path: coverage/faults/provider.py
    base: shared
    audience: scenario-operator
    sha256: a6860c48cc2114cecf3eb9a366c444ff9d71440134f15ba3fed718331d5924a0
  - path: coverage/faults/campaign.py
    base: shared
    audience: scenario-operator
    sha256: b66fd938ad572e709869aefa22f623d6bab6b3cad05d07d3101004492844c82f
  - path: coverage/faults/native_tool.py
    base: shared
    audience: scenario-operator
    sha256: 116df5e4ca2c184d27162f1dd1d4a5bb983a06c6107fdb24fb621a2438a26d75
  - path: coverage/faults/filesystem.py
    base: shared
    audience: scenario-operator
    sha256: 365567972d5d412dd1f6a8cf117742fe7d2d0791a5616b67fc989251a2b6da93
  - path: coverage/faults/boundary.c
    base: shared
    audience: scenario-operator
    sha256: d08e19ec246b05e4d2642f20cbfad5915bcfd82d9f5b938d0ef74b6f1fb56f17
  - path: coverage/faults/approvals.py
    base: shared
    audience: scenario-operator
    sha256: 22313486bcad87cf1afbe0cbd2f9b0dd843750fb901605ecd6343d316631918a
  - path: coverage/project/rtl/coverage_dut.sv
    base: shared
    audience: scenario-operator
    sha256: b9290be3023b03b31d4b5ac24ab38f1bb5f72c706ad153bc9d7d9694e2f28f46
  - path: coverage/project/rtl/properties16.sv
    base: shared
    audience: scenario-operator
    sha256: fb25c4c85212715ee3200ba74e6cb2405464e8c80183d2a80b6820f942d6a330
  - path: coverage/project/rtl/waiver_counter.sv
    base: shared
    audience: scenario-operator
    sha256: 841eb1ea3263039e35ac02459ef8be0225f4480a50f9ee092602783e58059d0d
  - path: coverage/project/rtl/properties4.sv
    base: shared
    audience: scenario-operator
    sha256: f5518ced99e8c7902c332241eb33d94f6a2e26cce79a316d55303676512a1f4b
  - path: coverage/project/rtl/constants.svh
    base: shared
    audience: scenario-operator
    sha256: f73cdd3b48a75ef8a23a22648fa1a986eab3b96422c31134562a3941629bba53
  - path: coverage/project/rtl/toggle.sv
    base: shared
    audience: scenario-operator
    sha256: 6f33e57fce5b74ae48c820392e1ed57311bf43bb92768536a6f9f1c23513e326
  - path: coverage/project/rtl/properties3.sv
    base: shared
    audience: scenario-operator
    sha256: ecd0d2ebd423991ca5b12ca9c472198f6c5c299784ca41d74c804e09d2cb485a
  - path: coverage/project/foreign/coverage_library.sv
    base: shared
    audience: scenario-operator
    sha256: cac802acb66125c04478853a0ff0d6b058d66ba1fc87a7e6d48e481d255dda5e
  - path: coverage/project/tb/cocotb_cases.py
    base: shared
    audience: scenario-operator
    sha256: 467fe73fbf8c80cf9ff0f6216fffe9f6ae10d6432e1931005993f658f0c71878
  - path: coverage/project/tb/scale_tb.sv
    base: shared
    audience: scenario-operator
    sha256: 64276850f0d90b26002c1a5321d8f1e6d3ecf9bafcab189109b2f899ddbaa5b0
  - path: coverage/project/tb/coverage_tb.sv
    base: shared
    audience: scenario-operator
    sha256: c5d68c11b8f0b25b3bfc1c29e8508e6746bc5a7d8d41715f1c537e5b407ba6da
  - path: coverage/project/tb/properties3_main.cpp
    base: shared
    audience: scenario-operator
    sha256: 0fe647329b68ce9ca38d55fe9992d32cd291e2b110a8c44a52224a94f2070c33
  - path: coverage/project/tb/waiver_tb.sv
    base: shared
    audience: scenario-operator
    sha256: d872d814256d2f161f412c585689db60e2282a50e70355bcd9a6a3c6ae58b5a9
  - path: coverage/project/tb/properties16_main.cpp
    base: shared
    audience: scenario-operator
    sha256: ccc3f8009c3286a3aca98e9d062598cabdaa38b7ff77c9f5069b79f00c2a1c4a
  - path: coverage/project/tb/properties4_main.cpp
    base: shared
    audience: scenario-operator
    sha256: b9fd74a44739cbe3e8c7b71b6b0894b56db15997976e06e82f74b4c0b56a2301
  - path: coverage/project/tb/cocotb_staged.py
    base: shared
    audience: scenario-operator
    sha256: 107bd339e165b03bec9694ba4c7f3c2c76f87f33429feba5f38be073c1118458
  - path: coverage/project/tb/staged_main.cpp
    base: shared
    audience: scenario-operator
    sha256: 4c5cd55dd55636eaa31ce31cd8e28faa041c4d0052c8df34f9d93c50ab1873fd
  - path: coverage/project/tb/coverage_main.cpp
    base: shared
    audience: scenario-operator
    sha256: 14f312bcf77304cfc021d5231262d105d78dbf678aa51432b9daca53dc73e738
  - path: coverage/project/tb/multi_tb.sv
    base: shared
    audience: scenario-operator
    sha256: ef05488f7510269c783bcaadb4cc3f47f1eb6cebb3f64dea612289dcd5c10284
  - path: coverage/project/tb/toggle_main.cpp
    base: shared
    audience: scenario-operator
    sha256: e1b88b2662c06aa663eea8a7ee9bd010a80cb9b36ce4831d38c944954c1ae8e5
  - path: coverage/project/project-data/staging.toml
    base: shared
    audience: scenario-operator
    sha256: 427ce254316a45b2cc662bde6a2754d98c30ea26b15cc509cb5eae473b290b59
  - path: coverage/project/project-data/tests.toml
    base: shared
    audience: scenario-operator
    sha256: 57a3c33e34158218bd387016c10c894ac08bfe307ddbb159c4f5fb535e565506
  - path: coverage/project/project-data/booley.toml
    base: shared
    audience: scenario-operator
    sha256: fa01bd3d777362378c4b137a7a8f288cb59385ac8fb30a5fb814579363ea4b58
  - path: coverage/project/scripts/stage_vector.py
    base: shared
    audience: scenario-operator
    sha256: f3ff5cdaf807e6096c6f3b717b7af7560a751db3af4566d5c2d6cddaf9a573d5
  resources:
  - owned-project
  - owned-runtime
  - owned-fault-controller
  - owned-artifact-root
- id: setup.catalog
  phase: prepare
  action: 'Read actual selected CLI help and MCP schemas: --coverage and permanent --cov, default-false boolean,
    exact required Analyst Campaign path, optional instruction; no legacy scorer options.'
  checks:
  - id: setup.catalog
    capabilities:
    - PUBLIC-CAPABILITY-DISCOVERY
    stimulus: 'Read actual selected CLI help and MCP schemas: --coverage and permanent --cov, default-false boolean,
      exact required Analyst Campaign path, optional instruction; no legacy scorer options.'
    expected: 'Read actual selected CLI help and MCP schemas: --coverage and permanent --cov, default-false boolean,
      exact required Analyst Campaign path, optional instruction; no legacy scorer options.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Read actual selected CLI help and MCP schemas: --coverage
      and permanent --cov, default-false boolean, exact required Analyst Campaign path, optional instruction; no
      legacy scorer options.'
    capture: evidence/coverage-lifecycle.setup.catalog
  timeout_minutes: 2
  requires:
  - setup.artifact
- id: oracle.positive
  phase: prepare
  action: Run python3 evaluator/controls.py positive from the hashed shared fixture. Positive control must accept
    literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control input.
    These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.positive
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py positive from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py positive from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py positive from the hashed
      shared fixture. Positive control must accept literal 4/8; each negative must reject its independently altered
      field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-lifecycle.oracle.positive
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-hit-count
  phase: prepare
  action: Run python3 evaluator/controls.py hit-count from the hashed shared fixture. Positive control must accept
    literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control input.
    These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-hit-count
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py hit-count from the hashed shared fixture. Positive control must
      accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py hit-count from the hashed shared fixture. Positive control must
      accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py hit-count from the hashed
      shared fixture. Positive control must accept literal 4/8; each negative must reject its independently altered
      field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-lifecycle.oracle.negative-hit-count
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-source-grouping
  phase: prepare
  action: Run python3 evaluator/controls.py source-grouping from the hashed shared fixture. Positive control must
    accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
    input. These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-source-grouping
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py source-grouping from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py source-grouping from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py source-grouping from
      the hashed shared fixture. Positive control must accept literal 4/8; each negative must reject its independently
      altered field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-lifecycle.oracle.negative-source-grouping
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-digest
  phase: prepare
  action: Run python3 evaluator/controls.py digest from the hashed shared fixture. Positive control must accept
    literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control input.
    These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-digest
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py digest from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py digest from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py digest from the hashed
      shared fixture. Positive control must accept literal 4/8; each negative must reject its independently altered
      field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-lifecycle.oracle.negative-digest
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-rational-verdict
  phase: prepare
  action: Run python3 evaluator/controls.py rational-verdict from the hashed shared fixture. Positive control must
    accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
    input. These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-rational-verdict
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py rational-verdict from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py rational-verdict from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py rational-verdict from
      the hashed shared fixture. Positive control must accept literal 4/8; each negative must reject its independently
      altered field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-lifecycle.oracle.negative-rational-verdict
  timeout_minutes: 2
  requires:
  - setup.ready
- id: baseline.native
  phase: baseline
  action: Produce fresh baseline.native according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.native,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.native
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.native according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.native,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.native according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.native,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.native according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.native, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-lifecycle.baseline.native
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.policy
  phase: baseline
  action: Produce fresh baseline.policy according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.policy,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.policy
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.policy according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.policy,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.policy according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.policy,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.policy according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.policy, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-lifecycle.baseline.policy
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.analysis
  phase: baseline
  action: Produce fresh baseline.analysis according to the exact baseline table in coverage/RUNBOOK.md. Capture
    immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.analysis,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.analysis
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.analysis according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.analysis,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.analysis according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.analysis,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.analysis according to the exact
      baseline table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket
      state outside every fault/prune root. For baseline.analysis, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-lifecycle.baseline.analysis
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.waiver
  phase: baseline
  action: Produce fresh baseline.waiver according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.waiver,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.waiver
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.waiver according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.waiver,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.waiver according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.waiver,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.waiver according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.waiver, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-lifecycle.baseline.waiver
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.retention
  phase: baseline
  action: Produce fresh baseline.retention according to the exact baseline table in coverage/RUNBOOK.md. Capture
    immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.retention,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.retention
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.retention according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.retention,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.retention according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.retention,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.retention according to the exact
      baseline table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket
      state outside every fault/prune root. For baseline.retention, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-lifecycle.baseline.retention
  timeout_minutes: 12
  requires:
  - setup.ready
- id: publish.manifest-failure
  phase: exercise
  action: Launch public sim --coverage under faults/filesystem.py --operation link --suffix /coverage.json --gate
    any; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline state is
    the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake plus
    reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Make only the owned Campaign
    publication destination fail → no usable Campaign pointer or accepted coverage evidence; previous authoritative
    Criteria retained. Fault.
  checks:
  - id: publish.manifest-failure
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Launch public sim --coverage under faults/filesystem.py --operation link --suffix /coverage.json --gate
      any; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline state
      is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Make only the
      owned Campaign publication destination fail → no usable Campaign pointer or accepted coverage evidence; previous
      authoritative Criteria retained. Fault.
    expected: Launch public sim --coverage under faults/filesystem.py --operation link --suffix /coverage.json --gate
      any; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline state
      is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Make only the
      owned Campaign publication destination fail → no usable Campaign pointer or accepted coverage evidence; previous
      authoritative Criteria retained. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Launch public sim --coverage under faults/filesystem.py
      --operation link --suffix /coverage.json --gate any; --owned is the run-owned Project/report root, control
      is a new sibling directory, and baseline state is the captured baseline.policy state. Require consumed handshake
      for IO failure or interrupted handshake plus reaped producer for --gate interrupt; preserve attempted temp
      JSON before its removal. Make only the owned Campaign publication destination fail → no usable Campaign pointer
      or accepted coverage evidence; previous authoritative Criteria retained. Fault.'
    capture: evidence/coverage-lifecycle.publish.manifest-failure
  timeout_minutes: 2
  requires:
  - baseline.policy
- id: publish.manifest-failure.restore
  phase: exercise
  action: Archive publish.manifest-failure result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: publish.manifest-failure.restore
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Archive publish.manifest-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive publish.manifest-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive publish.manifest-failure result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.policy exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.publish.manifest-failure.restore
  timeout_minutes: 2
  requires:
  - baseline.policy
  recovery:
    baseline: baseline.policy
    detection:
    - publish.manifest-failure
    instruction: Archive publish.manifest-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: publish.simulation-failure
  phase: exercise
  action: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /simulation.json --gate
    any; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline state is
    the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake plus
    reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Permit Campaign publication
    but deny Simulation publication → analysis unavailable for incomplete Target; no new accepted Criteria. Fault.
  checks:
  - id: publish.simulation-failure
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /simulation.json
      --gate any; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Permit Campaign
      publication but deny Simulation publication → analysis unavailable for incomplete Target; no new accepted
      Criteria. Fault.
    expected: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /simulation.json
      --gate any; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Permit Campaign
      publication but deny Simulation publication → analysis unavailable for incomplete Target; no new accepted
      Criteria. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Launch public sim --coverage under faults/filesystem.py
      --operation rename --suffix /simulation.json --gate any; --owned is the run-owned Project/report root, control
      is a new sibling directory, and baseline state is the captured baseline.policy state. Require consumed handshake
      for IO failure or interrupted handshake plus reaped producer for --gate interrupt; preserve attempted temp
      JSON before its removal. Permit Campaign publication but deny Simulation publication → analysis unavailable
      for incomplete Target; no new accepted Criteria. Fault.'
    capture: evidence/coverage-lifecycle.publish.simulation-failure
  timeout_minutes: 2
  requires:
  - baseline.policy
- id: publish.simulation-failure.restore
  phase: exercise
  action: Archive publish.simulation-failure result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: publish.simulation-failure.restore
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Archive publish.simulation-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive publish.simulation-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive publish.simulation-failure result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.policy exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.publish.simulation-failure.restore
  timeout_minutes: 2
  requires:
  - baseline.policy
  recovery:
    baseline: baseline.policy
    detection:
    - publish.simulation-failure
    instruction: Archive publish.simulation-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: publish.acceptance-failure
  phase: exercise
  action: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /booley_state.json
    --gate acceptance; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
    state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
    plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Fail the owned evidence/state
    publication boundary after reports → uncommitted transaction cannot replace prior authoritative acceptance.
    Fault.
  checks:
  - id: publish.acceptance-failure
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /booley_state.json
      --gate acceptance; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Fail the owned
      evidence/state publication boundary after reports → uncommitted transaction cannot replace prior authoritative
      acceptance. Fault.
    expected: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /booley_state.json
      --gate acceptance; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Fail the owned
      evidence/state publication boundary after reports → uncommitted transaction cannot replace prior authoritative
      acceptance. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Launch public sim --coverage under faults/filesystem.py
      --operation rename --suffix /booley_state.json --gate acceptance; --owned is the run-owned Project/report
      root, control is a new sibling directory, and baseline state is the captured baseline.policy state. Require
      consumed handshake for IO failure or interrupted handshake plus reaped producer for --gate interrupt; preserve
      attempted temp JSON before its removal. Fail the owned evidence/state publication boundary after reports →
      uncommitted transaction cannot replace prior authoritative acceptance. Fault.'
    capture: evidence/coverage-lifecycle.publish.acceptance-failure
  timeout_minutes: 2
  requires:
  - baseline.policy
- id: publish.acceptance-failure.restore
  phase: exercise
  action: Archive publish.acceptance-failure result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: publish.acceptance-failure.restore
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Archive publish.acceptance-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive publish.acceptance-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive publish.acceptance-failure result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.policy exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.publish.acceptance-failure.restore
  timeout_minutes: 2
  requires:
  - baseline.policy
  recovery:
    baseline: baseline.policy
    detection:
    - publish.acceptance-failure
    instruction: Archive publish.acceptance-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: publish.progress-failure
  phase: exercise
  action: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /progress.json --gate
    complete; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline state
    is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake plus
    reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Fail terminal progress
    publication after durable acceptance → rc2 while committed reports, measured verdicts and Criteria remain valid.
    Fault.
  checks:
  - id: publish.progress-failure
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /progress.json
      --gate complete; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Fail terminal
      progress publication after durable acceptance → rc2 while committed reports, measured verdicts and Criteria
      remain valid. Fault.
    expected: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /progress.json
      --gate complete; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Fail terminal
      progress publication after durable acceptance → rc2 while committed reports, measured verdicts and Criteria
      remain valid. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Launch public sim --coverage under faults/filesystem.py
      --operation rename --suffix /progress.json --gate complete; --owned is the run-owned Project/report root,
      control is a new sibling directory, and baseline state is the captured baseline.policy state. Require consumed
      handshake for IO failure or interrupted handshake plus reaped producer for --gate interrupt; preserve attempted
      temp JSON before its removal. Fail terminal progress publication after durable acceptance → rc2 while committed
      reports, measured verdicts and Criteria remain valid. Fault.'
    capture: evidence/coverage-lifecycle.publish.progress-failure
  timeout_minutes: 2
  requires:
  - baseline.policy
- id: publish.progress-failure.restore
  phase: exercise
  action: Archive publish.progress-failure result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: publish.progress-failure.restore
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Archive publish.progress-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive publish.progress-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive publish.progress-failure result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.policy exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.publish.progress-failure.restore
  timeout_minutes: 2
  requires:
  - baseline.policy
  recovery:
    baseline: baseline.policy
    detection:
    - publish.progress-failure
    instruction: Archive publish.progress-failure result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: publish.shared-abort
  phase: exercise
  action: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /simulation.json --gate
    any; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline state is
    the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake plus
    reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Cause shared publication
    failure in multi-Target execution → earlier results and pending Targets retained, later unsafe work aborted.
    Fault.
  checks:
  - id: publish.shared-abort
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /simulation.json
      --gate any; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Cause shared publication
      failure in multi-Target execution → earlier results and pending Targets retained, later unsafe work aborted.
      Fault.
    expected: Launch public sim --coverage under faults/filesystem.py --operation rename --suffix /simulation.json
      --gate any; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Cause shared publication
      failure in multi-Target execution → earlier results and pending Targets retained, later unsafe work aborted.
      Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Launch public sim --coverage under faults/filesystem.py
      --operation rename --suffix /simulation.json --gate any; --owned is the run-owned Project/report root, control
      is a new sibling directory, and baseline state is the captured baseline.policy state. Require consumed handshake
      for IO failure or interrupted handshake plus reaped producer for --gate interrupt; preserve attempted temp
      JSON before its removal. Cause shared publication failure in multi-Target execution → earlier results and
      pending Targets retained, later unsafe work aborted. Fault.'
    capture: evidence/coverage-lifecycle.publish.shared-abort
  timeout_minutes: 2
  requires:
  - baseline.policy
- id: publish.shared-abort.restore
  phase: exercise
  action: Archive publish.shared-abort result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.policy
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: publish.shared-abort.restore
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Archive publish.shared-abort result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive publish.shared-abort result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive publish.shared-abort result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.policy exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.publish.shared-abort.restore
  timeout_minutes: 2
  requires:
  - baseline.policy
  recovery:
    baseline: baseline.policy
    detection:
    - publish.shared-abort
    instruction: Archive publish.shared-abort result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: publish.interrupt
  phase: exercise
  action: Launch public sim --coverage under faults/filesystem.py --operation link --suffix /coverage.json --gate
    interrupt; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline state
    is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake plus
    reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Stop owned producer after
    native execution before completion → partial evidence cannot claim completion; lock released at process exit.
    Fault.
  checks:
  - id: publish.interrupt
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Launch public sim --coverage under faults/filesystem.py --operation link --suffix /coverage.json --gate
      interrupt; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Stop owned producer
      after native execution before completion → partial evidence cannot claim completion; lock released at process
      exit. Fault.
    expected: Launch public sim --coverage under faults/filesystem.py --operation link --suffix /coverage.json --gate
      interrupt; --owned is the run-owned Project/report root, control is a new sibling directory, and baseline
      state is the captured baseline.policy state. Require consumed handshake for IO failure or interrupted handshake
      plus reaped producer for --gate interrupt; preserve attempted temp JSON before its removal. Stop owned producer
      after native execution before completion → partial evidence cannot claim completion; lock released at process
      exit. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Launch public sim --coverage under faults/filesystem.py
      --operation link --suffix /coverage.json --gate interrupt; --owned is the run-owned Project/report root, control
      is a new sibling directory, and baseline state is the captured baseline.policy state. Require consumed handshake
      for IO failure or interrupted handshake plus reaped producer for --gate interrupt; preserve attempted temp
      JSON before its removal. Stop owned producer after native execution before completion → partial evidence cannot
      claim completion; lock released at process exit. Fault.'
    capture: evidence/coverage-lifecycle.publish.interrupt
  timeout_minutes: 2
  requires:
  - baseline.policy
- id: publish.interrupt.restore
  phase: exercise
  action: Archive publish.interrupt result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.policy
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: publish.interrupt.restore
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Archive publish.interrupt result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive publish.interrupt result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive publish.interrupt result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.policy exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.publish.interrupt.restore
  timeout_minutes: 2
  requires:
  - baseline.policy
  recovery:
    baseline: baseline.policy
    detection:
    - publish.interrupt
    instruction: Archive publish.interrupt result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.policy exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: publish.restart
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Start a fresh collection
    after interrupted producer cleanup → new invocation number and native state, no resume or overwrite of interrupted
    evidence.
  checks:
  - id: publish.restart
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Start a fresh
      collection after interrupted producer cleanup → new invocation number and native state, no resume or overwrite
      of interrupted evidence.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Start a fresh
      collection after interrupted producer cleanup → new invocation number and native state, no resume or overwrite
      of interrupted evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Start a fresh collection after interrupted producer cleanup → new invocation number
      and native state, no resume or overwrite of interrupted evidence.'
    capture: evidence/coverage-lifecycle.publish.restart
  timeout_minutes: 2
  requires:
  - baseline.policy
  - publish.interrupt
  - publish.interrupt.restore
- id: publish.immutable
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Repeat collection
    and analysis while retaining older Campaign → older manifest/point store remain byte-identical; consumers use
    explicit paths without a latest alias.
  checks:
  - id: publish.immutable
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Repeat collection
      and analysis while retaining older Campaign → older manifest/point store remain byte-identical; consumers
      use explicit paths without a latest alias.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Repeat collection
      and analysis while retaining older Campaign → older manifest/point store remain byte-identical; consumers
      use explicit paths without a latest alias.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/FLOW_IMPLEMENTATION.md#coverage-campaign-orchestration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Repeat collection and analysis while retaining older Campaign → older manifest/point
      store remain byte-identical; consumers use explicit paths without a latest alias.'
    capture: evidence/coverage-lifecycle.publish.immutable
  timeout_minutes: 2
  requires:
  - baseline.policy
- id: ticket.author
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
    and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Publicly
    create the preapproved verification Ticket below → exact Target/test/metric contract sealed; explicit approval
    trace is retained. Reuse TICKET-AUTHORING.
  checks:
  - id: ticket.author
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Publicly
      create the preapproved verification Ticket below → exact Target/test/metric contract sealed; explicit approval
      trace is retained. Reuse TICKET-AUTHORING.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Publicly
      create the preapproved verification Ticket below → exact Target/test/metric contract sealed; explicit approval
      trace is retained. Reuse TICKET-AUTHORING.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry
      has only the gap acceptance test for sim_generated. Publicly create the preapproved verification Ticket below
      → exact Target/test/metric contract sealed; explicit approval trace is retained. Reuse TICKET-AUTHORING.'
    capture: evidence/coverage-lifecycle.ticket.author
  timeout_minutes: 15
  requires:
  - baseline.policy
  assets:
  - path: coverage/tickets/close-test-gap.md
    base: shared
    audience: Developer Agent
    sha256: 3063bda64ae5ca1ff1be01d6226ba2f833edc6200055266de730f5810204012a
  - path: coverage/tickets/tests.toml
    base: shared
    audience: Developer Agent
    sha256: 4fd70c5c36a4710bb1b680c553cf04f2022b49c9617cbded3c3397b67e686c10
- id: ticket.gap
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
    and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Execute
    initial full selected suite with explicit collection → passing simulation but coverage miss for known decoder
    choice; acceptance blocked with exact failing evidence.
  checks:
  - id: ticket.gap
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Execute
      initial full selected suite with explicit collection → passing simulation but coverage miss for known decoder
      choice; acceptance blocked with exact failing evidence.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Execute
      initial full selected suite with explicit collection → passing simulation but coverage miss for known decoder
      choice; acceptance blocked with exact failing evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry
      has only the gap acceptance test for sim_generated. Execute initial full selected suite with explicit collection
      → passing simulation but coverage miss for known decoder choice; acceptance blocked with exact failing evidence.'
    capture: evidence/coverage-lifecycle.ticket.gap
  timeout_minutes: 2
  requires:
  - baseline.policy
  - ticket.author
- id: ticket.advice
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
    and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Ticket-bound
    Analyst explains exact failed Campaign → recommendation identifies test gap with delivered points; Criteria
    unchanged.
  checks:
  - id: ticket.advice
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Ticket-bound
      Analyst explains exact failed Campaign → recommendation identifies test gap with delivered points; Criteria
      unchanged.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Ticket-bound
      Analyst explains exact failed Campaign → recommendation identifies test gap with delivered points; Criteria
      unchanged.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry
      has only the gap acceptance test for sim_generated. Ticket-bound Analyst explains exact failed Campaign →
      recommendation identifies test gap with delivered points; Criteria unchanged.'
    capture: evidence/coverage-lifecycle.ticket.advice
  timeout_minutes: 8
  requires:
  - baseline.policy
  - ticket.gap
- id: ticket.no-implicit
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
    and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Run
    ordinary passing sim under same Ticket → no new coverage acceptance; coverage remains unmet.
  checks:
  - id: ticket.no-implicit
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Run
      ordinary passing sim under same Ticket → no new coverage acceptance; coverage remains unmet.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Run
      ordinary passing sim under same Ticket → no new coverage acceptance; coverage remains unmet.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry
      has only the gap acceptance test for sim_generated. Run ordinary passing sim under same Ticket → no new coverage
      acceptance; coverage remains unmet.'
    capture: evidence/coverage-lifecycle.ticket.no-implicit
  timeout_minutes: 2
  requires:
  - baseline.policy
  - ticket.gap
- id: ticket.improve
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
    and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Developer
    adds missing stimulus/assertions within approved TB Scope and recollects → full exact suite, correct DUT behavior
    and now-passing fixed cover-property threshold. RTL and policy unchanged.
  checks:
  - id: ticket.improve
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Developer
      adds missing stimulus/assertions within approved TB Scope and recollects → full exact suite, correct DUT behavior
      and now-passing fixed cover-property threshold. RTL and policy unchanged.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Developer
      adds missing stimulus/assertions within approved TB Scope and recollects → full exact suite, correct DUT behavior
      and now-passing fixed cover-property threshold. RTL and policy unchanged.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry
      has only the gap acceptance test for sim_generated. Developer adds missing stimulus/assertions within approved
      TB Scope and recollects → full exact suite, correct DUT behavior and now-passing fixed cover-property threshold.
      RTL and policy unchanged.'
    capture: evidence/coverage-lifecycle.ticket.improve
  timeout_minutes: 15
  requires:
  - baseline.policy
  - ticket.advice
  assets:
  - path: coverage/tickets/close-test-gap.md
    base: shared
    audience: Developer Agent
    sha256: 3063bda64ae5ca1ff1be01d6226ba2f833edc6200055266de730f5810204012a
  - path: coverage/tickets/tests.toml
    base: shared
    audience: Developer Agent
    sha256: 4fd70c5c36a4710bb1b680c553cf04f2022b49c9617cbded3c3397b67e686c10
- id: ticket.evidence
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
    and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Read
    current Criteria/Satisfaction Record → independent sim and coverage satisfied by durable exact Campaign/Simulation
    pointers at current source identity. Reuse CRITERION-EVIDENCE-BINDING.
  checks:
  - id: ticket.evidence
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Read
      current Criteria/Satisfaction Record → independent sim and coverage satisfied by durable exact Campaign/Simulation
      pointers at current source identity. Reuse CRITERION-EVIDENCE-BINDING.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Read
      current Criteria/Satisfaction Record → independent sim and coverage satisfied by durable exact Campaign/Simulation
      pointers at current source identity. Reuse CRITERION-EVIDENCE-BINDING.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry
      has only the gap acceptance test for sim_generated. Read current Criteria/Satisfaction Record → independent
      sim and coverage satisfied by durable exact Campaign/Simulation pointers at current source identity. Reuse
      CRITERION-EVIDENCE-BINDING.'
    capture: evidence/coverage-lifecycle.ticket.evidence
  timeout_minutes: 2
  requires:
  - baseline.policy
  - ticket.improve
- id: ticket.stale
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Create a separate
    disposable Ticket through the public authoring route with the same approved payload and final TB from ticket.improve,
    collect its passing exact suite, then change only that Ticket TB; retain this dedicated before-state for restoration.
    Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry has only the gap acceptance
    test for sim_generated. In an explicitly separate disposable sealed Ticket, modify relevant TB after passing
    evidence → old acceptance cannot satisfy current-source contract. Fault; reuse CRITERION-ACCEPTANCE-POLICY.
  checks:
  - id: ticket.stale
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Create a separate
      disposable Ticket through the public authoring route with the same approved payload and final TB from ticket.improve,
      collect its passing exact suite, then change only that Ticket TB; retain this dedicated before-state for restoration.
      Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry has only the gap acceptance
      test for sim_generated. In an explicitly separate disposable sealed Ticket, modify relevant TB after passing
      evidence → old acceptance cannot satisfy current-source contract. Fault; reuse CRITERION-ACCEPTANCE-POLICY.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Create a separate
      disposable Ticket through the public authoring route with the same approved payload and final TB from ticket.improve,
      collect its passing exact suite, then change only that Ticket TB; retain this dedicated before-state for restoration.
      Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry has only the gap acceptance
      test for sim_generated. In an explicitly separate disposable sealed Ticket, modify relevant TB after passing
      evidence → old acceptance cannot satisfy current-source contract. Fault; reuse CRITERION-ACCEPTANCE-POLICY.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Create a separate disposable Ticket through the public authoring route with the
      same approved payload and final TB from ticket.improve, collect its passing exact suite, then change only
      that Ticket TB; retain this dedicated before-state for restoration. Use tickets/close-test-gap.md and tickets/tests.toml;
      the Ticket Project registry has only the gap acceptance test for sim_generated. In an explicitly separate
      disposable sealed Ticket, modify relevant TB after passing evidence → old acceptance cannot satisfy current-source
      contract. Fault; reuse CRITERION-ACCEPTANCE-POLICY.'
    capture: evidence/coverage-lifecycle.ticket.stale
  timeout_minutes: 2
  requires:
  - ticket.improve
- id: ticket.stale.restore
  phase: exercise
  action: Archive ticket.stale result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore ticket.improve
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed. Restore the dedicated disposable Ticket TB to its captured passing bytes and rerun sim_generated
    gap --coverage in that Ticket; sim_properties3 evidence cannot establish recovery.
  checks:
  - id: ticket.stale.restore
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Archive ticket.stale result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore ticket.improve
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed. Restore the dedicated disposable Ticket TB to its captured passing bytes and rerun sim_generated
      gap --coverage in that Ticket; sim_properties3 evidence cannot establish recovery.
    expected: Archive ticket.stale result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore ticket.improve
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed. Restore the dedicated disposable Ticket TB to its captured passing bytes and rerun sim_generated
      gap --coverage in that Ticket; sim_properties3 evidence cannot establish recovery.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive ticket.stale result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore ticket.improve exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed. Restore the dedicated disposable Ticket
      TB to its captured passing bytes and rerun sim_generated gap --coverage in that Ticket; sim_properties3 evidence
      cannot establish recovery.'
    capture: evidence/coverage-lifecycle.ticket.stale.restore
  timeout_minutes: 2
  requires:
  - ticket.improve
  recovery:
    baseline: ticket.improve
    detection:
    - ticket.stale
    instruction: Archive ticket.stale result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore ticket.improve
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed. Restore the dedicated disposable Ticket TB to its captured passing bytes and rerun sim_generated
      gap --coverage in that Ticket; sim_properties3 evidence cannot establish recovery.
- id: ticket.regain
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
    and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Rerun
    explicit collection after restoration/current source → fresh evidence regains coverage acceptance; previous
    history retained.
  checks:
  - id: ticket.regain
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Rerun
      explicit collection after restoration/current source → fresh evidence regains coverage acceptance; previous
      history retained.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Rerun
      explicit collection after restoration/current source → fresh evidence regains coverage acceptance; previous
      history retained.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry
      has only the gap acceptance test for sim_generated. Rerun explicit collection after restoration/current source
      → fresh evidence regains coverage acceptance; previous history retained.'
    capture: evidence/coverage-lifecycle.ticket.regain
  timeout_minutes: 2
  requires:
  - baseline.policy
  - ticket.stale.restore
- id: ticket.advice-no-approval
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
    and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Ask
    Analyst for exclusions in Ticket Mode → candidates remain advisory, no waiver or Criterion mutation.
  checks:
  - id: ticket.advice-no-approval
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Ask
      Analyst for exclusions in Ticket Mode → candidates remain advisory, no waiver or Criterion mutation.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Ask
      Analyst for exclusions in Ticket Mode → candidates remain advisory, no waiver or Criterion mutation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry
      has only the gap acceptance test for sim_generated. Ask Analyst for exclusions in Ticket Mode → candidates
      remain advisory, no waiver or Criterion mutation.'
    capture: evidence/coverage-lifecycle.ticket.advice-no-approval
  timeout_minutes: 2
  requires:
  - baseline.policy
  - ticket.improve
- id: ticket.deliver
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
    and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Finish
    approved verification work through public route → clean committed TB-only diff and report linking original gap,
    Analyst advice, exact final coverage and retained failures. Reuse TICKET-DELIVERABLES.
  checks:
  - id: ticket.deliver
    capabilities:
    - COVERAGE-CRITERIA-POLICY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Finish
      approved verification work through public route → clean committed TB-only diff and report linking original
      gap, Analyst advice, exact final coverage and retained failures. Reuse TICKET-DELIVERABLES.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md
      and tickets/tests.toml; the Ticket Project registry has only the gap acceptance test for sim_generated. Finish
      approved verification work through public route → clean committed TB-only diff and report linking original
      gap, Analyst advice, exact final coverage and retained failures. Reuse TICKET-DELIVERABLES.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#native-coverage-configuration
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Use tickets/close-test-gap.md and tickets/tests.toml; the Ticket Project registry
      has only the gap acceptance test for sim_generated. Finish approved verification work through public route
      → clean committed TB-only diff and report linking original gap, Analyst advice, exact final coverage and retained
      failures. Reuse TICKET-DELIVERABLES.'
    capture: evidence/coverage-lifecycle.ticket.deliver
  timeout_minutes: 8
  requires:
  - baseline.policy
  - ticket.evidence
  - ticket.regain
  - ticket.advice-no-approval
- id: retain.native
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run documented exact
    --native-target command → only selected Target native payload disappears, availability reports pruned with bound
    identity.
  checks:
  - id: retain.native
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run documented
      exact --native-target command → only selected Target native payload disappears, availability reports pruned
      with bound identity.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run documented
      exact --native-target command → only selected Target native payload disappears, availability reports pruned
      with bound identity.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Run documented exact --native-target command → only selected Target native payload
      disappears, availability reports pruned with bound identity.'
    capture: evidence/coverage-lifecycle.retain.native
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.native-preserves
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Compare before/after
    native prune → byte-identical manifest, point store, Simulation and hook evidence; sibling Target and other
    invocation untouched.
  checks:
  - id: retain.native-preserves
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Compare before/after
      native prune → byte-identical manifest, point store, Simulation and hook evidence; sibling Target and other
      invocation untouched.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Compare before/after
      native prune → byte-identical manifest, point store, Simulation and hook evidence; sibling Target and other
      invocation untouched.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Compare before/after native prune → byte-identical manifest, point store, Simulation
      and hook evidence; sibling Target and other invocation untouched.'
    capture: evidence/coverage-lifecycle.retain.native-preserves
  timeout_minutes: 2
  requires:
  - baseline.retention
  - retain.native
- id: retain.native-analysis
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Analyze that exact
    pruned Campaign using real model → still valid and same observed measurement/evaluation.
  checks:
  - id: retain.native-analysis
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Analyze that exact
      pruned Campaign using real model → still valid and same observed measurement/evaluation.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Analyze that exact
      pruned Campaign using real model → still valid and same observed measurement/evaluation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Analyze that exact pruned Campaign using real model → still valid and same observed
      measurement/evaluation.'
    capture: evidence/coverage-lifecycle.retain.native-analysis
  timeout_minutes: 2
  requires:
  - baseline.retention
  - retain.native
- id: retain.native-retry
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Repeat same exact
    completed selection → safe retry, preserved normalized evidence.
  checks:
  - id: retain.native-retry
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Repeat same exact
      completed selection → safe retry, preserved normalized evidence.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Repeat same exact
      completed selection → safe retry, preserved normalized evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Repeat same exact completed selection → safe retry, preserved normalized evidence.'
    capture: evidence/coverage-lifecycle.retain.native-retry
  timeout_minutes: 2
  requires:
  - baseline.retention
  - retain.native
- id: retain.full
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run documented --full
    for selected completed invocation → exact reports/native payload removed and empty `.pruned-N` tombstone retained.
  checks:
  - id: retain.full
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run documented
      --full for selected completed invocation → exact reports/native payload removed and empty `.pruned-N` tombstone
      retained.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run documented
      --full for selected completed invocation → exact reports/native payload removed and empty `.pruned-N` tombstone
      retained.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Run documented --full for selected completed invocation → exact reports/native
      payload removed and empty `.pruned-N` tombstone retained.'
    capture: evidence/coverage-lifecycle.retain.full
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.full-analysis
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Analyze removed exact
    Campaign → unavailable error, no fallback to another invocation.
  checks:
  - id: retain.full-analysis
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Analyze removed
      exact Campaign → unavailable error, no fallback to another invocation.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Analyze removed
      exact Campaign → unavailable error, no fallback to another invocation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Analyze removed exact Campaign → unavailable error, no fallback to another invocation.'
    capture: evidence/coverage-lifecycle.retain.full-analysis
  timeout_minutes: 2
  requires:
  - baseline.retention
  - retain.full
- id: retain.number-reserved
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Collect after full
    prune → fresh number does not reuse tombstoned invocation; other reports unchanged.
  checks:
  - id: retain.number-reserved
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Collect after
      full prune → fresh number does not reuse tombstoned invocation; other reports unchanged.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Collect after
      full prune → fresh number does not reuse tombstoned invocation; other reports unchanged.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Collect after full prune → fresh number does not reuse tombstoned invocation;
      other reports unchanged.'
    capture: evidence/coverage-lifecycle.retain.number-reserved
  timeout_minutes: 2
  requires:
  - baseline.retention
  - retain.full
- id: retain.reject-native-ambiguous-target
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/ambiguous-target:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-native-ambiguous-target
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/ambiguous-target:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/ambiguous-target:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case native/ambiguous-target: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-native-ambiguous-target
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-native-ambiguous-target.restore
  phase: exercise
  action: Archive retain.reject-native-ambiguous-target result even if detection failed. Reverse only its owned
    injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
    selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
    evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
    approval/audit. Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-native-ambiguous-target.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-native-ambiguous-target result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-native-ambiguous-target result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-native-ambiguous-target result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-native-ambiguous-target.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-native-ambiguous-target
    instruction: Archive retain.reject-native-ambiguous-target result even if detection failed. Reverse only its
      owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-native-unknown-target
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unknown-target:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-native-unknown-target
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unknown-target:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unknown-target:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case native/unknown-target: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-native-unknown-target
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-native-unknown-target.restore
  phase: exercise
  action: Archive retain.reject-native-unknown-target result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-native-unknown-target.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-native-unknown-target result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-native-unknown-target result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-native-unknown-target result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-native-unknown-target.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-native-unknown-target
    instruction: Archive retain.reject-native-unknown-target result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-native-invalid-number
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/invalid-number:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-native-invalid-number
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/invalid-number:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/invalid-number:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case native/invalid-number: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-native-invalid-number
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-native-invalid-number.restore
  phase: exercise
  action: Archive retain.reject-native-invalid-number result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-native-invalid-number.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-native-invalid-number result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-native-invalid-number result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-native-invalid-number result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-native-invalid-number.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-native-invalid-number
    instruction: Archive retain.reject-native-invalid-number result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-full-invalid-number
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/invalid-number:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-full-invalid-number
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/invalid-number:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/invalid-number:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case full/invalid-number: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-full-invalid-number
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-full-invalid-number.restore
  phase: exercise
  action: Archive retain.reject-full-invalid-number result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-full-invalid-number.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-full-invalid-number result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-full-invalid-number result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-full-invalid-number result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-full-invalid-number.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-full-invalid-number
    instruction: Archive retain.reject-full-invalid-number result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-native-unsafe-path
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unsafe-path:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-native-unsafe-path
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unsafe-path:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unsafe-path:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case native/unsafe-path: Exercise exact invalid selection/fault in the named case
      of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-native-unsafe-path
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-native-unsafe-path.restore
  phase: exercise
  action: Archive retain.reject-native-unsafe-path result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-native-unsafe-path.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-native-unsafe-path result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive retain.reject-native-unsafe-path result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-native-unsafe-path result even if
      detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-native-unsafe-path.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-native-unsafe-path
    instruction: Archive retain.reject-native-unsafe-path result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-full-unsafe-path
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/unsafe-path:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-full-unsafe-path
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/unsafe-path:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/unsafe-path:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case full/unsafe-path: Exercise exact invalid selection/fault in the named case
      of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-full-unsafe-path
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-full-unsafe-path.restore
  phase: exercise
  action: Archive retain.reject-full-unsafe-path result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-full-unsafe-path.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-full-unsafe-path result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive retain.reject-full-unsafe-path result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-full-unsafe-path result even if
      detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-full-unsafe-path.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-full-unsafe-path
    instruction: Archive retain.reject-full-unsafe-path result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-native-symlink
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/symlink:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-native-symlink
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/symlink:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/symlink:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case native/symlink: Exercise exact invalid selection/fault in the named case
      of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-native-symlink
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-native-symlink.restore
  phase: exercise
  action: Archive retain.reject-native-symlink result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-native-symlink.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-native-symlink result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive retain.reject-native-symlink result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-native-symlink result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.retention exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-native-symlink.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-native-symlink
    instruction: Archive retain.reject-native-symlink result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: retain.reject-full-symlink
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/symlink:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-full-symlink
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/symlink:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/symlink:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case full/symlink: Exercise exact invalid selection/fault in the named case of
      RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-full-symlink
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-full-symlink.restore
  phase: exercise
  action: Archive retain.reject-full-symlink result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-full-symlink.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-full-symlink result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive retain.reject-full-symlink result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-full-symlink result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.retention exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-full-symlink.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-full-symlink
    instruction: Archive retain.reject-full-symlink result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: retain.reject-native-changed-native
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/changed-native:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-native-changed-native
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/changed-native:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/changed-native:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case native/changed-native: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-native-changed-native
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-native-changed-native.restore
  phase: exercise
  action: Archive retain.reject-native-changed-native result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-native-changed-native.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-native-changed-native result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-native-changed-native result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-native-changed-native result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-native-changed-native.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-native-changed-native
    instruction: Archive retain.reject-native-changed-native result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-full-changed-native
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/changed-native:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-full-changed-native
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/changed-native:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/changed-native:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case full/changed-native: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-full-changed-native
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-full-changed-native.restore
  phase: exercise
  action: Archive retain.reject-full-changed-native result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-full-changed-native.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-full-changed-native result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-full-changed-native result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-full-changed-native result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-full-changed-native.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-full-changed-native
    instruction: Archive retain.reject-full-changed-native result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-native-unknown-payload
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unknown-payload:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-native-unknown-payload
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unknown-payload:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unknown-payload:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case native/unknown-payload: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-native-unknown-payload
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-native-unknown-payload.restore
  phase: exercise
  action: Archive retain.reject-native-unknown-payload result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-native-unknown-payload.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-native-unknown-payload result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-native-unknown-payload result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-native-unknown-payload result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-native-unknown-payload.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-native-unknown-payload
    instruction: Archive retain.reject-native-unknown-payload result even if detection failed. Reverse only its
      owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-full-unknown-payload
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/unknown-payload:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-full-unknown-payload
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/unknown-payload:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/unknown-payload:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case full/unknown-payload: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-full-unknown-payload
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-full-unknown-payload.restore
  phase: exercise
  action: Archive retain.reject-full-unknown-payload result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-full-unknown-payload.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-full-unknown-payload result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-full-unknown-payload result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-full-unknown-payload result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-full-unknown-payload.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-full-unknown-payload
    instruction: Archive retain.reject-full-unknown-payload result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-native-unexplained-missing-native
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unexplained-missing-native:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-native-unexplained-missing-native
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unexplained-missing-native:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/unexplained-missing-native:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case native/unexplained-missing-native: Exercise exact invalid selection/fault
      in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-native-unexplained-missing-native
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-native-unexplained-missing-native.restore
  phase: exercise
  action: Archive retain.reject-native-unexplained-missing-native result even if detection failed. Reverse only
    its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
    selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
    evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
    approval/audit. Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-native-unexplained-missing-native.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-native-unexplained-missing-native result even if detection failed. Reverse only
      its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and
      environment selection. Restore baseline.retention exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-native-unexplained-missing-native result even if detection failed. Reverse only
      its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and
      environment selection. Restore baseline.retention exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-native-unexplained-missing-native
      result even if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare
      original hashes, permissions, executable and environment selection. Restore baseline.retention exact inputs
      and rerun its public valid operation in this run with fresh evidence; use real configured Analyst when the
      failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault
      release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-native-unexplained-missing-native.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-native-unexplained-missing-native
    instruction: Archive retain.reject-native-unexplained-missing-native result even if detection failed. Reverse
      only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.retention exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-full-unexplained-missing-native
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/unexplained-missing-native:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-full-unexplained-missing-native
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/unexplained-missing-native:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/unexplained-missing-native:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case full/unexplained-missing-native: Exercise exact invalid selection/fault in
      the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-full-unexplained-missing-native
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-full-unexplained-missing-native.restore
  phase: exercise
  action: Archive retain.reject-full-unexplained-missing-native result even if detection failed. Reverse only its
    owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
    selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
    evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
    approval/audit. Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-full-unexplained-missing-native.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-full-unexplained-missing-native result even if detection failed. Reverse only
      its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and
      environment selection. Restore baseline.retention exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-full-unexplained-missing-native result even if detection failed. Reverse only
      its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and
      environment selection. Restore baseline.retention exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-full-unexplained-missing-native
      result even if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare
      original hashes, permissions, executable and environment selection. Restore baseline.retention exact inputs
      and rerun its public valid operation in this run with fresh evidence; use real configured Analyst when the
      failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault
      release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-full-unexplained-missing-native.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-full-unexplained-missing-native
    instruction: Archive retain.reject-full-unexplained-missing-native result even if detection failed. Reverse
      only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.retention exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-native-tampered-points
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/tampered-points:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-native-tampered-points
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/tampered-points:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case native/tampered-points:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case native/tampered-points: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-native-tampered-points
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-native-tampered-points.restore
  phase: exercise
  action: Archive retain.reject-native-tampered-points result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-native-tampered-points.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-native-tampered-points result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-native-tampered-points result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-native-tampered-points result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-native-tampered-points.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-native-tampered-points
    instruction: Archive retain.reject-native-tampered-points result even if detection failed. Reverse only its
      owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.reject-full-tampered-points
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/tampered-points:
    Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
    byte-identical.'
  checks:
  - id: retain.reject-full-tampered-points
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/tampered-points:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case full/tampered-points:
      Exercise exact invalid selection/fault in the named case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels
      byte-identical.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case full/tampered-points: Exercise exact invalid selection/fault in the named
      case of RUNBOOK.md → rc2 before deletion, unaffected reports/sentinels byte-identical.'
    capture: evidence/coverage-lifecycle.retain.reject-full-tampered-points
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.reject-full-tampered-points.restore
  phase: exercise
  action: Archive retain.reject-full-tampered-points result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.reject-full-tampered-points.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.reject-full-tampered-points result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive retain.reject-full-tampered-points result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.reject-full-tampered-points result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.retention exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.reject-full-tampered-points.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.reject-full-tampered-points
    instruction: Archive retain.reject-full-tampered-points result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: retain.native-interrupted
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Deny only quarantine
    cleanup after journal/rename → availability pruning and evidence preserved; retry same exact selection after
    permission restoration completes pruned. Fault; restoration independently reachable.
  checks:
  - id: retain.native-interrupted
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Deny only quarantine
      cleanup after journal/rename → availability pruning and evidence preserved; retry same exact selection after
      permission restoration completes pruned. Fault; restoration independently reachable.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Deny only quarantine
      cleanup after journal/rename → availability pruning and evidence preserved; retry same exact selection after
      permission restoration completes pruned. Fault; restoration independently reachable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Deny only quarantine cleanup after journal/rename → availability pruning and evidence
      preserved; retry same exact selection after permission restoration completes pruned. Fault; restoration independently
      reachable.'
    capture: evidence/coverage-lifecycle.retain.native-interrupted
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.native-interrupted.restore
  phase: exercise
  action: Archive retain.native-interrupted result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.native-interrupted.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.native-interrupted result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive retain.native-interrupted result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.native-interrupted result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.retention exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.native-interrupted.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.native-interrupted
    instruction: Archive retain.native-interrupted result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: retain.full-interrupted
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Deny owned deletion
    after full-prune rename/journal → retry same selection after restoration completes empty tombstone; other invocations
    untouched. Fault.
  checks:
  - id: retain.full-interrupted
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Deny owned deletion
      after full-prune rename/journal → retry same selection after restoration completes empty tombstone; other
      invocations untouched. Fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Deny owned deletion
      after full-prune rename/journal → retry same selection after restoration completes empty tombstone; other
      invocations untouched. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Deny owned deletion after full-prune rename/journal → retry same selection after
      restoration completes empty tombstone; other invocations untouched. Fault.'
    capture: evidence/coverage-lifecycle.retain.full-interrupted
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.full-interrupted.restore
  phase: exercise
  action: Archive retain.full-interrupted result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: retain.full-interrupted.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.full-interrupted result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive retain.full-interrupted result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.full-interrupted result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.retention exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.full-interrupted.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.full-interrupted
    instruction: Archive retain.full-interrupted result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.retention exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: retain.active
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke pruning while
    owned producer demonstrably holds active invocation → rc2 without deleting payload or removing lock. Fault.
  checks:
  - id: retain.active
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke pruning
      while owned producer demonstrably holds active invocation → rc2 without deleting payload or removing lock.
      Fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke pruning
      while owned producer demonstrably holds active invocation → rc2 without deleting payload or removing lock.
      Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Invoke pruning while owned producer demonstrably holds active invocation → rc2
      without deleting payload or removing lock. Fault.'
    capture: evidence/coverage-lifecycle.retain.active
  timeout_minutes: 2
  requires:
  - baseline.retention
- id: retain.active.restore
  phase: exercise
  action: Archive retain.active result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.retention
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: retain.active.restore
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Archive retain.active result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.retention
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    expected: Archive retain.active result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.retention
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive retain.active result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.retention exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-lifecycle.retain.active.restore
  timeout_minutes: 2
  requires:
  - baseline.retention
  recovery:
    baseline: baseline.retention
    detection:
    - retain.active
    instruction: Archive retain.active result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.retention
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
- id: retain.interrupted-producer
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Stop/reap owned producer,
    then prune its exact interrupted invocation → process exit releases ownership and documented cleanup succeeds;
    never use progress as lock authority.
  checks:
  - id: retain.interrupted-producer
    capabilities:
    - COVERAGE-CAMPAIGN-RETENTION
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Stop/reap owned
      producer, then prune its exact interrupted invocation → process exit releases ownership and documented cleanup
      succeeds; never use progress as lock authority.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Stop/reap owned
      producer, then prune its exact interrupted invocation → process exit releases ownership and documented cleanup
      succeeds; never use progress as lock authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#exact-coverage-retention
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Stop/reap owned producer, then prune its exact interrupted invocation → process
      exit releases ownership and documented cleanup succeeds; never use progress as lock authority.'
    capture: evidence/coverage-lifecycle.retain.interrupted-producer
  timeout_minutes: 2
  requires:
  - baseline.retention
  - retain.active.restore
- id: final.regression
  phase: final
  action: With all injections disabled rerun native numeric half and four-property half cases, current accepted
    Ticket suite where applicable; compare fixed hit sets and independent verdicts. Preserve earlier failures.
  checks:
  - id: final.regression
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: With all injections disabled rerun native numeric half and four-property half cases, current accepted
      Ticket suite where applicable; compare fixed hit sets and independent verdicts. Preserve earlier failures.
    expected: With all injections disabled rerun native numeric half and four-property half cases, current accepted
      Ticket suite where applicable; compare fixed hit sets and independent verdicts. Preserve earlier failures.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: With all injections disabled rerun native numeric half
      and four-property half cases, current accepted Ticket suite where applicable; compare fixed hit sets and independent
      verdicts. Preserve earlier failures.'
    capture: evidence/coverage-lifecycle.final.regression
  timeout_minutes: 15
  requires:
  - setup.ready
- id: final.analysis
  phase: final
  action: Run the real configured Analyst on the exact fresh final.regression Campaign. Verify valid advisory report,
    correct closure and byte-identical source, Criteria and approvals.
  checks:
  - id: final.analysis
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Run the real configured Analyst on the exact fresh final.regression Campaign. Verify valid advisory
      report, correct closure and byte-identical source, Criteria and approvals.
    expected: Run the real configured Analyst on the exact fresh final.regression Campaign. Verify valid advisory
      report, correct closure and byte-identical source, Criteria and approvals.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run the real configured Analyst on the exact fresh final.regression
      Campaign. Verify valid advisory report, correct closure and byte-identical source, Criteria and approvals.'
    capture: evidence/coverage-lifecycle.final.analysis
  timeout_minutes: 4
  requires:
  - final.regression
- id: final.archive
  phase: final
  action: Verify every selected Check has immutable, hash-indexed evidence outside mutable/pruned state; record
    failures, blocked work and restoration separately.
  checks:
  - id: final.archive
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Verify every selected Check has immutable, hash-indexed evidence outside mutable/pruned state; record
      failures, blocked work and restoration separately.
    expected: Verify every selected Check has immutable, hash-indexed evidence outside mutable/pruned state; record
      failures, blocked work and restoration separately.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Verify every selected Check has immutable, hash-indexed
      evidence outside mutable/pruned state; record failures, blocked work and restoration separately.'
    capture: evidence/coverage-lifecycle.final.archive
  timeout_minutes: 5
- id: cleanup.processes
  phase: cleanup
  action: Stop and reap owned model clients, MCP servers, simulators and controller process groups; prove no descendants
    or fault handles remain.
  checks:
  - id: cleanup.processes
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Stop and reap owned model clients, MCP servers, simulators and controller process groups; prove no
      descendants or fault handles remain.
    expected: Stop and reap owned model clients, MCP servers, simulators and controller process groups; prove no
      descendants or fault handles remain.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Stop and reap owned model clients, MCP servers, simulators
      and controller process groups; prove no descendants or fault handles remain.'
    capture: evidence/coverage-lifecycle.cleanup.processes
  timeout_minutes: 5
- id: cleanup.runtime
  phase: cleanup
  action: Release owned Session Runtimes, attachments and Project inventory entries through product routes; preserve
    borrowed entries.
  checks:
  - id: cleanup.runtime
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Release owned Session Runtimes, attachments and Project inventory entries through product routes;
      preserve borrowed entries.
    expected: Release owned Session Runtimes, attachments and Project inventory entries through product routes;
      preserve borrowed entries.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Release owned Session Runtimes, attachments and Project
      inventory entries through product routes; preserve borrowed entries.'
    capture: evidence/coverage-lifecycle.cleanup.runtime
  timeout_minutes: 5
- id: cleanup.faults
  phase: cleanup
  action: Remove owned temporary executable wrappers, redirection variables, library injection, altered permissions
    and mounts; borrowed executable/credential bytes unchanged.
  checks:
  - id: cleanup.faults
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Remove owned temporary executable wrappers, redirection variables, library injection, altered permissions
      and mounts; borrowed executable/credential bytes unchanged.
    expected: Remove owned temporary executable wrappers, redirection variables, library injection, altered permissions
      and mounts; borrowed executable/credential bytes unchanged.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Remove owned temporary executable wrappers, redirection
      variables, library injection, altered permissions and mounts; borrowed executable/credential bytes unchanged.'
    capture: evidence/coverage-lifecycle.cleanup.faults
  timeout_minutes: 5
- id: cleanup.git
  phase: cleanup
  action: Release or retain only eligible inert owned Project/Ticket/worktree/branch state with exact identity,
    review owner, bounded location and deletion instructions.
  checks:
  - id: cleanup.git
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Release or retain only eligible inert owned Project/Ticket/worktree/branch state with exact identity,
      review owner, bounded location and deletion instructions.
    expected: Release or retain only eligible inert owned Project/Ticket/worktree/branch state with exact identity,
      review owner, bounded location and deletion instructions.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Release or retain only eligible inert owned Project/Ticket/worktree/branch
      state with exact identity, review owner, bounded location and deletion instructions.'
    capture: evidence/coverage-lifecycle.cleanup.git
  timeout_minutes: 5
- id: cleanup.storage
  phase: cleanup
  action: Release owned ephemeral volumes and scarce resources; preserve shared images/caches and archive records;
    eligible inert review state has an explicit ledger disposition.
  checks:
  - id: cleanup.storage
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Release owned ephemeral volumes and scarce resources; preserve shared images/caches and archive records;
      eligible inert review state has an explicit ledger disposition.
    expected: Release owned ephemeral volumes and scarce resources; preserve shared images/caches and archive records;
      eligible inert review state has an explicit ledger disposition.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Release owned ephemeral volumes and scarce resources;
      preserve shared images/caches and archive records; eligible inert review state has an explicit ledger disposition.'
    capture: evidence/coverage-lifecycle.cleanup.storage
  timeout_minutes: 5
- id: cleanup.records
  phase: cleanup
  action: Record every resource disposition and every selected Check result. Required cleanup failure prevents pass;
    unchanged remotes, no external publication. Deadline means incomplete missing work, never inferred success.
  checks:
  - id: cleanup.records
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Record every resource disposition and every selected Check result. Required cleanup failure prevents
      pass; unchanged remotes, no external publication. Deadline means incomplete missing work, never inferred success.
    expected: Record every resource disposition and every selected Check result. Required cleanup failure prevents
      pass; unchanged remotes, no external publication. Deadline means incomplete missing work, never inferred success.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Record every resource disposition and every selected Check
      result. Required cleanup failure prevents pass; unchanged remotes, no external publication. Deadline means
      incomplete missing work, never inferred success.'
    capture: evidence/coverage-lifecycle.cleanup.records
  timeout_minutes: 5
