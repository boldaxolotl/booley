# P-08 disposable probes

`riscv-tools`: inspect Session Runtime GCC/srec_cat/dtc/Spike and offline specs → declared Session Image programs ready. `riscv-firmware`: pre-run real firmware build and Spike use → fresh named firmware and expected execution evidence.

Evidence and recovery: Storage proof, versions, compile and execution logs, firmware digest; link existing Session Image recovery checks in this Scenario.
