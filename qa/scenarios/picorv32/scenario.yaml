format_version: 1
scenario_id: picorv32-published-demo-continuity
title: PicoRV32 published demo continuity
source: https://github.com/boldaxolotl/booley/issues/376#issuecomment-5596872672
inputs:
- id: release
  kind: pre-run
  value: |-
    Exact published booley-rtl release, package SHA-256, matching docs and immutable runtime image digest; no editable/local/development package or source imports.
  source: https://github.com/boldaxolotl/booley/releases
  verification: Record version, executable/import origin, package hash and image payload provenance before work.
- id: project
  kind: git
  value: b8fe2370cb9aa7d93617850169f42f07821865d6
  source: https://github.com/boldaxolotl/booley-prj-picorv32/tree/b8fe2370cb9aa7d93617850169f42f07821865d6
  verification: Verify exact object, clean source and per-file hashes; retain before preparation.
- id: upstream
  kind: git
  value: a473fc8fca393771d83b0ffcf0b14db3393339d8
  source: https://github.com/YosysHQ/picorv32/tree/a473fc8fca393771d83b0ffcf0b14db3393339d8
  verification: Verify exact object, clean source and per-file hashes; retain before preparation.
authority:
- |-
  Act only inside declared run-owned Projects, branches, fixture roots and runtimes; ledger every resource before mutation.
- |-
  Baseline source pins are immutable. Later edits require the attached prompt/Ticket scope; retain accepted Ticket outputs and discard temporary faults and Interactive experiments as prescribed.
- |-
  No external push, issue submission or permanent Booley repair. No source inspection before observing a product failure. No private internals as product proof.
- |-
  Keep credentials outside Projects and evidence; preserve borrowed installations, credentials, shared images/caches and unrelated state.
- |-
  Recovery stays within this live run and phase allowance; no coordinator resume or replay. Retain failures, classify missing work blocked and enter cleanup on time.
budget:
  deadline_minutes: 480
  cleanup_minutes: 20
  contingency_minutes: 15
  cleanup_start_minutes: 460
phases:
- id: prepare
  title: Preparation, host/admin probes and Doctor
  minutes: 50
- id: baseline
  title: Clean demo baseline and provisioned Linux Vivado
  minutes: 60
- id: interactive
  title: Interactive Mode, waveform and allocated inventory probes
  minutes: 45
- id: create
  title: Both Ticket Create calls and authoring checks
  minutes: 20
- id: ticket1
  title: Ticket 1 and its negative/recovery exercises
  minutes: 60
- id: ticket2
  title: Ticket 2, remaining allocated checks and complete final regression
  minutes: 210
- id: cleanup
  title: Cleanup
  minutes: 20
steps:
- id: inputs.project-pin
  phase: prepare
  action: |-
    Prepare fresh `boldaxolotl/booley-prj-picorv32@b8fe2370cb9aa7d93617850169f42f07821865d6`; prove exact clean Project checkout.
  checks:
  - id: inputs.project-pin
    capabilities:
    - D-03
    stimulus: |-
      Prepare fresh `boldaxolotl/booley-prj-picorv32@b8fe2370cb9aa7d93617850169f42f07821865d6`; prove exact clean Project checkout.
    expected: |-
      Prepare fresh `boldaxolotl/booley-prj-picorv32@b8fe2370cb9aa7d93617850169f42f07821865d6`; prove exact clean Project checkout.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Commit identity, clean status and initial tree hashes before initialization.
    capture: evidence/picorv32-published-demo-continuity.inputs.project-pin
- id: inputs.upstream-pin
  phase: prepare
  action: Prepare `YosysHQ/picorv32@a473fc8fca393771d83b0ffcf0b14db3393339d8`; prove exact clean upstream identity.
  checks:
  - id: inputs.upstream-pin
    capabilities:
    - D-03
    stimulus: Prepare `YosysHQ/picorv32@a473fc8fca393771d83b0ffcf0b14db3393339d8`; prove exact clean upstream identity.
    expected: Prepare `YosysHQ/picorv32@a473fc8fca393771d83b0ffcf0b14db3393339d8`; prove exact clean upstream identity.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Commit identity and source hashes before preparation.
    capture: evidence/picorv32-published-demo-continuity.inputs.upstream-pin
  requires:
  - inputs.project-pin
- id: inputs.release
  phase: prepare
  action: Install exact published release; forbid floating tags, editable/local/development installs and source-checkout
    imports.
  checks:
  - id: inputs.release
    capabilities:
    - H-01
    stimulus: Install exact published release; forbid floating tags, editable/local/development installs and source-checkout
      imports.
    expected: Install exact published release; forbid floating tags, editable/local/development installs and source-checkout
      imports.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Release/package hash, release-doc identity, runtime import origin and selected image digest in run
      inputs.
    capture: evidence/picorv32-published-demo-continuity.inputs.release
  requires:
  - inputs.project-pin
- id: inputs.authority
  phase: prepare
  action: |-
    Freeze provider/platform, eight-hour deadline, credentials mechanism without values, authorized operations and resource ownership before work.
  checks:
  - id: inputs.authority
    capabilities:
    - ST-04
    stimulus: |-
      Freeze provider/platform, eight-hour deadline, credentials mechanism without values, authorized operations and resource ownership before work.
    expected: |-
      Freeze provider/platform, eight-hour deadline, credentials mechanism without values, authorized operations and resource ownership before work.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Run declaration, capability probes and initial resource ledger.
    capture: evidence/picorv32-published-demo-continuity.inputs.authority
  requires:
  - inputs.project-pin
- id: project.separate-repository
  phase: prepare
  action: Create a separate clean `.booley_project` repository and initialize the prepared Project.
  checks:
  - id: project.separate-repository
    capabilities:
    - D-03
    stimulus: Create a separate clean `.booley_project` repository and initialize the prepared Project.
    expected: Create a separate clean `.booley_project` repository and initialize the prepared Project.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Repository topology, clean status, initialization output; frozen prepared recovery point.
    capture: evidence/picorv32-published-demo-continuity.project.separate-repository
  requires:
  - inputs.project-pin
- id: stealth.enabled
  phase: prepare
  action: Enable Stealth and set `ignore_native_cores = true`.
  checks:
  - id: stealth.enabled
    capabilities:
    - ST-01
    stimulus: Enable Stealth and set `ignore_native_cores = true`.
    expected: Enable Stealth and set `ignore_native_cores = true`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Effective config and runtime configuration evidence.
    capture: evidence/picorv32-published-demo-continuity.stealth.enabled
  requires:
  - inputs.project-pin
- id: stealth.hidden-projection
  phase: prepare
  action: Exercise hidden authored cores; only these may be projected.
  checks:
  - id: stealth.hidden-projection
    capabilities:
    - ST-01
    stimulus: Exercise hidden authored cores; only these may be projected.
    expected: Exercise hidden authored cores; only these may be projected.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Before/after projection inventory and resolved sources.
    capture: evidence/picorv32-published-demo-continuity.stealth.hidden-projection
  requires:
  - inputs.project-pin
- id: stealth.native-no-copy
  phase: prepare
  action: 'Inspect ignored native projections: no copied RTL may exist.'
  checks:
  - id: stealth.native-no-copy
    capabilities:
    - ST-01
    stimulus: 'Inspect ignored native projections: no copied RTL may exist.'
    expected: 'Inspect ignored native projections: no copied RTL may exist.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Projection paths and source/file inventory.
    capture: evidence/picorv32-published-demo-continuity.stealth.native-no-copy
  requires:
  - inputs.project-pin
- id: stealth.native-no-symlink
  phase: prepare
  action: 'Inspect ignored native projections: no symlinks may exist.'
  checks:
  - id: stealth.native-no-symlink
    capabilities:
    - ST-01
    stimulus: 'Inspect ignored native projections: no symlinks may exist.'
    expected: 'Inspect ignored native projections: no symlinks may exist.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Filesystem link inventory independent of copied-file test.
    capture: evidence/picorv32-published-demo-continuity.stealth.native-no-symlink
  requires:
  - inputs.project-pin
- id: stealth.commit-sanitation
  phase: prepare
  action: Exercise controlled banned-word commit sanitation.
  checks:
  - id: stealth.commit-sanitation
    capabilities:
    - ST-01
    - ST-03
    stimulus: Exercise controlled banned-word commit sanitation.
    expected: Exercise controlled banned-word commit sanitation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: |-
      Input commit text, hook invocation/result and resulting sanitized commit text; exact case to derive from public contract.
    capture: evidence/picorv32-published-demo-continuity.stealth.commit-sanitation
  requires:
  - inputs.project-pin
  assets:
  - path: probes/stealth.md
    audience: coordinator
    sha256: 1edf9faecf1bb1b0921eb6616a3e0dfe3bb903a45d8aaa7264d8f9b8931a6f0c
- id: stealth.attribution-removal
  phase: prepare
  action: Exercise attribution-trailer removal.
  checks:
  - id: stealth.attribution-removal
    capabilities:
    - ST-01
    - ST-03
    stimulus: Exercise attribution-trailer removal.
    expected: Exercise attribution-trailer removal.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Original and resulting commit messages with trailer comparison.
    capture: evidence/picorv32-published-demo-continuity.stealth.attribution-removal
  requires:
  - inputs.project-pin
  assets:
  - path: probes/stealth.md
    audience: coordinator
    sha256: 1edf9faecf1bb1b0921eb6616a3e0dfe3bb903a45d8aaa7264d8f9b8931a6f0c
- id: doctor.first-product-exercise
  phase: prepare
  action: Initialization/host preparation are setup; plain Doctor is the first Booley product exercise.
  checks:
  - id: doctor.first-product-exercise
    capabilities:
    - H-10
    stimulus: Initialization/host preparation are setup; plain Doctor is the first Booley product exercise.
    expected: Initialization/host preparation are setup; plain Doctor is the first Booley product exercise.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Ordered command record through first Doctor invocation.
    capture: evidence/picorv32-published-demo-continuity.doctor.first-product-exercise
  requires:
  - inputs.project-pin
- id: doctor.plain
  phase: prepare
  action: Run plain Doctor; require warning-free result.
  checks:
  - id: doctor.plain
    capabilities:
    - H-10
    stimulus: Run plain Doctor; require warning-free result.
    expected: Run plain Doctor; require warning-free result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Doctor report and underlying diagnostics.
    capture: evidence/picorv32-published-demo-continuity.doctor.plain
  requires:
  - inputs.project-pin
- id: doctor.deep
  phase: prepare
  action: Run deep Doctor after plain Doctor; require warning-free result.
  checks:
  - id: doctor.deep
    capabilities:
    - H-10
    stimulus: Run deep Doctor after plain Doctor; require warning-free result.
    expected: Run deep Doctor after plain Doctor; require warning-free result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Deep report and all selected probes.
    capture: evidence/picorv32-published-demo-continuity.doctor.deep
  requires:
  - inputs.project-pin
- id: doctor.plain-recheck
  phase: prepare
  action: Run final plain Doctor; require warning-free result before baseline.
  checks:
  - id: doctor.plain-recheck
    capabilities:
    - H-10
    stimulus: Run final plain Doctor; require warning-free result before baseline.
    expected: Run final plain Doctor; require warning-free result before baseline.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Recheck report; warning-free Doctor recovery point.
    capture: evidence/picorv32-published-demo-continuity.doctor.plain-recheck
  requires:
  - inputs.project-pin
- id: inventory.H-01.prerequisite
  phase: prepare
  action: |-
    Execute `prerequisite` using the attached scenario-owned probe contract.
    `install`: install exact published `booley-rtl` in isolated pipx environment and launch from new shell → metadata, executable, package provenance and version agree. `prerequisite`: omit/age one prerequisite in disposable host fixture → named rejection before mutation. `pip-distro`: on applicable externally-managed distro attempt documented pip route → documented rejection.
  checks:
  - id: inventory.H-01.prerequisite
    capabilities:
    - H-01
    stimulus: |-
      Execute `prerequisite` using the attached scenario-owned probe contract.
      `install`: install exact published `booley-rtl` in isolated pipx environment and launch from new shell → metadata, executable, package provenance and version agree. `prerequisite`: omit/age one prerequisite in disposable host fixture → named rejection before mutation. `pip-distro`: on applicable externally-managed distro attempt documented pip route → documented rejection.
    expected: |-
      Evaluate only the independently named `prerequisite` outcome in this contract.
      `install`: install exact published `booley-rtl` in isolated pipx environment and launch from new shell → metadata, executable, package provenance and version agree. `prerequisite`: omit/age one prerequisite in disposable host fixture → named rejection before mutation. `pip-distro`: on applicable externally-managed distro attempt documented pip route → documented rejection.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/README.md#installation
    evidence: |-
      Package metadata, executable resolution, pre/post owned-state snapshots. `prerequisite-recovery`: supply prerequisite and repeat readiness → success without orphan state. `pip-recovery`: use documented isolated route → same declared release ready. PicoRV32 separately records at least 21 GB Docker free space plus artifact headroom before RISC-V provisioning. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-01.prerequisite
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-01.md
    audience: coordinator
    sha256: b663e6e3bf0a36bac2fe8035aaa791da465d35886cf72a48bfee1ce0aedf5a9d
    base: shared
- id: inventory.H-01.prerequisite-recovery
  phase: prepare
  action: |-
    Execute `prerequisite-recovery` using the attached scenario-owned probe contract.
    `install`: install exact published `booley-rtl` in isolated pipx environment and launch from new shell → metadata, executable, package provenance and version agree. `prerequisite`: omit/age one prerequisite in disposable host fixture → named rejection before mutation. `pip-distro`: on applicable externally-managed distro attempt documented pip route → documented rejection.
  checks:
  - id: inventory.H-01.prerequisite-recovery
    capabilities:
    - H-01
    stimulus: |-
      Execute `prerequisite-recovery` using the attached scenario-owned probe contract.
      `install`: install exact published `booley-rtl` in isolated pipx environment and launch from new shell → metadata, executable, package provenance and version agree. `prerequisite`: omit/age one prerequisite in disposable host fixture → named rejection before mutation. `pip-distro`: on applicable externally-managed distro attempt documented pip route → documented rejection.
    expected: |-
      Evaluate only the independently named `prerequisite-recovery` outcome in this contract.
      `install`: install exact published `booley-rtl` in isolated pipx environment and launch from new shell → metadata, executable, package provenance and version agree. `prerequisite`: omit/age one prerequisite in disposable host fixture → named rejection before mutation. `pip-distro`: on applicable externally-managed distro attempt documented pip route → documented rejection.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/README.md#installation
    evidence: |-
      Package metadata, executable resolution, pre/post owned-state snapshots. `prerequisite-recovery`: supply prerequisite and repeat readiness → success without orphan state. `pip-recovery`: use documented isolated route → same declared release ready. PicoRV32 separately records at least 21 GB Docker free space plus artifact headroom before RISC-V provisioning. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-01.prerequisite-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-01.md
    audience: coordinator
    sha256: b663e6e3bf0a36bac2fe8035aaa791da465d35886cf72a48bfee1ce0aedf5a9d
    base: shared
- id: inventory.H-03.init-repeat
  phase: prepare
  action: |-
    Execute `init-repeat` using the attached scenario-owned probe contract.
    `init-repeat`: repeat plain init → no unintended file/spec drift. `init-check`: check-only on stale disposable state → reports work without mutation. `init-force`: force reconcile owned generated state → issued configuration restored, user sentinel intact. `seed`: init with declared seed → seeded policy/config appears. `skip-credentials`: initialize with skip flag → no credential mutation. `location-guard`: invoke host-only init inside runtime → actionable rejection. `provider`: select each provider in separate declared compatibility run → issued auth/client policy matches selection.
  checks:
  - id: inventory.H-03.init-repeat
    capabilities:
    - H-03
    stimulus: |-
      Execute `init-repeat` using the attached scenario-owned probe contract.
      `init-repeat`: repeat plain init → no unintended file/spec drift. `init-check`: check-only on stale disposable state → reports work without mutation. `init-force`: force reconcile owned generated state → issued configuration restored, user sentinel intact. `seed`: init with declared seed → seeded policy/config appears. `skip-credentials`: initialize with skip flag → no credential mutation. `location-guard`: invoke host-only init inside runtime → actionable rejection. `provider`: select each provider in separate declared compatibility run → issued auth/client policy matches selection.
    expected: |-
      Evaluate only the independently named `init-repeat` outcome in this contract.
      `init-repeat`: repeat plain init → no unintended file/spec drift. `init-check`: check-only on stale disposable state → reports work without mutation. `init-force`: force reconcile owned generated state → issued configuration restored, user sentinel intact. `seed`: init with declared seed → seeded policy/config appears. `skip-credentials`: initialize with skip flag → no credential mutation. `location-guard`: invoke host-only init inside runtime → actionable rejection. `provider`: select each provider in separate declared compatibility run → issued auth/client policy matches selection.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/SETUP.md#initialize-the-project--host
    evidence: |-
      Generated config/devcontainer, sanitized policy, hashes. Windows: `lf-auto` introduces clean CRLF fixture → local Git policy and LF reconciliation; `lf-compat` repeats with compatibility flag → accepted; `lf-dirty`, `lf-protected`, `lf-hardlink` each introduces named unsafe file class → refusal without changed bytes. `lf-recovery`: remove unsafe fixture condition and retry → normalized safe file. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-03.init-repeat
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-03.md
    audience: coordinator
    sha256: 469e9b533c14efc26fe00c3f1dae0c4d16accd3ed7dff080768b79c190eb7ac8
    base: shared
- id: inventory.H-03.provider
  phase: prepare
  action: |-
    Execute `provider` using the attached scenario-owned probe contract.
    `init-repeat`: repeat plain init → no unintended file/spec drift. `init-check`: check-only on stale disposable state → reports work without mutation. `init-force`: force reconcile owned generated state → issued configuration restored, user sentinel intact. `seed`: init with declared seed → seeded policy/config appears. `skip-credentials`: initialize with skip flag → no credential mutation. `location-guard`: invoke host-only init inside runtime → actionable rejection. `provider`: select each provider in separate declared compatibility run → issued auth/client policy matches selection.
  checks:
  - id: inventory.H-03.provider
    capabilities:
    - H-03
    stimulus: |-
      Execute `provider` using the attached scenario-owned probe contract.
      `init-repeat`: repeat plain init → no unintended file/spec drift. `init-check`: check-only on stale disposable state → reports work without mutation. `init-force`: force reconcile owned generated state → issued configuration restored, user sentinel intact. `seed`: init with declared seed → seeded policy/config appears. `skip-credentials`: initialize with skip flag → no credential mutation. `location-guard`: invoke host-only init inside runtime → actionable rejection. `provider`: select each provider in separate declared compatibility run → issued auth/client policy matches selection.
    expected: |-
      Evaluate only the independently named `provider` outcome in this contract.
      `init-repeat`: repeat plain init → no unintended file/spec drift. `init-check`: check-only on stale disposable state → reports work without mutation. `init-force`: force reconcile owned generated state → issued configuration restored, user sentinel intact. `seed`: init with declared seed → seeded policy/config appears. `skip-credentials`: initialize with skip flag → no credential mutation. `location-guard`: invoke host-only init inside runtime → actionable rejection. `provider`: select each provider in separate declared compatibility run → issued auth/client policy matches selection.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/SETUP.md#initialize-the-project--host
    evidence: |-
      Generated config/devcontainer, sanitized policy, hashes. Windows: `lf-auto` introduces clean CRLF fixture → local Git policy and LF reconciliation; `lf-compat` repeats with compatibility flag → accepted; `lf-dirty`, `lf-protected`, `lf-hardlink` each introduces named unsafe file class → refusal without changed bytes. `lf-recovery`: remove unsafe fixture condition and retry → normalized safe file. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-03.provider
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-03.md
    audience: coordinator
    sha256: 469e9b533c14efc26fe00c3f1dae0c4d16accd3ed7dff080768b79c190eb7ac8
    base: shared
- id: inventory.H-07.forget-granted
  phase: prepare
  action: |-
    Execute `forget-granted` using the attached scenario-owned probe contract.
    `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
  checks:
  - id: inventory.H-07.forget-granted
    capabilities:
    - H-07
    stimulus: |-
      Execute `forget-granted` using the attached scenario-owned probe contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    expected: |-
      Evaluate only the independently named `forget-granted` outcome in this contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#cli-reference
    evidence: |-
      Inventory snapshots and import tree; restore moved root before ledger cleanup, foreign roots unchanged. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-07.forget-granted
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-07.md
    audience: coordinator
    sha256: 92223e0432afa7ee0b4f3928128414b1475935677f8342c3f0ea8c40cbd36fef
- id: inventory.H-07.forget-revoked
  phase: prepare
  action: |-
    Execute `forget-revoked` using the attached scenario-owned probe contract.
    `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
  checks:
  - id: inventory.H-07.forget-revoked
    capabilities:
    - H-07
    stimulus: |-
      Execute `forget-revoked` using the attached scenario-owned probe contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    expected: |-
      Evaluate only the independently named `forget-revoked` outcome in this contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#cli-reference
    evidence: |-
      Inventory snapshots and import tree; restore moved root before ledger cleanup, foreign roots unchanged. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-07.forget-revoked
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-07.md
    audience: coordinator
    sha256: 92223e0432afa7ee0b4f3928128414b1475935677f8342c3f0ea8c40cbd36fef
- id: inventory.H-07.inventory-import
  phase: prepare
  action: |-
    Execute `inventory-import` using the attached scenario-owned probe contract.
    `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
  checks:
  - id: inventory.H-07.inventory-import
    capabilities:
    - H-07
    stimulus: |-
      Execute `inventory-import` using the attached scenario-owned probe contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    expected: |-
      Evaluate only the independently named `inventory-import` outcome in this contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#cli-reference
    evidence: |-
      Inventory snapshots and import tree; restore moved root before ledger cleanup, foreign roots unchanged. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-07.inventory-import
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-07.md
    audience: coordinator
    sha256: 92223e0432afa7ee0b4f3928128414b1475935677f8342c3f0ea8c40cbd36fef
- id: inventory.H-07.inventory-missing
  phase: prepare
  action: |-
    Execute `inventory-missing` using the attached scenario-owned probe contract.
    `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
  checks:
  - id: inventory.H-07.inventory-missing
    capabilities:
    - H-07
    stimulus: |-
      Execute `inventory-missing` using the attached scenario-owned probe contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    expected: |-
      Evaluate only the independently named `inventory-missing` outcome in this contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#cli-reference
    evidence: |-
      Inventory snapshots and import tree; restore moved root before ledger cleanup, foreign roots unchanged. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-07.inventory-missing
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-07.md
    audience: coordinator
    sha256: 92223e0432afa7ee0b4f3928128414b1475935677f8342c3f0ea8c40cbd36fef
- id: inventory.H-07.inventory-views
  phase: prepare
  action: |-
    Execute `inventory-views` using the attached scenario-owned probe contract.
    `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
  checks:
  - id: inventory.H-07.inventory-views
    capabilities:
    - H-07
    stimulus: |-
      Execute `inventory-views` using the attached scenario-owned probe contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    expected: |-
      Evaluate only the independently named `inventory-views` outcome in this contract.
      `inventory-import`: import bounded disposable root containing directory symlink → only explicitly bounded real roots remembered. `inventory-views`: list human/JSON → same exact roots/Grants. `inventory-missing`: move owned root aside → missing root retained. `forget-granted`: forget granted root → rejected. `forget-revoked`: revoke then forget → exactly that root removed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#cli-reference
    evidence: |-
      Inventory snapshots and import tree; restore moved root before ledger cleanup, foreign roots unchanged. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-07.inventory-views
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-07.md
    audience: coordinator
    sha256: 92223e0432afa7ee0b4f3928128414b1475935677f8342c3f0ea8c40cbd36fef
- id: inventory.H-10.doctor-recovery
  phase: prepare
  action: |-
    Execute `doctor-recovery` using the attached scenario-owned probe contract.
    `doctor-plain`, `doctor-deep`: run separately → current structured last.json and complete last.log, intended selected checks. `doctor-warning`: benign warning fixture → actionable warning. `waiver-valid`: add explicit unexpired matching waiver → warning waived. `waiver-expired`, `waiver-stale`: expire/change warning identity separately → waiver no longer applies. `failure-not-waivable`: hard config failure with waiver → remains failure. `health-stale`: change config → freshness reports stale. `doctor-recovery`: restore config/run Doctor → fresh clean health.
  checks:
  - id: inventory.H-10.doctor-recovery
    capabilities:
    - H-10
    stimulus: |-
      Execute `doctor-recovery` using the attached scenario-owned probe contract.
      `doctor-plain`, `doctor-deep`: run separately → current structured last.json and complete last.log, intended selected checks. `doctor-warning`: benign warning fixture → actionable warning. `waiver-valid`: add explicit unexpired matching waiver → warning waived. `waiver-expired`, `waiver-stale`: expire/change warning identity separately → waiver no longer applies. `failure-not-waivable`: hard config failure with waiver → remains failure. `health-stale`: change config → freshness reports stale. `doctor-recovery`: restore config/run Doctor → fresh clean health.
    expected: |-
      Evaluate only the independently named `doctor-recovery` outcome in this contract.
      `doctor-plain`, `doctor-deep`: run separately → current structured last.json and complete last.log, intended selected checks. `doctor-warning`: benign warning fixture → actionable warning. `waiver-valid`: add explicit unexpired matching waiver → warning waived. `waiver-expired`, `waiver-stale`: expire/change warning identity separately → waiver no longer applies. `failure-not-waivable`: hard config failure with waiver → remains failure. `health-stale`: change config → freshness reports stale. `doctor-recovery`: restore config/run Doctor → fresh clean health.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#first-verify-your-setup
    evidence: |-
      Separate report/log copies before overwrite, waiver text with expiry, config diffs. Deep Doctor retains manually suggested FPGA command rather than silently claiming FPGA execution. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-10.doctor-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-10.md
    audience: coordinator
    sha256: 822c9c0317d8821ea85469e752a3efb5a5b5acdd62a8904f0774dba6eb360c9f
    base: shared
- id: inventory.P-08.riscv-firmware
  phase: prepare
  action: |-
    Execute `riscv-firmware` using the attached scenario-owned probe contract.
    `riscv-tools`: inspect runtime GCC/srec_cat/dtc/Spike and offline specs → declared supported image tools ready. `riscv-firmware`: pre-run real firmware build and Spike use → fresh named firmware and expected execution evidence.
  checks:
  - id: inventory.P-08.riscv-firmware
    capabilities:
    - P-08
    stimulus: |-
      Execute `riscv-firmware` using the attached scenario-owned probe contract.
      `riscv-tools`: inspect runtime GCC/srec_cat/dtc/Spike and offline specs → declared supported image tools ready. `riscv-firmware`: pre-run real firmware build and Spike use → fresh named firmware and expected execution evidence.
    expected: |-
      Evaluate only the independently named `riscv-firmware` outcome in this contract.
      `riscv-tools`: inspect runtime GCC/srec_cat/dtc/Spike and offline specs → declared supported image tools ready. `riscv-firmware`: pre-run real firmware build and Spike use → fresh named firmware and expected execution evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#risc-v-toolchain-image-booley-sandbox-riscv
    evidence: |-
      Storage proof, versions, compile and execution logs, firmware digest; link existing journey image recovery checks. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-08.riscv-firmware
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-08.md
    audience: coordinator
    sha256: b019248fc24af8592e6298f2aa8de5150aab5a3a212623db5cd8c4b9c16e1a82
- id: inventory.P-08.riscv-tools
  phase: prepare
  action: |-
    Execute `riscv-tools` using the attached scenario-owned probe contract.
    `riscv-tools`: inspect runtime GCC/srec_cat/dtc/Spike and offline specs → declared supported image tools ready. `riscv-firmware`: pre-run real firmware build and Spike use → fresh named firmware and expected execution evidence.
  checks:
  - id: inventory.P-08.riscv-tools
    capabilities:
    - P-08
    stimulus: |-
      Execute `riscv-tools` using the attached scenario-owned probe contract.
      `riscv-tools`: inspect runtime GCC/srec_cat/dtc/Spike and offline specs → declared supported image tools ready. `riscv-firmware`: pre-run real firmware build and Spike use → fresh named firmware and expected execution evidence.
    expected: |-
      Evaluate only the independently named `riscv-tools` outcome in this contract.
      `riscv-tools`: inspect runtime GCC/srec_cat/dtc/Spike and offline specs → declared supported image tools ready. `riscv-firmware`: pre-run real firmware build and Spike use → fresh named firmware and expected execution evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#risc-v-toolchain-image-booley-sandbox-riscv
    evidence: |-
      Storage proof, versions, compile and execution logs, firmware digest; link existing journey image recovery checks. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-08.riscv-tools
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-08.md
    audience: coordinator
    sha256: b019248fc24af8592e6298f2aa8de5150aab5a3a212623db5cd8c4b9c16e1a82
- id: inventory.ST-01.stealth-history
  phase: prepare
  action: |-
    Execute `stealth-history` using the attached scenario-owned probe contract.
    `stealth-explicit`: inspect setup config → explicit selected value. PicoRV32 `stealth-history` → sanitized commit prose. UART `nonstealth-history` → verbatim benign commit prose and ordinary cores. UART `stealth-legacy`: omitted-key disposable fixture → documented legacy sanitization fallback, never non-stealth credit.
  checks:
  - id: inventory.ST-01.stealth-history
    capabilities:
    - ST-01
    stimulus: |-
      Execute `stealth-history` using the attached scenario-owned probe contract.
      `stealth-explicit`: inspect setup config → explicit selected value. PicoRV32 `stealth-history` → sanitized commit prose. UART `nonstealth-history` → verbatim benign commit prose and ordinary cores. UART `stealth-legacy`: omitted-key disposable fixture → documented legacy sanitization fallback, never non-stealth credit.
    expected: |-
      Evaluate only the independently named `stealth-history` outcome in this contract.
      `stealth-explicit`: inspect setup config → explicit selected value. PicoRV32 `stealth-history` → sanitized commit prose. UART `nonstealth-history` → verbatim benign commit prose and ordinary cores. UART `stealth-legacy`: omitted-key disposable fixture → documented legacy sanitization fallback, never non-stealth credit.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#stealth-mode-stealth
    evidence: |-
      Config/history and Project state paths; no private identifiers. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-01.stealth-history
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-01.md
    audience: coordinator
    sha256: b634d06ad369245303a693ff6928887fb0f92ddd008c5a8672e689f2c558ce82
    base: shared
  - path: probes/stealth.md
    audience: coordinator
    sha256: 1edf9faecf1bb1b0921eb6616a3e0dfe3bb903a45d8aaa7264d8f9b8931a6f0c
- id: inventory.ST-02.dual-repo
  phase: prepare
  action: |-
    Execute `dual-repo` using the attached scenario-owned probe contract.
    `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
  checks:
  - id: inventory.ST-02.dual-repo
    capabilities:
    - ST-02
    stimulus: |-
      Execute `dual-repo` using the attached scenario-owned probe contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    expected: |-
      Evaluate only the independently named `dual-repo` outcome in this contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#stealth-mode-stealth
    evidence: |-
      Core paths/content/hash, RTL file metadata and both Git statuses/history; restore fixture after negative clone observation. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-02.dual-repo
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-02.md
    audience: coordinator
    sha256: 106190450019a419e2de8bb69ccdc1b94576439fce9e78a3541dd3d872035ed8
- id: inventory.ST-02.fresh-clone
  phase: prepare
  action: |-
    Execute `fresh-clone` using the attached scenario-owned probe contract.
    `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
  checks:
  - id: inventory.ST-02.fresh-clone
    capabilities:
    - ST-02
    stimulus: |-
      Execute `fresh-clone` using the attached scenario-owned probe contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    expected: |-
      Evaluate only the independently named `fresh-clone` outcome in this contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#stealth-mode-stealth
    evidence: |-
      Core paths/content/hash, RTL file metadata and both Git statuses/history; restore fixture after negative clone observation. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-02.fresh-clone
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-02.md
    audience: coordinator
    sha256: 106190450019a419e2de8bb69ccdc1b94576439fce9e78a3541dd3d872035ed8
- id: inventory.ST-02.native-ignore
  phase: prepare
  action: |-
    Execute `native-ignore` using the attached scenario-owned probe contract.
    `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
  checks:
  - id: inventory.ST-02.native-ignore
    capabilities:
    - ST-02
    stimulus: |-
      Execute `native-ignore` using the attached scenario-owned probe contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    expected: |-
      Evaluate only the independently named `native-ignore` outcome in this contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#stealth-mode-stealth
    evidence: |-
      Core paths/content/hash, RTL file metadata and both Git statuses/history; restore fixture after negative clone observation. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-02.native-ignore
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-02.md
    audience: coordinator
    sha256: 106190450019a419e2de8bb69ccdc1b94576439fce9e78a3541dd3d872035ed8
- id: inventory.ST-02.projected-core
  phase: prepare
  action: |-
    Execute `projected-core` using the attached scenario-owned probe contract.
    `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
  checks:
  - id: inventory.ST-02.projected-core
    capabilities:
    - ST-02
    stimulus: |-
      Execute `projected-core` using the attached scenario-owned probe contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    expected: |-
      Evaluate only the independently named `projected-core` outcome in this contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#stealth-mode-stealth
    evidence: |-
      Core paths/content/hash, RTL file metadata and both Git statuses/history; restore fixture after negative clone observation. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-02.projected-core
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-02.md
    audience: coordinator
    sha256: 106190450019a419e2de8bb69ccdc1b94576439fce9e78a3541dd3d872035ed8
- id: inventory.ST-02.projection-refresh
  phase: prepare
  action: |-
    Execute `projection-refresh` using the attached scenario-owned probe contract.
    `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
  checks:
  - id: inventory.ST-02.projection-refresh
    capabilities:
    - ST-02
    stimulus: |-
      Execute `projection-refresh` using the attached scenario-owned probe contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    expected: |-
      Evaluate only the independently named `projection-refresh` outcome in this contract.
      `projected-core`: authored hidden core → valid projected root core resolves without copied/symlinked RTL. `projection-refresh`: authored core edit+refresh → new projected content. `native-ignore`: invalid native core with ignore_native_cores → valid authored target still resolves. `dual-repo`: inspect outer/inner histories → Project state excluded outside. `fresh-clone`: clean disposable outer clone → documented hidden-state limitation visible.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#stealth-mode-stealth
    evidence: |-
      Core paths/content/hash, RTL file metadata and both Git statuses/history; restore fixture after negative clone observation. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-02.projection-refresh
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-02.md
    audience: coordinator
    sha256: 106190450019a419e2de8bb69ccdc1b94576439fce9e78a3541dd3d872035ed8
- id: baseline.source-unchanged
  phase: baseline
  action: |-
    Build firmware and run continuity with both pinned source trees byte-identical; source changes begin only in later authorized disposable exercises.
  checks:
  - id: baseline.source-unchanged
    capabilities:
    - F-01
    stimulus: |-
      Build firmware and run continuity with both pinned source trees byte-identical; source changes begin only in later authorized disposable exercises.
    expected: |-
      Build firmware and run continuity with both pinned source trees byte-identical; source changes begin only in later authorized disposable exercises.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Pre/post tree identities and diff around the complete baseline.
    capture: evidence/picorv32-published-demo-continuity.baseline.source-unchanged
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: baseline.firmware
  phase: baseline
  action: Prepare demo firmware with recorded RISC-V compiler/image identity.
  checks:
  - id: baseline.firmware
    capabilities:
    - F-01
    stimulus: Prepare demo firmware with recorded RISC-V compiler/image identity.
    expected: Prepare demo firmware with recorded RISC-V compiler/image identity.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Firmware build command/log and image hashes; retain xPack GCC 15.2 calibration context.
    capture: evidence/picorv32-published-demo-continuity.baseline.firmware
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: baseline.targets
  phase: baseline
  action: Inventory the configured Targets before invoking them.
  checks:
  - id: baseline.targets
    capabilities:
    - P-02
    stimulus: Inventory the configured Targets before invoking them.
    expected: Inventory the configured Targets before invoking them.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Identity-bound inventory and exact chosen Target selectors.
    capture: evidence/picorv32-published-demo-continuity.baseline.targets
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: baseline.icarus-main-core
  phase: baseline
  action: Run the pinned unchanged main-core Icarus HDL simulation and require pass.
  checks:
  - id: baseline.icarus-main-core
    capabilities:
    - F-01
    - EDA-icarus-sim
    - EDA-resolve-sim
    stimulus: Run the pinned unchanged main-core Icarus HDL simulation and require pass.
    expected: Run the pinned unchanged main-core Icarus HDL simulation and require pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Normalized grade, actual simulator identity, logs and fresh simulation artifacts.
    capture: evidence/picorv32-published-demo-continuity.baseline.icarus-main-core
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: baseline.icarus-axi
  phase: baseline
  action: Run the pinned unchanged axi Icarus HDL simulation and require pass.
  checks:
  - id: baseline.icarus-axi
    capabilities:
    - F-01
    stimulus: Run the pinned unchanged axi Icarus HDL simulation and require pass.
    expected: Run the pinned unchanged axi Icarus HDL simulation and require pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Normalized grade, actual simulator identity, logs and fresh simulation artifacts.
    capture: evidence/picorv32-published-demo-continuity.baseline.icarus-axi
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: baseline.icarus-wishbone
  phase: baseline
  action: Run the pinned unchanged wishbone Icarus HDL simulation and require pass.
  checks:
  - id: baseline.icarus-wishbone
    capabilities:
    - F-01
    stimulus: Run the pinned unchanged wishbone Icarus HDL simulation and require pass.
    expected: Run the pinned unchanged wishbone Icarus HDL simulation and require pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Normalized grade, actual simulator identity, logs and fresh simulation artifacts.
    capture: evidence/picorv32-published-demo-continuity.baseline.icarus-wishbone
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: baseline.icarus-dhrystone
  phase: baseline
  action: Run the pinned unchanged dhrystone Icarus HDL simulation and require pass.
  checks:
  - id: baseline.icarus-dhrystone
    capabilities:
    - F-01
    stimulus: Run the pinned unchanged dhrystone Icarus HDL simulation and require pass.
    expected: Run the pinned unchanged dhrystone Icarus HDL simulation and require pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Normalized grade, actual simulator identity, logs and fresh simulation artifacts.
    capture: evidence/picorv32-published-demo-continuity.baseline.icarus-dhrystone
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: baseline.verilator-lint
  phase: baseline
  action: Run Verilator lint against the clean published demo; require its passing grade.
  checks:
  - id: baseline.verilator-lint
    capabilities:
    - F-02
    - EDA-verilator-lint
    stimulus: Run Verilator lint against the clean published demo; require its passing grade.
    expected: Run Verilator lint against the clean published demo; require its passing grade.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Target/EDA identity and lint report.
    capture: evidence/picorv32-published-demo-continuity.baseline.verilator-lint
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: baseline.physical-synthesis
  phase: baseline
  action: Run physical sv2v/Yosys/OpenROAD synthesis; preserve physical implementation evidence.
  checks:
  - id: baseline.physical-synthesis
    capabilities:
    - F-03
    - EDA-yosys-physical
    - EDA-sv2v
    - EDA-openroad
    stimulus: Run physical sv2v/Yosys/OpenROAD synthesis; preserve physical implementation evidence.
    expected: Run physical sv2v/Yosys/OpenROAD synthesis; preserve physical implementation evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Fresh synthesis/physical reports, metrics, artifacts and tool identities; edit-free continuity recovery
      point.
    capture: evidence/picorv32-published-demo-continuity.baseline.physical-synthesis
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.version
  phase: baseline
  action: Use exact Vivado 2025.2 x86-64 on canonical Linux.
  checks:
  - id: vivado.version
    capabilities:
    - F-04
    stimulus: Use exact Vivado 2025.2 x86-64 on canonical Linux.
    expected: Use exact Vivado 2025.2 x86-64 on canonical Linux.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Version probes tied to registered installation.
    capture: evidence/picorv32-published-demo-continuity.vivado.version
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.registration
  phase: baseline
  action: Create Installation Registration for borrowed installation.
  checks:
  - id: vivado.registration
    capabilities:
    - F-04
    stimulus: Create Installation Registration for borrowed installation.
    expected: Create Installation Registration for borrowed installation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Registration state and ledger ownership.
    capture: evidence/picorv32-published-demo-continuity.vivado.registration
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.project-grant
  phase: baseline
  action: Create Grant for the exact Project root.
  checks:
  - id: vivado.project-grant
    capabilities:
    - F-04
    stimulus: Create Grant for the exact Project root.
    expected: Create Grant for the exact Project root.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact canonical root and resulting Grant.
    capture: evidence/picorv32-published-demo-continuity.vivado.project-grant
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.readonly-mount
  phase: baseline
  action: Expose borrowed installation as a read-only mount inside the Session Runtime.
  checks:
  - id: vivado.readonly-mount
    capabilities:
    - F-04
    stimulus: Expose borrowed installation as a read-only mount inside the Session Runtime.
    expected: Expose borrowed installation as a read-only mount inside the Session Runtime.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Runtime mount metadata and read-only evidence.
    capture: evidence/picorv32-published-demo-continuity.vivado.readonly-mount
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.license-create
  phase: baseline
  action: Create License Profile.
  checks:
  - id: vivado.license-create
    capabilities:
    - F-04
    stimulus: Create License Profile.
    expected: Create License Profile.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Created identity and nonsecret metadata.
    capture: evidence/picorv32-published-demo-continuity.vivado.license-create
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.license-read
  phase: baseline
  action: Read back License Profile state.
  checks:
  - id: vivado.license-read
    capabilities:
    - F-04
    stimulus: Read back License Profile state.
    expected: Read back License Profile state.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Observed identity/configuration.
    capture: evidence/picorv32-published-demo-continuity.vivado.license-read
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.license-update
  phase: baseline
  action: Update the run-owned License Profile.
  checks:
  - id: vivado.license-update
    capabilities:
    - F-04
    stimulus: Update the run-owned License Profile.
    expected: Update the run-owned License Profile.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Before/after configuration and command result.
    capture: evidence/picorv32-published-demo-continuity.vivado.license-update
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.license-attach
  phase: baseline
  action: Attach the License Profile for runtime use.
  checks:
  - id: vivado.license-attach
    capabilities:
    - F-04
    stimulus: Attach the License Profile for runtime use.
    expected: Attach the License Profile for runtime use.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Association and runtime connectivity evidence.
    capture: evidence/picorv32-published-demo-continuity.vivado.license-attach
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.relay-fault
  phase: baseline
  action: Inject controlled relay failure and observe expected failure.
  checks:
  - id: vivado.relay-fault
    capabilities:
    - F-04
    - H-09
    stimulus: Inject controlled relay failure and observe expected failure.
    expected: Inject controlled relay failure and observe expected failure.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Baseline connectivity, fault action and failure diagnostic before recovery.
    capture: evidence/picorv32-published-demo-continuity.vivado.relay-fault
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.relay-recovery
  phase: baseline
  action: Restore relay and prove fresh successful operation.
  checks:
  - id: vivado.relay-recovery
    capabilities:
    - F-04
    - H-09
    stimulus: Restore relay and prove fresh successful operation.
    expected: Restore relay and prove fresh successful operation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Restoration action and post-restoration connectivity/artifact evidence.
    capture: evidence/picorv32-published-demo-continuity.vivado.relay-recovery
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.implementation
  phase: baseline
  action: Run implementation and require a fresh successful artifact.
  checks:
  - id: vivado.implementation
    capabilities:
    - F-04
    - EDA-vivado
    - EDA-resolve-fpga
    stimulus: Run implementation and require a fresh successful artifact.
    expected: Run implementation and require a fresh successful artifact.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Vivado grade and artifact identity/freshness.
    capture: evidence/picorv32-published-demo-continuity.vivado.implementation
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: vivado.license-delete
  phase: baseline
  action: Delete run-owned License Profile after dependents are released.
  checks:
  - id: vivado.license-delete
    capabilities:
    - F-04
    stimulus: Delete run-owned License Profile after dependents are released.
    expected: Delete run-owned License Profile after dependents are released.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Absence proof and cleanup ledger entry.
    capture: evidence/picorv32-published-demo-continuity.vivado.license-delete
  requires:
  - inputs.project-pin
  - doctor.plain-recheck
- id: inventory.F-02.lint-clean
  phase: baseline
  action: |-
    Execute `lint-clean` using the attached scenario-owned probe contract.
    `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
  checks:
  - id: inventory.F-02.lint-clean
    capabilities:
    - F-02
    stimulus: |-
      Execute `lint-clean` using the attached scenario-owned probe contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    expected: |-
      Evaluate only the independently named `lint-clean` outcome in this contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#lint
    evidence: |-
      Both linter versions, native waiver files, complete findings/report and filtered file lists. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-02.lint-clean
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-02.md
    audience: coordinator
    sha256: 3b1597267701ed5d6b6f4e5a3dcbd6995b2849bf94d3759ef354dc9f3a9e7249
    base: shared
- id: inventory.F-02.lint-dedupe
  phase: baseline
  action: |-
    Execute `lint-dedupe` using the attached scenario-owned probe contract.
    `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
  checks:
  - id: inventory.F-02.lint-dedupe
    capabilities:
    - F-02
    stimulus: |-
      Execute `lint-dedupe` using the attached scenario-owned probe contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    expected: |-
      Evaluate only the independently named `lint-dedupe` outcome in this contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#lint
    evidence: |-
      Both linter versions, native waiver files, complete findings/report and filtered file lists. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-02.lint-dedupe
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-02.md
    audience: coordinator
    sha256: 3b1597267701ed5d6b6f4e5a3dcbd6995b2849bf94d3759ef354dc9f3a9e7249
    base: shared
- id: inventory.F-02.lint-hard
  phase: baseline
  action: |-
    Execute `lint-hard` using the attached scenario-owned probe contract.
    `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
  checks:
  - id: inventory.F-02.lint-hard
    capabilities:
    - F-02
    stimulus: |-
      Execute `lint-hard` using the attached scenario-owned probe contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    expected: |-
      Evaluate only the independently named `lint-hard` outcome in this contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#lint
    evidence: |-
      Both linter versions, native waiver files, complete findings/report and filtered file lists. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-02.lint-hard
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-02.md
    audience: coordinator
    sha256: 3b1597267701ed5d6b6f4e5a3dcbd6995b2849bf94d3759ef354dc9f3a9e7249
    base: shared
- id: inventory.F-02.lint-infra
  phase: baseline
  action: |-
    Execute `lint-infra` using the attached scenario-owned probe contract.
    `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
  checks:
  - id: inventory.F-02.lint-infra
    capabilities:
    - F-02
    stimulus: |-
      Execute `lint-infra` using the attached scenario-owned probe contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    expected: |-
      Evaluate only the independently named `lint-infra` outcome in this contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#lint
    evidence: |-
      Both linter versions, native waiver files, complete findings/report and filtered file lists. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-02.lint-infra
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-02.md
    audience: coordinator
    sha256: 3b1597267701ed5d6b6f4e5a3dcbd6995b2849bf94d3759ef354dc9f3a9e7249
    base: shared
- id: inventory.F-02.lint-native-waiver
  phase: baseline
  action: |-
    Execute `lint-native-waiver` using the attached scenario-owned probe contract.
    `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
  checks:
  - id: inventory.F-02.lint-native-waiver
    capabilities:
    - F-02
    stimulus: |-
      Execute `lint-native-waiver` using the attached scenario-owned probe contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    expected: |-
      Evaluate only the independently named `lint-native-waiver` outcome in this contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#lint
    evidence: |-
      Both linter versions, native waiver files, complete findings/report and filtered file lists. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-02.lint-native-waiver
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-02.md
    audience: coordinator
    sha256: 3b1597267701ed5d6b6f4e5a3dcbd6995b2849bf94d3759ef354dc9f3a9e7249
    base: shared
- id: inventory.F-02.lint-nonfatal-warning
  phase: baseline
  action: |-
    Execute `lint-nonfatal-warning` using the attached scenario-owned probe contract.
    `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
  checks:
  - id: inventory.F-02.lint-nonfatal-warning
    capabilities:
    - F-02
    stimulus: |-
      Execute `lint-nonfatal-warning` using the attached scenario-owned probe contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    expected: |-
      Evaluate only the independently named `lint-nonfatal-warning` outcome in this contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#lint
    evidence: |-
      Both linter versions, native waiver files, complete findings/report and filtered file lists. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-02.lint-nonfatal-warning
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-02.md
    audience: coordinator
    sha256: 3b1597267701ed5d6b6f4e5a3dcbd6995b2849bf94d3759ef354dc9f3a9e7249
    base: shared
- id: inventory.F-02.lint-recovery
  phase: baseline
  action: |-
    Execute `lint-recovery` using the attached scenario-owned probe contract.
    `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
  checks:
  - id: inventory.F-02.lint-recovery
    capabilities:
    - F-02
    stimulus: |-
      Execute `lint-recovery` using the attached scenario-owned probe contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    expected: |-
      Evaluate only the independently named `lint-recovery` outcome in this contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#lint
    evidence: |-
      Both linter versions, native waiver files, complete findings/report and filtered file lists. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-02.lint-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-02.md
    audience: coordinator
    sha256: 3b1597267701ed5d6b6f4e5a3dcbd6995b2849bf94d3759ef354dc9f3a9e7249
    base: shared
- id: inventory.F-02.lint-scope
  phase: baseline
  action: |-
    Execute `lint-scope` using the attached scenario-owned probe contract.
    `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
  checks:
  - id: inventory.F-02.lint-scope
    capabilities:
    - F-02
    stimulus: |-
      Execute `lint-scope` using the attached scenario-owned probe contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    expected: |-
      Evaluate only the independently named `lint-scope` outcome in this contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#lint
    evidence: |-
      Both linter versions, native waiver files, complete findings/report and filtered file lists. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-02.lint-scope
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-02.md
    audience: coordinator
    sha256: 3b1597267701ed5d6b6f4e5a3dcbd6995b2849bf94d3759ef354dc9f3a9e7249
    base: shared
- id: inventory.F-02.lint-warning
  phase: baseline
  action: |-
    Execute `lint-warning` using the attached scenario-owned probe contract.
    `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
  checks:
  - id: inventory.F-02.lint-warning
    capabilities:
    - F-02
    stimulus: |-
      Execute `lint-warning` using the attached scenario-owned probe contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    expected: |-
      Evaluate only the independently named `lint-warning` outcome in this contract.
      `lint-clean`, `lint-warning`, `lint-hard`, `lint-infra`: separate clean/warning/syntax/tool-availability inputs → distinct report outcomes. `lint-nonfatal-warning`: warnings configured nonfatal → rc and report passed distinction preserved. `lint-scope`: selected files → only intended scope. `lint-dedupe`: duplicate cross-Target finding → one finding with proper provenance. `lint-native-waiver`: each linter's native waiver → intended finding waived only. `lint-recovery`: restore source/tool → clean fresh result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#lint
    evidence: |-
      Both linter versions, native waiver files, complete findings/report and filtered file lists. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-02.lint-warning
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-02.md
    audience: coordinator
    sha256: 3b1597267701ed5d6b6f4e5a3dcbd6995b2849bf94d3759ef354dc9f3a9e7249
    base: shared
- id: inventory.F-03.synth-advisory
  phase: baseline
  action: |-
    Execute `synth-advisory` using the attached scenario-owned probe contract.
    `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
  checks:
  - id: inventory.F-03.synth-advisory
    capabilities:
    - F-03
    stimulus: |-
      Execute `synth-advisory` using the attached scenario-owned probe contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    expected: |-
      Evaluate only the independently named `synth-advisory` outcome in this contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#synth
    evidence: |-
      Yosys/OpenROAD/frontend logs and versions, SDC/default clock, selected profile/expert override, metrics/baseline recipe. No signoff claim. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-03.synth-advisory
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-03.md
    audience: coordinator
    sha256: cba42c7242b8c0ec558a65062e18b94daec682b96df3059eede4b8ba0b2d27c9
    base: shared
- id: inventory.F-03.synth-baseline
  phase: baseline
  action: |-
    Execute `synth-baseline` using the attached scenario-owned probe contract.
    `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
  checks:
  - id: inventory.F-03.synth-baseline
    capabilities:
    - F-03
    stimulus: |-
      Execute `synth-baseline` using the attached scenario-owned probe contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    expected: |-
      Evaluate only the independently named `synth-baseline` outcome in this contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#synth
    evidence: |-
      Yosys/OpenROAD/frontend logs and versions, SDC/default clock, selected profile/expert override, metrics/baseline recipe. No signoff claim. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-03.synth-baseline
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-03.md
    audience: coordinator
    sha256: cba42c7242b8c0ec558a65062e18b94daec682b96df3059eede4b8ba0b2d27c9
    base: shared
- id: inventory.F-03.synth-frontend-incompatible
  phase: baseline
  action: |-
    Execute `synth-frontend-incompatible` using the attached scenario-owned probe contract.
    `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
  checks:
  - id: inventory.F-03.synth-frontend-incompatible
    capabilities:
    - F-03
    stimulus: |-
      Execute `synth-frontend-incompatible` using the attached scenario-owned probe contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    expected: |-
      Evaluate only the independently named `synth-frontend-incompatible` outcome in this contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#synth
    evidence: |-
      Yosys/OpenROAD/frontend logs and versions, SDC/default clock, selected profile/expert override, metrics/baseline recipe. No signoff claim. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-03.synth-frontend-incompatible
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-03.md
    audience: coordinator
    sha256: cba42c7242b8c0ec558a65062e18b94daec682b96df3059eede4b8ba0b2d27c9
    base: shared
- id: inventory.F-03.synth-frontend-missing
  phase: baseline
  action: |-
    Execute `synth-frontend-missing` using the attached scenario-owned probe contract.
    `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
  checks:
  - id: inventory.F-03.synth-frontend-missing
    capabilities:
    - F-03
    stimulus: |-
      Execute `synth-frontend-missing` using the attached scenario-owned probe contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    expected: |-
      Evaluate only the independently named `synth-frontend-missing` outcome in this contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#synth
    evidence: |-
      Yosys/OpenROAD/frontend logs and versions, SDC/default clock, selected profile/expert override, metrics/baseline recipe. No signoff claim. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-03.synth-frontend-missing
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-03.md
    audience: coordinator
    sha256: cba42c7242b8c0ec558a65062e18b94daec682b96df3059eede4b8ba0b2d27c9
    base: shared
- id: inventory.F-03.synth-latch
  phase: baseline
  action: |-
    Execute `synth-latch` using the attached scenario-owned probe contract.
    `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
  checks:
  - id: inventory.F-03.synth-latch
    capabilities:
    - F-03
    stimulus: |-
      Execute `synth-latch` using the attached scenario-owned probe contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    expected: |-
      Evaluate only the independently named `synth-latch` outcome in this contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#synth
    evidence: |-
      Yosys/OpenROAD/frontend logs and versions, SDC/default clock, selected profile/expert override, metrics/baseline recipe. No signoff claim. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-03.synth-latch
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-03.md
    audience: coordinator
    sha256: cba42c7242b8c0ec558a65062e18b94daec682b96df3059eede4b8ba0b2d27c9
    base: shared
- id: inventory.F-03.synth-metrics
  phase: baseline
  action: |-
    Execute `synth-metrics` using the attached scenario-owned probe contract.
    `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
  checks:
  - id: inventory.F-03.synth-metrics
    capabilities:
    - F-03
    stimulus: |-
      Execute `synth-metrics` using the attached scenario-owned probe contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    expected: |-
      Evaluate only the independently named `synth-metrics` outcome in this contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#synth
    evidence: |-
      Yosys/OpenROAD/frontend logs and versions, SDC/default clock, selected profile/expert override, metrics/baseline recipe. No signoff claim. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-03.synth-metrics
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-03.md
    audience: coordinator
    sha256: cba42c7242b8c0ec558a65062e18b94daec682b96df3059eede4b8ba0b2d27c9
    base: shared
- id: inventory.F-03.synth-recovery
  phase: baseline
  action: |-
    Execute `synth-recovery` using the attached scenario-owned probe contract.
    `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
  checks:
  - id: inventory.F-03.synth-recovery
    capabilities:
    - F-03
    stimulus: |-
      Execute `synth-recovery` using the attached scenario-owned probe contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    expected: |-
      Evaluate only the independently named `synth-recovery` outcome in this contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#synth
    evidence: |-
      Yosys/OpenROAD/frontend logs and versions, SDC/default clock, selected profile/expert override, metrics/baseline recipe. No signoff claim. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-03.synth-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-03.md
    audience: coordinator
    sha256: cba42c7242b8c0ec558a65062e18b94daec682b96df3059eede4b8ba0b2d27c9
    base: shared
- id: inventory.F-03.synth-threshold
  phase: baseline
  action: |-
    Execute `synth-threshold` using the attached scenario-owned probe contract.
    `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
  checks:
  - id: inventory.F-03.synth-threshold
    capabilities:
    - F-03
    stimulus: |-
      Execute `synth-threshold` using the attached scenario-owned probe contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    expected: |-
      Evaluate only the independently named `synth-threshold` outcome in this contract.
      `synth-metrics`: execute assigned mode/frontend → fresh metrics and actual tool provenance. `synth-latch`: bounded latching fixture → reported critical condition. `synth-advisory`: timing miss without hard threshold → advisory. `synth-threshold`: configured threshold violated → design failure. `synth-baseline`: compare declared baseline/candidate → numeric delta with both identities. `synth-frontend-missing`, `synth-frontend-incompatible`: distinct faults → appropriate config/execution failure. `synth-recovery`: restore supported frontend/design → valid fresh metrics.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#synth
    evidence: |-
      Yosys/OpenROAD/frontend logs and versions, SDC/default clock, selected profile/expert override, metrics/baseline recipe. No signoff claim. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-03.synth-threshold
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-03.md
    audience: coordinator
    sha256: cba42c7242b8c0ec558a65062e18b94daec682b96df3059eede4b8ba0b2d27c9
    base: shared
- id: inventory.H-09.profile-attach
  phase: baseline
  action: |-
    Execute `profile-attach` using the attached scenario-owned probe contract.
    `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
  checks:
  - id: inventory.H-09.profile-attach
    capabilities:
    - H-09
    stimulus: |-
      Execute `profile-attach` using the attached scenario-owned probe contract.
      `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
    expected: |-
      Evaluate only the independently named `profile-attach` outcome in this contract.
      `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/SUPPORTED-EDA-TOOLS.md#vivado-host-provisioning-policy
    evidence: |-
      Redacted registry/Grant/runtime policy, destination and cleanup evidence. Actual paid checkout/accounting/concurrency/return are optional experimental checks with per-run site authority; never inferred from relay success. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-09.profile-attach
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-09.md
    audience: coordinator
    sha256: 285abe1a52ace7cc56c9a691594ef857c0fd7524e678dc4012522120e627a692
- id: inventory.H-09.profile-cleanup
  phase: baseline
  action: |-
    Execute `profile-cleanup` using the attached scenario-owned probe contract.
    `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
  checks:
  - id: inventory.H-09.profile-cleanup
    capabilities:
    - H-09
    stimulus: |-
      Execute `profile-cleanup` using the attached scenario-owned probe contract.
      `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
    expected: |-
      Evaluate only the independently named `profile-cleanup` outcome in this contract.
      `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/SUPPORTED-EDA-TOOLS.md#vivado-host-provisioning-policy
    evidence: |-
      Redacted registry/Grant/runtime policy, destination and cleanup evidence. Actual paid checkout/accounting/concurrency/return are optional experimental checks with per-run site authority; never inferred from relay success. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-09.profile-cleanup
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-09.md
    audience: coordinator
    sha256: 285abe1a52ace7cc56c9a691594ef857c0fd7524e678dc4012522120e627a692
- id: inventory.H-09.profile-crud
  phase: baseline
  action: |-
    Execute `profile-crud` using the attached scenario-owned probe contract.
    `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
  checks:
  - id: inventory.H-09.profile-crud
    capabilities:
    - H-09
    stimulus: |-
      Execute `profile-crud` using the attached scenario-owned probe contract.
      `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
    expected: |-
      Evaluate only the independently named `profile-crud` outcome in this contract.
      `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/SUPPORTED-EDA-TOOLS.md#vivado-host-provisioning-policy
    evidence: |-
      Redacted registry/Grant/runtime policy, destination and cleanup evidence. Actual paid checkout/accounting/concurrency/return are optional experimental checks with per-run site authority; never inferred from relay success. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-09.profile-crud
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-09.md
    audience: coordinator
    sha256: 285abe1a52ace7cc56c9a691594ef857c0fd7524e678dc4012522120e627a692
- id: inventory.F-04.fpga-authority-fail
  phase: baseline
  action: |-
    Execute `fpga-authority-fail` using the attached scenario-owned probe contract.
    `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
  checks:
  - id: inventory.F-04.fpga-authority-fail
    capabilities:
    - F-04
    stimulus: |-
      Execute `fpga-authority-fail` using the attached scenario-owned probe contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    expected: |-
      Evaluate only the independently named `fpga-authority-fail` outcome in this contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#fpga
    evidence: |-
      Vivado 2025.2 log, source/cache keys, routed reports and baseline identities; Windows expected not runnable. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-04.fpga-authority-fail
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-04.md
    audience: coordinator
    sha256: 6297043982905fdcd6083c21a971202418a31162bdb087f7b75d91a54898e80a
- id: inventory.F-04.fpga-baseline
  phase: baseline
  action: |-
    Execute `fpga-baseline` using the attached scenario-owned probe contract.
    `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
  checks:
  - id: inventory.F-04.fpga-baseline
    capabilities:
    - F-04
    stimulus: |-
      Execute `fpga-baseline` using the attached scenario-owned probe contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    expected: |-
      Evaluate only the independently named `fpga-baseline` outcome in this contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#fpga
    evidence: |-
      Vivado 2025.2 log, source/cache keys, routed reports and baseline identities; Windows expected not runnable. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-04.fpga-baseline
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-04.md
    audience: coordinator
    sha256: 6297043982905fdcd6083c21a971202418a31162bdb087f7b75d91a54898e80a
- id: inventory.F-04.fpga-cache
  phase: baseline
  action: |-
    Execute `fpga-cache` using the attached scenario-owned probe contract.
    `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
  checks:
  - id: inventory.F-04.fpga-cache
    capabilities:
    - F-04
    stimulus: |-
      Execute `fpga-cache` using the attached scenario-owned probe contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    expected: |-
      Evaluate only the independently named `fpga-cache` outcome in this contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#fpga
    evidence: |-
      Vivado 2025.2 log, source/cache keys, routed reports and baseline identities; Windows expected not runnable. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-04.fpga-cache
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-04.md
    audience: coordinator
    sha256: 6297043982905fdcd6083c21a971202418a31162bdb087f7b75d91a54898e80a
- id: inventory.F-04.fpga-design-fail
  phase: baseline
  action: |-
    Execute `fpga-design-fail` using the attached scenario-owned probe contract.
    `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
  checks:
  - id: inventory.F-04.fpga-design-fail
    capabilities:
    - F-04
    stimulus: |-
      Execute `fpga-design-fail` using the attached scenario-owned probe contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    expected: |-
      Evaluate only the independently named `fpga-design-fail` outcome in this contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#fpga
    evidence: |-
      Vivado 2025.2 log, source/cache keys, routed reports and baseline identities; Windows expected not runnable. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-04.fpga-design-fail
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-04.md
    audience: coordinator
    sha256: 6297043982905fdcd6083c21a971202418a31162bdb087f7b75d91a54898e80a
- id: inventory.F-04.fpga-dry
  phase: baseline
  action: |-
    Execute `fpga-dry` using the attached scenario-owned probe contract.
    `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
  checks:
  - id: inventory.F-04.fpga-dry
    capabilities:
    - F-04
    stimulus: |-
      Execute `fpga-dry` using the attached scenario-owned probe contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    expected: |-
      Evaluate only the independently named `fpga-dry` outcome in this contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#fpga
    evidence: |-
      Vivado 2025.2 log, source/cache keys, routed reports and baseline identities; Windows expected not runnable. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-04.fpga-dry
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-04.md
    audience: coordinator
    sha256: 6297043982905fdcd6083c21a971202418a31162bdb087f7b75d91a54898e80a
- id: inventory.F-04.fpga-no-cache
  phase: baseline
  action: |-
    Execute `fpga-no-cache` using the attached scenario-owned probe contract.
    `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
  checks:
  - id: inventory.F-04.fpga-no-cache
    capabilities:
    - F-04
    stimulus: |-
      Execute `fpga-no-cache` using the attached scenario-owned probe contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    expected: |-
      Evaluate only the independently named `fpga-no-cache` outcome in this contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#fpga
    evidence: |-
      Vivado 2025.2 log, source/cache keys, routed reports and baseline identities; Windows expected not runnable. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-04.fpga-no-cache
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-04.md
    audience: coordinator
    sha256: 6297043982905fdcd6083c21a971202418a31162bdb087f7b75d91a54898e80a
- id: inventory.F-04.fpga-recovery
  phase: baseline
  action: |-
    Execute `fpga-recovery` using the attached scenario-owned probe contract.
    `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
  checks:
  - id: inventory.F-04.fpga-recovery
    capabilities:
    - F-04
    stimulus: |-
      Execute `fpga-recovery` using the attached scenario-owned probe contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    expected: |-
      Evaluate only the independently named `fpga-recovery` outcome in this contract.
      `fpga-dry`: dry-run → exact part/top/XDC/source inventory without implementation. `fpga-fresh`: implementation → routed timing/utilization and fresh artifacts. `fpga-cache`: same inputs → authenticated cache hit. `fpga-no-cache`: same inputs no-cache → actual fresh tool execution. `fpga-design-fail`: declared bad design/threshold → rc1 design result. `fpga-authority-fail`: remove run-owned Grant → rc2 without unauthorized execution. `fpga-baseline`: declared pair → exact metrics delta/provenance. `fpga-recovery`: restore authority/design → valid implementation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#fpga
    evidence: |-
      Vivado 2025.2 log, source/cache keys, routed reports and baseline identities; Windows expected not runnable. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-04.fpga-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-04.md
    audience: coordinator
    sha256: 6297043982905fdcd6083c21a971202418a31162bdb087f7b75d91a54898e80a
- id: inventory.H-08.grant
  phase: baseline
  action: |-
    Execute `grant` using the attached scenario-owned probe contract.
    `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
  checks:
  - id: inventory.H-08.grant
    capabilities:
    - H-08
    stimulus: |-
      Execute `grant` using the attached scenario-owned probe contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    expected: |-
      Evaluate only the independently named `grant` outcome in this contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#commercial-eda-provisioning
    evidence: |-
      Host registry snapshots, runtime mount metadata, write-rejection probe, actual FPGA report. Windows expected not runnable only for platform-scoped portions. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-08.grant
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-08.md
    audience: coordinator
    sha256: 8bea3814807475a0612c47861042697e7ba557ac0235be4bc83b5c39d9518ef9
- id: inventory.H-08.grant-denied
  phase: baseline
  action: |-
    Execute `grant-denied` using the attached scenario-owned probe contract.
    `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
  checks:
  - id: inventory.H-08.grant-denied
    capabilities:
    - H-08
    stimulus: |-
      Execute `grant-denied` using the attached scenario-owned probe contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    expected: |-
      Evaluate only the independently named `grant-denied` outcome in this contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#commercial-eda-provisioning
    evidence: |-
      Host registry snapshots, runtime mount metadata, write-rejection probe, actual FPGA report. Windows expected not runnable only for platform-scoped portions. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-08.grant-denied
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-08.md
    audience: coordinator
    sha256: 8bea3814807475a0612c47861042697e7ba557ac0235be4bc83b5c39d9518ef9
- id: inventory.H-08.grant-recovery
  phase: baseline
  action: |-
    Execute `grant-recovery` using the attached scenario-owned probe contract.
    `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
  checks:
  - id: inventory.H-08.grant-recovery
    capabilities:
    - H-08
    stimulus: |-
      Execute `grant-recovery` using the attached scenario-owned probe contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    expected: |-
      Evaluate only the independently named `grant-recovery` outcome in this contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#commercial-eda-provisioning
    evidence: |-
      Host registry snapshots, runtime mount metadata, write-rejection probe, actual FPGA report. Windows expected not runnable only for platform-scoped portions. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-08.grant-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-08.md
    audience: coordinator
    sha256: 8bea3814807475a0612c47861042697e7ba557ac0235be4bc83b5c39d9518ef9
- id: inventory.H-08.installation-cleanup
  phase: baseline
  action: |-
    Execute `installation-cleanup` using the attached scenario-owned probe contract.
    `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
  checks:
  - id: inventory.H-08.installation-cleanup
    capabilities:
    - H-08
    stimulus: |-
      Execute `installation-cleanup` using the attached scenario-owned probe contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    expected: |-
      Evaluate only the independently named `installation-cleanup` outcome in this contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#commercial-eda-provisioning
    evidence: |-
      Host registry snapshots, runtime mount metadata, write-rejection probe, actual FPGA report. Windows expected not runnable only for platform-scoped portions. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-08.installation-cleanup
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-08.md
    audience: coordinator
    sha256: 8bea3814807475a0612c47861042697e7ba557ac0235be4bc83b5c39d9518ef9
- id: inventory.H-08.installation-crud
  phase: baseline
  action: |-
    Execute `installation-crud` using the attached scenario-owned probe contract.
    `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
  checks:
  - id: inventory.H-08.installation-crud
    capabilities:
    - H-08
    stimulus: |-
      Execute `installation-crud` using the attached scenario-owned probe contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    expected: |-
      Evaluate only the independently named `installation-crud` outcome in this contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#commercial-eda-provisioning
    evidence: |-
      Host registry snapshots, runtime mount metadata, write-rejection probe, actual FPGA report. Windows expected not runnable only for platform-scoped portions. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-08.installation-crud
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-08.md
    audience: coordinator
    sha256: 8bea3814807475a0612c47861042697e7ba557ac0235be4bc83b5c39d9518ef9
- id: inventory.H-08.mount
  phase: baseline
  action: |-
    Execute `mount` using the attached scenario-owned probe contract.
    `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
  checks:
  - id: inventory.H-08.mount
    capabilities:
    - H-08
    stimulus: |-
      Execute `mount` using the attached scenario-owned probe contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    expected: |-
      Evaluate only the independently named `mount` outcome in this contract.
      `installation-crud`: register/list/show approved exact installation under run-unique name → canonical identity/version stable. `grant`: grant exact canonical Project → inventory shows exact relationship. `mount`: start runtime → fixed `/opt/booley-eda/vivado` read-only mount and executable version. `grant-denied`: revoke Grant then request runtime/tool → authority failure, no mount. `grant-recovery`: restore Grant → same mount usable. `installation-cleanup`: revoke/remove owned registration → absent, installation bytes untouched.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#commercial-eda-provisioning
    evidence: |-
      Host registry snapshots, runtime mount metadata, write-rejection probe, actual FPGA report. Windows expected not runnable only for platform-scoped portions. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-08.mount
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-08.md
    audience: coordinator
    sha256: 8bea3814807475a0612c47861042697e7ba557ac0235be4bc83b5c39d9518ef9
- id: inventory.H-09.relay-isolation
  phase: baseline
  action: |-
    Execute `relay-isolation` using the attached scenario-owned probe contract.
    `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
  checks:
  - id: inventory.H-09.relay-isolation
    capabilities:
    - H-09
    stimulus: |-
      Execute `relay-isolation` using the attached scenario-owned probe contract.
      `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
    expected: |-
      Evaluate only the independently named `relay-isolation` outcome in this contract.
      `profile-crud`: register/list/show disposable License Profile → same safe identity. `profile-attach`: attach to exact Grant → runtime policy matches authorized destination. `relay-isolation`: controlled relay infrastructure attempts unapproved destination → blocked. `relay-failure`: make approved controlled destination unavailable → precise diagnostic without leaked secret. `relay-recovery`: restore controlled endpoint → approved route usable. `profile-cleanup`: detach/remove owned profile and relay → no owned resources remain.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/SUPPORTED-EDA-TOOLS.md#vivado-host-provisioning-policy
    evidence: |-
      Redacted registry/Grant/runtime policy, destination and cleanup evidence. Actual paid checkout/accounting/concurrency/return are optional experimental checks with per-run site authority; never inferred from relay success. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.H-09.relay-isolation
  requires:
  - inputs.project-pin
  assets:
  - path: probes/H-09.md
    audience: coordinator
    sha256: 285abe1a52ace7cc56c9a691594ef857c0fd7524e678dc4012522120e627a692
- id: interactive.child-context
  phase: interactive
  action: |-
    Launch one long-lived child inside the same Session Runtime with Project cwd, PTY, runtime identity and MCP access preserved; direct inherited child or conforming `booley session enter -- booley` is allowed.
  checks:
  - id: interactive.child-context
    capabilities:
    - I-01
    stimulus: |-
      Launch one long-lived child inside the same Session Runtime with Project cwd, PTY, runtime identity and MCP access preserved; direct inherited child or conforming `booley session enter -- booley` is allowed.
    expected: |-
      Launch one long-lived child inside the same Session Runtime with Project cwd, PTY, runtime identity and MCP access preserved; direct inherited child or conforming `booley session enter -- booley` is allowed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Child/runtime/client/backend identities and launch context; no generic outer subagent substitution.
    capture: evidence/picorv32-published-demo-continuity.interactive.child-context
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
- id: interactive.readiness
  phase: interactive
  action: |-
    Send the exact first prompt from source annex; child confirms runtime identity, cleanliness, Doctor and Targets without source edits.
  checks:
  - id: interactive.readiness
    capabilities:
    - I-01
    stimulus: |-
      Send the exact first prompt from source annex; child confirms runtime identity, cleanliness, Doctor and Targets without source edits.
    expected: |-
      Send the exact first prompt from source annex; child confirms runtime identity, cleanliness, Doctor and Targets without source edits.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact prompt/hash, durable child log and state evidence; pre-fault recovery point.
    capture: evidence/picorv32-published-demo-continuity.interactive.readiness
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: prompts/interactive.md
    audience: Interactive Mode delegate
    sha256: 0b8392aeb602f81eca987d00935e308c8bc45b83bfd6dd75be94586d77447b4d
- id: interactive.wishbone-baseline
  phase: interactive
  action: Child runs traced Wishbone simulation before injection and inspects artifacts with B-Wave.
  checks:
  - id: interactive.wishbone-baseline
    capabilities:
    - I-01
    stimulus: Child runs traced Wishbone simulation before injection and inspects artifacts with B-Wave.
    expected: Child runs traced Wishbone simulation before injection and inspects artifacts with B-Wave.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Fresh passing trace, relevant queries/results and readiness report.
    capture: evidence/picorv32-published-demo-continuity.interactive.wishbone-baseline
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
- id: bwave.semantic-wave
  phase: interactive
  action: Invoke `wave` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
  checks:
  - id: bwave.semantic-wave
    capabilities:
    - W-03
    stimulus: Invoke `wave` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
    expected: Invoke `wave` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact query, trace identity, expected relationship and observed data.
    capture: evidence/picorv32-published-demo-continuity.bwave.semantic-wave
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.semantic-find
  phase: interactive
  action: Invoke `find` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
  checks:
  - id: bwave.semantic-find
    capabilities:
    - W-03
    stimulus: Invoke `find` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
    expected: Invoke `find` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact query, trace identity, expected relationship and observed data.
    capture: evidence/picorv32-published-demo-continuity.bwave.semantic-find
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.semantic-sample
  phase: interactive
  action: Invoke `sample` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
  checks:
  - id: bwave.semantic-sample
    capabilities:
    - W-03
    stimulus: Invoke `sample` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
    expected: Invoke `sample` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact query, trace identity, expected relationship and observed data.
    capture: evidence/picorv32-published-demo-continuity.bwave.semantic-sample
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.semantic-distance
  phase: interactive
  action: Invoke `distance` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
  checks:
  - id: bwave.semantic-distance
    capabilities:
    - W-03
    stimulus: Invoke `distance` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is
      insufficient.
    expected: Invoke `distance` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is
      insufficient.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact query, trace identity, expected relationship and observed data.
    capture: evidence/picorv32-published-demo-continuity.bwave.semantic-distance
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.semantic-value
  phase: interactive
  action: Invoke `value` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
  checks:
  - id: bwave.semantic-value
    capabilities:
    - W-03
    stimulus: Invoke `value` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
    expected: Invoke `value` on PicoRV32 Icarus trace and verify signal/time semantics; CLI success alone is insufficient.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact query, trace identity, expected relationship and observed data.
    capture: evidence/picorv32-published-demo-continuity.bwave.semantic-value
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.rejection-list
  phase: interactive
  action: |-
    Exercise the agreed virtual-signal option matrix: `list` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
  checks:
  - id: bwave.rejection-list
    capabilities:
    - W-03
    stimulus: |-
      Exercise the agreed virtual-signal option matrix: `list` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    expected: |-
      Exercise the agreed virtual-signal option matrix: `list` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: |-
      Exact invalid query, rejection diagnostic and exit status; literal `--virtual` conjunction and rejection forms are fixed in the amendment below.
    capture: evidence/picorv32-published-demo-continuity.bwave.rejection-list
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.rejection-signal
  phase: interactive
  action: |-
    Exercise the agreed virtual-signal option matrix: `signal` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
  checks:
  - id: bwave.rejection-signal
    capabilities:
    - W-03
    stimulus: |-
      Exercise the agreed virtual-signal option matrix: `signal` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    expected: |-
      Exercise the agreed virtual-signal option matrix: `signal` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: |-
      Exact invalid query, rejection diagnostic and exit status; literal `--virtual` conjunction and rejection forms are fixed in the amendment below.
    capture: evidence/picorv32-published-demo-continuity.bwave.rejection-signal
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.rejection-diff
  phase: interactive
  action: |-
    Exercise the agreed virtual-signal option matrix: `diff` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
  checks:
  - id: bwave.rejection-diff
    capabilities:
    - W-03
    stimulus: |-
      Exercise the agreed virtual-signal option matrix: `diff` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    expected: |-
      Exercise the agreed virtual-signal option matrix: `diff` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: |-
      Exact invalid query, rejection diagnostic and exit status; literal `--virtual` conjunction and rejection forms are fixed in the amendment below.
    capture: evidence/picorv32-published-demo-continuity.bwave.rejection-diff
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.rejection-stats
  phase: interactive
  action: |-
    Exercise the agreed virtual-signal option matrix: `stats` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
  checks:
  - id: bwave.rejection-stats
    capabilities:
    - W-03
    stimulus: |-
      Exercise the agreed virtual-signal option matrix: `stats` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    expected: |-
      Exercise the agreed virtual-signal option matrix: `stats` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: |-
      Exact invalid query, rejection diagnostic and exit status; literal `--virtual` conjunction and rejection forms are fixed in the amendment below.
    capture: evidence/picorv32-published-demo-continuity.bwave.rejection-stats
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.rejection-stuck
  phase: interactive
  action: |-
    Exercise the agreed virtual-signal option matrix: `stuck` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
  checks:
  - id: bwave.rejection-stuck
    capabilities:
    - W-03
    stimulus: |-
      Exercise the agreed virtual-signal option matrix: `stuck` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    expected: |-
      Exercise the agreed virtual-signal option matrix: `stuck` rejects the unsupported form with parser exit 2. Do not misread this as ordinary command rejection.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: |-
      Exact invalid query, rejection diagnostic and exit status; literal `--virtual` conjunction and rejection forms are fixed in the amendment below.
    capture: evidence/picorv32-published-demo-continuity.bwave.rejection-stuck
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: bwave.defect-preservation
  phase: interactive
  action: Known defects in `sample`, `value`, `signal` or `diff` remain blocking Findings; do not relabel as expected
    behavior.
  checks:
  - id: bwave.defect-preservation
    capabilities:
    - W-03
    stimulus: Known defects in `sample`, `value`, `signal` or `diff` remain blocking Findings; do not relabel as
      expected behavior.
    expected: Known defects in `sample`, `value`, `signal` or `diff` remain blocking Findings; do not relabel as
      expected behavior.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Original observation, public authority and scoped Finding linked to failing check.
    capture: evidence/picorv32-published-demo-continuity.bwave.defect-preservation
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  assets:
  - path: probes/virtual-signals.md
    audience: coordinator
    sha256: 0a81cc1f76ad36f397a0500c5cbb71811caa27c1c07077f480d3bbaee6c3960c
- id: interactive.inject
  phase: interactive
  action: |-
    Operator changes only OR reduction of `mem_wstrb[3:0]` deriving `we` to AND inside `picorv32_wb` in `picorv32.v`; hide mutation/location from next prompt.
  checks:
  - id: interactive.inject
    capabilities:
    - I-01
    stimulus: |-
      Operator changes only OR reduction of `mem_wstrb[3:0]` deriving `we` to AND inside `picorv32_wb` in `picorv32.v`; hide mutation/location from next prompt.
    expected: |-
      Operator changes only OR reduction of `mem_wstrb[3:0]` deriving `we` to AND inside `picorv32_wb` in `picorv32.v`; hide mutation/location from next prompt.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact patch, prior tree, operator authority, prompt and access separation.
    capture: evidence/picorv32-published-demo-continuity.interactive.inject
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  - interactive.wishbone-baseline
  assets:
  - path: probes/wishbone-fault.md
    audience: coordinator
    sha256: edd8b9ed5f5cf07ae46eadf0932be34a6e4bafb8025782da3d172a1de9d6b139
- id: interactive.reproduce
  phase: interactive
  action: |-
    Send exact second prompt to the same child; reproduce failing ordinary byte/halfword store behavior, preserving full-word behavior.
  checks:
  - id: interactive.reproduce
    capabilities:
    - I-01
    stimulus: |-
      Send exact second prompt to the same child; reproduce failing ordinary byte/halfword store behavior, preserving full-word behavior.
    expected: |-
      Send exact second prompt to the same child; reproduce failing ordinary byte/halfword store behavior, preserving full-word behavior.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Failed run, early byte-store `ERROR` signature and fresh nonempty trace/VCD.
    capture: evidence/picorv32-published-demo-continuity.interactive.reproduce
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  - interactive.inject
  assets:
  - path: prompts/repair-interactive.md
    audience: Interactive Mode delegate
    sha256: 92d6c1d18b25954ba6059a6c6b038e5de83aca1e40510ef37a209ffdb81d2531
- id: interactive.trace-observability
  phase: interactive
  action: |-
    Trace exposes `mem_wstrb`, `we`, `wbm_we_o`, `wbm_sel_o`, `wbm_stb_o`, `wbm_cyc_o`, `wbm_ack_i`, `mem_valid`, `mem_ready`, `ram_we`.
  checks:
  - id: interactive.trace-observability
    capabilities:
    - W-01
    stimulus: |-
      Trace exposes `mem_wstrb`, `we`, `wbm_we_o`, `wbm_sel_o`, `wbm_stb_o`, `wbm_cyc_o`, `wbm_ack_i`, `mem_valid`, `mem_ready`, `ram_we`.
    expected: |-
      Trace exposes `mem_wstrb`, `we`, `wbm_we_o`, `wbm_sel_o`, `wbm_stb_o`, `wbm_cyc_o`, `wbm_ack_i`, `mem_valid`, `mem_ready`, `ram_we`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Signal inventory and identity-bound trace.
    capture: evidence/picorv32-published-demo-continuity.interactive.trace-observability
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
- id: interactive.bwave-diagnosis
  phase: interactive
  action: Child diagnoses violated relationship using B-Wave, not hard-coded timestamps.
  checks:
  - id: interactive.bwave-diagnosis
    capabilities:
    - W-03
    - EDA-bwave-icarus
    stimulus: Child diagnoses violated relationship using B-Wave, not hard-coded timestamps.
    expected: Child diagnoses violated relationship using B-Wave, not hard-coded timestamps.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Queries, values and causal explanation grounded in relationships.
    capture: evidence/picorv32-published-demo-continuity.interactive.bwave-diagnosis
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
- id: interactive.repair
  phase: interactive
  action: Child repairs root cause without weakening tests, removing stimulus, waivers or unrelated edits.
  checks:
  - id: interactive.repair
    capabilities:
    - T-02
    stimulus: Child repairs root cause without weakening tests, removing stimulus, waivers or unrelated edits.
    expected: Child repairs root cause without weakening tests, removing stimulus, waivers or unrelated edits.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Patch and scope/test comparison.
    capture: evidence/picorv32-published-demo-continuity.interactive.repair
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
  - interactive.wishbone-baseline
- id: interactive.rerun-simulation
  phase: interactive
  action: Child reruns relevant simulation after repair; require fresh passing result.
  checks:
  - id: interactive.rerun-simulation
    capabilities:
    - I-01
    stimulus: Child reruns relevant simulation after repair; require fresh passing result.
    expected: Child reruns relevant simulation after repair; require fresh passing result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: New simulation verdict and artifacts.
    capture: evidence/picorv32-published-demo-continuity.interactive.rerun-simulation
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
- id: interactive.rerun-lint
  phase: interactive
  action: Child reruns relevant lint Target; require clean result.
  checks:
  - id: interactive.rerun-lint
    capabilities:
    - F-02
    stimulus: Child reruns relevant lint Target; require clean result.
    expected: Child reruns relevant lint Target; require clean result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Fresh lint result and Target identity.
    capture: evidence/picorv32-published-demo-continuity.interactive.rerun-lint
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
- id: interactive.local-commit
  phase: interactive
  action: Child commits repair locally; remote push remains blocked.
  checks:
  - id: interactive.local-commit
    capabilities:
    - I-01
    stimulus: Child commits repair locally; remote push remains blocked.
    expected: Child commits repair locally; remote push remains blocked.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Commit identity, branch state and push-authority enforcement evidence.
    capture: evidence/picorv32-published-demo-continuity.interactive.local-commit
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
- id: interactive.restore
  phase: interactive
  action: Restore clean pre-exercise checkpoint and discard all Interactive changes.
  checks:
  - id: interactive.restore
    capabilities:
    - I-01
    stimulus: Restore clean pre-exercise checkpoint and discard all Interactive changes.
    expected: Restore clean pre-exercise checkpoint and discard all Interactive changes.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Tree/branch comparison and removal proof; restored post-Interactive recovery point.
    capture: evidence/picorv32-published-demo-continuity.interactive.restore
  requires:
  - interactive.wishbone-baseline
  recovery:
    baseline: interactive.wishbone-baseline
    detection:
    - interactive.reproduce
    instruction: Restore clean pre-exercise checkpoint and discard all Interactive changes.
- id: interactive.supported-client
  phase: interactive
  action: |-
    When claiming actual supported VS Code integration, exercise this journey through that client and verify runtime attachment/context and MCP operations.
  checks:
  - id: interactive.supported-client
    capabilities:
    - I-01
    stimulus: |-
      When claiming actual supported VS Code integration, exercise this journey through that client and verify runtime attachment/context and MCP operations.
    expected: |-
      When claiming actual supported VS Code integration, exercise this journey through that client and verify runtime attachment/context and MCP operations.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Actual client/backend/version, attachment evidence and durable client operation logs; headless child
      is insufficient.
    capture: evidence/picorv32-published-demo-continuity.interactive.supported-client
  requires:
  - inputs.project-pin
  - baseline.physical-synthesis
  - baseline.icarus-wishbone
- id: inventory.D-03.docs-contract
  phase: interactive
  action: |-
    Execute `docs-contract` using the attached scenario-owned probe contract.
    `docs-contract`: each step follows released README/Setup/Usage/Config/Flow/EDA/Troubleshooting/help/skills → sufficient coherent public route or specific Finding with observation first. `docs-recovery`: documented recovery after seeded fault → user can resume from named checkpoint without hidden maintainer knowledge.
  checks:
  - id: inventory.D-03.docs-contract
    capabilities:
    - D-03
    stimulus: |-
      Execute `docs-contract` using the attached scenario-owned probe contract.
      `docs-contract`: each step follows released README/Setup/Usage/Config/Flow/EDA/Troubleshooting/help/skills → sufficient coherent public route or specific Finding with observation first. `docs-recovery`: documented recovery after seeded fault → user can resume from named checkpoint without hidden maintainer knowledge.
    expected: |-
      Evaluate only the independently named `docs-contract` outcome in this contract.
      `docs-contract`: each step follows released README/Setup/Usage/Config/Flow/EDA/Troubleshooting/help/skills → sufficient coherent public route or specific Finding with observation first. `docs-recovery`: documented recovery after seeded fault → user can resume from named checkpoint without hidden maintainer knowledge.
    authority_ref: https://github.com/boldaxolotl/booley/issues/376#issuecomment-5596872672
    evidence: |-
      Frozen public sources, first observation timestamp, later verification source if used, exact missing/contradictory instruction. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.D-03.docs-contract
  requires:
  - inputs.project-pin
  assets:
  - path: probes/D-03.md
    audience: coordinator
    sha256: a5f257d0a8d9e8926efd0d4c86eb1aed16adf302b4dfad9e1499337f9a9fc947
    base: shared
- id: inventory.D-03.docs-recovery
  phase: interactive
  action: |-
    Execute `docs-recovery` using the attached scenario-owned probe contract.
    `docs-contract`: each step follows released README/Setup/Usage/Config/Flow/EDA/Troubleshooting/help/skills → sufficient coherent public route or specific Finding with observation first. `docs-recovery`: documented recovery after seeded fault → user can resume from named checkpoint without hidden maintainer knowledge.
  checks:
  - id: inventory.D-03.docs-recovery
    capabilities:
    - D-03
    stimulus: |-
      Execute `docs-recovery` using the attached scenario-owned probe contract.
      `docs-contract`: each step follows released README/Setup/Usage/Config/Flow/EDA/Troubleshooting/help/skills → sufficient coherent public route or specific Finding with observation first. `docs-recovery`: documented recovery after seeded fault → user can resume from named checkpoint without hidden maintainer knowledge.
    expected: |-
      Evaluate only the independently named `docs-recovery` outcome in this contract.
      `docs-contract`: each step follows released README/Setup/Usage/Config/Flow/EDA/Troubleshooting/help/skills → sufficient coherent public route or specific Finding with observation first. `docs-recovery`: documented recovery after seeded fault → user can resume from named checkpoint without hidden maintainer knowledge.
    authority_ref: https://github.com/boldaxolotl/booley/issues/376#issuecomment-5596872672
    evidence: |-
      Frozen public sources, first observation timestamp, later verification source if used, exact missing/contradictory instruction. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.D-03.docs-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/D-03.md
    audience: coordinator
    sha256: a5f257d0a8d9e8926efd0d4c86eb1aed16adf302b4dfad9e1499337f9a9fc947
    base: shared
- id: inventory.I-01.interactive-commit
  phase: interactive
  action: |-
    Execute `interactive-commit` using the attached scenario-owned probe contract.
    `client-session`: actual selected VS Code client calls status/targets/Flow/Specialist → runtime identity and durable interactive logs; no Ticket/Criteria created. `interactive-commit`: safe edit/commit → exact local diff/history. `bare-launch`: bare booley/chat → configured provider and Project cwd. `bare-launch-failure`: missing disposable client executable → actionable diagnostic. `bare-launch-recovery`: restore executable → native CLI launches.
  checks:
  - id: inventory.I-01.interactive-commit
    capabilities:
    - I-01
    stimulus: |-
      Execute `interactive-commit` using the attached scenario-owned probe contract.
      `client-session`: actual selected VS Code client calls status/targets/Flow/Specialist → runtime identity and durable interactive logs; no Ticket/Criteria created. `interactive-commit`: safe edit/commit → exact local diff/history. `bare-launch`: bare booley/chat → configured provider and Project cwd. `bare-launch-failure`: missing disposable client executable → actionable diagnostic. `bare-launch-recovery`: restore executable → native CLI launches.
    expected: |-
      Evaluate only the independently named `interactive-commit` outcome in this contract.
      `client-session`: actual selected VS Code client calls status/targets/Flow/Specialist → runtime identity and durable interactive logs; no Ticket/Criteria created. `interactive-commit`: safe edit/commit → exact local diff/history. `bare-launch`: bare booley/chat → configured provider and Project cwd. `bare-launch-failure`: missing disposable client executable → actionable diagnostic. `bare-launch-recovery`: restore executable → native CLI launches.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#interactive-mode
    evidence: |-
      Supported-client transcript is required for client coverage; shell/MCP harness cannot replace it. Provider compatibility executions remain separate. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.I-01.interactive-commit
  requires:
  - inputs.project-pin
  assets:
  - path: probes/I-01.md
    audience: coordinator
    sha256: b2055584e0d6dd6cdd9fe8a55841ebb56d2c1d37886ad9f4017cc6247a0767e0
    base: shared
- id: inventory.I-05.catalog-coherence
  phase: interactive
  action: |-
    Execute `catalog-coherence` using the attached scenario-owned probe contract.
    `public-navigation`: record consulted help/cheat/MCP/skill for each step → reproducible public route. `catalog-coherence`: compare advertised Flow/Specialist names → agreement, or distinct docs failure recorded before investigation.
  checks:
  - id: inventory.I-05.catalog-coherence
    capabilities:
    - I-05
    stimulus: |-
      Execute `catalog-coherence` using the attached scenario-owned probe contract.
      `public-navigation`: record consulted help/cheat/MCP/skill for each step → reproducible public route. `catalog-coherence`: compare advertised Flow/Specialist names → agreement, or distinct docs failure recorded before investigation.
    expected: |-
      Evaluate only the independently named `catalog-coherence` outcome in this contract.
      `public-navigation`: record consulted help/cheat/MCP/skill for each step → reproducible public route. `catalog-coherence`: compare advertised Flow/Specialist names → agreement, or distinct docs failure recorded before investigation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/src/booley/data/cheatsheet.md
    evidence: |-
      Exact released documentation/help snapshots; unavailable GUI does not erase accessible CLI documentation checks. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.I-05.catalog-coherence
  requires:
  - inputs.project-pin
  assets:
  - path: probes/I-05.md
    audience: coordinator
    sha256: a8e16ffa4a13a133b751adbd9fdf91391f7585cc9f57e7b5a133481b860d945e
    base: shared
- id: inventory.I-05.public-navigation
  phase: interactive
  action: |-
    Execute `public-navigation` using the attached scenario-owned probe contract.
    `public-navigation`: record consulted help/cheat/MCP/skill for each step → reproducible public route. `catalog-coherence`: compare advertised Flow/Specialist names → agreement, or distinct docs failure recorded before investigation.
  checks:
  - id: inventory.I-05.public-navigation
    capabilities:
    - I-05
    stimulus: |-
      Execute `public-navigation` using the attached scenario-owned probe contract.
      `public-navigation`: record consulted help/cheat/MCP/skill for each step → reproducible public route. `catalog-coherence`: compare advertised Flow/Specialist names → agreement, or distinct docs failure recorded before investigation.
    expected: |-
      Evaluate only the independently named `public-navigation` outcome in this contract.
      `public-navigation`: record consulted help/cheat/MCP/skill for each step → reproducible public route. `catalog-coherence`: compare advertised Flow/Specialist names → agreement, or distinct docs failure recorded before investigation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/src/booley/data/cheatsheet.md
    evidence: |-
      Exact released documentation/help snapshots; unavailable GUI does not erase accessible CLI documentation checks. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.I-05.public-navigation
  requires:
  - inputs.project-pin
  assets:
  - path: probes/I-05.md
    audience: coordinator
    sha256: a8e16ffa4a13a133b751adbd9fdf91391f7585cc9f57e7b5a133481b860d945e
    base: shared
- id: inventory.ST-04.interrupt-cleanup
  phase: interactive
  action: |-
    Execute `interrupt-cleanup` using the attached scenario-owned probe contract.
    `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
  checks:
  - id: inventory.ST-04.interrupt-cleanup
    capabilities:
    - ST-04
    stimulus: |-
      Execute `interrupt-cleanup` using the attached scenario-owned probe contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    expected: |-
      Evaluate only the independently named `interrupt-cleanup` outcome in this contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/ARCHITECTURE.md#security--trust-model
    evidence: |-
      Mount/capability metadata and benign probe outputs; do not enumerate unrelated secrets. Shared process evidence can map H-06 explicitly. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-04.interrupt-cleanup
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-04.md
    audience: coordinator
    sha256: 3bb12df2a833d085468870f7f1de96518b57d6a539495d99db6a3b2922232da4
    base: shared
- id: inventory.ST-04.mount-boundary
  phase: interactive
  action: |-
    Execute `mount-boundary` using the attached scenario-owned probe contract.
    `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
  checks:
  - id: inventory.ST-04.mount-boundary
    capabilities:
    - ST-04
    stimulus: |-
      Execute `mount-boundary` using the attached scenario-owned probe contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    expected: |-
      Evaluate only the independently named `mount-boundary` outcome in this contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/ARCHITECTURE.md#security--trust-model
    evidence: |-
      Mount/capability metadata and benign probe outputs; do not enumerate unrelated secrets. Shared process evidence can map H-06 explicitly. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-04.mount-boundary
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-04.md
    audience: coordinator
    sha256: 3bb12df2a833d085468870f7f1de96518b57d6a539495d99db6a3b2922232da4
    base: shared
- id: inventory.ST-04.network-deny
  phase: interactive
  action: |-
    Execute `network-deny` using the attached scenario-owned probe contract.
    `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
  checks:
  - id: inventory.ST-04.network-deny
    capabilities:
    - ST-04
    stimulus: |-
      Execute `network-deny` using the attached scenario-owned probe contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    expected: |-
      Evaluate only the independently named `network-deny` outcome in this contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/ARCHITECTURE.md#security--trust-model
    evidence: |-
      Mount/capability metadata and benign probe outputs; do not enumerate unrelated secrets. Shared process evidence can map H-06 explicitly. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-04.network-deny
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-04.md
    audience: coordinator
    sha256: 3bb12df2a833d085468870f7f1de96518b57d6a539495d99db6a3b2922232da4
    base: shared
- id: inventory.ST-04.origin-push-deny
  phase: interactive
  action: |-
    Execute `origin-push-deny` using the attached scenario-owned probe contract.
    `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
  checks:
  - id: inventory.ST-04.origin-push-deny
    capabilities:
    - ST-04
    stimulus: |-
      Execute `origin-push-deny` using the attached scenario-owned probe contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    expected: |-
      Evaluate only the independently named `origin-push-deny` outcome in this contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/ARCHITECTURE.md#security--trust-model
    evidence: |-
      Mount/capability metadata and benign probe outputs; do not enumerate unrelated secrets. Shared process evidence can map H-06 explicitly. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-04.origin-push-deny
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-04.md
    audience: coordinator
    sha256: 3bb12df2a833d085468870f7f1de96518b57d6a539495d99db6a3b2922232da4
    base: shared
- id: inventory.ST-04.pdk-readonly
  phase: interactive
  action: |-
    Execute `pdk-readonly` using the attached scenario-owned probe contract.
    `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
  checks:
  - id: inventory.ST-04.pdk-readonly
    capabilities:
    - ST-04
    stimulus: |-
      Execute `pdk-readonly` using the attached scenario-owned probe contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    expected: |-
      Evaluate only the independently named `pdk-readonly` outcome in this contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/ARCHITECTURE.md#security--trust-model
    evidence: |-
      Mount/capability metadata and benign probe outputs; do not enumerate unrelated secrets. Shared process evidence can map H-06 explicitly. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-04.pdk-readonly
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-04.md
    audience: coordinator
    sha256: 3bb12df2a833d085468870f7f1de96518b57d6a539495d99db6a3b2922232da4
    base: shared
- id: inventory.ST-04.provider-allow
  phase: interactive
  action: |-
    Execute `provider-allow` using the attached scenario-owned probe contract.
    `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
  checks:
  - id: inventory.ST-04.provider-allow
    capabilities:
    - ST-04
    stimulus: |-
      Execute `provider-allow` using the attached scenario-owned probe contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    expected: |-
      Evaluate only the independently named `provider-allow` outcome in this contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/ARCHITECTURE.md#security--trust-model
    evidence: |-
      Mount/capability metadata and benign probe outputs; do not enumerate unrelated secrets. Shared process evidence can map H-06 explicitly. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-04.provider-allow
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-04.md
    audience: coordinator
    sha256: 3bb12df2a833d085468870f7f1de96518b57d6a539495d99db6a3b2922232da4
    base: shared
- id: inventory.ST-04.runtime-user
  phase: interactive
  action: |-
    Execute `runtime-user` using the attached scenario-owned probe contract.
    `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
  checks:
  - id: inventory.ST-04.runtime-user
    capabilities:
    - ST-04
    stimulus: |-
      Execute `runtime-user` using the attached scenario-owned probe contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    expected: |-
      Evaluate only the independently named `runtime-user` outcome in this contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/ARCHITECTURE.md#security--trust-model
    evidence: |-
      Mount/capability metadata and benign probe outputs; do not enumerate unrelated secrets. Shared process evidence can map H-06 explicitly. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-04.runtime-user
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-04.md
    audience: coordinator
    sha256: 3bb12df2a833d085468870f7f1de96518b57d6a539495d99db6a3b2922232da4
    base: shared
- id: inventory.W-01.trace-fifo
  phase: interactive
  action: |-
    Execute `trace-fifo` using the attached scenario-owned probe contract.
    `trace-fresh`: traced real sim → FST newer than invocation, nonempty and queryable with expected scopes/counts. Taxi `trace-native`: direct native FST → no conversion required. PicoRV32 `trace-vcd-convert`, `trace-fifo`: VCD file/FIFO conversion → queryable FST with same ground truth. Taxi `trace-raw-vcd`, `trace-legacy`, `trace-empty`, `trace-truncated`: each invalid query store → actionable rejection. `trace-recovery`: regenerate valid trace → correct data.
  checks:
  - id: inventory.W-01.trace-fifo
    capabilities:
    - W-01
    stimulus: |-
      Execute `trace-fifo` using the attached scenario-owned probe contract.
      `trace-fresh`: traced real sim → FST newer than invocation, nonempty and queryable with expected scopes/counts. Taxi `trace-native`: direct native FST → no conversion required. PicoRV32 `trace-vcd-convert`, `trace-fifo`: VCD file/FIFO conversion → queryable FST with same ground truth. Taxi `trace-raw-vcd`, `trace-legacy`, `trace-empty`, `trace-truncated`: each invalid query store → actionable rejection. `trace-recovery`: regenerate valid trace → correct data.
    expected: |-
      Evaluate only the independently named `trace-fifo` outcome in this contract.
      `trace-fresh`: traced real sim → FST newer than invocation, nonempty and queryable with expected scopes/counts. Taxi `trace-native`: direct native FST → no conversion required. PicoRV32 `trace-vcd-convert`, `trace-fifo`: VCD file/FIFO conversion → queryable FST with same ground truth. Taxi `trace-raw-vcd`, `trace-legacy`, `trace-empty`, `trace-truncated`: each invalid query store → actionable rejection. `trace-recovery`: regenerate valid trace → correct data.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FEATURES.md#waveform-based-debug
    evidence: |-
      Original/conversion/queried artifact identities, metadata and values; never count raw VCD as successful query store. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.W-01.trace-fifo
  requires:
  - inputs.project-pin
  assets:
  - path: probes/W-01.md
    audience: coordinator
    sha256: 6b49889e5dae5690b12119f6f4b0e7527a1eb50f937ef6c65a8453eefd23abe6
    base: shared
  - base: shared
    path: probes/bwave-matrices.md
    audience: coordinator
    sha256: a16a051d9c72772134d0eb056aaf2af3b050c24bc00c3412eb3ab438d2d6564e
- id: inventory.W-01.trace-fresh
  phase: interactive
  action: |-
    Execute `trace-fresh` using the attached scenario-owned probe contract.
    `trace-fresh`: traced real sim → FST newer than invocation, nonempty and queryable with expected scopes/counts. Taxi `trace-native`: direct native FST → no conversion required. PicoRV32 `trace-vcd-convert`, `trace-fifo`: VCD file/FIFO conversion → queryable FST with same ground truth. Taxi `trace-raw-vcd`, `trace-legacy`, `trace-empty`, `trace-truncated`: each invalid query store → actionable rejection. `trace-recovery`: regenerate valid trace → correct data.
  checks:
  - id: inventory.W-01.trace-fresh
    capabilities:
    - W-01
    stimulus: |-
      Execute `trace-fresh` using the attached scenario-owned probe contract.
      `trace-fresh`: traced real sim → FST newer than invocation, nonempty and queryable with expected scopes/counts. Taxi `trace-native`: direct native FST → no conversion required. PicoRV32 `trace-vcd-convert`, `trace-fifo`: VCD file/FIFO conversion → queryable FST with same ground truth. Taxi `trace-raw-vcd`, `trace-legacy`, `trace-empty`, `trace-truncated`: each invalid query store → actionable rejection. `trace-recovery`: regenerate valid trace → correct data.
    expected: |-
      Evaluate only the independently named `trace-fresh` outcome in this contract.
      `trace-fresh`: traced real sim → FST newer than invocation, nonempty and queryable with expected scopes/counts. Taxi `trace-native`: direct native FST → no conversion required. PicoRV32 `trace-vcd-convert`, `trace-fifo`: VCD file/FIFO conversion → queryable FST with same ground truth. Taxi `trace-raw-vcd`, `trace-legacy`, `trace-empty`, `trace-truncated`: each invalid query store → actionable rejection. `trace-recovery`: regenerate valid trace → correct data.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FEATURES.md#waveform-based-debug
    evidence: |-
      Original/conversion/queried artifact identities, metadata and values; never count raw VCD as successful query store. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.W-01.trace-fresh
  requires:
  - inputs.project-pin
  assets:
  - path: probes/W-01.md
    audience: coordinator
    sha256: 6b49889e5dae5690b12119f6f4b0e7527a1eb50f937ef6c65a8453eefd23abe6
    base: shared
  - base: shared
    path: probes/bwave-matrices.md
    audience: coordinator
    sha256: a16a051d9c72772134d0eb056aaf2af3b050c24bc00c3412eb3ab438d2d6564e
- id: inventory.W-01.trace-vcd-convert
  phase: interactive
  action: |-
    Execute `trace-vcd-convert` using the attached scenario-owned probe contract.
    `trace-fresh`: traced real sim → FST newer than invocation, nonempty and queryable with expected scopes/counts. Taxi `trace-native`: direct native FST → no conversion required. PicoRV32 `trace-vcd-convert`, `trace-fifo`: VCD file/FIFO conversion → queryable FST with same ground truth. Taxi `trace-raw-vcd`, `trace-legacy`, `trace-empty`, `trace-truncated`: each invalid query store → actionable rejection. `trace-recovery`: regenerate valid trace → correct data.
  checks:
  - id: inventory.W-01.trace-vcd-convert
    capabilities:
    - W-01
    stimulus: |-
      Execute `trace-vcd-convert` using the attached scenario-owned probe contract.
      `trace-fresh`: traced real sim → FST newer than invocation, nonempty and queryable with expected scopes/counts. Taxi `trace-native`: direct native FST → no conversion required. PicoRV32 `trace-vcd-convert`, `trace-fifo`: VCD file/FIFO conversion → queryable FST with same ground truth. Taxi `trace-raw-vcd`, `trace-legacy`, `trace-empty`, `trace-truncated`: each invalid query store → actionable rejection. `trace-recovery`: regenerate valid trace → correct data.
    expected: |-
      Evaluate only the independently named `trace-vcd-convert` outcome in this contract.
      `trace-fresh`: traced real sim → FST newer than invocation, nonempty and queryable with expected scopes/counts. Taxi `trace-native`: direct native FST → no conversion required. PicoRV32 `trace-vcd-convert`, `trace-fifo`: VCD file/FIFO conversion → queryable FST with same ground truth. Taxi `trace-raw-vcd`, `trace-legacy`, `trace-empty`, `trace-truncated`: each invalid query store → actionable rejection. `trace-recovery`: regenerate valid trace → correct data.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FEATURES.md#waveform-based-debug
    evidence: |-
      Original/conversion/queried artifact identities, metadata and values; never count raw VCD as successful query store. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.W-01.trace-vcd-convert
  requires:
  - inputs.project-pin
  assets:
  - path: probes/W-01.md
    audience: coordinator
    sha256: 6b49889e5dae5690b12119f6f4b0e7527a1eb50f937ef6c65a8453eefd23abe6
    base: shared
  - base: shared
    path: probes/bwave-matrices.md
    audience: coordinator
    sha256: a16a051d9c72772134d0eb056aaf2af3b050c24bc00c3412eb3ab438d2d6564e
- id: inventory.ST-04.vivado-readonly
  phase: interactive
  action: |-
    Execute `vivado-readonly` using the attached scenario-owned probe contract.
    `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
  checks:
  - id: inventory.ST-04.vivado-readonly
    capabilities:
    - ST-04
    stimulus: |-
      Execute `vivado-readonly` using the attached scenario-owned probe contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    expected: |-
      Evaluate only the independently named `vivado-readonly` outcome in this contract.
      `runtime-user`: inspect UID/capabilities → non-root/restricted. `mount-boundary`: inspect mounts → absent host home/SSH/Docker socket. `pdk-readonly`: benign owned-name write attempt to PDK mount → refused. PicoRV32 `vivado-readonly` same on provisioned mount → refused. `network-deny`: controlled undeclared route → blocked. `provider-allow`: real selected-provider call → works through allowed path. `origin-push-deny`: runtime attempts harmless local-fixture origin push → prohibited by documented agent boundary. `interrupt-cleanup`: interrupt owned work → no descendants.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/ARCHITECTURE.md#security--trust-model
    evidence: |-
      Mount/capability metadata and benign probe outputs; do not enumerate unrelated secrets. Shared process evidence can map H-06 explicitly. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-04.vivado-readonly
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-04.md
    audience: coordinator
    sha256: 3bb12df2a833d085468870f7f1de96518b57d6a539495d99db6a3b2922232da4
    base: shared
- id: inventory.I-01.client-session
  phase: interactive
  action: |-
    Execute `client-session` using the attached scenario-owned probe contract.
    `client-session`: actual selected VS Code client calls status/targets/Flow/Specialist → runtime identity and durable interactive logs; no Ticket/Criteria created. `interactive-commit`: safe edit/commit → exact local diff/history. `bare-launch`: bare booley/chat → configured provider and Project cwd. `bare-launch-failure`: missing disposable client executable → actionable diagnostic. `bare-launch-recovery`: restore executable → native CLI launches.
  checks:
  - id: inventory.I-01.client-session
    capabilities:
    - I-01
    stimulus: |-
      Execute `client-session` using the attached scenario-owned probe contract.
      `client-session`: actual selected VS Code client calls status/targets/Flow/Specialist → runtime identity and durable interactive logs; no Ticket/Criteria created. `interactive-commit`: safe edit/commit → exact local diff/history. `bare-launch`: bare booley/chat → configured provider and Project cwd. `bare-launch-failure`: missing disposable client executable → actionable diagnostic. `bare-launch-recovery`: restore executable → native CLI launches.
    expected: |-
      Evaluate only the independently named `client-session` outcome in this contract.
      `client-session`: actual selected VS Code client calls status/targets/Flow/Specialist → runtime identity and durable interactive logs; no Ticket/Criteria created. `interactive-commit`: safe edit/commit → exact local diff/history. `bare-launch`: bare booley/chat → configured provider and Project cwd. `bare-launch-failure`: missing disposable client executable → actionable diagnostic. `bare-launch-recovery`: restore executable → native CLI launches.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#interactive-mode
    evidence: |-
      Supported-client transcript is required for client coverage; shell/MCP harness cannot replace it. Provider compatibility executions remain separate. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.I-01.client-session
  requires:
  - inputs.project-pin
  assets:
  - path: probes/I-01.md
    audience: coordinator
    sha256: b2055584e0d6dd6cdd9fe8a55841ebb56d2c1d37886ad9f4017cc6247a0767e0
    base: shared
- id: create.dhrystone-self-checking-cycle-contract.payload
  phase: create
  action: |-
    Invoke Ticket Create skill for `dhrystone-self-checking-cycle-contract` with complete literal annex field set and `--agent --no-confirm`, using provider spelling.
  checks:
  - id: create.dhrystone-self-checking-cycle-contract.payload
    capabilities:
    - D-03
    stimulus: |-
      Invoke Ticket Create skill for `dhrystone-self-checking-cycle-contract` with complete literal annex field set and `--agent --no-confirm`, using provider spelling.
    expected: |-
      Invoke Ticket Create skill for `dhrystone-self-checking-cycle-contract` with complete literal annex field set and `--agent --no-confirm`, using provider spelling.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact invocation, full prompt hash and payload; this is a skill invocation, not ordinary CLI.
    capture: evidence/picorv32-published-demo-continuity.create.dhrystone-self-checking-cycle-contract.payload
  requires:
  - inputs.project-pin
  - interactive.restore
  assets:
  - path: tickets/continuity.md
    audience: Developer Agent
    sha256: 3d98db91369906cd94e5854c8facbdcecb0fa7ecf07e6aff4dc56b4c16cbf205
- id: create.dhrystone-self-checking-cycle-contract.board
  phase: create
  action: Creation returns board path and `queued` state for `dhrystone-self-checking-cycle-contract`.
  checks:
  - id: create.dhrystone-self-checking-cycle-contract.board
    capabilities:
    - D-03
    stimulus: Creation returns board path and `queued` state for `dhrystone-self-checking-cycle-contract`.
    expected: Creation returns board path and `queued` state for `dhrystone-self-checking-cycle-contract`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Returned path, parsed ticket and board state.
    capture: evidence/picorv32-published-demo-continuity.create.dhrystone-self-checking-cycle-contract.board
  requires:
  - inputs.project-pin
  - interactive.restore
- id: create.dhrystone-self-checking-cycle-contract.basis
  phase: create
  action: |-
    Enqueue of `dhrystone-self-checking-cycle-contract` automatically publishes immutable Acceptance Basis; no manual seal or second confirmation.
  checks:
  - id: create.dhrystone-self-checking-cycle-contract.basis
    capabilities:
    - T-05
    stimulus: |-
      Enqueue of `dhrystone-self-checking-cycle-contract` automatically publishes immutable Acceptance Basis; no manual seal or second confirmation.
    expected: |-
      Enqueue of `dhrystone-self-checking-cycle-contract` automatically publishes immutable Acceptance Basis; no manual seal or second confirmation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Published Basis receipt/identity.
    capture: evidence/picorv32-published-demo-continuity.create.dhrystone-self-checking-cycle-contract.basis
  requires:
  - inputs.project-pin
  - interactive.restore
- id: create.rv32-zbb-pcpi.payload
  phase: create
  action: |-
    Invoke Ticket Create skill for `rv32-zbb-pcpi` with complete literal annex field set and `--agent --no-confirm`, using provider spelling.
  checks:
  - id: create.rv32-zbb-pcpi.payload
    capabilities:
    - D-03
    stimulus: |-
      Invoke Ticket Create skill for `rv32-zbb-pcpi` with complete literal annex field set and `--agent --no-confirm`, using provider spelling.
    expected: |-
      Invoke Ticket Create skill for `rv32-zbb-pcpi` with complete literal annex field set and `--agent --no-confirm`, using provider spelling.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact invocation, full prompt hash and payload; this is a skill invocation, not ordinary CLI.
    capture: evidence/picorv32-published-demo-continuity.create.rv32-zbb-pcpi.payload
  requires:
  - inputs.project-pin
  - interactive.restore
  assets:
  - path: tickets/evolution.md
    audience: Developer Agent
    sha256: a133c11cf5aeff5b82576b302814a7215782244f8b627b7fa5391c56bb5f37f9
- id: create.rv32-zbb-pcpi.board
  phase: create
  action: Creation returns board path and `waiting` state for `rv32-zbb-pcpi`.
  checks:
  - id: create.rv32-zbb-pcpi.board
    capabilities:
    - D-03
    stimulus: Creation returns board path and `waiting` state for `rv32-zbb-pcpi`.
    expected: Creation returns board path and `waiting` state for `rv32-zbb-pcpi`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Returned path, parsed ticket and board state.
    capture: evidence/picorv32-published-demo-continuity.create.rv32-zbb-pcpi.board
  requires:
  - inputs.project-pin
  - interactive.restore
- id: create.rv32-zbb-pcpi.basis
  phase: create
  action: Enqueue of `rv32-zbb-pcpi` automatically publishes immutable Acceptance Basis; no manual seal or second
    confirmation.
  checks:
  - id: create.rv32-zbb-pcpi.basis
    capabilities:
    - T-05
    stimulus: Enqueue of `rv32-zbb-pcpi` automatically publishes immutable Acceptance Basis; no manual seal or second
      confirmation.
    expected: Enqueue of `rv32-zbb-pcpi` automatically publishes immutable Acceptance Basis; no manual seal or second
      confirmation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Published Basis receipt/identity.
    capture: evidence/picorv32-published-demo-continuity.create.rv32-zbb-pcpi.basis
  requires:
  - inputs.project-pin
  - interactive.restore
- id: create.barrier
  phase: create
  action: Create both Tickets before running either; Ticket 2 declares Ticket 1 dependency.
  checks:
  - id: create.barrier
    capabilities:
    - D-03
    stimulus: Create both Tickets before running either; Ticket 2 declares Ticket 1 dependency.
    expected: Create both Tickets before running either; Ticket 2 declares Ticket 1 dependency.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Ordered creation/run records; two-Ticket creation recovery point.
    capture: evidence/picorv32-published-demo-continuity.create.barrier
  requires:
  - inputs.project-pin
  - interactive.restore
  - create.dhrystone-self-checking-cycle-contract.basis
  - create.rv32-zbb-pcpi.basis
- id: create.authoring-boundary
  phase: create
  action: |-
    Ticket creation authors only approved Target definitions, owned `tests.toml` tables and empty `[new]` placeholders; Developer authors implementation.
  checks:
  - id: create.authoring-boundary
    capabilities:
    - D-03
    stimulus: |-
      Ticket creation authors only approved Target definitions, owned `tests.toml` tables and empty `[new]` placeholders; Developer authors implementation.
    expected: |-
      Ticket creation authors only approved Target definitions, owned `tests.toml` tables and empty `[new]` placeholders; Developer authors implementation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Before/after source and configuration diff.
    capture: evidence/picorv32-published-demo-continuity.create.authoring-boundary
  requires:
  - inputs.project-pin
  - interactive.restore
- id: retry.exact-allowance
  phase: create
  action: |-
    At most one automatic retry with `max_attempts: 1`, only exact `API Error: Response stalled mid-stream`; crashes, design failures, timeouts, context or usage failures are not retried.
  checks:
  - id: retry.exact-allowance
    capabilities:
    - D-03
    stimulus: |-
      At most one automatic retry with `max_attempts: 1`, only exact `API Error: Response stalled mid-stream`; crashes, design failures, timeouts, context or usage failures are not retried.
    expected: |-
      At most one automatic retry with `max_attempts: 1`, only exact `API Error: Response stalled mid-stream`; crashes, design failures, timeouts, context or usage failures are not retried.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: All attempts, exact error signature and retry decisions; retain original observations.
    capture: evidence/picorv32-published-demo-continuity.retry.exact-allowance
  requires:
  - inputs.project-pin
  - interactive.restore
- id: ticket1.scope
  phase: ticket1
  action: |-
    Run `booley run --ticket dhrystone-self-checking-cycle-contract`; verification changes only `dhrystone/dhry_1.c` and `dhrystone/testbench.v`.
  checks:
  - id: ticket1.scope
    capabilities:
    - T-02
    stimulus: |-
      Run `booley run --ticket dhrystone-self-checking-cycle-contract`; verification changes only `dhrystone/dhry_1.c` and `dhrystone/testbench.v`.
    expected: |-
      Run `booley run --ticket dhrystone-self-checking-cycle-contract`; verification changes only `dhrystone/dhry_1.c` and `dhrystone/testbench.v`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact call and complete diff.
    capture: evidence/picorv32-published-demo-continuity.ticket1.scope
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.fixed-iterations
  phase: ticket1
  action: Keep fixed 100-iteration demo.
  checks:
  - id: ticket1.fixed-iterations
    capabilities:
    - T-02
    stimulus: Keep fixed 100-iteration demo.
    expected: Keep fixed 100-iteration demo.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Firmware code and execution evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket1.fixed-iterations
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.firmware-validation
  phase: ticket1
  action: Validate deterministic final Dhrystone results before success/cycle output; mismatch prints error and
    traps.
  checks:
  - id: ticket1.firmware-validation
    capabilities:
    - T-02
    stimulus: Validate deterministic final Dhrystone results before success/cycle output; mismatch prints error
      and traps.
    expected: Validate deterministic final Dhrystone results before success/cycle output; mismatch prints error
      and traps.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Implementation and execution log tied to expected values.
    capture: evidence/picorv32-published-demo-continuity.ticket1.firmware-validation
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.success-magic
  phase: ticket1
  action: Preserve `123456789` write to MMIO `0x20000000`; testbench passes only validated success.
  checks:
  - id: ticket1.success-magic
    capabilities:
    - T-02
    stimulus: Preserve `123456789` write to MMIO `0x20000000`; testbench passes only validated success.
    expected: Preserve `123456789` write to MMIO `0x20000000`; testbench passes only validated success.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Firmware/testbench evidence and passing run ordering.
    capture: evidence/picorv32-published-demo-continuity.ticket1.success-magic
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.cycle-record
  phase: ticket1
  action: Emit exactly `[SIM_CYCLES] dhry <User_Time>` after validation with deterministic timeout.
  checks:
  - id: ticket1.cycle-record
    capabilities:
    - T-02
    stimulus: Emit exactly `[SIM_CYCLES] dhry <User_Time>` after validation with deterministic timeout.
    expected: Emit exactly `[SIM_CYCLES] dhry <User_Time>` after validation with deterministic timeout.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Output record ordering, count and timeout definition.
    capture: evidence/picorv32-published-demo-continuity.ticket1.cycle-record
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.elaboration
  phase: ticket1
  action: 'Require `elab_pass: [sim_dhry_checked]`.'
  checks:
  - id: ticket1.elaboration
    capabilities:
    - T-02
    stimulus: 'Require `elab_pass: [sim_dhry_checked]`.'
    expected: 'Require `elab_pass: [sim_dhry_checked]`.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Normalized Elaboration Check and Target identity.
    capture: evidence/picorv32-published-demo-continuity.ticket1.elaboration
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.simulation
  phase: ticket1
  action: Require `dhrystone/testbench.v @ sim_dhry_checked @ dhry @ pass -> pass`.
  checks:
  - id: ticket1.simulation
    capabilities:
    - T-02
    stimulus: Require `dhrystone/testbench.v @ sim_dhry_checked @ dhry @ pass -> pass`.
    expected: Require `dhrystone/testbench.v @ sim_dhry_checked @ dhry @ pass -> pass`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Acceptance Basis/candidate evidence and test verdict.
    capture: evidence/picorv32-published-demo-continuity.ticket1.simulation
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.cycle-cap
  phase: ticket1
  action: |-
    Require `cycle_count_max: 110000` inclusive for Target `sim_dhry_checked`, test `dhry`; no baseline count needed. Calibration is xPack GCC 15.2 `109734` cycles.
  checks:
  - id: ticket1.cycle-cap
    capabilities:
    - T-02
    stimulus: |-
      Require `cycle_count_max: 110000` inclusive for Target `sim_dhry_checked`, test `dhry`; no baseline count needed. Calibration is xPack GCC 15.2 `109734` cycles.
    expected: |-
      Require `cycle_count_max: 110000` inclusive for Target `sim_dhry_checked`, test `dhry`; no baseline count needed. Calibration is xPack GCC 15.2 `109734` cycles.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Parsed candidate cycle count, exact threshold and acceptance result.
    capture: evidence/picorv32-published-demo-continuity.ticket1.cycle-cap
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.tb-review
  phase: ticket1
  action: Require `review_tb_quality_done` bound to `sim_dhry_checked`.
  checks:
  - id: ticket1.tb-review
    capabilities:
    - S-04
    stimulus: Require `review_tb_quality_done` bound to `sim_dhry_checked`.
    expected: Require `review_tb_quality_done` bound to `sim_dhry_checked`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Terminal review report and Target binding.
    capture: evidence/picorv32-published-demo-continuity.ticket1.tb-review
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.persistent-target
  phase: ticket1
  action: Target Plan uses persistent `sim_dhry_checked`; register `[sim_dhry_checked] dhry`; remains selectable
    after acceptance.
  checks:
  - id: ticket1.persistent-target
    capabilities:
    - P-02
    stimulus: Target Plan uses persistent `sim_dhry_checked`; register `[sim_dhry_checked] dhry`; remains selectable
      after acceptance.
    expected: Target Plan uses persistent `sim_dhry_checked`; register `[sim_dhry_checked] dhry`; remains selectable
      after acceptance.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Created definitions/test table and post-acceptance fresh inventory.
    capture: evidence/picorv32-published-demo-continuity.ticket1.persistent-target
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.done
  phase: ticket1
  action: 'Reach `destination: done`.'
  checks:
  - id: ticket1.done
    capabilities:
    - T-02
    stimulus: 'Reach `destination: done`.'
    expected: 'Reach `destination: done`.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Board terminal state and acceptance evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket1.done
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.merge
  phase: ticket1
  action: Merge accepted local changes.
  checks:
  - id: ticket1.merge
    capabilities:
    - T-02
    stimulus: Merge accepted local changes.
    expected: Merge accepted local changes.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Accepted commit and destination branch identity.
    capture: evidence/picorv32-published-demo-continuity.ticket1.merge
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.workspace-cleanup
  phase: ticket1
  action: Clean Ticket worktree after acceptance.
  checks:
  - id: ticket1.workspace-cleanup
    capabilities:
    - T-02
    stimulus: Clean Ticket worktree after acceptance.
    expected: Clean Ticket worktree after acceptance.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Absence proof and resource entry.
    capture: evidence/picorv32-published-demo-continuity.ticket1.workspace-cleanup
  requires:
  - inputs.project-pin
  - create.barrier
- id: ticket1.triage-report
  phase: ticket1
  action: Generate triage report.
  checks:
  - id: ticket1.triage-report
    capabilities:
    - T-02
    stimulus: Generate triage report.
    expected: Generate triage report.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Fresh report artifact and ticket association.
    capture: evidence/picorv32-published-demo-continuity.ticket1.triage-report
  requires:
  - inputs.project-pin
  - create.barrier
- id: guard.negative-result
  phase: ticket1
  action: From disposable post-Ticket-1 recovery point corrupt one expected result; require deterministic failure.
  checks:
  - id: guard.negative-result
    capabilities:
    - D-03
    stimulus: From disposable post-Ticket-1 recovery point corrupt one expected result; require deterministic failure.
    expected: From disposable post-Ticket-1 recovery point corrupt one expected result; require deterministic failure.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Exact corruption, failing run and deterministic diagnostic.
    capture: evidence/picorv32-published-demo-continuity.guard.negative-result
  requires:
  - inputs.project-pin
  - create.barrier
  - ticket1.merge
  assets:
  - path: probes/dhrystone-guard.md
    audience: coordinator
    sha256: 3cfdccef4489822d9fe5f13086fa2e918a3449bbc6e2692077425e212c0cc5c8
- id: guard.no-success
  phase: ticket1
  action: Corrupted run emits no success magic.
  checks:
  - id: guard.no-success
    capabilities:
    - D-03
    stimulus: Corrupted run emits no success magic.
    expected: Corrupted run emits no success magic.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Complete negative-run output/MMIO evidence.
    capture: evidence/picorv32-published-demo-continuity.guard.no-success
  requires:
  - inputs.project-pin
  - create.barrier
- id: guard.no-cycles
  phase: ticket1
  action: Corrupted run emits no `[SIM_CYCLES]` record.
  checks:
  - id: guard.no-cycles
    capabilities:
    - D-03
    stimulus: Corrupted run emits no `[SIM_CYCLES]` record.
    expected: Corrupted run emits no `[SIM_CYCLES]` record.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Complete negative-run output.
    capture: evidence/picorv32-published-demo-continuity.guard.no-cycles
  requires:
  - inputs.project-pin
  - create.barrier
- id: guard.restore-rerun
  phase: ticket1
  action: Restore original expected result and rerun successfully.
  checks:
  - id: guard.restore-rerun
    capabilities:
    - D-03
    stimulus: Restore original expected result and rerun successfully.
    expected: Restore original expected result and rerun successfully.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Restored hashes, new passing result; Dhrystone guard recovery point.
    capture: evidence/picorv32-published-demo-continuity.guard.restore-rerun
  requires:
  - ticket1.merge
  recovery:
    baseline: ticket1.merge
    detection:
    - guard.negative-result
    instruction: Restore original expected result and rerun successfully.
- id: guard.discard
  phase: ticket1
  action: Discard guard disposable branch after evidence is retained.
  checks:
  - id: guard.discard
    capabilities:
    - D-03
    stimulus: Discard guard disposable branch after evidence is retained.
    expected: Discard guard disposable branch after evidence is retained.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Branch/worktree removal and resource reconciliation.
    capture: evidence/picorv32-published-demo-continuity.guard.discard
  requires:
  - inputs.project-pin
  - create.barrier
- id: inventory.F-01.sim-design-fail
  phase: ticket1
  action: |-
    Execute `sim-design-fail` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-design-fail
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-design-fail` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-design-fail` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-design-fail
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-elab
  phase: ticket1
  action: |-
    Execute `sim-elab` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-elab
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-elab` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-elab` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-elab
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-elab-args
  phase: ticket1
  action: |-
    Execute `sim-elab-args` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-elab-args
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-elab-args` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-elab-args` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-elab-args
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-elab-persistence
  phase: ticket1
  action: |-
    Execute `sim-elab-persistence` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-elab-persistence
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-elab-persistence` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-elab-persistence` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-elab-persistence
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-inconclusive
  phase: ticket1
  action: |-
    Execute `sim-inconclusive` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-inconclusive
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-inconclusive` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-inconclusive` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-inconclusive
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-infra
  phase: ticket1
  action: |-
    Execute `sim-infra` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-infra
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-infra` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-infra` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-infra
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-multi
  phase: ticket1
  action: |-
    Execute `sim-multi` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-multi
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-multi` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-multi` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-multi
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-pass
  phase: ticket1
  action: |-
    Execute `sim-pass` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-pass
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-pass` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-pass` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-pass
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-recovery
  phase: ticket1
  action: |-
    Execute `sim-recovery` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-recovery
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-recovery` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-recovery` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-standalone-fail
  phase: ticket1
  action: |-
    Execute `sim-standalone-fail` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-standalone-fail
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-standalone-fail` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-standalone-fail` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-standalone-fail
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.F-01.sim-standalone-pass
  phase: ticket1
  action: |-
    Execute `sim-standalone-pass` using the attached scenario-owned probe contract.
    `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
  checks:
  - id: inventory.F-01.sim-standalone-pass
    capabilities:
    - F-01
    stimulus: |-
      Execute `sim-standalone-pass` using the attached scenario-owned probe contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    expected: |-
      Evaluate only the independently named `sim-standalone-pass` outcome in this contract.
      `sim-grades`: use independently named subchecks `sim-pass`, `sim-design-fail`, `sim-inconclusive`, `sim-infra` → correct normalized result and report class. `sim-multi`: selected pass+fail+pass Targets → all attempted, strongest grade retained. `sim-elab`: elab-only → durable successful elaboration. `sim-elab-args`: add run-only argument to elab → rejected. `sim-standalone-pass`, `sim-standalone-fail`: standalone good/bad module → distinct results. `sim-elab-persistence`: successful elaboration followed by failing run → elaboration remains durable.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#sim
    evidence: |-
      sim target JSON, artifacts/logs, exact target/test lists; restored failing fixture rerun earns separate `sim-recovery`. Focus/skip/traces reuse P-04/P-05/W-01 only through named mapping. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.F-01.sim-standalone-pass
  requires:
  - inputs.project-pin
  assets:
  - path: probes/F-01.md
    audience: coordinator
    sha256: 9a12c0c30957cc8eddc35478b4bff8f758a0a4fa131c61189c0dd752469270ec
- id: inventory.P-04.cycle-invalid
  phase: ticket1
  action: |-
    Execute `cycle-invalid` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.cycle-invalid
    capabilities:
    - P-04
    stimulus: |-
      Execute `cycle-invalid` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `cycle-invalid` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.cycle-invalid
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-04.cycle-valid
  phase: ticket1
  action: |-
    Execute `cycle-valid` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.cycle-valid
    capabilities:
    - P-04
    stimulus: |-
      Execute `cycle-valid` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `cycle-valid` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.cycle-valid
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-04.sentinel-both
  phase: ticket1
  action: |-
    Execute `sentinel-both` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.sentinel-both
    capabilities:
    - P-04
    stimulus: |-
      Execute `sentinel-both` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `sentinel-both` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.sentinel-both
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-04.sentinel-fail
  phase: ticket1
  action: |-
    Execute `sentinel-fail` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.sentinel-fail
    capabilities:
    - P-04
    stimulus: |-
      Execute `sentinel-fail` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `sentinel-fail` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.sentinel-fail
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-04.sentinel-none
  phase: ticket1
  action: |-
    Execute `sentinel-none` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.sentinel-none
    capabilities:
    - P-04
    stimulus: |-
      Execute `sentinel-none` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `sentinel-none` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.sentinel-none
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-04.sentinel-pass
  phase: ticket1
  action: |-
    Execute `sentinel-pass` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.sentinel-pass
    capabilities:
    - P-04
    stimulus: |-
      Execute `sentinel-pass` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `sentinel-pass` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.sentinel-pass
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-04.sentinel-recovery
  phase: ticket1
  action: |-
    Execute `sentinel-recovery` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.sentinel-recovery
    capabilities:
    - P-04
    stimulus: |-
      Execute `sentinel-recovery` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `sentinel-recovery` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.sentinel-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-04.skip-default
  phase: ticket1
  action: |-
    Execute `skip-default` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.skip-default
    capabilities:
    - P-04
    stimulus: |-
      Execute `skip-default` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `skip-default` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.skip-default
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-04.skip-override
  phase: ticket1
  action: |-
    Execute `skip-override` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.skip-override
    capabilities:
    - P-04
    stimulus: |-
      Execute `skip-override` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `skip-override` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.skip-override
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-04.test-token
  phase: ticket1
  action: |-
    Execute `test-token` using the attached scenario-owned probe contract.
    `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
  checks:
  - id: inventory.P-04.test-token
    capabilities:
    - P-04
    stimulus: |-
      Execute `test-token` using the attached scenario-owned probe contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    expected: |-
      Evaluate only the independently named `test-token` outcome in this contract.
      `sentinel-pass`, `sentinel-fail`, `sentinel-both`, `sentinel-none`: fixture emits each combination → pass, fail, fail, inconclusive respectively. `test-token`: select one plusarg/getopt token → exactly named test executed. `skip-default`: default run → skipped test absent. `skip-override`: explicit selection → same test runs. `cycle-valid`: emit named count → exact parsed count. `cycle-invalid`: malformed/missing count → cannot satisfy Criterion.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Per-test verdicts and raw logs; `sentinel-recovery` restores valid fixture → pass; malformed count is not silently zero. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-04.test-token
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-04.md
    audience: coordinator
    sha256: 1e1d38cd8c314f4fb4ce4aa4f53f415f514666373140f5f36599ffa47ed6f4a4
- id: inventory.P-06.disk-budget
  phase: ticket1
  action: |-
    Execute `disk-budget` using the attached scenario-owned probe contract.
    `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
  checks:
  - id: inventory.P-06.disk-budget
    capabilities:
    - P-06
    stimulus: |-
      Execute `disk-budget` using the attached scenario-owned probe contract.
      `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
    expected: |-
      Evaluate only the independently named `disk-budget` outcome in this contract.
      `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Environment variable names/nonsecret values, command/workdir, staged artifact digest, kill diagnostics/process evidence. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-06.disk-budget
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-06.md
    audience: coordinator
    sha256: fa1eeaec653bc20cceb1ebb69110c2e535ff5274d8cd0af6578122310e178aad
- id: inventory.P-06.frozen-clock
  phase: ticket1
  action: |-
    Execute `frozen-clock` using the attached scenario-owned probe contract.
    `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
  checks:
  - id: inventory.P-06.frozen-clock
    capabilities:
    - P-06
    stimulus: |-
      Execute `frozen-clock` using the attached scenario-owned probe contract.
      `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
    expected: |-
      Evaluate only the independently named `frozen-clock` outcome in this contract.
      `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Environment variable names/nonsecret values, command/workdir, staged artifact digest, kill diagnostics/process evidence. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-06.frozen-clock
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-06.md
    audience: coordinator
    sha256: fa1eeaec653bc20cceb1ebb69110c2e535ff5274d8cd0af6578122310e178aad
- id: inventory.P-06.prerun-input
  phase: ticket1
  action: |-
    Execute `prerun-input` using the attached scenario-owned probe contract.
    `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
  checks:
  - id: inventory.P-06.prerun-input
    capabilities:
    - P-06
    stimulus: |-
      Execute `prerun-input` using the attached scenario-owned probe contract.
      `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
    expected: |-
      Evaluate only the independently named `prerun-input` outcome in this contract.
      `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Environment variable names/nonsecret values, command/workdir, staged artifact digest, kill diagnostics/process evidence. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-06.prerun-input
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-06.md
    audience: coordinator
    sha256: fa1eeaec653bc20cceb1ebb69110c2e535ff5274d8cd0af6578122310e178aad
- id: inventory.P-06.prerun-recovery
  phase: ticket1
  action: |-
    Execute `prerun-recovery` using the attached scenario-owned probe contract.
    `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
  checks:
  - id: inventory.P-06.prerun-recovery
    capabilities:
    - P-06
    stimulus: |-
      Execute `prerun-recovery` using the attached scenario-owned probe contract.
      `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
    expected: |-
      Evaluate only the independently named `prerun-recovery` outcome in this contract.
      `prerun-input`: documented env/pre-run substitution/workdir stages firmware/vector → test consumes named fresh input without TB edits. `frozen-clock`: bounded frozen simulation → documented watchdog classification. `disk-budget`: bounded disposable output exceeds configured budget → killed with largest-file diagnostics. `prerun-recovery`: restore normal stimulus and rerun → fresh artifact/verdict, no surviving producer.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#simulation--passfail-sentinels-flowssim
    evidence: |-
      Environment variable names/nonsecret values, command/workdir, staged artifact digest, kill diagnostics/process evidence. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.P-06.prerun-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/P-06.md
    audience: coordinator
    sha256: fa1eeaec653bc20cceb1ebb69110c2e535ff5274d8cd0af6578122310e178aad
- id: refresh.old-basis
  phase: ticket2
  action: Retain waiting Ticket 2 original Basis before provider acceptance.
  checks:
  - id: refresh.old-basis
    capabilities:
    - T-05
    stimulus: Retain waiting Ticket 2 original Basis before provider acceptance.
    expected: Retain waiting Ticket 2 original Basis before provider acceptance.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Original immutable Basis and waiting state.
    capture: evidence/picorv32-published-demo-continuity.refresh.old-basis
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: refresh.provider-surface
  phase: ticket2
  action: |-
    After Ticket 1 acceptance automatic pre-execution Basis Refresh includes accepted Dhrystone source and persistent Target.
  checks:
  - id: refresh.provider-surface
    capabilities:
    - T-05
    stimulus: |-
      After Ticket 1 acceptance automatic pre-execution Basis Refresh includes accepted Dhrystone source and persistent Target.
    expected: |-
      After Ticket 1 acceptance automatic pre-execution Basis Refresh includes accepted Dhrystone source and persistent Target.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Old/new Basis IDs and source/Target comparison.
    capture: evidence/picorv32-published-demo-continuity.refresh.provider-surface
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: refresh.queue-transition
  phase: ticket2
  action: After refreshed provider verification atomically promote waiting to queue without user approval.
  checks:
  - id: refresh.queue-transition
    capabilities:
    - T-05
    stimulus: After refreshed provider verification atomically promote waiting to queue without user approval.
    expected: After refreshed provider verification atomically promote waiting to queue without user approval.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Board transition and published receipt ordering; Basis Refresh recovery point.
    capture: evidence/picorv32-published-demo-continuity.refresh.queue-transition
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: refresh.authored-drift
  phase: ticket2
  action: Authored-input drift blocks for return-to-draft instead of changing authority.
  checks:
  - id: refresh.authored-drift
    capabilities:
    - T-05
    stimulus: Authored-input drift blocks for return-to-draft instead of changing authority.
    expected: Authored-input drift blocks for return-to-draft instead of changing authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Controlled drift and observed state/diagnostic; see gap list for fixture isolation.
    capture: evidence/picorv32-published-demo-continuity.refresh.authored-drift
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
  assets:
  - path: probes/basis-refresh.md
    audience: coordinator
    sha256: b8f5141e2532b1b47e28e97ed17907c2a818bfd64a4d8aee84bcd03b991bef10
- id: refresh.incompatible-provider
  phase: ticket2
  action: Incompatible provider surface blocks instead of silently refreshing authority.
  checks:
  - id: refresh.incompatible-provider
    capabilities:
    - T-05
    stimulus: Incompatible provider surface blocks instead of silently refreshing authority.
    expected: Incompatible provider surface blocks instead of silently refreshing authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Controlled incompatibility and block evidence; restore waiting state before real Ticket 2.
    capture: evidence/picorv32-published-demo-continuity.refresh.incompatible-provider
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
  assets:
  - path: probes/basis-refresh.md
    audience: coordinator
    sha256: b8f5141e2532b1b47e28e97ed17907c2a818bfd64a4d8aee84bcd03b991bef10
- id: ticket2.scope
  phase: ticket2
  action: |-
    Run separate `booley run --ticket rv32-zbb-pcpi`; type feature, dependency Ticket 1; exact Scope `picorv32.v`, `testbench.v`, `testbench_wb.v`, `Makefile`, `tests/zbb.S [new]`.
  checks:
  - id: ticket2.scope
    capabilities:
    - T-02
    stimulus: |-
      Run separate `booley run --ticket rv32-zbb-pcpi`; type feature, dependency Ticket 1; exact Scope `picorv32.v`, `testbench.v`, `testbench_wb.v`, `Makefile`, `tests/zbb.S [new]`.
    expected: |-
      Run separate `booley run --ticket rv32-zbb-pcpi`; type feature, dependency Ticket 1; exact Scope `picorv32.v`, `testbench.v`, `testbench_wb.v`, `Makefile`, `tests/zbb.S [new]`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Invocation, parsed Ticket and complete diff.
    capture: evidence/picorv32-published-demo-continuity.ticket2.scope
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
  - refresh.queue-transition
- id: ticket2.isa-authority
  phase: ticket2
  action: Use `/opt/riscv-docs/riscv-isa-manual.html` as technical authority.
  checks:
  - id: ticket2.isa-authority
    capabilities:
    - T-02
    stimulus: Use `/opt/riscv-docs/riscv-isa-manual.html` as technical authority.
    expected: Use `/opt/riscv-docs/riscv-isa-manual.html` as technical authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Consulted document identity and ISA behavior evaluation.
    capture: evidence/picorv32-published-demo-continuity.ticket2.isa-authority
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.pcpi-timing
  phase: ticket2
  action: Use internal registered PCPI implementation with fixed one-cycle response.
  checks:
  - id: ticket2.pcpi-timing
    capabilities:
    - T-02
    stimulus: Use internal registered PCPI implementation with fixed one-cycle response.
    expected: Use internal registered PCPI implementation with fixed one-cycle response.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: RTL and timing simulation evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.pcpi-timing
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.enable-default
  phase: ticket2
  action: Add `ENABLE_ZBB` default 0 across core, AXI and Wishbone wrappers.
  checks:
  - id: ticket2.enable-default
    capabilities:
    - T-02
    stimulus: Add `ENABLE_ZBB` default 0 across core, AXI and Wishbone wrappers.
    expected: Add `ENABLE_ZBB` default 0 across core, AXI and Wishbone wrappers.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Each wrapper declaration plus disabled/default execution evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.enable-default
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.disabled-trap
  phase: ticket2
  action: |-
    Arm distinct MMIO marker immediately before first Zbb encoding; require ensuing illegal-instruction trap; unrelated trap cannot pass.
  checks:
  - id: ticket2.disabled-trap
    capabilities:
    - T-02
    stimulus: |-
      Arm distinct MMIO marker immediately before first Zbb encoding; require ensuing illegal-instruction trap; unrelated trap cannot pass.
    expected: |-
      Arm distinct MMIO marker immediately before first Zbb encoding; require ensuing illegal-instruction trap; unrelated trap cannot pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction/marker/trap sequence and exact disabled test oracle.
    capture: evidence/picorv32-published-demo-continuity.ticket2.disabled-trap
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-andn
  phase: ticket2
  action: Implement and verify RV32 Zbb `ANDN` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-andn
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `ANDN` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `ANDN` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-andn
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-orn
  phase: ticket2
  action: Implement and verify RV32 Zbb `ORN` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-orn
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `ORN` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `ORN` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-orn
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-xnor
  phase: ticket2
  action: Implement and verify RV32 Zbb `XNOR` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-xnor
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `XNOR` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `XNOR` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-xnor
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-clz
  phase: ticket2
  action: Implement and verify RV32 Zbb `CLZ` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-clz
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `CLZ` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `CLZ` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-clz
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-ctz
  phase: ticket2
  action: Implement and verify RV32 Zbb `CTZ` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-ctz
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `CTZ` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `CTZ` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-ctz
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-cpop
  phase: ticket2
  action: Implement and verify RV32 Zbb `CPOP` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-cpop
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `CPOP` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `CPOP` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-cpop
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-min
  phase: ticket2
  action: Implement and verify RV32 Zbb `MIN` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-min
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `MIN` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `MIN` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-min
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-minu
  phase: ticket2
  action: Implement and verify RV32 Zbb `MINU` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-minu
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `MINU` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `MINU` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-minu
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-max
  phase: ticket2
  action: Implement and verify RV32 Zbb `MAX` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-max
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `MAX` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `MAX` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-max
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-maxu
  phase: ticket2
  action: Implement and verify RV32 Zbb `MAXU` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-maxu
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `MAXU` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `MAXU` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-maxu
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-sext-b
  phase: ticket2
  action: Implement and verify RV32 Zbb `SEXT.B` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-sext-b
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `SEXT.B` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `SEXT.B` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-sext-b
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-sext-h
  phase: ticket2
  action: Implement and verify RV32 Zbb `SEXT.H` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-sext-h
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `SEXT.H` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `SEXT.H` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-sext-h
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-zext-h
  phase: ticket2
  action: Implement and verify RV32 Zbb `ZEXT.H` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-zext-h
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `ZEXT.H` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `ZEXT.H` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-zext-h
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-rol
  phase: ticket2
  action: Implement and verify RV32 Zbb `ROL` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-rol
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `ROL` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `ROL` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-rol
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-ror
  phase: ticket2
  action: Implement and verify RV32 Zbb `ROR` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-ror
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `ROR` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `ROR` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-ror
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-rori
  phase: ticket2
  action: Implement and verify RV32 Zbb `RORI` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-rori
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `RORI` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `RORI` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-rori
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-orc-b
  phase: ticket2
  action: Implement and verify RV32 Zbb `ORC.B` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-orc-b
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `ORC.B` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `ORC.B` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-orc-b
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.zbb-rev8
  phase: ticket2
  action: Implement and verify RV32 Zbb `REV8` against pinned ISA authority.
  checks:
  - id: ticket2.zbb-rev8
    capabilities:
    - T-02
    stimulus: Implement and verify RV32 Zbb `REV8` against pinned ISA authority.
    expected: Implement and verify RV32 Zbb `REV8` against pinned ISA authority.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Instruction stimulus, expected result and observed execution for enabled configuration.
    capture: evidence/picorv32-published-demo-continuity.ticket2.zbb-rev8
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.enabled-core
  phase: ticket2
  action: Execute enabled Zbb through core wrapper.
  checks:
  - id: ticket2.enabled-core
    capabilities:
    - T-02
    stimulus: Execute enabled Zbb through core wrapper.
    expected: Execute enabled Zbb through core wrapper.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Test manifest and fresh wrapper-specific simulation evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.enabled-core
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.enabled-axi
  phase: ticket2
  action: Execute enabled Zbb through axi wrapper.
  checks:
  - id: ticket2.enabled-axi
    capabilities:
    - T-02
    stimulus: Execute enabled Zbb through axi wrapper.
    expected: Execute enabled Zbb through axi wrapper.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Test manifest and fresh wrapper-specific simulation evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.enabled-axi
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.enabled-wishbone
  phase: ticket2
  action: Execute enabled Zbb through wishbone wrapper.
  checks:
  - id: ticket2.enabled-wishbone
    capabilities:
    - T-02
    stimulus: Execute enabled Zbb through wishbone wrapper.
    expected: Execute enabled Zbb through wishbone wrapper.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Test manifest and fresh wrapper-specific simulation evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.enabled-wishbone
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.elab-sim_core_zbb
  phase: ticket2
  action: Require `elab_pass` for `sim_core_zbb`.
  checks:
  - id: ticket2.elab-sim_core_zbb
    capabilities:
    - T-02
    stimulus: Require `elab_pass` for `sim_core_zbb`.
    expected: Require `elab_pass` for `sim_core_zbb`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Target-bound normalized Elaboration Check.
    capture: evidence/picorv32-published-demo-continuity.ticket2.elab-sim_core_zbb
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.sim-sim_core_zbb
  phase: ticket2
  action: Require `sim_pass` fail-to-pass for `sim_core_zbb`.
  checks:
  - id: ticket2.sim-sim_core_zbb
    capabilities:
    - T-02
    stimulus: Require `sim_pass` fail-to-pass for `sim_core_zbb`.
    expected: Require `sim_pass` fail-to-pass for `sim_core_zbb`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Baseline failure and candidate pass with test identity.
    capture: evidence/picorv32-published-demo-continuity.ticket2.sim-sim_core_zbb
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.elab-sim_wb_zbb
  phase: ticket2
  action: Require `elab_pass` for `sim_wb_zbb`.
  checks:
  - id: ticket2.elab-sim_wb_zbb
    capabilities:
    - T-02
    stimulus: Require `elab_pass` for `sim_wb_zbb`.
    expected: Require `elab_pass` for `sim_wb_zbb`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Target-bound normalized Elaboration Check.
    capture: evidence/picorv32-published-demo-continuity.ticket2.elab-sim_wb_zbb
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.sim-sim_wb_zbb
  phase: ticket2
  action: Require `sim_pass` fail-to-pass for `sim_wb_zbb`.
  checks:
  - id: ticket2.sim-sim_wb_zbb
    capabilities:
    - T-02
    stimulus: Require `sim_pass` fail-to-pass for `sim_wb_zbb`.
    expected: Require `sim_pass` fail-to-pass for `sim_wb_zbb`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Baseline failure and candidate pass with test identity.
    capture: evidence/picorv32-published-demo-continuity.ticket2.sim-sim_wb_zbb
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.elab-sim_zbb_disabled
  phase: ticket2
  action: Require `elab_pass` for `sim_zbb_disabled`.
  checks:
  - id: ticket2.elab-sim_zbb_disabled
    capabilities:
    - T-02
    stimulus: Require `elab_pass` for `sim_zbb_disabled`.
    expected: Require `elab_pass` for `sim_zbb_disabled`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Target-bound normalized Elaboration Check.
    capture: evidence/picorv32-published-demo-continuity.ticket2.elab-sim_zbb_disabled
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.sim-sim_zbb_disabled
  phase: ticket2
  action: Require `sim_pass` fail-to-pass for `sim_zbb_disabled`.
  checks:
  - id: ticket2.sim-sim_zbb_disabled
    capabilities:
    - T-02
    stimulus: Require `sim_pass` fail-to-pass for `sim_zbb_disabled`.
    expected: Require `sim_pass` fail-to-pass for `sim_zbb_disabled`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Baseline failure and candidate pass with test identity.
    capture: evidence/picorv32-published-demo-continuity.ticket2.sim-sim_zbb_disabled
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.elab-sim_axi_zbb
  phase: ticket2
  action: Elaborate the explicitly approved AXI Zbb Target using existing `testbench` top.
  checks:
  - id: ticket2.elab-sim_axi_zbb
    capabilities:
    - T-02
    stimulus: Elaborate the explicitly approved AXI Zbb Target using existing `testbench` top.
    expected: Elaborate the explicitly approved AXI Zbb Target using existing `testbench` top.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Target/source/parameter identity and normalized elaboration result.
    capture: evidence/picorv32-published-demo-continuity.ticket2.elab-sim_axi_zbb
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.sim-sim_axi_zbb
  phase: ticket2
  action: Run Zbb firmware through AXI Target and require Basis fail/candidate pass.
  checks:
  - id: ticket2.sim-sim_axi_zbb
    capabilities:
    - T-02
    stimulus: Run Zbb firmware through AXI Target and require Basis fail/candidate pass.
    expected: Run Zbb firmware through AXI Target and require Basis fail/candidate pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Test manifest including every agreed opcode, fresh Basis/candidate simulation and AXI wrapper identity.
    capture: evidence/picorv32-published-demo-continuity.ticket2.sim-sim_axi_zbb
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.regression-main-core
  phase: ticket2
  action: Require existing default main-core simulation pass-to-pass.
  checks:
  - id: ticket2.regression-main-core
    capabilities:
    - T-02
    stimulus: Require existing default main-core simulation pass-to-pass.
    expected: Require existing default main-core simulation pass-to-pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Acceptance Basis and candidate simulation evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.regression-main-core
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.regression-axi
  phase: ticket2
  action: Require existing default axi simulation pass-to-pass.
  checks:
  - id: ticket2.regression-axi
    capabilities:
    - T-02
    stimulus: Require existing default axi simulation pass-to-pass.
    expected: Require existing default axi simulation pass-to-pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Acceptance Basis and candidate simulation evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.regression-axi
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.regression-wishbone
  phase: ticket2
  action: Require existing default wishbone simulation pass-to-pass.
  checks:
  - id: ticket2.regression-wishbone
    capabilities:
    - T-02
    stimulus: Require existing default wishbone simulation pass-to-pass.
    expected: Require existing default wishbone simulation pass-to-pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Acceptance Basis and candidate simulation evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.regression-wishbone
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.provider-execution
  phase: ticket2
  action: Genuinely execute refreshed `sim_dhry_checked` pass-to-pass, not merely select its definition.
  checks:
  - id: ticket2.provider-execution
    capabilities:
    - T-02
    stimulus: Genuinely execute refreshed `sim_dhry_checked` pass-to-pass, not merely select its definition.
    expected: Genuinely execute refreshed `sim_dhry_checked` pass-to-pass, not merely select its definition.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Actual Flow invocation and simulator artifact/test result.
    capture: evidence/picorv32-published-demo-continuity.ticket2.provider-execution
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.standalone
  phase: ticket2
  action: 'Require `elaborate_standalone: true`.'
  checks:
  - id: ticket2.standalone
    capabilities:
    - T-02
    stimulus: 'Require `elaborate_standalone: true`.'
    expected: 'Require `elaborate_standalone: true`.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Standalone elaboration evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.standalone
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.lint
  phase: ticket2
  action: Require clean `lint_core_zbb` and preserve default lint behavior.
  checks:
  - id: ticket2.lint
    capabilities:
    - F-02
    stimulus: Require clean `lint_core_zbb` and preserve default lint behavior.
    expected: Require clean `lint_core_zbb` and preserve default lint behavior.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Candidate/default Target lint results.
    capture: evidence/picorv32-published-demo-continuity.ticket2.lint
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.mutation
  phase: ticket2
  action: Mutate `picorv32.v` via `sim_core_zbb`; at least 14 detected out of 15, targeting Zbb decode/results/PCPI/enable
    gating.
  checks:
  - id: ticket2.mutation
    capabilities:
    - S-05
    stimulus: Mutate `picorv32.v` via `sim_core_zbb`; at least 14 detected out of 15, targeting Zbb decode/results/PCPI/enable
      gating.
    expected: Mutate `picorv32.v` via `sim_core_zbb`; at least 14 detected out of 15, targeting Zbb decode/results/PCPI/enable
      gating.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Campaign manifest, each mutant/outcome, restoration and threshold evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.mutation
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.synthesis-cell
  phase: ticket2
  action: Directed pair baseline `synth_core`, candidate `synth_core_zbb`; cell increase at most 11%, measured with
    Zbb enabled.
  checks:
  - id: ticket2.synthesis-cell
    capabilities:
    - F-03
    stimulus: Directed pair baseline `synth_core`, candidate `synth_core_zbb`; cell increase at most 11%, measured
      with Zbb enabled.
    expected: Directed pair baseline `synth_core`, candidate `synth_core_zbb`; cell increase at most 11%, measured
      with Zbb enabled.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Fresh paired metrics and exact inclusive comparison.
    capture: evidence/picorv32-published-demo-continuity.ticket2.synthesis-cell
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.synthesis-timing
  phase: ticket2
  action: 'Same physical synthesis pair: critical-path increase at most 3%; no relaxed limit.'
  checks:
  - id: ticket2.synthesis-timing
    capabilities:
    - F-03
    stimulus: 'Same physical synthesis pair: critical-path increase at most 3%; no relaxed limit.'
    expected: 'Same physical synthesis pair: critical-path increase at most 3%; no relaxed limit.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Paired critical-path metrics and comparison.
    capture: evidence/picorv32-published-demo-continuity.ticket2.synthesis-timing
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.fpga
  phase: ticket2
  action: 'Require `fpga_impl_ok` for `fpga_core_zbb`: successful completion and fresh artifact; no LUT threshold.'
  checks:
  - id: ticket2.fpga
    capabilities:
    - F-04
    stimulus: 'Require `fpga_impl_ok` for `fpga_core_zbb`: successful completion and fresh artifact; no LUT threshold.'
    expected: 'Require `fpga_impl_ok` for `fpga_core_zbb`: successful completion and fresh artifact; no LUT threshold.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Actual implementation verdict and artifact identity.
    capture: evidence/picorv32-published-demo-continuity.ticket2.fpga
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.review-bugs
  phase: ticket2
  action: Require terminal `review_rtl_bugs_done` (advisory completion, not clean disposition).
  checks:
  - id: ticket2.review-bugs
    capabilities:
    - S-04
    stimulus: Require terminal `review_rtl_bugs_done` (advisory completion, not clean disposition).
    expected: Require terminal `review_rtl_bugs_done` (advisory completion, not clean disposition).
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Focus-specific terminal report and criterion result.
    capture: evidence/picorv32-published-demo-continuity.ticket2.review-bugs
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.review-protocol
  phase: ticket2
  action: Require terminal `review_rtl_protocol_done` (advisory completion, not clean disposition).
  checks:
  - id: ticket2.review-protocol
    capabilities:
    - S-04
    stimulus: Require terminal `review_rtl_protocol_done` (advisory completion, not clean disposition).
    expected: Require terminal `review_rtl_protocol_done` (advisory completion, not clean disposition).
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Focus-specific terminal report and criterion result.
    capture: evidence/picorv32-published-demo-continuity.ticket2.review-protocol
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.review-spec
  phase: ticket2
  action: Require terminal `review_rtl_spec_done` (advisory completion, not clean disposition).
  checks:
  - id: ticket2.review-spec
    capabilities:
    - S-04
    stimulus: Require terminal `review_rtl_spec_done` (advisory completion, not clean disposition).
    expected: Require terminal `review_rtl_spec_done` (advisory completion, not clean disposition).
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Focus-specific terminal report and criterion result.
    capture: evidence/picorv32-published-demo-continuity.ticket2.review-spec
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.review-code_style
  phase: ticket2
  action: Require terminal `review_rtl_code_style_done` (advisory completion, not clean disposition).
  checks:
  - id: ticket2.review-code_style
    capabilities:
    - S-04
    stimulus: Require terminal `review_rtl_code_style_done` (advisory completion, not clean disposition).
    expected: Require terminal `review_rtl_code_style_done` (advisory completion, not clean disposition).
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Focus-specific terminal report and criterion result.
    capture: evidence/picorv32-published-demo-continuity.ticket2.review-code_style
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.review-optimization
  phase: ticket2
  action: Require terminal `review_rtl_optimization_done` (advisory completion, not clean disposition).
  checks:
  - id: ticket2.review-optimization
    capabilities:
    - S-04
    stimulus: Require terminal `review_rtl_optimization_done` (advisory completion, not clean disposition).
    expected: Require terminal `review_rtl_optimization_done` (advisory completion, not clean disposition).
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Focus-specific terminal report and criterion result.
    capture: evidence/picorv32-published-demo-continuity.ticket2.review-optimization
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.review-security
  phase: ticket2
  action: Require terminal `review_rtl_security_done` (advisory completion, not clean disposition).
  checks:
  - id: ticket2.review-security
    capabilities:
    - S-04
    stimulus: Require terminal `review_rtl_security_done` (advisory completion, not clean disposition).
    expected: Require terminal `review_rtl_security_done` (advisory completion, not clean disposition).
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Focus-specific terminal report and criterion result.
    capture: evidence/picorv32-published-demo-continuity.ticket2.review-security
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.tb-review
  phase: ticket2
  action: Require Target-bound `review_tb_quality_done`.
  checks:
  - id: ticket2.tb-review
    capabilities:
    - S-04
    stimulus: Require Target-bound `review_tb_quality_done`.
    expected: Require Target-bound `review_tb_quality_done`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Terminal TB-quality report and binding.
    capture: evidence/picorv32-published-demo-continuity.ticket2.tb-review
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.bugs-clean
  phase: ticket2
  action: Also require corrective `review_rtl_bugs_clean`.
  checks:
  - id: ticket2.bugs-clean
    capabilities:
    - T-02
    stimulus: Also require corrective `review_rtl_bugs_clean`.
    expected: Also require corrective `review_rtl_bugs_clean`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Clean disposition after any required corrective work.
    capture: evidence/picorv32-published-demo-continuity.ticket2.bugs-clean
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.ephemeral-retention
  phase: ticket2
  action: |-
    Six approved ephemeral Targets remain available during execution and acceptance: `sim_core_zbb`, `sim_wb_zbb`, `sim_zbb_disabled`, `lint_core_zbb`, `synth_core_zbb`, `fpga_core_zbb`.
  checks:
  - id: ticket2.ephemeral-retention
    capabilities:
    - T-02
    stimulus: |-
      Six approved ephemeral Targets remain available during execution and acceptance: `sim_core_zbb`, `sim_wb_zbb`, `sim_zbb_disabled`, `lint_core_zbb`, `synth_core_zbb`, `fpga_core_zbb`.
    expected: |-
      Six approved ephemeral Targets remain available during execution and acceptance: `sim_core_zbb`, `sim_wb_zbb`, `sim_zbb_disabled`, `lint_core_zbb`, `synth_core_zbb`, `fpga_core_zbb`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Target Plan and inventory at execution/acceptance.
    capture: evidence/picorv32-published-demo-continuity.ticket2.ephemeral-retention
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.ephemeral-removal
  phase: ticket2
  action: |-
    After acceptance remove those definitions and unambiguously owned test tables automatically; no retired `on_success.remove_targets`.
  checks:
  - id: ticket2.ephemeral-removal
    capabilities:
    - T-02
    stimulus: |-
      After acceptance remove those definitions and unambiguously owned test tables automatically; no retired `on_success.remove_targets`.
    expected: |-
      After acceptance remove those definitions and unambiguously owned test tables automatically; no retired `on_success.remove_targets`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Before/after definitions/test tables and retained unrelated content.
    capture: evidence/picorv32-published-demo-continuity.ticket2.ephemeral-removal
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.done
  phase: ticket2
  action: 'Reach `destination: done`.'
  checks:
  - id: ticket2.done
    capabilities:
    - T-02
    stimulus: 'Reach `destination: done`.'
    expected: 'Reach `destination: done`.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Board terminal state and acceptance evidence.
    capture: evidence/picorv32-published-demo-continuity.ticket2.done
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.merge
  phase: ticket2
  action: Merge accepted local changes.
  checks:
  - id: ticket2.merge
    capabilities:
    - T-02
    stimulus: Merge accepted local changes.
    expected: Merge accepted local changes.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Accepted commit and destination branch identity.
    capture: evidence/picorv32-published-demo-continuity.ticket2.merge
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.workspace-cleanup
  phase: ticket2
  action: Clean Ticket worktree after acceptance.
  checks:
  - id: ticket2.workspace-cleanup
    capabilities:
    - T-02
    stimulus: Clean Ticket worktree after acceptance.
    expected: Clean Ticket worktree after acceptance.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Absence proof and resource entry.
    capture: evidence/picorv32-published-demo-continuity.ticket2.workspace-cleanup
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: ticket2.triage-report
  phase: ticket2
  action: Generate triage report.
  checks:
  - id: ticket2.triage-report
    capabilities:
    - T-02
    stimulus: Generate triage report.
    expected: Generate triage report.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Fresh report artifact and ticket association.
    capture: evidence/picorv32-published-demo-continuity.ticket2.triage-report
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: final.combined-regression
  phase: ticket2
  action: |-
    Execute full combined supported regression against merged result; retain all applicable simulation, lint and physical artifacts.
  checks:
  - id: final.combined-regression
    capabilities:
    - D-03
    stimulus: |-
      Execute full combined supported regression against merged result; retain all applicable simulation, lint and physical artifacts.
    expected: |-
      Execute full combined supported regression against merged result; retain all applicable simulation, lint and physical artifacts.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Fresh test manifest/results and merged identity; final regression recovery point.
    capture: evidence/picorv32-published-demo-continuity.final.combined-regression
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
  - ticket2.merge
- id: coverage.criterion-families
  phase: ticket2
  action: |-
    Across Tickets account for all 15 agreed Criterion families; Linux includes FPGA, Windows explicitly excludes provisioned FPGA.
  checks:
  - id: coverage.criterion-families
    capabilities:
    - D-03
    stimulus: |-
      Across Tickets account for all 15 agreed Criterion families; Linux includes FPGA, Windows explicitly excludes provisioned FPGA.
    expected: |-
      Across Tickets account for all 15 agreed Criterion families; Linux includes FPGA, Windows explicitly excludes provisioned FPGA.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Generated reverse coverage view referencing actual criterion/check results, never a substitute for
      those results.
    capture: evidence/picorv32-published-demo-continuity.coverage.criterion-families
  requires:
  - inputs.project-pin
  - ticket1.merge
  - guard.restore-rerun
- id: inventory.S-01.review-optimization
  phase: ticket2
  action: |-
    Execute `review-optimization` using the attached scenario-owned probe contract.
    Each of `review-rtl-bugs` (picorv32), `review-protocol` (taxi), `review-spec` (uart), `review-code-style` (taxi), `review-optimization` (picorv32), `review-security` (uart), `review-tb-quality` (uart) invokes that public focus → report explicitly identifies it. UART: `review-done` seeded real finding → advisory done evidence; `review-clean-blocked` unresolved MINOR or greater → clean not met; `review-clean-waived` explicit finding waiver → clean current; `review-stale` relevant edit → both stale; `review-rerun` corrected source rerun → fresh evidence. `review-guidance`: controlled Project-guide conflict → supplied Project guide wins, bundled guides still present.
  checks:
  - id: inventory.S-01.review-optimization
    capabilities:
    - S-01
    stimulus: |-
      Execute `review-optimization` using the attached scenario-owned probe contract.
      Each of `review-rtl-bugs` (picorv32), `review-protocol` (taxi), `review-spec` (uart), `review-code-style` (taxi), `review-optimization` (picorv32), `review-security` (uart), `review-tb-quality` (uart) invokes that public focus → report explicitly identifies it. UART: `review-done` seeded real finding → advisory done evidence; `review-clean-blocked` unresolved MINOR or greater → clean not met; `review-clean-waived` explicit finding waiver → clean current; `review-stale` relevant edit → both stale; `review-rerun` corrected source rerun → fresh evidence. `review-guidance`: controlled Project-guide conflict → supplied Project guide wins, bundled guides still present.
    expected: |-
      Evaluate only the independently named `review-optimization` outcome in this contract.
      Each of `review-rtl-bugs` (picorv32), `review-protocol` (taxi), `review-spec` (uart), `review-code-style` (taxi), `review-optimization` (picorv32), `review-security` (uart), `review-tb-quality` (uart) invokes that public focus → report explicitly identifies it. UART: `review-done` seeded real finding → advisory done evidence; `review-clean-blocked` unresolved MINOR or greater → clean not met; `review-clean-waived` explicit finding waiver → clean current; `review-stale` relevant edit → both stale; `review-rerun` corrected source rerun → fresh evidence. `review-guidance`: controlled Project-guide conflict → supplied Project guide wins, bundled guides still present.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#reviewer
    evidence: |-
      Report/transcript/finding/waiver and source identity; spec review binds actual Ticket/spec path. Document incorrect CRITICAL-only prose as Finding. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-01.review-optimization
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-01.md
    audience: coordinator
    sha256: 0cbce199870487cbb0e4ecdeae1adf2e367e88a65bf22beb1c1e758f428c62b6
    base: shared
- id: inventory.S-01.review-rtl-bugs
  phase: ticket2
  action: |-
    Execute `review-rtl-bugs` using the attached scenario-owned probe contract.
    Each of `review-rtl-bugs` (picorv32), `review-protocol` (taxi), `review-spec` (uart), `review-code-style` (taxi), `review-optimization` (picorv32), `review-security` (uart), `review-tb-quality` (uart) invokes that public focus → report explicitly identifies it. UART: `review-done` seeded real finding → advisory done evidence; `review-clean-blocked` unresolved MINOR or greater → clean not met; `review-clean-waived` explicit finding waiver → clean current; `review-stale` relevant edit → both stale; `review-rerun` corrected source rerun → fresh evidence. `review-guidance`: controlled Project-guide conflict → supplied Project guide wins, bundled guides still present.
  checks:
  - id: inventory.S-01.review-rtl-bugs
    capabilities:
    - S-01
    stimulus: |-
      Execute `review-rtl-bugs` using the attached scenario-owned probe contract.
      Each of `review-rtl-bugs` (picorv32), `review-protocol` (taxi), `review-spec` (uart), `review-code-style` (taxi), `review-optimization` (picorv32), `review-security` (uart), `review-tb-quality` (uart) invokes that public focus → report explicitly identifies it. UART: `review-done` seeded real finding → advisory done evidence; `review-clean-blocked` unresolved MINOR or greater → clean not met; `review-clean-waived` explicit finding waiver → clean current; `review-stale` relevant edit → both stale; `review-rerun` corrected source rerun → fresh evidence. `review-guidance`: controlled Project-guide conflict → supplied Project guide wins, bundled guides still present.
    expected: |-
      Evaluate only the independently named `review-rtl-bugs` outcome in this contract.
      Each of `review-rtl-bugs` (picorv32), `review-protocol` (taxi), `review-spec` (uart), `review-code-style` (taxi), `review-optimization` (picorv32), `review-security` (uart), `review-tb-quality` (uart) invokes that public focus → report explicitly identifies it. UART: `review-done` seeded real finding → advisory done evidence; `review-clean-blocked` unresolved MINOR or greater → clean not met; `review-clean-waived` explicit finding waiver → clean current; `review-stale` relevant edit → both stale; `review-rerun` corrected source rerun → fresh evidence. `review-guidance`: controlled Project-guide conflict → supplied Project guide wins, bundled guides still present.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#reviewer
    evidence: |-
      Report/transcript/finding/waiver and source identity; spec review binds actual Ticket/spec path. Document incorrect CRITICAL-only prose as Finding. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-01.review-rtl-bugs
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-01.md
    audience: coordinator
    sha256: 0cbce199870487cbb0e4ecdeae1adf2e367e88a65bf22beb1c1e758f428c62b6
    base: shared
- id: inventory.S-04.criterion-cycle
  phase: ticket2
  action: |-
    Execute `criterion-cycle` using the attached scenario-owned probe contract.
    `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
  checks:
  - id: inventory.S-04.criterion-cycle
    capabilities:
    - S-04
    stimulus: |-
      Execute `criterion-cycle` using the attached scenario-owned probe contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    expected: |-
      Evaluate only the independently named `criterion-cycle` outcome in this contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#acceptance-criteria
    evidence: |-
      Sealed Ticket, exact family/Target/test/ref binding and acceptance evaluation. Each focus and family is separate. Standalone Flow success alone earns none of these checks; hidden coverage criterion excluded. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-04.criterion-cycle
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-04.md
    audience: coordinator
    sha256: 8c26e67a8c475814dc87d4c05ceed932b58c34da00dead672783f83ebafbf73f
    base: shared
- id: inventory.S-04.criterion-elab
  phase: ticket2
  action: |-
    Execute `criterion-elab` using the attached scenario-owned probe contract.
    `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
  checks:
  - id: inventory.S-04.criterion-elab
    capabilities:
    - S-04
    stimulus: |-
      Execute `criterion-elab` using the attached scenario-owned probe contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    expected: |-
      Evaluate only the independently named `criterion-elab` outcome in this contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#acceptance-criteria
    evidence: |-
      Sealed Ticket, exact family/Target/test/ref binding and acceptance evaluation. Each focus and family is separate. Standalone Flow success alone earns none of these checks; hidden coverage criterion excluded. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-04.criterion-elab
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-04.md
    audience: coordinator
    sha256: 8c26e67a8c475814dc87d4c05ceed932b58c34da00dead672783f83ebafbf73f
    base: shared
- id: inventory.S-04.criterion-lint
  phase: ticket2
  action: |-
    Execute `criterion-lint` using the attached scenario-owned probe contract.
    `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
  checks:
  - id: inventory.S-04.criterion-lint
    capabilities:
    - S-04
    stimulus: |-
      Execute `criterion-lint` using the attached scenario-owned probe contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    expected: |-
      Evaluate only the independently named `criterion-lint` outcome in this contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#acceptance-criteria
    evidence: |-
      Sealed Ticket, exact family/Target/test/ref binding and acceptance evaluation. Each focus and family is separate. Standalone Flow success alone earns none of these checks; hidden coverage criterion excluded. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-04.criterion-lint
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-04.md
    audience: coordinator
    sha256: 8c26e67a8c475814dc87d4c05ceed932b58c34da00dead672783f83ebafbf73f
    base: shared
- id: inventory.S-04.criterion-review-optimization
  phase: ticket2
  action: |-
    Execute `criterion-review-optimization` using the attached scenario-owned probe contract.
    `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
  checks:
  - id: inventory.S-04.criterion-review-optimization
    capabilities:
    - S-04
    stimulus: |-
      Execute `criterion-review-optimization` using the attached scenario-owned probe contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    expected: |-
      Evaluate only the independently named `criterion-review-optimization` outcome in this contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#acceptance-criteria
    evidence: |-
      Sealed Ticket, exact family/Target/test/ref binding and acceptance evaluation. Each focus and family is separate. Standalone Flow success alone earns none of these checks; hidden coverage criterion excluded. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-04.criterion-review-optimization
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-04.md
    audience: coordinator
    sha256: 8c26e67a8c475814dc87d4c05ceed932b58c34da00dead672783f83ebafbf73f
    base: shared
- id: inventory.S-04.criterion-review-rtl-bugs
  phase: ticket2
  action: |-
    Execute `criterion-review-rtl-bugs` using the attached scenario-owned probe contract.
    `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
  checks:
  - id: inventory.S-04.criterion-review-rtl-bugs
    capabilities:
    - S-04
    stimulus: |-
      Execute `criterion-review-rtl-bugs` using the attached scenario-owned probe contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    expected: |-
      Evaluate only the independently named `criterion-review-rtl-bugs` outcome in this contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#acceptance-criteria
    evidence: |-
      Sealed Ticket, exact family/Target/test/ref binding and acceptance evaluation. Each focus and family is separate. Standalone Flow success alone earns none of these checks; hidden coverage criterion excluded. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-04.criterion-review-rtl-bugs
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-04.md
    audience: coordinator
    sha256: 8c26e67a8c475814dc87d4c05ceed932b58c34da00dead672783f83ebafbf73f
    base: shared
- id: inventory.S-04.criterion-sim
  phase: ticket2
  action: |-
    Execute `criterion-sim` using the attached scenario-owned probe contract.
    `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
  checks:
  - id: inventory.S-04.criterion-sim
    capabilities:
    - S-04
    stimulus: |-
      Execute `criterion-sim` using the attached scenario-owned probe contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    expected: |-
      Evaluate only the independently named `criterion-sim` outcome in this contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#acceptance-criteria
    evidence: |-
      Sealed Ticket, exact family/Target/test/ref binding and acceptance evaluation. Each focus and family is separate. Standalone Flow success alone earns none of these checks; hidden coverage criterion excluded. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-04.criterion-sim
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-04.md
    audience: coordinator
    sha256: 8c26e67a8c475814dc87d4c05ceed932b58c34da00dead672783f83ebafbf73f
    base: shared
- id: inventory.S-04.criterion-standalone
  phase: ticket2
  action: |-
    Execute `criterion-standalone` using the attached scenario-owned probe contract.
    `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
  checks:
  - id: inventory.S-04.criterion-standalone
    capabilities:
    - S-04
    stimulus: |-
      Execute `criterion-standalone` using the attached scenario-owned probe contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    expected: |-
      Evaluate only the independently named `criterion-standalone` outcome in this contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#acceptance-criteria
    evidence: |-
      Sealed Ticket, exact family/Target/test/ref binding and acceptance evaluation. Each focus and family is separate. Standalone Flow success alone earns none of these checks; hidden coverage criterion excluded. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-04.criterion-standalone
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-04.md
    audience: coordinator
    sha256: 8c26e67a8c475814dc87d4c05ceed932b58c34da00dead672783f83ebafbf73f
    base: shared
- id: inventory.S-04.criterion-synthesis
  phase: ticket2
  action: |-
    Execute `criterion-synthesis` using the attached scenario-owned probe contract.
    `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
  checks:
  - id: inventory.S-04.criterion-synthesis
    capabilities:
    - S-04
    stimulus: |-
      Execute `criterion-synthesis` using the attached scenario-owned probe contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    expected: |-
      Evaluate only the independently named `criterion-synthesis` outcome in this contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#acceptance-criteria
    evidence: |-
      Sealed Ticket, exact family/Target/test/ref binding and acceptance evaluation. Each focus and family is separate. Standalone Flow success alone earns none of these checks; hidden coverage criterion excluded. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-04.criterion-synthesis
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-04.md
    audience: coordinator
    sha256: 8c26e67a8c475814dc87d4c05ceed932b58c34da00dead672783f83ebafbf73f
    base: shared
- id: inventory.S-06.baseline-mismatch
  phase: ticket2
  action: |-
    Execute `baseline-mismatch` using the attached scenario-owned probe contract.
    `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
  checks:
  - id: inventory.S-06.baseline-mismatch
    capabilities:
    - S-06
    stimulus: |-
      Execute `baseline-mismatch` using the attached scenario-owned probe contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    expected: |-
      Evaluate only the independently named `baseline-mismatch` outcome in this contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#threshold-parameters
    evidence: |-
      Both refs, Target recipes, numeric operands and result; no generic report-exists oracle. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-06.baseline-mismatch
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-06.md
    audience: coordinator
    sha256: 461d097018d7d6adce29e1d0f109983eda8b02c53e77422483aa6f480d898466
- id: inventory.S-06.baseline-missing
  phase: ticket2
  action: |-
    Execute `baseline-missing` using the attached scenario-owned probe contract.
    `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
  checks:
  - id: inventory.S-06.baseline-missing
    capabilities:
    - S-06
    stimulus: |-
      Execute `baseline-missing` using the attached scenario-owned probe contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    expected: |-
      Evaluate only the independently named `baseline-missing` outcome in this contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#threshold-parameters
    evidence: |-
      Both refs, Target recipes, numeric operands and result; no generic report-exists oracle. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-06.baseline-missing
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-06.md
    audience: coordinator
    sha256: 461d097018d7d6adce29e1d0f109983eda8b02c53e77422483aa6f480d898466
- id: inventory.S-06.baseline-recovery
  phase: ticket2
  action: |-
    Execute `baseline-recovery` using the attached scenario-owned probe contract.
    `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
  checks:
  - id: inventory.S-06.baseline-recovery
    capabilities:
    - S-06
    stimulus: |-
      Execute `baseline-recovery` using the attached scenario-owned probe contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    expected: |-
      Evaluate only the independently named `baseline-recovery` outcome in this contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#threshold-parameters
    evidence: |-
      Both refs, Target recipes, numeric operands and result; no generic report-exists oracle. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-06.baseline-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-06.md
    audience: coordinator
    sha256: 461d097018d7d6adce29e1d0f109983eda8b02c53e77422483aa6f480d898466
- id: inventory.S-06.threshold-absolute
  phase: ticket2
  action: |-
    Execute `threshold-absolute` using the attached scenario-owned probe contract.
    `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
  checks:
  - id: inventory.S-06.threshold-absolute
    capabilities:
    - S-06
    stimulus: |-
      Execute `threshold-absolute` using the attached scenario-owned probe contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    expected: |-
      Evaluate only the independently named `threshold-absolute` outcome in this contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#threshold-parameters
    evidence: |-
      Both refs, Target recipes, numeric operands and result; no generic report-exists oracle. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-06.threshold-absolute
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-06.md
    audience: coordinator
    sha256: 461d097018d7d6adce29e1d0f109983eda8b02c53e77422483aa6f480d898466
- id: inventory.S-06.threshold-clock
  phase: ticket2
  action: |-
    Execute `threshold-clock` using the attached scenario-owned probe contract.
    `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
  checks:
  - id: inventory.S-06.threshold-clock
    capabilities:
    - S-06
    stimulus: |-
      Execute `threshold-clock` using the attached scenario-owned probe contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    expected: |-
      Evaluate only the independently named `threshold-clock` outcome in this contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#threshold-parameters
    evidence: |-
      Both refs, Target recipes, numeric operands and result; no generic report-exists oracle. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-06.threshold-clock
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-06.md
    audience: coordinator
    sha256: 461d097018d7d6adce29e1d0f109983eda8b02c53e77422483aa6f480d898466
- id: inventory.S-06.threshold-directed
  phase: ticket2
  action: |-
    Execute `threshold-directed` using the attached scenario-owned probe contract.
    `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
  checks:
  - id: inventory.S-06.threshold-directed
    capabilities:
    - S-06
    stimulus: |-
      Execute `threshold-directed` using the attached scenario-owned probe contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    expected: |-
      Evaluate only the independently named `threshold-directed` outcome in this contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#threshold-parameters
    evidence: |-
      Both refs, Target recipes, numeric operands and result; no generic report-exists oracle. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-06.threshold-directed
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-06.md
    audience: coordinator
    sha256: 461d097018d7d6adce29e1d0f109983eda8b02c53e77422483aa6f480d898466
- id: inventory.S-06.threshold-relative
  phase: ticket2
  action: |-
    Execute `threshold-relative` using the attached scenario-owned probe contract.
    `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
  checks:
  - id: inventory.S-06.threshold-relative
    capabilities:
    - S-06
    stimulus: |-
      Execute `threshold-relative` using the attached scenario-owned probe contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    expected: |-
      Evaluate only the independently named `threshold-relative` outcome in this contract.
      `threshold-absolute`: declared absolute count/PPA bound → computed pass/fail from exact metric. `threshold-relative`: baseline-relative bound → exact delta. `threshold-directed`: baseline/candidate Target pair → role direction retained. `threshold-clock`: named-clock bound → only declared clock metric. `baseline-missing`, `baseline-mismatch`: absent/incompatible basis → fail closed. `baseline-recovery`: restore exact compatible basis → evaluated result.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#threshold-parameters
    evidence: |-
      Both refs, Target recipes, numeric operands and result; no generic report-exists oracle. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-06.threshold-relative
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-06.md
    audience: coordinator
    sha256: 461d097018d7d6adce29e1d0f109983eda8b02c53e77422483aa6f480d898466
- id: inventory.ST-03.body-reject
  phase: ticket2
  action: |-
    Execute `body-reject` using the attached scenario-owned probe contract.
    `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
  checks:
  - id: inventory.ST-03.body-reject
    capabilities:
    - ST-03
    stimulus: |-
      Execute `body-reject` using the attached scenario-owned probe contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    expected: |-
      Evaluate only the independently named `body-reject` outcome in this contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#enforcing-the-subject-convention-enforce_convention
    evidence: |-
      Commit messages/diffs, hook output and exact local remote; no public push. `history-recovery`: restore compliant inputs → valid local commit/push and no unsafe files. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-03.body-reject
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-03.md
    audience: coordinator
    sha256: 8dfb6b4db200fa76ac1f7e9264e06ec8e672bce80f21cefd9c02c9583c796709
- id: inventory.ST-03.escape-sanitized
  phase: ticket2
  action: |-
    Execute `escape-sanitized` using the attached scenario-owned probe contract.
    `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
  checks:
  - id: inventory.ST-03.escape-sanitized
    capabilities:
    - ST-03
    stimulus: |-
      Execute `escape-sanitized` using the attached scenario-owned probe contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    expected: |-
      Evaluate only the independently named `escape-sanitized` outcome in this contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#enforcing-the-subject-convention-enforce_convention
    evidence: |-
      Commit messages/diffs, hook output and exact local remote; no public push. `history-recovery`: restore compliant inputs → valid local commit/push and no unsafe files. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-03.escape-sanitized
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-03.md
    audience: coordinator
    sha256: 8dfb6b4db200fa76ac1f7e9264e06ec8e672bce80f21cefd9c02c9583c796709
- id: inventory.ST-03.history-recovery
  phase: ticket2
  action: |-
    Execute `history-recovery` using the attached scenario-owned probe contract.
    `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
  checks:
  - id: inventory.ST-03.history-recovery
    capabilities:
    - ST-03
    stimulus: |-
      Execute `history-recovery` using the attached scenario-owned probe contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    expected: |-
      Evaluate only the independently named `history-recovery` outcome in this contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#enforcing-the-subject-convention-enforce_convention
    evidence: |-
      Commit messages/diffs, hook output and exact local remote; no public push. `history-recovery`: restore compliant inputs → valid local commit/push and no unsafe files. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-03.history-recovery
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-03.md
    audience: coordinator
    sha256: 8dfb6b4db200fa76ac1f7e9264e06ec8e672bce80f21cefd9c02c9583c796709
- id: inventory.ST-03.push-guard-escape
  phase: ticket2
  action: |-
    Execute `push-guard-escape` using the attached scenario-owned probe contract.
    `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
  checks:
  - id: inventory.ST-03.push-guard-escape
    capabilities:
    - ST-03
    stimulus: |-
      Execute `push-guard-escape` using the attached scenario-owned probe contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    expected: |-
      Evaluate only the independently named `push-guard-escape` outcome in this contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#enforcing-the-subject-convention-enforce_convention
    evidence: |-
      Commit messages/diffs, hook output and exact local remote; no public push. `history-recovery`: restore compliant inputs → valid local commit/push and no unsafe files. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-03.push-guard-escape
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-03.md
    audience: coordinator
    sha256: 8dfb6b4db200fa76ac1f7e9264e06ec8e672bce80f21cefd9c02c9583c796709
- id: inventory.ST-03.push-identity-allow
  phase: ticket2
  action: |-
    Execute `push-identity-allow` using the attached scenario-owned probe contract.
    `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
  checks:
  - id: inventory.ST-03.push-identity-allow
    capabilities:
    - ST-03
    stimulus: |-
      Execute `push-identity-allow` using the attached scenario-owned probe contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    expected: |-
      Evaluate only the independently named `push-identity-allow` outcome in this contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#enforcing-the-subject-convention-enforce_convention
    evidence: |-
      Commit messages/diffs, hook output and exact local remote; no public push. `history-recovery`: restore compliant inputs → valid local commit/push and no unsafe files. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-03.push-identity-allow
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-03.md
    audience: coordinator
    sha256: 8dfb6b4db200fa76ac1f7e9264e06ec8e672bce80f21cefd9c02c9583c796709
- id: inventory.ST-03.push-identity-reject
  phase: ticket2
  action: |-
    Execute `push-identity-reject` using the attached scenario-owned probe contract.
    `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
  checks:
  - id: inventory.ST-03.push-identity-reject
    capabilities:
    - ST-03
    stimulus: |-
      Execute `push-identity-reject` using the attached scenario-owned probe contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    expected: |-
      Evaluate only the independently named `push-identity-reject` outcome in this contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#enforcing-the-subject-convention-enforce_convention
    evidence: |-
      Commit messages/diffs, hook output and exact local remote; no public push. `history-recovery`: restore compliant inputs → valid local commit/push and no unsafe files. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-03.push-identity-reject
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-03.md
    audience: coordinator
    sha256: 8dfb6b4db200fa76ac1f7e9264e06ec8e672bce80f21cefd9c02c9583c796709
- id: inventory.ST-03.push-path-reject
  phase: ticket2
  action: |-
    Execute `push-path-reject` using the attached scenario-owned probe contract.
    `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
  checks:
  - id: inventory.ST-03.push-path-reject
    capabilities:
    - ST-03
    stimulus: |-
      Execute `push-path-reject` using the attached scenario-owned probe contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    expected: |-
      Evaluate only the independently named `push-path-reject` outcome in this contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#enforcing-the-subject-convention-enforce_convention
    evidence: |-
      Commit messages/diffs, hook output and exact local remote; no public push. `history-recovery`: restore compliant inputs → valid local commit/push and no unsafe files. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-03.push-path-reject
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-03.md
    audience: coordinator
    sha256: 8dfb6b4db200fa76ac1f7e9264e06ec8e672bce80f21cefd9c02c9583c796709
- id: inventory.ST-03.push-symlink-reject
  phase: ticket2
  action: |-
    Execute `push-symlink-reject` using the attached scenario-owned probe contract.
    `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
  checks:
  - id: inventory.ST-03.push-symlink-reject
    capabilities:
    - ST-03
    stimulus: |-
      Execute `push-symlink-reject` using the attached scenario-owned probe contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    expected: |-
      Evaluate only the independently named `push-symlink-reject` outcome in this contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#enforcing-the-subject-convention-enforce_convention
    evidence: |-
      Commit messages/diffs, hook output and exact local remote; no public push. `history-recovery`: restore compliant inputs → valid local commit/push and no unsafe files. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-03.push-symlink-reject
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-03.md
    audience: coordinator
    sha256: 8dfb6b4db200fa76ac1f7e9264e06ec8e672bce80f21cefd9c02c9583c796709
- id: inventory.ST-03.subject-reject
  phase: ticket2
  action: |-
    Execute `subject-reject` using the attached scenario-owned probe contract.
    `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
  checks:
  - id: inventory.ST-03.subject-reject
    capabilities:
    - ST-03
    stimulus: |-
      Execute `subject-reject` using the attached scenario-owned probe contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    expected: |-
      Evaluate only the independently named `subject-reject` outcome in this contract.
      `sanitize-word`, `sanitize-trailer`: separate benign banned token/trailer commits → rewritten as documented. `subject-reject`, `body-reject`: invalid convention/overlong body → refused, not truncated. `escape-sanitized`: documented environment escape → convention/body bypass but sanitization remains. `push-identity-allow`, `push-identity-reject`: disposable local remote and identities → respective allow/reject. `push-path-reject`, `push-symlink-reject`: banned path/target → rejected. `push-guard-escape`: documented guard escape on disposable local remote → documented bypass only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/CONFIG.md#enforcing-the-subject-convention-enforce_convention
    evidence: |-
      Commit messages/diffs, hook output and exact local remote; no public push. `history-recovery`: restore compliant inputs → valid local commit/push and no unsafe files. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.ST-03.subject-reject
  requires:
  - inputs.project-pin
  assets:
  - path: probes/ST-03.md
    audience: coordinator
    sha256: 8dfb6b4db200fa76ac1f7e9264e06ec8e672bce80f21cefd9c02c9583c796709
- id: inventory.T-02.ticket-type-bug-fix
  phase: ticket2
  action: |-
    Execute `ticket-type-bug-fix` using the attached scenario-owned probe contract.
    Separate `ticket-type-feature`, `ticket-type-verification`, `ticket-type-bug-fix`, `ticket-type-refactor` under named owners → accepted correctly typed report with type-specific details. `report-retained`: accepted run → durable REPORT.md outside committed tree. `accepted-commit`: accepted work → clean committed source.
  checks:
  - id: inventory.T-02.ticket-type-bug-fix
    capabilities:
    - T-02
    stimulus: |-
      Execute `ticket-type-bug-fix` using the attached scenario-owned probe contract.
      Separate `ticket-type-feature`, `ticket-type-verification`, `ticket-type-bug-fix`, `ticket-type-refactor` under named owners → accepted correctly typed report with type-specific details. `report-retained`: accepted run → durable REPORT.md outside committed tree. `accepted-commit`: accepted work → clean committed source.
    expected: |-
      Evaluate only the independently named `ticket-type-bug-fix` outcome in this contract.
      Separate `ticket-type-feature`, `ticket-type-verification`, `ticket-type-bug-fix`, `ticket-type-refactor` under named owners → accepted correctly typed report with type-specific details. `report-retained`: accepted run → durable REPORT.md outside committed tree. `accepted-commit`: accepted work → clean committed source.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FEATURES.md#ticket-driven-workflow
    evidence: |-
      Reports, policy and Git status/history; deliberately configured no-report outcome is recorded separately with justification, not a missing report inferred pass. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.T-02.ticket-type-bug-fix
  requires:
  - inputs.project-pin
  assets:
  - path: probes/T-02.md
    audience: coordinator
    sha256: c86ea5b23edca7d8789ea9d07347d3ebf9ce2f1c223f6bd2f2182688bf4ab249
    base: shared
- id: inventory.T-02.ticket-type-refactor
  phase: ticket2
  action: |-
    Execute `ticket-type-refactor` using the attached scenario-owned probe contract.
    Separate `ticket-type-feature`, `ticket-type-verification`, `ticket-type-bug-fix`, `ticket-type-refactor` under named owners → accepted correctly typed report with type-specific details. `report-retained`: accepted run → durable REPORT.md outside committed tree. `accepted-commit`: accepted work → clean committed source.
  checks:
  - id: inventory.T-02.ticket-type-refactor
    capabilities:
    - T-02
    stimulus: |-
      Execute `ticket-type-refactor` using the attached scenario-owned probe contract.
      Separate `ticket-type-feature`, `ticket-type-verification`, `ticket-type-bug-fix`, `ticket-type-refactor` under named owners → accepted correctly typed report with type-specific details. `report-retained`: accepted run → durable REPORT.md outside committed tree. `accepted-commit`: accepted work → clean committed source.
    expected: |-
      Evaluate only the independently named `ticket-type-refactor` outcome in this contract.
      Separate `ticket-type-feature`, `ticket-type-verification`, `ticket-type-bug-fix`, `ticket-type-refactor` under named owners → accepted correctly typed report with type-specific details. `report-retained`: accepted run → durable REPORT.md outside committed tree. `accepted-commit`: accepted work → clean committed source.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FEATURES.md#ticket-driven-workflow
    evidence: |-
      Reports, policy and Git status/history; deliberately configured no-report outcome is recorded separately with justification, not a missing report inferred pass. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.T-02.ticket-type-refactor
  requires:
  - inputs.project-pin
  assets:
  - path: probes/T-02.md
    audience: coordinator
    sha256: c86ea5b23edca7d8789ea9d07347d3ebf9ce2f1c223f6bd2f2182688bf4ab249
    base: shared
- id: inventory.S-04.criterion-fpga
  phase: ticket2
  action: |-
    Execute `criterion-fpga` using the attached scenario-owned probe contract.
    `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
  checks:
  - id: inventory.S-04.criterion-fpga
    capabilities:
    - S-04
    stimulus: |-
      Execute `criterion-fpga` using the attached scenario-owned probe contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    expected: |-
      Evaluate only the independently named `criterion-fpga` outcome in this contract.
      `criterion-elab`, `criterion-standalone`, `criterion-lint`, `criterion-sim`, `criterion-cycle`, `criterion-synthesis` (picorv32); `criterion-fpga` (picorv32 Linux); `criterion-mutation` (uart); each `criterion-review-<focus>` under the S-01 focus owner → a real Ticket sealed with that supported family and bound evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/USAGE.md#acceptance-criteria
    evidence: |-
      Sealed Ticket, exact family/Target/test/ref binding and acceptance evaluation. Each focus and family is separate. Standalone Flow success alone earns none of these checks; hidden coverage criterion excluded. Retain argv, exit, stdout/stderr, fresh invocation, source/Target/runtime identities, before/after state and raw artifacts before restoration.
    capture: evidence/picorv32-published-demo-continuity.inventory.S-04.criterion-fpga
  requires:
  - inputs.project-pin
  assets:
  - path: probes/S-04.md
    audience: coordinator
    sha256: 8c26e67a8c475814dc87d4c05ceed932b58c34da00dead672783f83ebafbf73f
    base: shared
- id: cleanup.branches
  phase: cleanup
  action: Reconcile and remove every run-owned branches in dependency order.
  checks:
  - id: cleanup.branches
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned branches in dependency order.
    expected: Reconcile and remove every run-owned branches in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.branches
- id: cleanup.worktrees
  phase: cleanup
  action: Reconcile and remove every run-owned worktrees in dependency order.
  checks:
  - id: cleanup.worktrees
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned worktrees in dependency order.
    expected: Reconcile and remove every run-owned worktrees in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.worktrees
- id: cleanup.targets
  phase: cleanup
  action: Reconcile and remove every run-owned Targets in dependency order.
  checks:
  - id: cleanup.targets
    capabilities:
    - P-02
    stimulus: Reconcile and remove every run-owned Targets in dependency order.
    expected: Reconcile and remove every run-owned Targets in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.targets
- id: cleanup.projections
  phase: cleanup
  action: Reconcile and remove every run-owned projections in dependency order.
  checks:
  - id: cleanup.projections
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned projections in dependency order.
    expected: Reconcile and remove every run-owned projections in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.projections
- id: cleanup.hooks
  phase: cleanup
  action: Reconcile and remove every run-owned hooks in dependency order.
  checks:
  - id: cleanup.hooks
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned hooks in dependency order.
    expected: Reconcile and remove every run-owned hooks in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.hooks
- id: cleanup.runtimes
  phase: cleanup
  action: Reconcile and remove every run-owned runtimes in dependency order.
  checks:
  - id: cleanup.runtimes
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned runtimes in dependency order.
    expected: Reconcile and remove every run-owned runtimes in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.runtimes
- id: cleanup.processes
  phase: cleanup
  action: Reconcile and remove every run-owned processes in dependency order.
  checks:
  - id: cleanup.processes
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned processes in dependency order.
    expected: Reconcile and remove every run-owned processes in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.processes
- id: cleanup.project-grants
  phase: cleanup
  action: Reconcile and remove every run-owned Project-Grants in dependency order.
  checks:
  - id: cleanup.project-grants
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned Project-Grants in dependency order.
    expected: Reconcile and remove every run-owned Project-Grants in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.project-grants
- id: cleanup.license-profiles
  phase: cleanup
  action: Reconcile and remove every run-owned License-Profiles in dependency order.
  checks:
  - id: cleanup.license-profiles
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned License-Profiles in dependency order.
    expected: Reconcile and remove every run-owned License-Profiles in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.license-profiles
- id: cleanup.relays
  phase: cleanup
  action: Reconcile and remove every run-owned relays in dependency order.
  checks:
  - id: cleanup.relays
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned relays in dependency order.
    expected: Reconcile and remove every run-owned relays in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.relays
- id: cleanup.installation-registrations
  phase: cleanup
  action: Reconcile and remove every run-owned Installation-Registrations in dependency order.
  checks:
  - id: cleanup.installation-registrations
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned Installation-Registrations in dependency order.
    expected: Reconcile and remove every run-owned Installation-Registrations in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.installation-registrations
- id: cleanup.mounts
  phase: cleanup
  action: Reconcile and remove every run-owned mounts in dependency order.
  checks:
  - id: cleanup.mounts
    capabilities:
    - H-06
    stimulus: Reconcile and remove every run-owned mounts in dependency order.
    expected: Reconcile and remove every run-owned mounts in dependency order.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Owned identity, actual cleanup action and independent absence/state proof.
    capture: evidence/picorv32-published-demo-continuity.cleanup.mounts
- id: cleanup.preserve-borrowed
  phase: cleanup
  action: Preserve borrowed Vivado installation, credentials, base images, caches and pre-existing host administration.
  checks:
  - id: cleanup.preserve-borrowed
    capabilities:
    - H-06
    stimulus: Preserve borrowed Vivado installation, credentials, base images, caches and pre-existing host administration.
    expected: Preserve borrowed Vivado installation, credentials, base images, caches and pre-existing host administration.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Before/after preserved resource identities; no secret values.
    capture: evidence/picorv32-published-demo-continuity.cleanup.preserve-borrowed
- id: cleanup.clean-pins
  phase: cleanup
  action: Require clean pinned repositories at final cleanup.
  checks:
  - id: cleanup.clean-pins
    capabilities:
    - H-06
    stimulus: Require clean pinned repositories at final cleanup.
    expected: Require clean pinned repositories at final cleanup.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Tree/status against initial pins; accepted evolution commits retained as evidence before restoration.
    capture: evidence/picorv32-published-demo-continuity.cleanup.clean-pins
- id: cleanup.remotes
  phase: cleanup
  action: Remotes remain unchanged and no branch is pushed.
  checks:
  - id: cleanup.remotes
    capabilities:
    - H-06
    stimulus: Remotes remain unchanged and no branch is pushed.
    expected: Remotes remain unchanged and no branch is pushed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Initial/final remote config/ref evidence and command authority log.
    capture: evidence/picorv32-published-demo-continuity.cleanup.remotes
- id: evidence.finalize
  phase: cleanup
  action: Finalize record, Findings, recovery links and resource disposition with no secret values.
  checks:
  - id: evidence.finalize
    capabilities:
    - D-03
    stimulus: Finalize record, Findings, recovery links and resource disposition with no secret values.
    expected: Finalize record, Findings, recovery links and resource disposition with no secret values.
    authority_ref: https://github.com/boldaxolotl/booley/blob/b163fd1f45b76f3950005678e500e695232832fb/qa/handoff/picorv32.md
    evidence: Compact run files and immutable external evidence; cleanup-complete recovery point.
    capture: evidence/picorv32-published-demo-continuity.evidence.finalize
