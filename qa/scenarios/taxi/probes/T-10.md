# T-10 disposable probes

`console`: named run → documented full-screen Console. `dry-run`, `check-ready`: invoke separately → no state transition. `idle-drained`: drained loop reaches timeout → correct exit without phantom claim. `board-human`: show/briefing → correct Ticket state. `board-machine`: review record → exact same identity/state.

Evidence and recovery: Terminal capture, before/after Ticket Board snapshots and machine record. Screenshot required only for actual layout claim.
