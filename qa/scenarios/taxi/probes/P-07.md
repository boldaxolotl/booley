# P-07 disposable probes

`derived-image`: bake pinned requirements → Session Runtime dependency versions match Session Image identity. `dependency-refresh`: change permitted dependency pin → stale Session Image detected and rebuilt identity used. `runtime-offline`: attempt Session Runtime dependency acquisition → no undeclared network install. `host-skill-readonly`: opt in disposable skill → visible but write denied. `hook-once`: new Ticket Workspace with bounded hook → exactly one successful invocation; resume → no repeat. `hook-failure`: controlled nonzero hook → Ticket Workspace blocked. `hook-recovery`: fix fixture and documented retry → usable Ticket Workspace.

Evidence and recovery: Session Image/pip manifests and payload provenance, skill mount metadata, hook counter/log, and Ticket Workspace identity.
