# T-04 disposable probes

`triage-discover`: public triage skill available. `triage-approve`: approved bounded correction → done. `triage-reset`: reset a separate disposable Ticket → clean queued workspace, retired prior branch/worktree, archived prior evidence. `triage-archive`: archive another disposable Ticket → archived. `review-queue-invalid`: ordinary review→queue request → rejection.

Evidence and recovery: Human decisions, before/after Board and workspace IDs, archived report location. Reset/cleanup only exact owned Tickets.

