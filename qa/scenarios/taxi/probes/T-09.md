# T-09 disposable probes

`atomic-claim`: two runs/two Tickets → separate claims and Ticket Workspaces. `artifact-isolation`: concurrent runs → no cross-Ticket source/evidence. `class-cap`: bounded class limit → excess Job waits. `queue-cancel`: cancel waiting Job → cancelled, no execution. `queue-full`: tiny full queue → admission blocked.

Evidence and recovery: Ticket IDs/worktree paths, class/admission timeline and file manifests; Interactive priority mapping uses I-04 exact check.
