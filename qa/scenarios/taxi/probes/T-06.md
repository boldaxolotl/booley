# T-06 disposable probes

`basis-pin`: enqueue baseline-relative Ticket → immutable refs/Target input basis. `basis-edit-reject`: control input edit during Developer Agent execution → rejected. `basis-redraft`: return-to-draft/revise/reseal → legitimate new basis. `target-disposition`: accept declared persistent/replacement/ephemeral Target Plan → destination contains exactly derived accepted Targets, ephemeral removed.

Evidence and recovery: Before/after sealed basis and destination core/history manifests.
