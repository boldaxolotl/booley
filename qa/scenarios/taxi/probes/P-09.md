# P-09 disposable probes

`vendored-ignore`: invalid core behind FUSESOC_IGNORE → excluded while valid native core resolves. `many-cores`: enumerate fixture cores → exact qualified set. Submodule companion uses the complete 26-check list in [the approved companion](../fixtures/submodules.md), including live top-level/nested RTL dependency, fresh passing simulation, required-source absence with warm/cold builds, dependency-specific simulation failure, and exact restoration/fresh pass. The exact qualified IDs appear centrally in [profiles](../../../profiles.yaml).

Evidence and recovery: Vendored/core manifest and discovery output. Direct Taxi checkout has no submodules and earns no submodule credit. Companion must prove both Ticket and baseline worktrees offline and must fail simulation when required submodule RTL is missing; root-only synthesis earns no functional-dependency credit.
