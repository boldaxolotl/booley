# T-01 disposable probes

`ticket-draft`: create selected detail mode → one complete approval draft. `ticket-guidance`: conflicting Project and explicit one-Ticket creation guidance → explicit override wins. `ticket-invalid-criterion`, `ticket-invalid-scope`, `ticket-invalid-target`: invalid fixture of each class → no enqueue. `ticket-seal`: approved valid draft enqueued → exact approved sealed contract.

Evidence and recovery: Draft/approval/sealed snapshots and validation errors; no agent impersonates live human approval.

