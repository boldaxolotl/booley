format_version: 1
scenario_id: coverage-measurement
title: 'Native coverage: measurement'
source: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
check_sets:
- id: coverage-measurement-core
  checks:
  - setup.artifact
  - setup.ready
  - setup.catalog
  - oracle.positive
  - oracle.negative-hit-count
  - oracle.negative-source-grouping
  - oracle.negative-digest
  - oracle.negative-rational-verdict
  - baseline.native
  - baseline.policy
  - baseline.analysis
  - baseline.waiver
  - baseline.retention
  - baseline.custom
  - numeric.fixture
  - numeric.toggle-zero
  - numeric.toggle-quarter
  - numeric.toggle-half
  - numeric.toggle-one-direction
  - numeric.toggle-full
  - numeric.repeat
  - numeric.test-union
  - numeric.test-overlap
  - numeric.properties-half
  - numeric.properties-full
  - numeric.sixteen-values
  - numeric.threshold-equal
  - numeric.threshold-above
  - numeric.reset-window
  - collect.default-off
  - collect.cli
  - collect.alias
  - collect.mcp
  - collect.mcp-type
  - collect.mcp-type.restore
  - collect.generated
  - collect.custom
  - collect.hdl
  - collect.cocotb
  - collect.order
  - collect.full-suite
  - collect.full-suite.restore
  - collect.explicit-skip-override
  - collect.explicit-skip-override.restore
  - collect.criterion-suite
  - collect.explicit-suite
  - collect.skipped-required
  - collect.skipped-required.restore
  - collect.icarus
  - collect.icarus.restore
  - collect.mixed-icarus
  - collect.mixed-icarus.restore
  - collect.elab-only
  - collect.elab-only.restore
  - collect.elab-standalone
  - collect.elab-standalone.restore
  - collect.tool-version
  - collect.tool-version.restore
  - collect.cache-normal
  - collect.cache-trace
  - collect.cache-coverage
  - collect.cache-repeat
  - collect.pre-sim
  - window.reset-default
  - window.reset-excluded
  - window.custom-write-pass
  - window.custom-write-fail
  - window.invalid-type
  - window.invalid-type.restore
  - window.unknown-hook
  - window.unknown-hook.restore
  - window.duplicate-declaration
  - window.duplicate-declaration.restore
  - window.missing-write-declaration
  - window.missing-write-declaration.restore
  - window.included-start-declaration
  - window.included-start-declaration.restore
  - window.noncustom-declaration
  - window.noncustom-declaration.restore
  - window.global-override
  - window.global-override.restore
  - window.call-override
  - window.call-override.restore
  - window.missing-start
  - window.missing-start.restore
  - window.duplicate-start
  - window.duplicate-start.restore
  - window.missing-write
  - window.missing-write.restore
  - window.duplicate-write
  - window.duplicate-write.restore
  - window.out-of-order
  - window.out-of-order.restore
  - window.failed-hook
  - window.failed-hook.restore
  - native-fault.missing
  - native-fault.missing.restore
  - native-fault.stale
  - native-fault.stale.restore
  - native-fault.malformed
  - native-fault.malformed.restore
  - native-fault.incompatible
  - native-fault.incompatible.restore
  - native-fault.merge-mismatch
  - native-fault.merge-mismatch.restore
  - native-fault.target-continue
  - native-fault.target-continue.restore
  - native-fault.source-drift
  - native-fault.source-drift.restore
  - native-fault.target-drift
  - native-fault.target-drift.restore
  - collect.pre-sim-cocotb
  - collect.pre-sim-cocotb.restore
  - final.regression
  - final.analysis
  - final.archive
  - cleanup.processes
  - cleanup.runtime
  - cleanup.faults
  - cleanup.git
  - cleanup.storage
  - cleanup.records
configured_scenarios:
- id: coverage-measurement-ubuntu-codex-cli
  required: true
  parameters:
    host_os: Ubuntu 24.04
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Codex
    interactive_mode_client: native CLI
    ticket_mode_backend: Codex
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-measurement-core
  exclusions: []
- id: coverage-measurement-ubuntu-claude-cli
  required: true
  parameters:
    host_os: Ubuntu 24.04
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Claude
    interactive_mode_client: native CLI
    ticket_mode_backend: Claude
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-measurement-core
  exclusions: []
- id: coverage-measurement-windows-codex-cli
  required: true
  parameters:
    host_os: Windows with Docker Desktop WSL2
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Codex
    interactive_mode_client: native CLI
    ticket_mode_backend: Codex
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-measurement-core
  exclusions: []
- id: coverage-measurement-windows-claude-cli
  required: true
  parameters:
    host_os: Windows with Docker Desktop WSL2
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Claude
    interactive_mode_client: native CLI
    ticket_mode_backend: Claude
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-measurement-core
  exclusions: []
inputs:
- id: release
  kind: pre-run
  value: Exact immutable booley-rtl package SHA-256, full source commit, matching docs, runtime image digest and
    selected provider/model identity.
  source: https://github.com/boldaxolotl/booley/blob/main/README.md
  verification: Record identities before setup; pre-run selection cannot alter fixture bytes, suites or thresholds.
- id: verilator
  kind: git
  value: ea338be98e1e838d3518809ce8899f85a009963c
  source: https://github.com/verilator/verilator/tree/ea338be98e1e838d3518809ce8899f85a009963c
  verification: Record exact tool source identity and v5.052; 5.050 rejected.
authority:
- Maintainer approved fixture/policy/waiver inputs only; act inside ledgered run-owned Project/runtime and fault
  roots.
- No product/SDK patch or private evaluator import. Simulated external inputs are labeled boundary evidence; live-provider
  Checks require real selected client.
- Preserve pins, thresholds, exact suites and every original failure. No push, issue submission, remote publication
  or permanent repair.
- Restoration never depends on successful detection. Enter unconditional cleanup by minute 450; preserve borrowed
  resources and secrets.
budget:
  deadline_minutes: 480
  cleanup_minutes: 30
  contingency_minutes: 104
  cleanup_start_minutes: 450
  final_regression_start_minutes: 426
phases:
- id: prepare
  title: Artifact, fixture and independent oracle preparation
  minutes: 52
- id: baseline
  title: Fresh named local baselines
  minutes: 72
- id: exercise
  title: Expanded checks and independent restorations
  minutes: 198
- id: final
  title: Fresh regression, Analyst and evidence archive
  minutes: 24
- id: cleanup
  title: Unconditional resource cleanup
  minutes: 30
steps:
- id: setup.artifact
  phase: prepare
  action: Verify exact immutable installed package/source/docs/image identities and pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c.
    Record actual selected provider/SDK/executable identities; no source-checkout imports.
  checks:
  - id: setup.artifact
    capabilities:
    - PRODUCT-ARTIFACT-INSTALLATION
    - EDA-VERILATOR-SIMULATION
    stimulus: Verify exact immutable installed package/source/docs/image identities and pinned Verilator v5.052
      ea338be98e1e838d3518809ce8899f85a009963c. Record actual selected provider/SDK/executable identities; no source-checkout
      imports.
    expected: Verify exact immutable installed package/source/docs/image identities and pinned Verilator v5.052
      ea338be98e1e838d3518809ce8899f85a009963c. Record actual selected provider/SDK/executable identities; no source-checkout
      imports.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Verify exact immutable installed package/source/docs/image
      identities and pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c. Record actual selected provider/SDK/executable
      identities; no source-checkout imports.'
    capture: evidence/coverage-measurement.setup.artifact
  timeout_minutes: 10
- id: setup.ready
  phase: prepare
  action: 'Follow shared coverage/RUNBOOK.md setup: fresh run-owned Project/runtime, copy exact hashed fixture,
    initialize via product, install tests into resolved project-data root, compile scoped fault library and run
    evaluator/fault controls. Freeze owned-root/resource ledger. Use the selected native CLI client and configured
    Specialist model. No artifacts from other Scenario Runs.'
  checks:
  - id: setup.ready
    capabilities:
    - PROJECT-INITIALIZATION
    - SESSION-RUNTIME-LIFECYCLE
    stimulus: 'Follow shared coverage/RUNBOOK.md setup: fresh run-owned Project/runtime, copy exact hashed fixture,
      initialize via product, install tests into resolved project-data root, compile scoped fault library and run
      evaluator/fault controls. Freeze owned-root/resource ledger. Use the selected native CLI client and configured
      Specialist model. No artifacts from other Scenario Runs.'
    expected: 'Follow shared coverage/RUNBOOK.md setup: fresh run-owned Project/runtime, copy exact hashed fixture,
      initialize via product, install tests into resolved project-data root, compile scoped fault library and run
      evaluator/fault controls. Freeze owned-root/resource ledger. Use the selected native CLI client and configured
      Specialist model. No artifacts from other Scenario Runs.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Follow shared coverage/RUNBOOK.md setup: fresh run-owned
      Project/runtime, copy exact hashed fixture, initialize via product, install tests into resolved project-data
      root, compile scoped fault library and run evaluator/fault controls. Freeze owned-root/resource ledger. Use
      the selected native CLI client and configured Specialist model. No artifacts from other Scenario Runs.'
    capture: evidence/coverage-measurement.setup.ready
  timeout_minutes: 30
  requires:
  - setup.artifact
  assets:
  - path: coverage/baselines.json
    base: shared
    audience: scenario-operator
    sha256: 8e74bce480e395d5390009106fbf697867faeeb9a2f7118179b66a98635f5c20
  - path: coverage/policies.json
    base: shared
    audience: scenario-operator
    sha256: 82736ce9dc0244a7b833b81bd331699dafc6f11b426703bb7bfcf2da4c319d58
  - path: coverage/expected.json
    base: shared
    audience: scenario-operator
    sha256: f0f5edcf078329bf468fc5418609a644c20952264c6845308ae33725563e7c41
  - path: coverage/RUNBOOK.md
    base: shared
    audience: scenario-operator
    sha256: 216e061687934ee83fcc07166e3ed61b1bebd76ba8fa719792e657b10ec0a84e
  - path: coverage/approval-inputs.json
    base: shared
    audience: scenario-operator
    sha256: 2ed73b45e21afe0104f11bfa533e0f040ac20a55c34bae5ca533145c910306c0
  - path: coverage/project/coverage.core
    base: shared
    audience: scenario-operator
    sha256: 7400301012dc1e66b977488348c94c61c057c0a0ebbdbbce96c5f3dd188366fc
  - path: coverage/evaluator/controls.py
    base: shared
    audience: independent evaluator
    sha256: 8136daced7ac3abe620472d010346fe63b3ddd9662667e377b880b4dea5d9283
  - path: coverage/evaluator/measurements.py
    base: shared
    audience: independent evaluator
    sha256: b9cc311d59850c0acf34cd0364f4a7d1d2d4b0c87492df441473ae9ef3f00dbc
  - path: coverage/proof/parity.ys
    base: shared
    audience: scenario-operator
    sha256: 9efe18a93df8f320fcea6ff24b6dbc47d54a0d6ccb2129bb8a1956b878e8769c
  - path: coverage/tickets/close-test-gap.md
    base: shared
    audience: scenario-operator
    sha256: 3063bda64ae5ca1ff1be01d6226ba2f833edc6200055266de730f5810204012a
  - path: coverage/tickets/tests.toml
    base: shared
    audience: scenario-operator
    sha256: 4fd70c5c36a4710bb1b680c553cf04f2022b49c9617cbded3c3397b67e686c10
  - path: coverage/faults/provider.py
    base: shared
    audience: scenario-operator
    sha256: a6860c48cc2114cecf3eb9a366c444ff9d71440134f15ba3fed718331d5924a0
  - path: coverage/faults/campaign.py
    base: shared
    audience: scenario-operator
    sha256: b66fd938ad572e709869aefa22f623d6bab6b3cad05d07d3101004492844c82f
  - path: coverage/faults/native_tool.py
    base: shared
    audience: scenario-operator
    sha256: 116df5e4ca2c184d27162f1dd1d4a5bb983a06c6107fdb24fb621a2438a26d75
  - path: coverage/faults/filesystem.py
    base: shared
    audience: scenario-operator
    sha256: 365567972d5d412dd1f6a8cf117742fe7d2d0791a5616b67fc989251a2b6da93
  - path: coverage/faults/boundary.c
    base: shared
    audience: scenario-operator
    sha256: d08e19ec246b05e4d2642f20cbfad5915bcfd82d9f5b938d0ef74b6f1fb56f17
  - path: coverage/faults/approvals.py
    base: shared
    audience: scenario-operator
    sha256: 22313486bcad87cf1afbe0cbd2f9b0dd843750fb901605ecd6343d316631918a
  - path: coverage/project/rtl/coverage_dut.sv
    base: shared
    audience: scenario-operator
    sha256: b9290be3023b03b31d4b5ac24ab38f1bb5f72c706ad153bc9d7d9694e2f28f46
  - path: coverage/project/rtl/properties16.sv
    base: shared
    audience: scenario-operator
    sha256: fb25c4c85212715ee3200ba74e6cb2405464e8c80183d2a80b6820f942d6a330
  - path: coverage/project/rtl/waiver_counter.sv
    base: shared
    audience: scenario-operator
    sha256: 841eb1ea3263039e35ac02459ef8be0225f4480a50f9ee092602783e58059d0d
  - path: coverage/project/rtl/properties4.sv
    base: shared
    audience: scenario-operator
    sha256: f5518ced99e8c7902c332241eb33d94f6a2e26cce79a316d55303676512a1f4b
  - path: coverage/project/rtl/constants.svh
    base: shared
    audience: scenario-operator
    sha256: f73cdd3b48a75ef8a23a22648fa1a986eab3b96422c31134562a3941629bba53
  - path: coverage/project/rtl/toggle.sv
    base: shared
    audience: scenario-operator
    sha256: 6f33e57fce5b74ae48c820392e1ed57311bf43bb92768536a6f9f1c23513e326
  - path: coverage/project/rtl/properties3.sv
    base: shared
    audience: scenario-operator
    sha256: ecd0d2ebd423991ca5b12ca9c472198f6c5c299784ca41d74c804e09d2cb485a
  - path: coverage/project/foreign/coverage_library.sv
    base: shared
    audience: scenario-operator
    sha256: cac802acb66125c04478853a0ff0d6b058d66ba1fc87a7e6d48e481d255dda5e
  - path: coverage/project/tb/cocotb_cases.py
    base: shared
    audience: scenario-operator
    sha256: 467fe73fbf8c80cf9ff0f6216fffe9f6ae10d6432e1931005993f658f0c71878
  - path: coverage/project/tb/scale_tb.sv
    base: shared
    audience: scenario-operator
    sha256: 64276850f0d90b26002c1a5321d8f1e6d3ecf9bafcab189109b2f899ddbaa5b0
  - path: coverage/project/tb/coverage_tb.sv
    base: shared
    audience: scenario-operator
    sha256: c5d68c11b8f0b25b3bfc1c29e8508e6746bc5a7d8d41715f1c537e5b407ba6da
  - path: coverage/project/tb/properties3_main.cpp
    base: shared
    audience: scenario-operator
    sha256: 0fe647329b68ce9ca38d55fe9992d32cd291e2b110a8c44a52224a94f2070c33
  - path: coverage/project/tb/waiver_tb.sv
    base: shared
    audience: scenario-operator
    sha256: d872d814256d2f161f412c585689db60e2282a50e70355bcd9a6a3c6ae58b5a9
  - path: coverage/project/tb/properties16_main.cpp
    base: shared
    audience: scenario-operator
    sha256: ccc3f8009c3286a3aca98e9d062598cabdaa38b7ff77c9f5069b79f00c2a1c4a
  - path: coverage/project/tb/properties4_main.cpp
    base: shared
    audience: scenario-operator
    sha256: b9fd74a44739cbe3e8c7b71b6b0894b56db15997976e06e82f74b4c0b56a2301
  - path: coverage/project/tb/cocotb_staged.py
    base: shared
    audience: scenario-operator
    sha256: 107bd339e165b03bec9694ba4c7f3c2c76f87f33429feba5f38be073c1118458
  - path: coverage/project/tb/staged_main.cpp
    base: shared
    audience: scenario-operator
    sha256: 4c5cd55dd55636eaa31ce31cd8e28faa041c4d0052c8df34f9d93c50ab1873fd
  - path: coverage/project/tb/coverage_main.cpp
    base: shared
    audience: scenario-operator
    sha256: 14f312bcf77304cfc021d5231262d105d78dbf678aa51432b9daca53dc73e738
  - path: coverage/project/tb/multi_tb.sv
    base: shared
    audience: scenario-operator
    sha256: ef05488f7510269c783bcaadb4cc3f47f1eb6cebb3f64dea612289dcd5c10284
  - path: coverage/project/tb/toggle_main.cpp
    base: shared
    audience: scenario-operator
    sha256: e1b88b2662c06aa663eea8a7ee9bd010a80cb9b36ce4831d38c944954c1ae8e5
  - path: coverage/project/project-data/staging.toml
    base: shared
    audience: scenario-operator
    sha256: 427ce254316a45b2cc662bde6a2754d98c30ea26b15cc509cb5eae473b290b59
  - path: coverage/project/project-data/tests.toml
    base: shared
    audience: scenario-operator
    sha256: 57a3c33e34158218bd387016c10c894ac08bfe307ddbb159c4f5fb535e565506
  - path: coverage/project/project-data/booley.toml
    base: shared
    audience: scenario-operator
    sha256: fa01bd3d777362378c4b137a7a8f288cb59385ac8fb30a5fb814579363ea4b58
  - path: coverage/project/scripts/stage_vector.py
    base: shared
    audience: scenario-operator
    sha256: f3ff5cdaf807e6096c6f3b717b7af7560a751db3af4566d5c2d6cddaf9a573d5
  resources:
  - owned-project
  - owned-runtime
  - owned-fault-controller
  - owned-artifact-root
- id: setup.catalog
  phase: prepare
  action: 'Read actual selected CLI help and MCP schemas: --coverage and permanent --cov, default-false boolean,
    exact required Analyst Campaign path, optional instruction; no legacy scorer options.'
  checks:
  - id: setup.catalog
    capabilities:
    - PUBLIC-CAPABILITY-DISCOVERY
    stimulus: 'Read actual selected CLI help and MCP schemas: --coverage and permanent --cov, default-false boolean,
      exact required Analyst Campaign path, optional instruction; no legacy scorer options.'
    expected: 'Read actual selected CLI help and MCP schemas: --coverage and permanent --cov, default-false boolean,
      exact required Analyst Campaign path, optional instruction; no legacy scorer options.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Read actual selected CLI help and MCP schemas: --coverage
      and permanent --cov, default-false boolean, exact required Analyst Campaign path, optional instruction; no
      legacy scorer options.'
    capture: evidence/coverage-measurement.setup.catalog
  timeout_minutes: 2
  requires:
  - setup.artifact
- id: oracle.positive
  phase: prepare
  action: Run python3 evaluator/controls.py positive from the hashed shared fixture. Positive control must accept
    literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control input.
    These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.positive
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py positive from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py positive from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py positive from the hashed
      shared fixture. Positive control must accept literal 4/8; each negative must reject its independently altered
      field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-measurement.oracle.positive
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-hit-count
  phase: prepare
  action: Run python3 evaluator/controls.py hit-count from the hashed shared fixture. Positive control must accept
    literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control input.
    These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-hit-count
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py hit-count from the hashed shared fixture. Positive control must
      accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py hit-count from the hashed shared fixture. Positive control must
      accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py hit-count from the hashed
      shared fixture. Positive control must accept literal 4/8; each negative must reject its independently altered
      field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-measurement.oracle.negative-hit-count
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-source-grouping
  phase: prepare
  action: Run python3 evaluator/controls.py source-grouping from the hashed shared fixture. Positive control must
    accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
    input. These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-source-grouping
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py source-grouping from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py source-grouping from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py source-grouping from
      the hashed shared fixture. Positive control must accept literal 4/8; each negative must reject its independently
      altered field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-measurement.oracle.negative-source-grouping
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-digest
  phase: prepare
  action: Run python3 evaluator/controls.py digest from the hashed shared fixture. Positive control must accept
    literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control input.
    These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-digest
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py digest from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py digest from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py digest from the hashed
      shared fixture. Positive control must accept literal 4/8; each negative must reject its independently altered
      field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-measurement.oracle.negative-digest
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-rational-verdict
  phase: prepare
  action: Run python3 evaluator/controls.py rational-verdict from the hashed shared fixture. Positive control must
    accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
    input. These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-rational-verdict
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py rational-verdict from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py rational-verdict from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py rational-verdict from
      the hashed shared fixture. Positive control must accept literal 4/8; each negative must reject its independently
      altered field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-measurement.oracle.negative-rational-verdict
  timeout_minutes: 2
  requires:
  - setup.ready
- id: baseline.native
  phase: baseline
  action: Produce fresh baseline.native according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.native,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.native
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.native according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.native,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.native according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.native,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.native according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.native, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-measurement.baseline.native
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.policy
  phase: baseline
  action: Produce fresh baseline.policy according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.policy,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.policy
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.policy according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.policy,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.policy according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.policy,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.policy according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.policy, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-measurement.baseline.policy
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.analysis
  phase: baseline
  action: Produce fresh baseline.analysis according to the exact baseline table in coverage/RUNBOOK.md. Capture
    immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.analysis,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.analysis
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.analysis according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.analysis,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.analysis according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.analysis,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.analysis according to the exact
      baseline table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket
      state outside every fault/prune root. For baseline.analysis, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-measurement.baseline.analysis
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.waiver
  phase: baseline
  action: Produce fresh baseline.waiver according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.waiver,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.waiver
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.waiver according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.waiver,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.waiver according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.waiver,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.waiver according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.waiver, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-measurement.baseline.waiver
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.retention
  phase: baseline
  action: Produce fresh baseline.retention according to the exact baseline table in coverage/RUNBOOK.md. Capture
    immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.retention,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.retention
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.retention according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.retention,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.retention according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.retention,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.retention according to the exact
      baseline table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket
      state outside every fault/prune root. For baseline.retention, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-measurement.baseline.retention
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.custom
  phase: baseline
  action: Produce fresh baseline.custom according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.custom,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.custom
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.custom according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.custom,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.custom according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.custom,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.custom according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.custom, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-measurement.baseline.custom
  timeout_minutes: 12
  requires:
  - setup.ready
- id: numeric.fixture
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Collect the fixed
    numeric Targets; prove exactly eight RTL directional toggle points, four and sixteen explicitly labeled cover
    properties. No denominator adaptation.
  checks:
  - id: numeric.fixture
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Collect the fixed
      numeric Targets; prove exactly eight RTL directional toggle points, four and sixteen explicitly labeled cover
      properties. No denominator adaptation.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Collect the fixed
      numeric Targets; prove exactly eight RTL directional toggle points, four and sixteen explicitly labeled cover
      properties. No denominator adaptation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Collect the fixed numeric Targets; prove exactly eight RTL directional toggle
      points, four and sixteen explicitly labeled cover properties. No denominator adaptation.'
    capture: evidence/coverage-measurement.numeric.fixture
  timeout_minutes: 2
  requires:
  - baseline.native
- id: numeric.toggle-zero
  phase: exercise
  action: Use expected.json case zero; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
    with every raw run. Hold vector at `0000` → 0 of 8 directional points hit, exactly 0%. A valid zero differs
    from a zero-eligible blocked metric.
  checks:
  - id: numeric.toggle-zero
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case zero; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. Hold vector at `0000` → 0 of 8 directional points hit, exactly 0%. A valid zero differs
      from a zero-eligible blocked metric.
    expected: Use expected.json case zero; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. Hold vector at `0000` → 0 of 8 directional points hit, exactly 0%. A valid zero differs
      from a zero-eligible blocked metric.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case zero; run its exact Target and
      tests with explicit --coverage, then evaluator/measurements.py with every raw run. Hold vector at `0000` →
      0 of 8 directional points hit, exactly 0%. A valid zero differs from a zero-eligible blocked metric.'
    capture: evidence/coverage-measurement.numeric.toggle-zero
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.toggle-quarter
  phase: exercise
  action: 'Use expected.json case quarter; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
    with every raw run. `0000 → 0001 → 0000` → only bit 0 rises and falls: 2 of 8, exactly 25%.'
  checks:
  - id: numeric.toggle-quarter
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: 'Use expected.json case quarter; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. `0000 → 0001 → 0000` → only bit 0 rises and falls: 2 of 8, exactly 25%.'
    expected: 'Use expected.json case quarter; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. `0000 → 0001 → 0000` → only bit 0 rises and falls: 2 of 8, exactly 25%.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case quarter; run its exact Target and
      tests with explicit --coverage, then evaluator/measurements.py with every raw run. `0000 → 0001 → 0000` →
      only bit 0 rises and falls: 2 of 8, exactly 25%.'
    capture: evidence/coverage-measurement.numeric.toggle-quarter
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.toggle-half
  phase: exercise
  action: 'Use expected.json case half; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
    with every raw run. `0000 → 0011 → 0000` → bits 0 and 1 rise and fall: 4 of 8, exactly 50%; bits 2 and 3 stay
    uncovered.'
  checks:
  - id: numeric.toggle-half
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: 'Use expected.json case half; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. `0000 → 0011 → 0000` → bits 0 and 1 rise and fall: 4 of 8, exactly 50%; bits 2 and 3 stay
      uncovered.'
    expected: 'Use expected.json case half; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. `0000 → 0011 → 0000` → bits 0 and 1 rise and fall: 4 of 8, exactly 50%; bits 2 and 3 stay
      uncovered.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case half; run its exact Target and
      tests with explicit --coverage, then evaluator/measurements.py with every raw run. `0000 → 0011 → 0000` →
      bits 0 and 1 rise and fall: 4 of 8, exactly 50%; bits 2 and 3 stay uncovered.'
    capture: evidence/coverage-measurement.numeric.toggle-half
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.toggle-one-direction
  phase: exercise
  action: 'Use expected.json case rise; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
    with every raw run. `0000 → 1111`, finish before any in-window falling transition → four rising points hit and
    all four falling points unhit: 4 of 8, exactly 50%.'
  checks:
  - id: numeric.toggle-one-direction
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: 'Use expected.json case rise; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. `0000 → 1111`, finish before any in-window falling transition → four rising points hit
      and all four falling points unhit: 4 of 8, exactly 50%.'
    expected: 'Use expected.json case rise; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. `0000 → 1111`, finish before any in-window falling transition → four rising points hit
      and all four falling points unhit: 4 of 8, exactly 50%.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case rise; run its exact Target and
      tests with explicit --coverage, then evaluator/measurements.py with every raw run. `0000 → 1111`, finish before
      any in-window falling transition → four rising points hit and all four falling points unhit: 4 of 8, exactly
      50%.'
    capture: evidence/coverage-measurement.numeric.toggle-one-direction
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.toggle-full
  phase: exercise
  action: 'Use expected.json case full; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
    with every raw run. `0000 → 1111 → 0000` → all eight directions hit: 8 of 8, exactly 100%, although only two
    distinct numeric values were visited.'
  checks:
  - id: numeric.toggle-full
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: 'Use expected.json case full; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. `0000 → 1111 → 0000` → all eight directions hit: 8 of 8, exactly 100%, although only two
      distinct numeric values were visited.'
    expected: 'Use expected.json case full; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. `0000 → 1111 → 0000` → all eight directions hit: 8 of 8, exactly 100%, although only two
      distinct numeric values were visited.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case full; run its exact Target and
      tests with explicit --coverage, then evaluator/measurements.py with every raw run. `0000 → 1111 → 0000` →
      all eight directions hit: 8 of 8, exactly 100%, although only two distinct numeric values were visited.'
    capture: evidence/coverage-measurement.numeric.toggle-full
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.repeat
  phase: exercise
  action: Use expected.json case repeat; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
    with every raw run. Repeat the half-coverage sequence three times with identical sampling → same four covered
    points, exactly 50%; retained native per-point hit counts reflect repeated transitions, not new covered points.
  checks:
  - id: numeric.repeat
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case repeat; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. Repeat the half-coverage sequence three times with identical sampling → same four covered
      points, exactly 50%; retained native per-point hit counts reflect repeated transitions, not new covered points.
    expected: Use expected.json case repeat; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. Repeat the half-coverage sequence three times with identical sampling → same four covered
      points, exactly 50%; retained native per-point hit counts reflect repeated transitions, not new covered points.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case repeat; run its exact Target and
      tests with explicit --coverage, then evaluator/measurements.py with every raw run. Repeat the half-coverage
      sequence three times with identical sampling → same four covered points, exactly 50%; retained native per-point
      hit counts reflect repeated transitions, not new covered points.'
    capture: evidence/coverage-measurement.numeric.repeat
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.test-union
  phase: exercise
  action: Use expected.json case union; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
    with every raw run. One selected test toggles only bits 0/1 in both directions; another toggles only bits 2/3
    in both directions → each test has 4 of 8, Campaign union has 8 of 8, exactly 100%. Verify exact per-test incidence,
    not an average of two 50% figures.
  checks:
  - id: numeric.test-union
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case union; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. One selected test toggles only bits 0/1 in both directions; another toggles only bits
      2/3 in both directions → each test has 4 of 8, Campaign union has 8 of 8, exactly 100%. Verify exact per-test
      incidence, not an average of two 50% figures.
    expected: Use expected.json case union; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. One selected test toggles only bits 0/1 in both directions; another toggles only bits
      2/3 in both directions → each test has 4 of 8, Campaign union has 8 of 8, exactly 100%. Verify exact per-test
      incidence, not an average of two 50% figures.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case union; run its exact Target and
      tests with explicit --coverage, then evaluator/measurements.py with every raw run. One selected test toggles
      only bits 0/1 in both directions; another toggles only bits 2/3 in both directions → each test has 4 of 8,
      Campaign union has 8 of 8, exactly 100%. Verify exact per-test incidence, not an average of two 50% figures.'
    capture: evidence/coverage-measurement.numeric.test-union
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.test-overlap
  phase: exercise
  action: Use expected.json case overlap; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
    with every raw run. Two tests both toggle only bits 0/1 → each 4 of 8 and merged union still 4 of 8, exactly
    50%; repeated coverage cannot inflate the percentage.
  checks:
  - id: numeric.test-overlap
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case overlap; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. Two tests both toggle only bits 0/1 → each 4 of 8 and merged union still 4 of 8, exactly
      50%; repeated coverage cannot inflate the percentage.
    expected: Use expected.json case overlap; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. Two tests both toggle only bits 0/1 → each 4 of 8 and merged union still 4 of 8, exactly
      50%; repeated coverage cannot inflate the percentage.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case overlap; run its exact Target and
      tests with explicit --coverage, then evaluator/measurements.py with every raw run. Two tests both toggle only
      bits 0/1 → each 4 of 8 and merged union still 4 of 8, exactly 50%; repeated coverage cannot inflate the percentage.'
    capture: evidence/coverage-measurement.numeric.test-overlap
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.properties-half
  phase: exercise
  action: Use expected.json case properties4-half; run its exact Target and tests with explicit --coverage, then
    evaluator/measurements.py with every raw run. Four authored equality cover properties for values 0, 1, 2, 3;
    sample only 0 and 1 → exact hit set {0,1}, 2 of 4, exactly 50%.
  checks:
  - id: numeric.properties-half
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case properties4-half; run its exact Target and tests with explicit --coverage,
      then evaluator/measurements.py with every raw run. Four authored equality cover properties for values 0, 1,
      2, 3; sample only 0 and 1 → exact hit set {0,1}, 2 of 4, exactly 50%.
    expected: Use expected.json case properties4-half; run its exact Target and tests with explicit --coverage,
      then evaluator/measurements.py with every raw run. Four authored equality cover properties for values 0, 1,
      2, 3; sample only 0 and 1 → exact hit set {0,1}, 2 of 4, exactly 50%.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case properties4-half; run its exact
      Target and tests with explicit --coverage, then evaluator/measurements.py with every raw run. Four authored
      equality cover properties for values 0, 1, 2, 3; sample only 0 and 1 → exact hit set {0,1}, 2 of 4, exactly
      50%.'
    capture: evidence/coverage-measurement.numeric.properties-half
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.properties-full
  phase: exercise
  action: Use expected.json case properties4-full; run its exact Target and tests with explicit --coverage, then
    evaluator/measurements.py with every raw run. Sample 0, 1, 2, 3 on the same four-property Target → 4 of 4, exactly
    100%.
  checks:
  - id: numeric.properties-full
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case properties4-full; run its exact Target and tests with explicit --coverage,
      then evaluator/measurements.py with every raw run. Sample 0, 1, 2, 3 on the same four-property Target → 4
      of 4, exactly 100%.
    expected: Use expected.json case properties4-full; run its exact Target and tests with explicit --coverage,
      then evaluator/measurements.py with every raw run. Sample 0, 1, 2, 3 on the same four-property Target → 4
      of 4, exactly 100%.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case properties4-full; run its exact
      Target and tests with explicit --coverage, then evaluator/measurements.py with every raw run. Sample 0, 1,
      2, 3 on the same four-property Target → 4 of 4, exactly 100%.'
    capture: evidence/coverage-measurement.numeric.properties-full
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.sixteen-values
  phase: exercise
  action: Use expected.json case properties16-half; run its exact Target and tests with explicit --coverage, then
    evaluator/measurements.py with every raw run. Sixteen authored equality cover properties for all four-bit values;
    sample only 0 and 1 → 2 of 16, exactly 12.5%. This is explicitly authored property coverage, not a claimed generic
    value-coverage metric.
  checks:
  - id: numeric.sixteen-values
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case properties16-half; run its exact Target and tests with explicit --coverage,
      then evaluator/measurements.py with every raw run. Sixteen authored equality cover properties for all four-bit
      values; sample only 0 and 1 → 2 of 16, exactly 12.5%. This is explicitly authored property coverage, not a
      claimed generic value-coverage metric.
    expected: Use expected.json case properties16-half; run its exact Target and tests with explicit --coverage,
      then evaluator/measurements.py with every raw run. Sixteen authored equality cover properties for all four-bit
      values; sample only 0 and 1 → 2 of 16, exactly 12.5%. This is explicitly authored property coverage, not a
      claimed generic value-coverage metric.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case properties16-half; run its exact
      Target and tests with explicit --coverage, then evaluator/measurements.py with every raw run. Sixteen authored
      equality cover properties for all four-bit values; sample only 0 and 1 → 2 of 16, exactly 12.5%. This is explicitly
      authored property coverage, not a claimed generic value-coverage metric.'
    capture: evidence/coverage-measurement.numeric.sixteen-values
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.threshold-equal
  phase: exercise
  action: Use expected.json case threshold-equal; run its exact Target and tests with explicit --coverage, then
    evaluator/measurements.py with every raw run. Half-toggle stimulus with fixed min_pct 50 → exactly 4/8 and policy
    pass.
  checks:
  - id: numeric.threshold-equal
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case threshold-equal; run its exact Target and tests with explicit --coverage, then
      evaluator/measurements.py with every raw run. Half-toggle stimulus with fixed min_pct 50 → exactly 4/8 and
      policy pass.
    expected: Use expected.json case threshold-equal; run its exact Target and tests with explicit --coverage, then
      evaluator/measurements.py with every raw run. Half-toggle stimulus with fixed min_pct 50 → exactly 4/8 and
      policy pass.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case threshold-equal; run its exact
      Target and tests with explicit --coverage, then evaluator/measurements.py with every raw run. Half-toggle
      stimulus with fixed min_pct 50 → exactly 4/8 and policy pass.'
    capture: evidence/coverage-measurement.numeric.threshold-equal
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.threshold-above
  phase: exercise
  action: Use expected.json case threshold-above; run its exact Target and tests with explicit --coverage, then
    evaluator/measurements.py with every raw run. Identical half-toggle stimulus under a separate fixed min_pct
    50.01 policy → exactly 4/8 and policy fail; simulation still passes.
  checks:
  - id: numeric.threshold-above
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case threshold-above; run its exact Target and tests with explicit --coverage, then
      evaluator/measurements.py with every raw run. Identical half-toggle stimulus under a separate fixed min_pct
      50.01 policy → exactly 4/8 and policy fail; simulation still passes.
    expected: Use expected.json case threshold-above; run its exact Target and tests with explicit --coverage, then
      evaluator/measurements.py with every raw run. Identical half-toggle stimulus under a separate fixed min_pct
      50.01 policy → exactly 4/8 and policy fail; simulation still passes.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case threshold-above; run its exact
      Target and tests with explicit --coverage, then evaluator/measurements.py with every raw run. Identical half-toggle
      stimulus under a separate fixed min_pct 50.01 policy → exactly 4/8 and policy fail; simulation still passes.'
    capture: evidence/coverage-measurement.numeric.threshold-above
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: numeric.reset-window
  phase: exercise
  action: Use expected.json case reset; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
    with every raw run. Before the start hook, exercise all vector directions; after start, perform only half-toggle
    sequence → raw retained counters and Booley rollups show exactly 4/8, not reset-inflated 8/8.
  checks:
  - id: numeric.reset-window
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Use expected.json case reset; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. Before the start hook, exercise all vector directions; after start, perform only half-toggle
      sequence → raw retained counters and Booley rollups show exactly 4/8, not reset-inflated 8/8.
    expected: Use expected.json case reset; run its exact Target and tests with explicit --coverage, then evaluator/measurements.py
      with every raw run. Before the start hook, exercise all vector directions; after start, perform only half-toggle
      sequence → raw retained counters and Booley rollups show exactly 4/8, not reset-inflated 8/8.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use expected.json case reset; run its exact Target and
      tests with explicit --coverage, then evaluator/measurements.py with every raw run. Before the start hook,
      exercise all vector directions; after start, perform only half-toggle sequence → raw retained counters and
      Booley rollups show exactly 4/8, not reset-inflated 8/8.'
    capture: evidence/coverage-measurement.numeric.reset-window
  timeout_minutes: 2
  requires:
  - baseline.native
  - numeric.fixture
- id: collect.default-off
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run simulation with
    an authored coverage Criterion but omit collection → ordinary simulation, no native Campaign and no new coverage
    acceptance. Capture T as well.
  checks:
  - id: collect.default-off
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run simulation
      with an authored coverage Criterion but omit collection → ordinary simulation, no native Campaign and no new
      coverage acceptance. Capture T as well.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run simulation
      with an authored coverage Criterion but omit collection → ordinary simulation, no native Campaign and no new
      coverage acceptance. Capture T as well.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Run simulation with an authored coverage Criterion but omit collection → ordinary
      simulation, no native Campaign and no new coverage acceptance. Capture T as well.'
    capture: evidence/coverage-measurement.collect.default-off
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.cli
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. `booley flow sim --target
    sim_generated --coverage` → complete ungated V3 Campaign and `not_requested`.
  checks:
  - id: collect.cli
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. `booley flow sim
      --target sim_generated --coverage` → complete ungated V3 Campaign and `not_requested`.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. `booley flow sim
      --target sim_generated --coverage` → complete ungated V3 Campaign and `not_requested`.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. `booley flow sim --target sim_generated --coverage` → complete ungated V3 Campaign
      and `not_requested`.'
    capture: evidence/coverage-measurement.collect.cli
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.alias
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run the same Target
    with `--cov` → equivalent measured semantics in a fresh numbered invocation.
  checks:
  - id: collect.alias
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run the same Target
      with `--cov` → equivalent measured semantics in a fresh numbered invocation.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run the same Target
      with `--cov` → equivalent measured semantics in a fresh numbered invocation.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Run the same Target with `--cov` → equivalent measured semantics in a fresh numbered
      invocation.'
    capture: evidence/coverage-measurement.collect.alias
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.mcp
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Actual selected native
    client submits `sim` with `coverage: true` → fresh Campaign pointer and structured per-Target truths; capture
    client transcript. Reuse INTERACTIVE-MODE.'
  checks:
  - id: collect.mcp
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Actual selected
      native client submits `sim` with `coverage: true` → fresh Campaign pointer and structured per-Target truths;
      capture client transcript. Reuse INTERACTIVE-MODE.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Actual selected
      native client submits `sim` with `coverage: true` → fresh Campaign pointer and structured per-Target truths;
      capture client transcript. Reuse INTERACTIVE-MODE.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Actual selected native client submits `sim` with `coverage: true` → fresh Campaign
      pointer and structured per-Target truths; capture client transcript. Reuse INTERACTIVE-MODE.'
    capture: evidence/coverage-measurement.collect.mcp
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.mcp-type
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Send string `"true"`
    instead of boolean → schema rejection before simulation. X; fault.
  checks:
  - id: collect.mcp-type
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Send string `"true"`
      instead of boolean → schema rejection before simulation. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Send string `"true"`
      instead of boolean → schema rejection before simulation. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Send string `"true"` instead of boolean → schema rejection before simulation.
      X; fault.'
    capture: evidence/coverage-measurement.collect.mcp-type
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.mcp-type.restore
  phase: exercise
  action: Archive collect.mcp-type result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: collect.mcp-type.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.mcp-type result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    expected: Archive collect.mcp-type result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.mcp-type result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.native exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.mcp-type.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.mcp-type
    instruction: Archive collect.mcp-type result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: collect.generated
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run generated-main
    harness with two selected tests → separate successful native databases/processes and exact verdicts.
  checks:
  - id: collect.generated
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run generated-main
      harness with two selected tests → separate successful native databases/processes and exact verdicts.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run generated-main
      harness with two selected tests → separate successful native databases/processes and exact verdicts.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Run generated-main harness with two selected tests → separate successful native
      databases/processes and exact verdicts.'
    capture: evidence/coverage-measurement.collect.generated
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.custom
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run declared custom
    C++ main → matching independent per-test evidence.
  checks:
  - id: collect.custom
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run declared custom
      C++ main → matching independent per-test evidence.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run declared custom
      C++ main → matching independent per-test evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Run declared custom C++ main → matching independent per-test evidence.'
    capture: evidence/coverage-measurement.collect.custom
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.hdl
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run HDL-testbench
    Target → matching independent per-test evidence.
  checks:
  - id: collect.hdl
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run HDL-testbench
      Target → matching independent per-test evidence.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run HDL-testbench
      Target → matching independent per-test evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Run HDL-testbench Target → matching independent per-test evidence.'
    capture: evidence/coverage-measurement.collect.hdl
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.cocotb
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run two Cocotb tests
    with collection → one process/database per test and XML-bound test verdicts. Ordinary batched Cocotb behavior
    is not the coverage oracle.
  checks:
  - id: collect.cocotb
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run two Cocotb
      tests with collection → one process/database per test and XML-bound test verdicts. Ordinary batched Cocotb
      behavior is not the coverage oracle.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Run two Cocotb
      tests with collection → one process/database per test and XML-bound test verdicts. Ordinary batched Cocotb
      behavior is not the coverage oracle.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Run two Cocotb tests with collection → one process/database per test and XML-bound
      test verdicts. Ordinary batched Cocotb behavior is not the coverage oracle.'
    capture: evidence/coverage-measurement.collect.cocotb
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.order
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Supply reversed Target/test
    selection → stable sequential Target/test execution, no cross-Target merge.
  checks:
  - id: collect.order
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Supply reversed
      Target/test selection → stable sequential Target/test execution, no cross-Target merge.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Supply reversed
      Target/test selection → stable sequential Target/test execution, no cross-Target merge.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Supply reversed Target/test selection → stable sequential Target/test execution,
      no cross-Target merge.'
    capture: evidence/coverage-measurement.collect.order
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.full-suite
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Collect without a
    Criterion or selection → full runnable registered suite; default-skipped test absent.
  checks:
  - id: collect.full-suite
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Collect without
      a Criterion or selection → full runnable registered suite; default-skipped test absent.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Collect without
      a Criterion or selection → full runnable registered suite; default-skipped test absent.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Collect without a Criterion or selection → full runnable registered suite; default-skipped
      test absent.'
    capture: evidence/coverage-measurement.collect.full-suite
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.full-suite.restore
  phase: exercise
  action: Archive collect.full-suite result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: collect.full-suite.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.full-suite result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive collect.full-suite result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.full-suite result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.native exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.full-suite.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.full-suite
    instruction: Archive collect.full-suite result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: collect.explicit-skip-override
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Explicitly select
    default-skipped registered test → it executes and is correctly attributed.
  checks:
  - id: collect.explicit-skip-override
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Explicitly select
      default-skipped registered test → it executes and is correctly attributed.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Explicitly select
      default-skipped registered test → it executes and is correctly attributed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Explicitly select default-skipped registered test → it executes and is correctly
      attributed.'
    capture: evidence/coverage-measurement.collect.explicit-skip-override
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.explicit-skip-override.restore
  phase: exercise
  action: Archive collect.explicit-skip-override result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use
    real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: collect.explicit-skip-override.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.explicit-skip-override result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive collect.explicit-skip-override result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.explicit-skip-override result even if
      detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.native exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.explicit-skip-override.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.explicit-skip-override
    instruction: Archive collect.explicit-skip-override result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: collect.criterion-suite
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. With exact policy
    suite, omit invocation filters → that suite takes precedence over the full registered suite. T.
  checks:
  - id: collect.criterion-suite
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. With exact policy
      suite, omit invocation filters → that suite takes precedence over the full registered suite. T.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. With exact policy
      suite, omit invocation filters → that suite takes precedence over the full registered suite. T.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. With exact policy suite, omit invocation filters → that suite takes precedence
      over the full registered suite. T.'
    capture: evidence/coverage-measurement.collect.criterion-suite
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.explicit-suite
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Explicitly select
    a different registered suite from policy → selected evidence is collected, gated evaluation blocked, rc2. T.
  checks:
  - id: collect.explicit-suite
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Explicitly select
      a different registered suite from policy → selected evidence is collected, gated evaluation blocked, rc2.
      T.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Explicitly select
      a different registered suite from policy → selected evidence is collected, gated evaluation blocked, rc2.
      T.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Explicitly select a different registered suite from policy → selected evidence
      is collected, gated evaluation blocked, rc2. T.'
    capture: evidence/coverage-measurement.collect.explicit-suite
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.skipped-required
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Skip a required policy
    test → visible exact-suite mismatch, not a reduced passing denominator. T; fault.
  checks:
  - id: collect.skipped-required
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Skip a required
      policy test → visible exact-suite mismatch, not a reduced passing denominator. T; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Skip a required
      policy test → visible exact-suite mismatch, not a reduced passing denominator. T; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Skip a required policy test → visible exact-suite mismatch, not a reduced passing
      denominator. T; fault.'
    capture: evidence/coverage-measurement.collect.skipped-required
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.skipped-required.restore
  phase: exercise
  action: Archive collect.skipped-required result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: collect.skipped-required.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.skipped-required result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive collect.skipped-required result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.skipped-required result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.native exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.skipped-required.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.skipped-required
    instruction: Archive collect.skipped-required result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: collect.icarus
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Request Icarus coverage
    → whole invocation rejected before build/report allocation. X; fault.
  checks:
  - id: collect.icarus
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Request Icarus
      coverage → whole invocation rejected before build/report allocation. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Request Icarus
      coverage → whole invocation rejected before build/report allocation. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Request Icarus coverage → whole invocation rejected before build/report allocation.
      X; fault.'
    capture: evidence/coverage-measurement.collect.icarus
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.icarus.restore
  phase: exercise
  action: Archive collect.icarus result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: collect.icarus.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.icarus result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    expected: Archive collect.icarus result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.icarus result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.native exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.icarus.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.icarus
    instruction: Archive collect.icarus result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: collect.mixed-icarus
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Request valid Verilator
    plus Icarus → atomic rejection including the valid Target, no build/report allocation. X; fault.
  checks:
  - id: collect.mixed-icarus
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Request valid
      Verilator plus Icarus → atomic rejection including the valid Target, no build/report allocation. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Request valid
      Verilator plus Icarus → atomic rejection including the valid Target, no build/report allocation. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Request valid Verilator plus Icarus → atomic rejection including the valid Target,
      no build/report allocation. X; fault.'
    capture: evidence/coverage-measurement.collect.mixed-icarus
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.mixed-icarus.restore
  phase: exercise
  action: Archive collect.mixed-icarus result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: collect.mixed-icarus.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.mixed-icarus result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive collect.mixed-icarus result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.mixed-icarus result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.native exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.mixed-icarus.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.mixed-icarus
    instruction: Archive collect.mixed-icarus result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: collect.elab-only
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Add coverage to elab-only
    → preflight rejection without side effects. X; fault.
  checks:
  - id: collect.elab-only
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Add coverage to
      elab-only → preflight rejection without side effects. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Add coverage to
      elab-only → preflight rejection without side effects. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Add coverage to elab-only → preflight rejection without side effects. X; fault.'
    capture: evidence/coverage-measurement.collect.elab-only
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.elab-only.restore
  phase: exercise
  action: Archive collect.elab-only result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: collect.elab-only.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.elab-only result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive collect.elab-only result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.elab-only result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.native exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.elab-only.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.elab-only
    instruction: Archive collect.elab-only result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: collect.elab-standalone
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Add coverage to elab-only-standalone
    → same boundary rejection. X; fault.
  checks:
  - id: collect.elab-standalone
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Add coverage to
      elab-only-standalone → same boundary rejection. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Add coverage to
      elab-only-standalone → same boundary rejection. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Add coverage to elab-only-standalone → same boundary rejection. X; fault.'
    capture: evidence/coverage-measurement.collect.elab-standalone
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.elab-standalone.restore
  phase: exercise
  action: Archive collect.elab-standalone result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: collect.elab-standalone.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.elab-standalone result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive collect.elab-standalone result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.elab-standalone result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.native exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.elab-standalone.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.elab-standalone
    instruction: Archive collect.elab-standalone result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: collect.tool-version
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Disposable tool-selection
    fixture advertises excluded 5.050 → actionable coverage compatibility rejection; restore exact pinned real tool.
    X; fault, no native-success credit.
  checks:
  - id: collect.tool-version
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Disposable tool-selection
      fixture advertises excluded 5.050 → actionable coverage compatibility rejection; restore exact pinned real
      tool. X; fault, no native-success credit.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Disposable tool-selection
      fixture advertises excluded 5.050 → actionable coverage compatibility rejection; restore exact pinned real
      tool. X; fault, no native-success credit.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Disposable tool-selection fixture advertises excluded 5.050 → actionable coverage
      compatibility rejection; restore exact pinned real tool. X; fault, no native-success credit.'
    capture: evidence/coverage-measurement.collect.tool-version
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.tool-version.restore
  phase: exercise
  action: Archive collect.tool-version result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.native
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: collect.tool-version.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.tool-version result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive collect.tool-version result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.tool-version result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.native exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.tool-version.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.tool-version
    instruction: Archive collect.tool-version result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: collect.cache-normal
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Warm plain simulation
    then collect → coverage compile recipe/cache identity differs; no uninstrumented cache reuse.
  checks:
  - id: collect.cache-normal
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Warm plain simulation
      then collect → coverage compile recipe/cache identity differs; no uninstrumented cache reuse.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Warm plain simulation
      then collect → coverage compile recipe/cache identity differs; no uninstrumented cache reuse.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Warm plain simulation then collect → coverage compile recipe/cache identity differs;
      no uninstrumented cache reuse.'
    capture: evidence/coverage-measurement.collect.cache-normal
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.cache-trace
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Warm trace-only then
    trace+coverage → different build identity; both queryable trace and native evidence produced. Reuse TRACE-ARTIFACT.
  checks:
  - id: collect.cache-trace
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Warm trace-only
      then trace+coverage → different build identity; both queryable trace and native evidence produced. Reuse TRACE-ARTIFACT.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Warm trace-only
      then trace+coverage → different build identity; both queryable trace and native evidence produced. Reuse TRACE-ARTIFACT.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Warm trace-only then trace+coverage → different build identity; both queryable
      trace and native evidence produced. Reuse TRACE-ARTIFACT.'
    capture: evidence/coverage-measurement.collect.cache-trace
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.cache-coverage
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Compare coverage-only
    and trace+coverage → separate recipe/cache identities.
  checks:
  - id: collect.cache-coverage
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Compare coverage-only
      and trace+coverage → separate recipe/cache identities.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Compare coverage-only
      and trace+coverage → separate recipe/cache identities.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Compare coverage-only and trace+coverage → separate recipe/cache identities.'
    capture: evidence/coverage-measurement.collect.cache-coverage
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.cache-repeat
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Repeat same valid
    coverage recipe → any reused build still runs fresh simulator processes and owns new native artifacts.
  checks:
  - id: collect.cache-repeat
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Repeat same valid
      coverage recipe → any reused build still runs fresh simulator processes and owns new native artifacts.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Repeat same valid
      coverage recipe → any reused build still runs fresh simulator processes and owns new native artifacts.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Repeat same valid coverage recipe → any reused build still runs fresh simulator
      processes and owns new native artifacts.'
    capture: evidence/coverage-measurement.collect.cache-repeat
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.pre-sim
  phase: exercise
  action: Merge project-data/staging.toml into the resolved Project config, then explicitly collect sim_staged tests
    half and upper; its driver must consume each staged qa-vector.txt. Verify stage order and exact expected.json
    staged-union 8/8 union with per-test 4/8. Restore the config afterward. Pre-sim fixture records run directory/test
    selector and stages a deterministic vector → every coverage test, including Cocotb, consumes its own fresh selected
    input.
  checks:
  - id: collect.pre-sim
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Merge project-data/staging.toml into the resolved Project config, then explicitly collect sim_staged
      tests half and upper; its driver must consume each staged qa-vector.txt. Verify stage order and exact expected.json
      staged-union 8/8 union with per-test 4/8. Restore the config afterward. Pre-sim fixture records run directory/test
      selector and stages a deterministic vector → every coverage test, including Cocotb, consumes its own fresh
      selected input.
    expected: Merge project-data/staging.toml into the resolved Project config, then explicitly collect sim_staged
      tests half and upper; its driver must consume each staged qa-vector.txt. Verify stage order and exact expected.json
      staged-union 8/8 union with per-test 4/8. Restore the config afterward. Pre-sim fixture records run directory/test
      selector and stages a deterministic vector → every coverage test, including Cocotb, consumes its own fresh
      selected input.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Merge project-data/staging.toml into the resolved Project
      config, then explicitly collect sim_staged tests half and upper; its driver must consume each staged qa-vector.txt.
      Verify stage order and exact expected.json staged-union 8/8 union with per-test 4/8. Restore the config afterward.
      Pre-sim fixture records run directory/test selector and stages a deterministic vector → every coverage test,
      including Cocotb, consumes its own fresh selected input.'
    capture: evidence/coverage-measurement.collect.pre-sim
  timeout_minutes: 2
  requires:
  - baseline.native
- id: window.reset-default
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Omit reset control
    and exercise reset-only activity → recorded window includes initialization/reset.
  checks:
  - id: window.reset-default
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Omit reset control
      and exercise reset-only activity → recorded window includes initialization/reset.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Omit reset control
      and exercise reset-only activity → recorded window includes initialization/reset.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Omit reset control and exercise reset-only activity → recorded window includes
      initialization/reset.'
    capture: evidence/coverage-measurement.window.reset-default
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.reset-excluded
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Set Target reset_included
    false and call exactly-once start after reset → window excludes reset with successful per-process hook evidence.
  checks:
  - id: window.reset-excluded
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Set Target reset_included
      false and call exactly-once start after reset → window excludes reset with successful per-process hook evidence.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Set Target reset_included
      false and call exactly-once start after reset → window excludes reset with successful per-process hook evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Set Target reset_included false and call exactly-once start after reset → window
      excludes reset with successful per-process hook evidence.'
    capture: evidence/coverage-measurement.window.reset-excluded
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.custom-write-pass
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case pass: Custom
    main declares write_hook and calls write exactly once on both normal pass and fail terminal paths → each process
    produces valid evidence. Separate IDs `window.custom-write-pass` and `window.custom-write-fail`.'
  checks:
  - id: window.custom-write-pass
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case pass: Custom
      main declares write_hook and calls write exactly once on both normal pass and fail terminal paths → each process
      produces valid evidence. Separate IDs `window.custom-write-pass` and `window.custom-write-fail`.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case pass: Custom
      main declares write_hook and calls write exactly once on both normal pass and fail terminal paths → each process
      produces valid evidence. Separate IDs `window.custom-write-pass` and `window.custom-write-fail`.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case pass: Custom main declares write_hook and calls write exactly once on both
      normal pass and fail terminal paths → each process produces valid evidence. Separate IDs `window.custom-write-pass`
      and `window.custom-write-fail`.'
    capture: evidence/coverage-measurement.window.custom-write-pass
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.custom-write-fail
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case fail: Custom
    main declares write_hook and calls write exactly once on both normal pass and fail terminal paths → each process
    produces valid evidence. Separate IDs `window.custom-write-pass` and `window.custom-write-fail`.'
  checks:
  - id: window.custom-write-fail
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case fail: Custom
      main declares write_hook and calls write exactly once on both normal pass and fail terminal paths → each process
      produces valid evidence. Separate IDs `window.custom-write-pass` and `window.custom-write-fail`.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case fail: Custom
      main declares write_hook and calls write exactly once on both normal pass and fail terminal paths → each process
      produces valid evidence. Separate IDs `window.custom-write-pass` and `window.custom-write-fail`.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case fail: Custom main declares write_hook and calls write exactly once on both
      normal pass and fail terminal paths → each process produces valid evidence. Separate IDs `window.custom-write-pass`
      and `window.custom-write-fail`.'
    capture: evidence/coverage-measurement.window.custom-write-fail
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.invalid-type
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Set reset_included
    to non-boolean → preflight rejection. X; fault.
  checks:
  - id: window.invalid-type
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Set reset_included
      to non-boolean → preflight rejection. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Set reset_included
      to non-boolean → preflight rejection. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Set reset_included to non-boolean → preflight rejection. X; fault.'
    capture: evidence/coverage-measurement.window.invalid-type
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.invalid-type.restore
  phase: exercise
  action: Archive window.invalid-type result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.custom
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: window.invalid-type.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.invalid-type result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.invalid-type result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.invalid-type result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.invalid-type.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.invalid-type
    instruction: Archive window.invalid-type result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.unknown-hook
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declare an unknown
    custom hook → preflight rejection. X; fault.
  checks:
  - id: window.unknown-hook
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declare an unknown
      custom hook → preflight rejection. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declare an unknown
      custom hook → preflight rejection. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Declare an unknown custom hook → preflight rejection. X; fault.'
    capture: evidence/coverage-measurement.window.unknown-hook
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.unknown-hook.restore
  phase: exercise
  action: Archive window.unknown-hook result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.custom
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: window.unknown-hook.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.unknown-hook result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.unknown-hook result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.unknown-hook result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.unknown-hook.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.unknown-hook
    instruction: Archive window.unknown-hook result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.duplicate-declaration
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Duplicate a custom
    hook declaration → preflight rejection. X; fault.
  checks:
  - id: window.duplicate-declaration
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Duplicate a custom
      hook declaration → preflight rejection. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Duplicate a custom
      hook declaration → preflight rejection. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Duplicate a custom hook declaration → preflight rejection. X; fault.'
    capture: evidence/coverage-measurement.window.duplicate-declaration
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.duplicate-declaration.restore
  phase: exercise
  action: Archive window.duplicate-declaration result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use
    real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: window.duplicate-declaration.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.duplicate-declaration result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.duplicate-declaration result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.duplicate-declaration result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.duplicate-declaration.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.duplicate-declaration
    instruction: Archive window.duplicate-declaration result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.missing-write-declaration
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Omit required custom
    write declaration → preflight rejection. X; fault.
  checks:
  - id: window.missing-write-declaration
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Omit required
      custom write declaration → preflight rejection. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Omit required
      custom write declaration → preflight rejection. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Omit required custom write declaration → preflight rejection. X; fault.'
    capture: evidence/coverage-measurement.window.missing-write-declaration
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.missing-write-declaration.restore
  phase: exercise
  action: Archive window.missing-write-declaration result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use
    real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: window.missing-write-declaration.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.missing-write-declaration result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.missing-write-declaration result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.missing-write-declaration result even if
      detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.custom exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.missing-write-declaration.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.missing-write-declaration
    instruction: Archive window.missing-write-declaration result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: window.included-start-declaration
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declare start while
    reset is included → preflight rejection. X; fault.
  checks:
  - id: window.included-start-declaration
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declare start
      while reset is included → preflight rejection. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declare start
      while reset is included → preflight rejection. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Declare start while reset is included → preflight rejection. X; fault.'
    capture: evidence/coverage-measurement.window.included-start-declaration
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.included-start-declaration.restore
  phase: exercise
  action: Archive window.included-start-declaration result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use
    real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: window.included-start-declaration.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.included-start-declaration result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    expected: Archive window.included-start-declaration result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.included-start-declaration result even
      if detection failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes,
      permissions, executable and environment selection. Restore baseline.custom exact inputs and rerun its public
      valid operation in this run with fresh evidence; use real configured Analyst when the failed operation was
      analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.included-start-declaration.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.included-start-declaration
    instruction: Archive window.included-start-declaration result even if detection failed. Reverse only its owned
      injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment
      selection. Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh
      evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate
      approval/audit. Mark any unreversed fault release-failed.
- id: window.noncustom-declaration
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declare custom_main_hooks
    on a noncustom harness → rejection under Target control contract. X; fault.
  checks:
  - id: window.noncustom-declaration
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declare custom_main_hooks
      on a noncustom harness → rejection under Target control contract. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declare custom_main_hooks
      on a noncustom harness → rejection under Target control contract. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Declare custom_main_hooks on a noncustom harness → rejection under Target control
      contract. X; fault.'
    capture: evidence/coverage-measurement.window.noncustom-declaration
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.noncustom-declaration.restore
  phase: exercise
  action: Archive window.noncustom-declaration result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use
    real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: window.noncustom-declaration.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.noncustom-declaration result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.noncustom-declaration result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.noncustom-declaration result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.noncustom-declaration.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.noncustom-declaration
    instruction: Archive window.noncustom-declaration result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.global-override
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Put coverage window
    controls in `[flows.sim]` → rejected rather than overriding Target ownership. X; fault.
  checks:
  - id: window.global-override
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Put coverage window
      controls in `[flows.sim]` → rejected rather than overriding Target ownership. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Put coverage window
      controls in `[flows.sim]` → rejected rather than overriding Target ownership. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Put coverage window controls in `[flows.sim]` → rejected rather than overriding
      Target ownership. X; fault.'
    capture: evidence/coverage-measurement.window.global-override
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.global-override.restore
  phase: exercise
  action: Archive window.global-override result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: window.global-override.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.global-override result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.global-override result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.global-override result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.global-override.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.global-override
    instruction: Archive window.global-override result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.call-override
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Supply a per-call
    reset/window override → rejected, Target unchanged. X; fault.
  checks:
  - id: window.call-override
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Supply a per-call
      reset/window override → rejected, Target unchanged. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Supply a per-call
      reset/window override → rejected, Target unchanged. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Supply a per-call reset/window override → rejected, Target unchanged. X; fault.'
    capture: evidence/coverage-measurement.window.call-override
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.call-override.restore
  phase: exercise
  action: Archive window.call-override result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.custom
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: window.call-override.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.call-override result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.call-override result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.call-override result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.call-override.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.call-override
    instruction: Archive window.call-override result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.missing-start
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Reset-excluded custom
    test omits start → collection invalid. X; fault.
  checks:
  - id: window.missing-start
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Reset-excluded
      custom test omits start → collection invalid. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Reset-excluded
      custom test omits start → collection invalid. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Reset-excluded custom test omits start → collection invalid. X; fault.'
    capture: evidence/coverage-measurement.window.missing-start
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.missing-start.restore
  phase: exercise
  action: Archive window.missing-start result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.custom
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: window.missing-start.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.missing-start result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.missing-start result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.missing-start result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.missing-start.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.missing-start
    instruction: Archive window.missing-start result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.duplicate-start
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke start twice
    → collection invalid. X; fault.
  checks:
  - id: window.duplicate-start
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke start twice
      → collection invalid. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke start twice
      → collection invalid. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Invoke start twice → collection invalid. X; fault.'
    capture: evidence/coverage-measurement.window.duplicate-start
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.duplicate-start.restore
  phase: exercise
  action: Archive window.duplicate-start result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: window.duplicate-start.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.duplicate-start result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.duplicate-start result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.duplicate-start result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.duplicate-start.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.duplicate-start
    instruction: Archive window.duplicate-start result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.missing-write
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Normal terminal path
    omits write → collection invalid. X; fault.
  checks:
  - id: window.missing-write
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Normal terminal
      path omits write → collection invalid. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Normal terminal
      path omits write → collection invalid. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Normal terminal path omits write → collection invalid. X; fault.'
    capture: evidence/coverage-measurement.window.missing-write
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.missing-write.restore
  phase: exercise
  action: Archive window.missing-write result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.custom
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: window.missing-write.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.missing-write result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.missing-write result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.missing-write result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.missing-write.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.missing-write
    instruction: Archive window.missing-write result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.duplicate-write
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke write twice
    → collection invalid. X; fault.
  checks:
  - id: window.duplicate-write
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke write twice
      → collection invalid. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke write twice
      → collection invalid. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Invoke write twice → collection invalid. X; fault.'
    capture: evidence/coverage-measurement.window.duplicate-write
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.duplicate-write.restore
  phase: exercise
  action: Archive window.duplicate-write result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: window.duplicate-write.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.duplicate-write result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.duplicate-write result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.duplicate-write result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.duplicate-write.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.duplicate-write
    instruction: Archive window.duplicate-write result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.out-of-order
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke write before
    required start → collection invalid. X; fault.
  checks:
  - id: window.out-of-order
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke write before
      required start → collection invalid. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Invoke write before
      required start → collection invalid. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Invoke write before required start → collection invalid. X; fault.'
    capture: evidence/coverage-measurement.window.out-of-order
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.out-of-order.restore
  phase: exercise
  action: Archive window.out-of-order result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.custom
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: window.out-of-order.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.out-of-order result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.out-of-order result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.out-of-order result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.out-of-order.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.out-of-order
    instruction: Archive window.out-of-order result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: window.failed-hook
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Make only the owned
    hook output destination unwritable → failed call invalidates collection. X; fault.
  checks:
  - id: window.failed-hook
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Make only the
      owned hook output destination unwritable → failed call invalidates collection. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Make only the
      owned hook output destination unwritable → failed call invalidates collection. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Make only the owned hook output destination unwritable → failed call invalidates
      collection. X; fault.'
    capture: evidence/coverage-measurement.window.failed-hook
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: window.failed-hook.restore
  phase: exercise
  action: Archive window.failed-hook result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.custom
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: window.failed-hook.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive window.failed-hook result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive window.failed-hook result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive window.failed-hook result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.window.failed-hook.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - window.failed-hook
    instruction: Archive window.failed-hook result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: native-fault.missing
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declared test-boundary
    fixture removes its own raw database after real execution → collection error, rc2, retained simulation truth.
    X; fault.
  checks:
  - id: native-fault.missing
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declared test-boundary
      fixture removes its own raw database after real execution → collection error, rc2, retained simulation truth.
      X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Declared test-boundary
      fixture removes its own raw database after real execution → collection error, rc2, retained simulation truth.
      X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Declared test-boundary fixture removes its own raw database after real execution
      → collection error, rc2, retained simulation truth. X; fault.'
    capture: evidence/coverage-measurement.native-fault.missing
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: native-fault.missing.restore
  phase: exercise
  action: Archive native-fault.missing result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.custom
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: native-fault.missing.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive native-fault.missing result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive native-fault.missing result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive native-fault.missing result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.native-fault.missing.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - native-fault.missing
    instruction: Archive native-fault.missing result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: native-fault.stale
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Substitute a previous
    invocation's raw database at the native boundary → stale evidence rejected. X; fault.
  checks:
  - id: native-fault.stale
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Substitute a previous
      invocation's raw database at the native boundary → stale evidence rejected. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Substitute a previous
      invocation's raw database at the native boundary → stale evidence rejected. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Substitute a previous invocation''s raw database at the native boundary → stale
      evidence rejected. X; fault.'
    capture: evidence/coverage-measurement.native-fault.stale
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: native-fault.stale.restore
  phase: exercise
  action: Archive native-fault.stale result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.custom
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: native-fault.stale.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive native-fault.stale result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive native-fault.stale result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive native-fault.stale result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.native-fault.stale.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - native-fault.stale
    instruction: Archive native-fault.stale result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: native-fault.malformed
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Substitute truncated/malformed
    native bytes → collection error, no valid points accepted. X; fault.
  checks:
  - id: native-fault.malformed
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Substitute truncated/malformed
      native bytes → collection error, no valid points accepted. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Substitute truncated/malformed
      native bytes → collection error, no valid points accepted. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Substitute truncated/malformed native bytes → collection error, no valid points
      accepted. X; fault.'
    capture: evidence/coverage-measurement.native-fault.malformed
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: native-fault.malformed.restore
  phase: exercise
  action: Archive native-fault.malformed result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: native-fault.malformed.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive native-fault.malformed result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive native-fault.malformed result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive native-fault.malformed result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.native-fault.malformed.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - native-fault.malformed
    instruction: Archive native-fault.malformed result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: native-fault.incompatible
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Supply unsupported
    native format at query boundary → no normalized points and gated evaluation blocked. X; fault.
  checks:
  - id: native-fault.incompatible
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Supply unsupported
      native format at query boundary → no normalized points and gated evaluation blocked. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Supply unsupported
      native format at query boundary → no normalized points and gated evaluation blocked. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Supply unsupported native format at query boundary → no normalized points and
      gated evaluation blocked. X; fault.'
    capture: evidence/coverage-measurement.native-fault.incompatible
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: native-fault.incompatible.restore
  phase: exercise
  action: Archive native-fault.incompatible result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: native-fault.incompatible.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive native-fault.incompatible result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive native-fault.incompatible result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive native-fault.incompatible result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.native-fault.incompatible.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - native-fault.incompatible
    instruction: Archive native-fault.incompatible result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: native-fault.merge-mismatch
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Modify only the merged
    query result in a forwarding EDA boundary fixture → equivalence check rejects it. X; fault.
  checks:
  - id: native-fault.merge-mismatch
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Modify only the
      merged query result in a forwarding EDA boundary fixture → equivalence check rejects it. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Modify only the
      merged query result in a forwarding EDA boundary fixture → equivalence check rejects it. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Modify only the merged query result in a forwarding EDA boundary fixture → equivalence
      check rejects it. X; fault.'
    capture: evidence/coverage-measurement.native-fault.merge-mismatch
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: native-fault.merge-mismatch.restore
  phase: exercise
  action: Archive native-fault.merge-mismatch result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use
    real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: native-fault.merge-mismatch.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive native-fault.merge-mismatch result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive native-fault.merge-mismatch result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive native-fault.merge-mismatch result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.native-fault.merge-mismatch.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - native-fault.merge-mismatch
    instruction: Archive native-fault.merge-mismatch result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: native-fault.target-continue
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Make first selected
    Target fail collection locally, followed by healthy Target → later Target still attempted and its exact truths
    retained, overall rc2. X; fault.
  checks:
  - id: native-fault.target-continue
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Make first selected
      Target fail collection locally, followed by healthy Target → later Target still attempted and its exact truths
      retained, overall rc2. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Make first selected
      Target fail collection locally, followed by healthy Target → later Target still attempted and its exact truths
      retained, overall rc2. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Make first selected Target fail collection locally, followed by healthy Target
      → later Target still attempted and its exact truths retained, overall rc2. X; fault.'
    capture: evidence/coverage-measurement.native-fault.target-continue
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: native-fault.target-continue.restore
  phase: exercise
  action: Archive native-fault.target-continue result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use
    real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: native-fault.target-continue.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive native-fault.target-continue result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive native-fault.target-continue result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive native-fault.target-continue result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.native-fault.target-continue.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - native-fault.target-continue
    instruction: Archive native-fault.target-continue result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: native-fault.source-drift
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Bounded pre-sim fixture
    changes a declared source after planning → source fingerprint drift rejected. X; fault.
  checks:
  - id: native-fault.source-drift
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Bounded pre-sim
      fixture changes a declared source after planning → source fingerprint drift rejected. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Bounded pre-sim
      fixture changes a declared source after planning → source fingerprint drift rejected. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Bounded pre-sim fixture changes a declared source after planning → source fingerprint
      drift rejected. X; fault.'
    capture: evidence/coverage-measurement.native-fault.source-drift
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: native-fault.source-drift.restore
  phase: exercise
  action: Archive native-fault.source-drift result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: native-fault.source-drift.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive native-fault.source-drift result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive native-fault.source-drift result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive native-fault.source-drift result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.native-fault.source-drift.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - native-fault.source-drift
    instruction: Archive native-fault.source-drift result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: native-fault.target-drift
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Same controlled stimulus
    changes Target recipe after planning → recipe drift rejected. X; fault.
  checks:
  - id: native-fault.target-drift
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Same controlled
      stimulus changes Target recipe after planning → recipe drift rejected. X; fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Same controlled
      stimulus changes Target recipe after planning → recipe drift rejected. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Same controlled stimulus changes Target recipe after planning → recipe drift rejected.
      X; fault.'
    capture: evidence/coverage-measurement.native-fault.target-drift
  timeout_minutes: 2
  requires:
  - baseline.custom
- id: native-fault.target-drift.restore
  phase: exercise
  action: Archive native-fault.target-drift result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: native-fault.target-drift.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive native-fault.target-drift result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive native-fault.target-drift result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive native-fault.target-drift result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.custom exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.native-fault.target-drift.restore
  timeout_minutes: 2
  requires:
  - baseline.custom
  recovery:
    baseline: baseline.custom
    detection:
    - native-fault.target-drift
    instruction: Archive native-fault.target-drift result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.custom exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: collect.pre-sim-cocotb
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Merge project-data/staging.toml
    into the resolved Project config; explicitly collect sim_cocotb_staged tests gap and full. Require one pre-sim
    batch stage log with both test names; each test reads and checks its own fixed vector from qa-cocotb-vectors.json,
    and per-test native databases remain independently attributed. Remove a selected vector for a controlled failed
    staging control, then restore config/input and recollect. Fault.
  checks:
  - id: collect.pre-sim-cocotb
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Merge project-data/staging.toml
      into the resolved Project config; explicitly collect sim_cocotb_staged tests gap and full. Require one pre-sim
      batch stage log with both test names; each test reads and checks its own fixed vector from qa-cocotb-vectors.json,
      and per-test native databases remain independently attributed. Remove a selected vector for a controlled failed
      staging control, then restore config/input and recollect. Fault.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Merge project-data/staging.toml
      into the resolved Project config; explicitly collect sim_cocotb_staged tests gap and full. Require one pre-sim
      batch stage log with both test names; each test reads and checks its own fixed vector from qa-cocotb-vectors.json,
      and per-test native databases remain independently attributed. Remove a selected vector for a controlled failed
      staging control, then restore config/input and recollect. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Merge project-data/staging.toml into the resolved Project config; explicitly collect
      sim_cocotb_staged tests gap and full. Require one pre-sim batch stage log with both test names; each test
      reads and checks its own fixed vector from qa-cocotb-vectors.json, and per-test native databases remain independently
      attributed. Remove a selected vector for a controlled failed staging control, then restore config/input and
      recollect. Fault.'
    capture: evidence/coverage-measurement.collect.pre-sim-cocotb
  timeout_minutes: 2
  requires:
  - baseline.native
- id: collect.pre-sim-cocotb.restore
  phase: exercise
  action: Archive collect.pre-sim-cocotb result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: collect.pre-sim-cocotb.restore
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Archive collect.pre-sim-cocotb result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive collect.pre-sim-cocotb result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive collect.pre-sim-cocotb result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.native exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-measurement.collect.pre-sim-cocotb.restore
  timeout_minutes: 2
  requires:
  - baseline.native
  recovery:
    baseline: baseline.native
    detection:
    - collect.pre-sim-cocotb
    instruction: Archive collect.pre-sim-cocotb result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.native exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: final.regression
  phase: final
  action: With all injections disabled rerun native numeric half and four-property half cases, current accepted
    Ticket suite where applicable; compare fixed hit sets and independent verdicts. Preserve earlier failures.
  checks:
  - id: final.regression
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: With all injections disabled rerun native numeric half and four-property half cases, current accepted
      Ticket suite where applicable; compare fixed hit sets and independent verdicts. Preserve earlier failures.
    expected: With all injections disabled rerun native numeric half and four-property half cases, current accepted
      Ticket suite where applicable; compare fixed hit sets and independent verdicts. Preserve earlier failures.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: With all injections disabled rerun native numeric half
      and four-property half cases, current accepted Ticket suite where applicable; compare fixed hit sets and independent
      verdicts. Preserve earlier failures.'
    capture: evidence/coverage-measurement.final.regression
  timeout_minutes: 15
  requires:
  - setup.ready
- id: final.analysis
  phase: final
  action: Run the real configured Analyst on the exact fresh final.regression Campaign. Verify valid advisory report,
    correct closure and byte-identical source, Criteria and approvals.
  checks:
  - id: final.analysis
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Run the real configured Analyst on the exact fresh final.regression Campaign. Verify valid advisory
      report, correct closure and byte-identical source, Criteria and approvals.
    expected: Run the real configured Analyst on the exact fresh final.regression Campaign. Verify valid advisory
      report, correct closure and byte-identical source, Criteria and approvals.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run the real configured Analyst on the exact fresh final.regression
      Campaign. Verify valid advisory report, correct closure and byte-identical source, Criteria and approvals.'
    capture: evidence/coverage-measurement.final.analysis
  timeout_minutes: 4
  requires:
  - final.regression
- id: final.archive
  phase: final
  action: Verify every selected Check has immutable, hash-indexed evidence outside mutable/pruned state; record
    failures, blocked work and restoration separately.
  checks:
  - id: final.archive
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Verify every selected Check has immutable, hash-indexed evidence outside mutable/pruned state; record
      failures, blocked work and restoration separately.
    expected: Verify every selected Check has immutable, hash-indexed evidence outside mutable/pruned state; record
      failures, blocked work and restoration separately.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Verify every selected Check has immutable, hash-indexed
      evidence outside mutable/pruned state; record failures, blocked work and restoration separately.'
    capture: evidence/coverage-measurement.final.archive
  timeout_minutes: 5
- id: cleanup.processes
  phase: cleanup
  action: Stop and reap owned model clients, MCP servers, simulators and controller process groups; prove no descendants
    or fault handles remain.
  checks:
  - id: cleanup.processes
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Stop and reap owned model clients, MCP servers, simulators and controller process groups; prove no
      descendants or fault handles remain.
    expected: Stop and reap owned model clients, MCP servers, simulators and controller process groups; prove no
      descendants or fault handles remain.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Stop and reap owned model clients, MCP servers, simulators
      and controller process groups; prove no descendants or fault handles remain.'
    capture: evidence/coverage-measurement.cleanup.processes
  timeout_minutes: 5
- id: cleanup.runtime
  phase: cleanup
  action: Release owned Session Runtimes, attachments and Project inventory entries through product routes; preserve
    borrowed entries.
  checks:
  - id: cleanup.runtime
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Release owned Session Runtimes, attachments and Project inventory entries through product routes;
      preserve borrowed entries.
    expected: Release owned Session Runtimes, attachments and Project inventory entries through product routes;
      preserve borrowed entries.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Release owned Session Runtimes, attachments and Project
      inventory entries through product routes; preserve borrowed entries.'
    capture: evidence/coverage-measurement.cleanup.runtime
  timeout_minutes: 5
- id: cleanup.faults
  phase: cleanup
  action: Remove owned temporary executable wrappers, redirection variables, library injection, altered permissions
    and mounts; borrowed executable/credential bytes unchanged.
  checks:
  - id: cleanup.faults
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Remove owned temporary executable wrappers, redirection variables, library injection, altered permissions
      and mounts; borrowed executable/credential bytes unchanged.
    expected: Remove owned temporary executable wrappers, redirection variables, library injection, altered permissions
      and mounts; borrowed executable/credential bytes unchanged.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Remove owned temporary executable wrappers, redirection
      variables, library injection, altered permissions and mounts; borrowed executable/credential bytes unchanged.'
    capture: evidence/coverage-measurement.cleanup.faults
  timeout_minutes: 5
- id: cleanup.git
  phase: cleanup
  action: Release or retain only eligible inert owned Project/Ticket/worktree/branch state with exact identity,
    review owner, bounded location and deletion instructions.
  checks:
  - id: cleanup.git
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Release or retain only eligible inert owned Project/Ticket/worktree/branch state with exact identity,
      review owner, bounded location and deletion instructions.
    expected: Release or retain only eligible inert owned Project/Ticket/worktree/branch state with exact identity,
      review owner, bounded location and deletion instructions.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Release or retain only eligible inert owned Project/Ticket/worktree/branch
      state with exact identity, review owner, bounded location and deletion instructions.'
    capture: evidence/coverage-measurement.cleanup.git
  timeout_minutes: 5
- id: cleanup.storage
  phase: cleanup
  action: Release owned ephemeral volumes and scarce resources; preserve shared images/caches and archive records;
    eligible inert review state has an explicit ledger disposition.
  checks:
  - id: cleanup.storage
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Release owned ephemeral volumes and scarce resources; preserve shared images/caches and archive records;
      eligible inert review state has an explicit ledger disposition.
    expected: Release owned ephemeral volumes and scarce resources; preserve shared images/caches and archive records;
      eligible inert review state has an explicit ledger disposition.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Release owned ephemeral volumes and scarce resources;
      preserve shared images/caches and archive records; eligible inert review state has an explicit ledger disposition.'
    capture: evidence/coverage-measurement.cleanup.storage
  timeout_minutes: 5
- id: cleanup.records
  phase: cleanup
  action: Record every resource disposition and every selected Check result. Required cleanup failure prevents pass;
    unchanged remotes, no external publication. Deadline means incomplete missing work, never inferred success.
  checks:
  - id: cleanup.records
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Record every resource disposition and every selected Check result. Required cleanup failure prevents
      pass; unchanged remotes, no external publication. Deadline means incomplete missing work, never inferred success.
    expected: Record every resource disposition and every selected Check result. Required cleanup failure prevents
      pass; unchanged remotes, no external publication. Deadline means incomplete missing work, never inferred success.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Record every resource disposition and every selected Check
      result. Required cleanup failure prevents pass; unchanged remotes, no external publication. Deadline means
      incomplete missing work, never inferred success.'
    capture: evidence/coverage-measurement.cleanup.records
  timeout_minutes: 5
