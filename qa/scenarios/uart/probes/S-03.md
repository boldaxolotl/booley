# S-03 disposable probes

`isolation-visible`: start independent Specialist → workspace/prompt manifest contains only allowed side. `isolation-hidden`: inspect retained permitted transcript/manifest → opposite RTL/TB side absent. `isolation-complete`: successful completion → source restored. `isolation-failure`: bounded failed Specialist → source restored and scoped workspace cleaned.

Evidence and recovery: Allowed/forbidden file manifests and before/after hashes, sanitized transcripts; no private filesystem access to bypass isolation.

