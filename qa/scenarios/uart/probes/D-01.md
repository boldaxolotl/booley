# D-01 disposable probes

`feedback-kinds`: append each defect/friction/impression/win → retained distinct entries. `feedback-list`: listing → same entries. `feedback-triage`: classify project/Booley/docs/unknown → persisted classifications. `feedback-filed`: mark fixture filed → excluded from next candidate report. `feedback-concurrent`: simultaneous appends → all complete entries retained. `feedback-atomic`: controlled triage/filed rewrite → valid complete log. `feedback-regenerate`: restart/regenerate → current persistent report.

Evidence and recovery: Before/after log/report copies and entry IDs; product Findings Log is separate from suite results/findings.jsonl.
