# H-11 disposable probes

`upgrade-pending`: create Project using immediately preceding published release then install current exact release → pending review identifies endpoints. `upgrade-heal`: documented heal/health/image reconciliation → current declared payload and clean health. `upgrade-ack`: acknowledge exact verified target → no pending change.

Evidence and recovery: Both released package identities, image payloads, health reports and upgrade state before/after. No acknowledgement following unsuccessful repair.
