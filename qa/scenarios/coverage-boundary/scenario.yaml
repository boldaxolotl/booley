format_version: 1
scenario_id: coverage-boundary
title: 'Native coverage: boundary'
source: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
check_sets:
- id: coverage-boundary-core
  checks:
  - setup.artifact
  - setup.ready
  - setup.catalog
  - oracle.positive
  - oracle.negative-hit-count
  - oracle.negative-source-grouping
  - oracle.negative-digest
  - oracle.negative-rational-verdict
  - baseline.native
  - baseline.policy
  - baseline.analysis
  - baseline.waiver
  - baseline.retention
  - baseline.scale
  - query.overview
  - query.metric
  - query.source
  - query.covered
  - query.uncovered
  - query.disposition-eligible
  - query.disposition-waived
  - query.disposition-unscored
  - query.pages
  - query.changed-filter
  - query.exact-reference
  - query.verified-source
  - query.scope
  - query.cross-campaign
  - query.cross-campaign.restore
  - query.bad-cursor
  - query.bad-cursor.restore
  - query.cursor-filter
  - query.cursor-filter.restore
  - query.undelivered
  - query.undelivered.restore
  - query.response-budget
  - query.response-budget.restore
  - query.total-budget
  - query.total-budget.restore
  - candidate.excluded
  - candidate.excluded.restore
  - candidate.unreachable-proof
  - candidate.unreachable-proof.restore
  - candidate.missing-proof
  - candidate.missing-proof.restore
  - candidate.observed-hit
  - candidate.observed-hit.restore
  - candidate.missing-source
  - candidate.missing-source.restore
  - candidate.missing-evidence
  - candidate.missing-evidence.restore
  - candidate.non-rtl
  - candidate.non-rtl.restore
  - candidate.unscored
  - candidate.unscored.restore
  - candidate.waived
  - candidate.waived.restore
  - candidate.duplicate
  - candidate.duplicate.restore
  - candidate.invalid-reason
  - candidate.invalid-reason.restore
  - candidate.unknown
  - candidate.unknown.restore
  - model.malformed-json
  - model.malformed-json.restore
  - model.missing-fields
  - model.missing-fields.restore
  - model.incomplete
  - model.incomplete.restore
  - model.invented-reference
  - model.invented-reference.restore
  - model.context-exhausted
  - model.context-exhausted.restore
  - model.failure-preserves
  - query.invalid-limit
  - query.invalid-limit.restore
  - final.regression
  - final.analysis
  - final.archive
  - cleanup.processes
  - cleanup.runtime
  - cleanup.faults
  - cleanup.git
  - cleanup.storage
  - cleanup.records
configured_scenarios:
- id: coverage-boundary-ubuntu-codex-cli
  required: true
  parameters:
    host_os: Ubuntu 24.04
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Codex
    interactive_mode_client: native CLI
    ticket_mode_backend: Codex
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-boundary-core
  exclusions: []
- id: coverage-boundary-ubuntu-claude-cli
  required: true
  parameters:
    host_os: Ubuntu 24.04
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Claude
    interactive_mode_client: native CLI
    ticket_mode_backend: Claude
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-boundary-core
  exclusions: []
- id: coverage-boundary-windows-codex-cli
  required: true
  parameters:
    host_os: Windows with Docker Desktop WSL2
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Codex
    interactive_mode_client: native CLI
    ticket_mode_backend: Codex
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-boundary-core
  exclusions: []
- id: coverage-boundary-windows-claude-cli
  required: true
  parameters:
    host_os: Windows with Docker Desktop WSL2
    cpu_architecture: x86-64
    native_host: true
    agent_provider: Claude
    interactive_mode_client: native CLI
    ticket_mode_backend: Claude
  pre_run_requirements:
  - Immutable installed product artifact SHA-256, full source commit, matching docs and image digest; no editable/source
    install.
  - Docker/storage and selected provider authentication; no credential bytes in evidence.
  - Pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c, Yosys formal SAT support, gcc and Python in
    issued Linux Session Runtime.
  - Exact Specialist model and external CLI/SDK identities; Codex exact-model metadata cache available when selected.
  - Authority over declared disposable Projects/Tickets/local branches, runtime processes and fault roots; no host-wide
    LD_PRELOAD or binary replacement.
  - Read shared coverage/RUNBOOK.md exact workload, baseline, approval and fault recipes. Numeric/policy thresholds
    cannot be varied.
  check_sets:
  - coverage-boundary-core
  exclusions: []
inputs:
- id: release
  kind: pre-run
  value: Exact immutable booley-rtl package SHA-256, full source commit, matching docs, runtime image digest and
    selected provider/model identity.
  source: https://github.com/boldaxolotl/booley/blob/main/README.md
  verification: Record identities before setup; pre-run selection cannot alter fixture bytes, suites or thresholds.
- id: verilator
  kind: git
  value: ea338be98e1e838d3518809ce8899f85a009963c
  source: https://github.com/verilator/verilator/tree/ea338be98e1e838d3518809ce8899f85a009963c
  verification: Record exact tool source identity and v5.052; 5.050 rejected.
authority:
- Maintainer approved fixture/policy/waiver inputs only; act inside ledgered run-owned Project/runtime and fault
  roots.
- No product/SDK patch or private evaluator import. Simulated external inputs are labeled boundary evidence; live-provider
  Checks require real selected client.
- Preserve pins, thresholds, exact suites and every original failure. No push, issue submission, remote publication
  or permanent repair.
- Restoration never depends on successful detection. Enter unconditional cleanup by minute 450; preserve borrowed
  resources and secrets.
budget:
  deadline_minutes: 480
  cleanup_minutes: 30
  contingency_minutes: 29
  cleanup_start_minutes: 450
  final_regression_start_minutes: 426
phases:
- id: prepare
  title: Artifact, fixture and independent oracle preparation
  minutes: 52
- id: baseline
  title: Fresh named local baselines
  minutes: 75
- id: exercise
  title: Expanded checks and independent restorations
  minutes: 270
- id: final
  title: Fresh regression, Analyst and evidence archive
  minutes: 24
- id: cleanup
  title: Unconditional resource cleanup
  minutes: 30
steps:
- id: setup.artifact
  phase: prepare
  action: Verify exact immutable installed package/source/docs/image identities and pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c.
    Record actual selected provider/SDK/executable identities; no source-checkout imports.
  checks:
  - id: setup.artifact
    capabilities:
    - PRODUCT-ARTIFACT-INSTALLATION
    - EDA-VERILATOR-SIMULATION
    stimulus: Verify exact immutable installed package/source/docs/image identities and pinned Verilator v5.052
      ea338be98e1e838d3518809ce8899f85a009963c. Record actual selected provider/SDK/executable identities; no source-checkout
      imports.
    expected: Verify exact immutable installed package/source/docs/image identities and pinned Verilator v5.052
      ea338be98e1e838d3518809ce8899f85a009963c. Record actual selected provider/SDK/executable identities; no source-checkout
      imports.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Verify exact immutable installed package/source/docs/image
      identities and pinned Verilator v5.052 ea338be98e1e838d3518809ce8899f85a009963c. Record actual selected provider/SDK/executable
      identities; no source-checkout imports.'
    capture: evidence/coverage-boundary.setup.artifact
  timeout_minutes: 10
- id: setup.ready
  phase: prepare
  action: 'Follow shared coverage/RUNBOOK.md setup: fresh run-owned Project/runtime, copy exact hashed fixture,
    initialize via product, install tests into resolved project-data root, compile scoped fault library and run
    evaluator/fault controls. Freeze owned-root/resource ledger. Use the selected native CLI client and configured
    Specialist model. No artifacts from other Scenario Runs.'
  checks:
  - id: setup.ready
    capabilities:
    - PROJECT-INITIALIZATION
    - SESSION-RUNTIME-LIFECYCLE
    stimulus: 'Follow shared coverage/RUNBOOK.md setup: fresh run-owned Project/runtime, copy exact hashed fixture,
      initialize via product, install tests into resolved project-data root, compile scoped fault library and run
      evaluator/fault controls. Freeze owned-root/resource ledger. Use the selected native CLI client and configured
      Specialist model. No artifacts from other Scenario Runs.'
    expected: 'Follow shared coverage/RUNBOOK.md setup: fresh run-owned Project/runtime, copy exact hashed fixture,
      initialize via product, install tests into resolved project-data root, compile scoped fault library and run
      evaluator/fault controls. Freeze owned-root/resource ledger. Use the selected native CLI client and configured
      Specialist model. No artifacts from other Scenario Runs.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Follow shared coverage/RUNBOOK.md setup: fresh run-owned
      Project/runtime, copy exact hashed fixture, initialize via product, install tests into resolved project-data
      root, compile scoped fault library and run evaluator/fault controls. Freeze owned-root/resource ledger. Use
      the selected native CLI client and configured Specialist model. No artifacts from other Scenario Runs.'
    capture: evidence/coverage-boundary.setup.ready
  timeout_minutes: 30
  requires:
  - setup.artifact
  assets:
  - path: coverage/baselines.json
    base: shared
    audience: scenario-operator
    sha256: 8e74bce480e395d5390009106fbf697867faeeb9a2f7118179b66a98635f5c20
  - path: coverage/policies.json
    base: shared
    audience: scenario-operator
    sha256: 82736ce9dc0244a7b833b81bd331699dafc6f11b426703bb7bfcf2da4c319d58
  - path: coverage/expected.json
    base: shared
    audience: scenario-operator
    sha256: f0f5edcf078329bf468fc5418609a644c20952264c6845308ae33725563e7c41
  - path: coverage/RUNBOOK.md
    base: shared
    audience: scenario-operator
    sha256: 216e061687934ee83fcc07166e3ed61b1bebd76ba8fa719792e657b10ec0a84e
  - path: coverage/approval-inputs.json
    base: shared
    audience: scenario-operator
    sha256: 2ed73b45e21afe0104f11bfa533e0f040ac20a55c34bae5ca533145c910306c0
  - path: coverage/project/coverage.core
    base: shared
    audience: scenario-operator
    sha256: 7400301012dc1e66b977488348c94c61c057c0a0ebbdbbce96c5f3dd188366fc
  - path: coverage/evaluator/controls.py
    base: shared
    audience: independent evaluator
    sha256: 8136daced7ac3abe620472d010346fe63b3ddd9662667e377b880b4dea5d9283
  - path: coverage/evaluator/measurements.py
    base: shared
    audience: independent evaluator
    sha256: b9cc311d59850c0acf34cd0364f4a7d1d2d4b0c87492df441473ae9ef3f00dbc
  - path: coverage/proof/parity.ys
    base: shared
    audience: scenario-operator
    sha256: 9efe18a93df8f320fcea6ff24b6dbc47d54a0d6ccb2129bb8a1956b878e8769c
  - path: coverage/tickets/close-test-gap.md
    base: shared
    audience: scenario-operator
    sha256: 3063bda64ae5ca1ff1be01d6226ba2f833edc6200055266de730f5810204012a
  - path: coverage/tickets/tests.toml
    base: shared
    audience: scenario-operator
    sha256: 4fd70c5c36a4710bb1b680c553cf04f2022b49c9617cbded3c3397b67e686c10
  - path: coverage/faults/provider.py
    base: shared
    audience: scenario-operator
    sha256: a6860c48cc2114cecf3eb9a366c444ff9d71440134f15ba3fed718331d5924a0
  - path: coverage/faults/campaign.py
    base: shared
    audience: scenario-operator
    sha256: b66fd938ad572e709869aefa22f623d6bab6b3cad05d07d3101004492844c82f
  - path: coverage/faults/native_tool.py
    base: shared
    audience: scenario-operator
    sha256: 116df5e4ca2c184d27162f1dd1d4a5bb983a06c6107fdb24fb621a2438a26d75
  - path: coverage/faults/filesystem.py
    base: shared
    audience: scenario-operator
    sha256: 365567972d5d412dd1f6a8cf117742fe7d2d0791a5616b67fc989251a2b6da93
  - path: coverage/faults/boundary.c
    base: shared
    audience: scenario-operator
    sha256: d08e19ec246b05e4d2642f20cbfad5915bcfd82d9f5b938d0ef74b6f1fb56f17
  - path: coverage/faults/approvals.py
    base: shared
    audience: scenario-operator
    sha256: 22313486bcad87cf1afbe0cbd2f9b0dd843750fb901605ecd6343d316631918a
  - path: coverage/project/rtl/coverage_dut.sv
    base: shared
    audience: scenario-operator
    sha256: b9290be3023b03b31d4b5ac24ab38f1bb5f72c706ad153bc9d7d9694e2f28f46
  - path: coverage/project/rtl/properties16.sv
    base: shared
    audience: scenario-operator
    sha256: fb25c4c85212715ee3200ba74e6cb2405464e8c80183d2a80b6820f942d6a330
  - path: coverage/project/rtl/waiver_counter.sv
    base: shared
    audience: scenario-operator
    sha256: 841eb1ea3263039e35ac02459ef8be0225f4480a50f9ee092602783e58059d0d
  - path: coverage/project/rtl/properties4.sv
    base: shared
    audience: scenario-operator
    sha256: f5518ced99e8c7902c332241eb33d94f6a2e26cce79a316d55303676512a1f4b
  - path: coverage/project/rtl/constants.svh
    base: shared
    audience: scenario-operator
    sha256: f73cdd3b48a75ef8a23a22648fa1a986eab3b96422c31134562a3941629bba53
  - path: coverage/project/rtl/toggle.sv
    base: shared
    audience: scenario-operator
    sha256: 6f33e57fce5b74ae48c820392e1ed57311bf43bb92768536a6f9f1c23513e326
  - path: coverage/project/rtl/properties3.sv
    base: shared
    audience: scenario-operator
    sha256: ecd0d2ebd423991ca5b12ca9c472198f6c5c299784ca41d74c804e09d2cb485a
  - path: coverage/project/foreign/coverage_library.sv
    base: shared
    audience: scenario-operator
    sha256: cac802acb66125c04478853a0ff0d6b058d66ba1fc87a7e6d48e481d255dda5e
  - path: coverage/project/tb/cocotb_cases.py
    base: shared
    audience: scenario-operator
    sha256: 467fe73fbf8c80cf9ff0f6216fffe9f6ae10d6432e1931005993f658f0c71878
  - path: coverage/project/tb/scale_tb.sv
    base: shared
    audience: scenario-operator
    sha256: 64276850f0d90b26002c1a5321d8f1e6d3ecf9bafcab189109b2f899ddbaa5b0
  - path: coverage/project/tb/coverage_tb.sv
    base: shared
    audience: scenario-operator
    sha256: c5d68c11b8f0b25b3bfc1c29e8508e6746bc5a7d8d41715f1c537e5b407ba6da
  - path: coverage/project/tb/properties3_main.cpp
    base: shared
    audience: scenario-operator
    sha256: 0fe647329b68ce9ca38d55fe9992d32cd291e2b110a8c44a52224a94f2070c33
  - path: coverage/project/tb/waiver_tb.sv
    base: shared
    audience: scenario-operator
    sha256: d872d814256d2f161f412c585689db60e2282a50e70355bcd9a6a3c6ae58b5a9
  - path: coverage/project/tb/properties16_main.cpp
    base: shared
    audience: scenario-operator
    sha256: ccc3f8009c3286a3aca98e9d062598cabdaa38b7ff77c9f5069b79f00c2a1c4a
  - path: coverage/project/tb/properties4_main.cpp
    base: shared
    audience: scenario-operator
    sha256: b9fd74a44739cbe3e8c7b71b6b0894b56db15997976e06e82f74b4c0b56a2301
  - path: coverage/project/tb/cocotb_staged.py
    base: shared
    audience: scenario-operator
    sha256: 107bd339e165b03bec9694ba4c7f3c2c76f87f33429feba5f38be073c1118458
  - path: coverage/project/tb/staged_main.cpp
    base: shared
    audience: scenario-operator
    sha256: 4c5cd55dd55636eaa31ce31cd8e28faa041c4d0052c8df34f9d93c50ab1873fd
  - path: coverage/project/tb/coverage_main.cpp
    base: shared
    audience: scenario-operator
    sha256: 14f312bcf77304cfc021d5231262d105d78dbf678aa51432b9daca53dc73e738
  - path: coverage/project/tb/multi_tb.sv
    base: shared
    audience: scenario-operator
    sha256: ef05488f7510269c783bcaadb4cc3f47f1eb6cebb3f64dea612289dcd5c10284
  - path: coverage/project/tb/toggle_main.cpp
    base: shared
    audience: scenario-operator
    sha256: e1b88b2662c06aa663eea8a7ee9bd010a80cb9b36ce4831d38c944954c1ae8e5
  - path: coverage/project/project-data/staging.toml
    base: shared
    audience: scenario-operator
    sha256: 427ce254316a45b2cc662bde6a2754d98c30ea26b15cc509cb5eae473b290b59
  - path: coverage/project/project-data/tests.toml
    base: shared
    audience: scenario-operator
    sha256: 57a3c33e34158218bd387016c10c894ac08bfe307ddbb159c4f5fb535e565506
  - path: coverage/project/project-data/booley.toml
    base: shared
    audience: scenario-operator
    sha256: fa01bd3d777362378c4b137a7a8f288cb59385ac8fb30a5fb814579363ea4b58
  - path: coverage/project/scripts/stage_vector.py
    base: shared
    audience: scenario-operator
    sha256: f3ff5cdaf807e6096c6f3b717b7af7560a751db3af4566d5c2d6cddaf9a573d5
  resources:
  - owned-project
  - owned-runtime
  - owned-fault-controller
  - owned-artifact-root
- id: setup.catalog
  phase: prepare
  action: 'Read actual selected CLI help and MCP schemas: --coverage and permanent --cov, default-false boolean,
    exact required Analyst Campaign path, optional instruction; no legacy scorer options.'
  checks:
  - id: setup.catalog
    capabilities:
    - PUBLIC-CAPABILITY-DISCOVERY
    stimulus: 'Read actual selected CLI help and MCP schemas: --coverage and permanent --cov, default-false boolean,
      exact required Analyst Campaign path, optional instruction; no legacy scorer options.'
    expected: 'Read actual selected CLI help and MCP schemas: --coverage and permanent --cov, default-false boolean,
      exact required Analyst Campaign path, optional instruction; no legacy scorer options.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Read actual selected CLI help and MCP schemas: --coverage
      and permanent --cov, default-false boolean, exact required Analyst Campaign path, optional instruction; no
      legacy scorer options.'
    capture: evidence/coverage-boundary.setup.catalog
  timeout_minutes: 2
  requires:
  - setup.artifact
- id: oracle.positive
  phase: prepare
  action: Run python3 evaluator/controls.py positive from the hashed shared fixture. Positive control must accept
    literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control input.
    These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.positive
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py positive from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py positive from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py positive from the hashed
      shared fixture. Positive control must accept literal 4/8; each negative must reject its independently altered
      field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-boundary.oracle.positive
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-hit-count
  phase: prepare
  action: Run python3 evaluator/controls.py hit-count from the hashed shared fixture. Positive control must accept
    literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control input.
    These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-hit-count
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py hit-count from the hashed shared fixture. Positive control must
      accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py hit-count from the hashed shared fixture. Positive control must
      accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py hit-count from the hashed
      shared fixture. Positive control must accept literal 4/8; each negative must reject its independently altered
      field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-boundary.oracle.negative-hit-count
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-source-grouping
  phase: prepare
  action: Run python3 evaluator/controls.py source-grouping from the hashed shared fixture. Positive control must
    accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
    input. These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-source-grouping
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py source-grouping from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py source-grouping from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py source-grouping from
      the hashed shared fixture. Positive control must accept literal 4/8; each negative must reject its independently
      altered field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-boundary.oracle.negative-source-grouping
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-digest
  phase: prepare
  action: Run python3 evaluator/controls.py digest from the hashed shared fixture. Positive control must accept
    literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control input.
    These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-digest
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py digest from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py digest from the hashed shared fixture. Positive control must accept
      literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
      input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py digest from the hashed
      shared fixture. Positive control must accept literal 4/8; each negative must reject its independently altered
      field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-boundary.oracle.negative-digest
  timeout_minutes: 2
  requires:
  - setup.ready
- id: oracle.negative-rational-verdict
  phase: prepare
  action: Run python3 evaluator/controls.py rational-verdict from the hashed shared fixture. Positive control must
    accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit and control
    input. These are evaluator self-tests, not product evidence.
  checks:
  - id: oracle.negative-rational-verdict
    capabilities:
    - COVERAGE-CAMPAIGN-STORAGE
    stimulus: Run python3 evaluator/controls.py rational-verdict from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    expected: Run python3 evaluator/controls.py rational-verdict from the hashed shared fixture. Positive control
      must accept literal 4/8; each negative must reject its independently altered field. Preserve stdout, exit
      and control input. These are evaluator self-tests, not product evidence.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run python3 evaluator/controls.py rational-verdict from
      the hashed shared fixture. Positive control must accept literal 4/8; each negative must reject its independently
      altered field. Preserve stdout, exit and control input. These are evaluator self-tests, not product evidence.'
    capture: evidence/coverage-boundary.oracle.negative-rational-verdict
  timeout_minutes: 2
  requires:
  - setup.ready
- id: baseline.native
  phase: baseline
  action: Produce fresh baseline.native according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.native,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.native
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.native according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.native,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.native according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.native,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.native according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.native, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-boundary.baseline.native
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.policy
  phase: baseline
  action: Produce fresh baseline.policy according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.policy,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.policy
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.policy according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.policy,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.policy according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.policy,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.policy according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.policy, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-boundary.baseline.policy
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.analysis
  phase: baseline
  action: Produce fresh baseline.analysis according to the exact baseline table in coverage/RUNBOOK.md. Capture
    immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.analysis,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.analysis
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.analysis according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.analysis,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.analysis according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.analysis,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.analysis according to the exact
      baseline table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket
      state outside every fault/prune root. For baseline.analysis, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-boundary.baseline.analysis
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.waiver
  phase: baseline
  action: Produce fresh baseline.waiver according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.waiver,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.waiver
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.waiver according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.waiver,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.waiver according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.waiver,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.waiver according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.waiver, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-boundary.baseline.waiver
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.retention
  phase: baseline
  action: Produce fresh baseline.retention according to the exact baseline table in coverage/RUNBOOK.md. Capture
    immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.retention,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.retention
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.retention according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.retention,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.retention according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.retention,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.retention according to the exact
      baseline table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket
      state outside every fault/prune root. For baseline.retention, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-boundary.baseline.retention
  timeout_minutes: 12
  requires:
  - setup.ready
- id: baseline.scale
  phase: baseline
  action: Produce fresh baseline.scale according to the exact baseline table in coverage/RUNBOOK.md. Capture immutable
    source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.scale,
    no earlier Scenario artifact can substitute.
  checks:
  - id: baseline.scale
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Produce fresh baseline.scale according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.scale,
      no earlier Scenario artifact can substitute.
    expected: Produce fresh baseline.scale according to the exact baseline table in coverage/RUNBOOK.md. Capture
      immutable source/config/native/Campaign/Simulation and Ticket state outside every fault/prune root. For baseline.scale,
      no earlier Scenario artifact can substitute.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Produce fresh baseline.scale according to the exact baseline
      table in coverage/RUNBOOK.md. Capture immutable source/config/native/Campaign/Simulation and Ticket state
      outside every fault/prune root. For baseline.scale, no earlier Scenario artifact can substitute.'
    capture: evidence/coverage-boundary.baseline.scale
  timeout_minutes: 15
  requires:
  - setup.ready
- id: query.overview
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for overview of
    the large Campaign → bounded counts/limitations match retained evidence, no full point population in prompt.
  checks:
  - id: query.overview
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for overview
      of the large Campaign → bounded counts/limitations match retained evidence, no full point population in prompt.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for overview
      of the large Campaign → bounded counts/limitations match retained evidence, no full point population in prompt.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Ask for overview of the large Campaign → bounded counts/limitations match retained
      evidence, no full point population in prompt.'
    capture: evidence/coverage-boundary.query.overview
  timeout_minutes: 15
  requires:
  - baseline.scale
- id: query.metric
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for branch-only
    points → each returned point matches the filter and exact retained identity.
  checks:
  - id: query.metric
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for branch-only
      points → each returned point matches the filter and exact retained identity.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for branch-only
      points → each returned point matches the filter and exact retained identity.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Ask for branch-only points → each returned point matches the filter and exact
      retained identity.'
    capture: evidence/coverage-boundary.query.metric
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.source
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for points from
    one declared source → no foreign source records.
  checks:
  - id: query.source
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for points
      from one declared source → no foreign source records.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for points
      from one declared source → no foreign source records.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Ask for points from one declared source → no foreign source records.'
    capture: evidence/coverage-boundary.query.source
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.covered
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case covered: Separately
    request covered and uncovered points → both sets agree with hit incidence.'
  checks:
  - id: query.covered
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case covered:
      Separately request covered and uncovered points → both sets agree with hit incidence.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case covered:
      Separately request covered and uncovered points → both sets agree with hit incidence.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case covered: Separately request covered and uncovered points → both sets agree
      with hit incidence.'
    capture: evidence/coverage-boundary.query.covered
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.uncovered
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case uncovered: Separately
    request covered and uncovered points → both sets agree with hit incidence.'
  checks:
  - id: query.uncovered
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case uncovered:
      Separately request covered and uncovered points → both sets agree with hit incidence.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case uncovered:
      Separately request covered and uncovered points → both sets agree with hit incidence.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case uncovered: Separately request covered and uncovered points → both sets agree
      with hit incidence.'
    capture: evidence/coverage-boundary.query.uncovered
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.disposition-eligible
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case eligible: Request
    eligible, waived, unscored separately → exact disposition; waived records retain complete approval/proof provenance.'
  checks:
  - id: query.disposition-eligible
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case eligible:
      Request eligible, waived, unscored separately → exact disposition; waived records retain complete approval/proof
      provenance.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case eligible:
      Request eligible, waived, unscored separately → exact disposition; waived records retain complete approval/proof
      provenance.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case eligible: Request eligible, waived, unscored separately → exact disposition;
      waived records retain complete approval/proof provenance.'
    capture: evidence/coverage-boundary.query.disposition-eligible
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.disposition-waived
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case waived: Request
    eligible, waived, unscored separately → exact disposition; waived records retain complete approval/proof provenance.'
  checks:
  - id: query.disposition-waived
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case waived:
      Request eligible, waived, unscored separately → exact disposition; waived records retain complete approval/proof
      provenance.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case waived:
      Request eligible, waived, unscored separately → exact disposition; waived records retain complete approval/proof
      provenance.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case waived: Request eligible, waived, unscored separately → exact disposition;
      waived records retain complete approval/proof provenance.'
    capture: evidence/coverage-boundary.query.disposition-waived
  timeout_minutes: 4
  requires:
  - baseline.waiver
- id: query.disposition-unscored
  phase: exercise
  action: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case unscored: Request
    eligible, waived, unscored separately → exact disposition; waived records retain complete approval/proof provenance.'
  checks:
  - id: query.disposition-unscored
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case unscored:
      Request eligible, waived, unscored separately → exact disposition; waived records retain complete approval/proof
      provenance.'
    expected: 'Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Case unscored:
      Request eligible, waived, unscored separately → exact disposition; waived records retain complete approval/proof
      provenance.'
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Case unscored: Request eligible, waived, unscored separately → exact disposition;
      waived records retain complete approval/proof provenance.'
    capture: evidence/coverage-boundary.query.disposition-unscored
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.pages
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for two successive
    small pages and the remaining bounded result set → deterministic order, valid continuation, no missing/duplicated
    points.
  checks:
  - id: query.pages
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for two successive
      small pages and the remaining bounded result set → deterministic order, valid continuation, no missing/duplicated
      points.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask for two successive
      small pages and the remaining bounded result set → deterministic order, valid continuation, no missing/duplicated
      points.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Ask for two successive small pages and the remaining bounded result set → deterministic
      order, valid continuation, no missing/duplicated points.'
    capture: evidence/coverage-boundary.query.pages
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.changed-filter
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Complete first filtered
    query, then query different source/metric → no stale population from prior filter.
  checks:
  - id: query.changed-filter
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Complete first
      filtered query, then query different source/metric → no stale population from prior filter.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Complete first
      filtered query, then query different source/metric → no stale population from prior filter.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Complete first filtered query, then query different source/metric → no stale population
      from prior filter.'
    capture: evidence/coverage-boundary.query.changed-filter
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.exact-reference
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Re-query previously
    delivered short reference → exact same Campaign point; published report resolves it back to full exact ID.
  checks:
  - id: query.exact-reference
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Re-query previously
      delivered short reference → exact same Campaign point; published report resolves it back to full exact ID.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Re-query previously
      delivered short reference → exact same Campaign point; published report resolves it back to full exact ID.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Re-query previously delivered short reference → exact same Campaign point; published
      report resolves it back to full exact ID.'
    capture: evidence/coverage-boundary.query.exact-reference
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.verified-source
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Request source excerpts
    for delivered references → bounded correct file/line context from verified immutable closure.
  checks:
  - id: query.verified-source
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Request source
      excerpts for delivered references → bounded correct file/line context from verified immutable closure.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Request source
      excerpts for delivered references → bounded correct file/line context from verified immutable closure.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Request source excerpts for delivered references → bounded correct file/line context
      from verified immutable closure.'
    capture: evidence/coverage-boundary.query.verified-source
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.scope
  phase: exercise
  action: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask to inspect only
    a small slice of large Campaign → audit identifies exact delivered IDs, unique retrieved count, byte use and
    partial scope; no all-points claim.
  checks:
  - id: query.scope
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask to inspect
      only a small slice of large Campaign → audit identifies exact delivered IDs, unique retrieved count, byte
      use and partial scope; no all-points claim.
    expected: Use the fixed Target/test/policy and boundary recipe in shared coverage/RUNBOOK.md. Ask to inspect
      only a small slice of large Campaign → audit identifies exact delivered IDs, unique retrieved count, byte
      use and partial scope; no all-points claim.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Use the fixed Target/test/policy and boundary recipe in
      shared coverage/RUNBOOK.md. Ask to inspect only a small slice of large Campaign → audit identifies exact delivered
      IDs, unique retrieved count, byte use and partial scope; no all-points claim.'
    capture: evidence/coverage-boundary.query.scope
  timeout_minutes: 15
  requires:
  - baseline.scale
- id: query.cross-campaign
  phase: exercise
  action: Select provider boundary fixture case cross-campaign, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Set QA_FOREIGN_CAMPAIGN to the actual canonical baseline.native Campaign while this call is bound to baseline.analysis.
    Record the rejected extra campaign selector and unchanged bound Campaign. In a fresh Analyst session, request
    another Campaign's path/undelivered point → no cross-Campaign evidence access. X; fault.
  checks:
  - id: query.cross-campaign
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case cross-campaign, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Set QA_FOREIGN_CAMPAIGN to the actual canonical baseline.native Campaign while this call is bound to baseline.analysis.
      Record the rejected extra campaign selector and unchanged bound Campaign. In a fresh Analyst session, request
      another Campaign's path/undelivered point → no cross-Campaign evidence access. X; fault.
    expected: Select provider boundary fixture case cross-campaign, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Set QA_FOREIGN_CAMPAIGN to the actual canonical baseline.native Campaign while this call is bound to baseline.analysis.
      Record the rejected extra campaign selector and unchanged bound Campaign. In a fresh Analyst session, request
      another Campaign's path/undelivered point → no cross-Campaign evidence access. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case cross-campaign,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. Set QA_FOREIGN_CAMPAIGN to the actual canonical baseline.native
      Campaign while this call is bound to baseline.analysis. Record the rejected extra campaign selector and unchanged
      bound Campaign. In a fresh Analyst session, request another Campaign''s path/undelivered point → no cross-Campaign
      evidence access. X; fault.'
    capture: evidence/coverage-boundary.query.cross-campaign
  timeout_minutes: 4
  requires:
  - baseline.analysis
  - baseline.native
- id: query.cross-campaign.restore
  phase: exercise
  action: Archive query.cross-campaign result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: query.cross-campaign.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive query.cross-campaign result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive query.cross-campaign result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive query.cross-campaign result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.query.cross-campaign.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - query.cross-campaign
    instruction: Archive query.cross-campaign result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: query.bad-cursor
  phase: exercise
  action: Select provider boundary fixture case bad-cursor, using the exact executable selection and real MCP initialization
    recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Request malformed
    cursor through isolated evidence boundary fixture → explicit query error, no fabricated page. X; fault.
  checks:
  - id: query.bad-cursor
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case bad-cursor, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request malformed cursor through isolated evidence boundary fixture → explicit query error, no fabricated
      page. X; fault.
    expected: Select provider boundary fixture case bad-cursor, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request malformed cursor through isolated evidence boundary fixture → explicit query error, no fabricated
      page. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case bad-cursor, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Request malformed cursor through isolated evidence boundary fixture
      → explicit query error, no fabricated page. X; fault.'
    capture: evidence/coverage-boundary.query.bad-cursor
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.bad-cursor.restore
  phase: exercise
  action: Archive query.bad-cursor result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: query.bad-cursor.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive query.bad-cursor result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    expected: Archive query.bad-cursor result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive query.bad-cursor result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.query.bad-cursor.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - query.bad-cursor
    instruction: Archive query.bad-cursor result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: query.cursor-filter
  phase: exercise
  action: Select provider boundary fixture case cursor-filter, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Reuse valid cursor with changed filters → rejected rather than serving mismatched continuation. X; fault.
  checks:
  - id: query.cursor-filter
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case cursor-filter, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Reuse valid cursor with changed filters → rejected rather than serving mismatched continuation. X; fault.
    expected: Select provider boundary fixture case cursor-filter, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Reuse valid cursor with changed filters → rejected rather than serving mismatched continuation. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case cursor-filter, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Reuse valid cursor with changed filters → rejected rather than serving
      mismatched continuation. X; fault.'
    capture: evidence/coverage-boundary.query.cursor-filter
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.cursor-filter.restore
  phase: exercise
  action: Archive query.cursor-filter result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: query.cursor-filter.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive query.cursor-filter result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive query.cursor-filter result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive query.cursor-filter result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.query.cursor-filter.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - query.cursor-filter
    instruction: Archive query.cursor-filter result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: query.undelivered
  phase: exercise
  action: Select provider boundary fixture case undelivered, using the exact executable selection and real MCP initialization
    recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Request source/exact
    lookup using an undelivered short reference → rejected, no new authorization from guessed handle. X; fault.
  checks:
  - id: query.undelivered
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case undelivered, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request source/exact lookup using an undelivered short reference → rejected, no new authorization from guessed
      handle. X; fault.
    expected: Select provider boundary fixture case undelivered, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request source/exact lookup using an undelivered short reference → rejected, no new authorization from guessed
      handle. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case undelivered, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Request source/exact lookup using an undelivered short reference
      → rejected, no new authorization from guessed handle. X; fault.'
    capture: evidence/coverage-boundary.query.undelivered
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.undelivered.restore
  phase: exercise
  action: Archive query.undelivered result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: query.undelivered.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive query.undelivered result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive query.undelivered result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive query.undelivered result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.query.undelivered.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - query.undelivered
    instruction: Archive query.undelivered result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: query.response-budget
  phase: exercise
  action: Select provider boundary fixture case response-budget, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Use baseline.scale with legal limit=100; compare compact UTF-8 JSON (ensure_ascii=False, separators comma/colon)
    response to the 65536-byte limit and audit delivered records. A schema range error cannot pass this Check. Request
    oversized page/source context using fixed large fixture → response remains within declared boundary or returns
    actionable boundary error, never leaks an oversized payload; audit only credits delivered records.
  checks:
  - id: query.response-budget
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case response-budget, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Use baseline.scale with legal limit=100; compare compact UTF-8 JSON (ensure_ascii=False, separators comma/colon)
      response to the 65536-byte limit and audit delivered records. A schema range error cannot pass this Check.
      Request oversized page/source context using fixed large fixture → response remains within declared boundary
      or returns actionable boundary error, never leaks an oversized payload; audit only credits delivered records.
    expected: Select provider boundary fixture case response-budget, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Use baseline.scale with legal limit=100; compare compact UTF-8 JSON (ensure_ascii=False, separators comma/colon)
      response to the 65536-byte limit and audit delivered records. A schema range error cannot pass this Check.
      Request oversized page/source context using fixed large fixture → response remains within declared boundary
      or returns actionable boundary error, never leaks an oversized payload; audit only credits delivered records.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case response-budget,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. Use baseline.scale with legal limit=100; compare compact
      UTF-8 JSON (ensure_ascii=False, separators comma/colon) response to the 65536-byte limit and audit delivered
      records. A schema range error cannot pass this Check. Request oversized page/source context using fixed large
      fixture → response remains within declared boundary or returns actionable boundary error, never leaks an oversized
      payload; audit only credits delivered records.'
    capture: evidence/coverage-boundary.query.response-budget
  timeout_minutes: 4
  requires:
  - baseline.scale
- id: query.response-budget.restore
  phase: exercise
  action: Archive query.response-budget result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.scale exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured
    Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any
    unreversed fault release-failed.
  checks:
  - id: query.response-budget.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive query.response-budget result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.scale exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive query.response-budget result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.scale exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive query.response-budget result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.scale exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.query.response-budget.restore
  timeout_minutes: 4
  requires:
  - baseline.scale
  recovery:
    baseline: baseline.scale
    detection:
    - query.response-budget
    instruction: Archive query.response-budget result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.scale exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: query.total-budget
  phase: exercise
  action: Select provider boundary fixture case total-budget, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Literal instruction requests repeated valid pages until cumulative evidence limit → explicit exhausted-boundary
    failure, no empty-success report or credit for undelivered final page. X; fault.
  checks:
  - id: query.total-budget
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case total-budget, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Literal instruction requests repeated valid pages until cumulative evidence limit → explicit exhausted-boundary
      failure, no empty-success report or credit for undelivered final page. X; fault.
    expected: Select provider boundary fixture case total-budget, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Literal instruction requests repeated valid pages until cumulative evidence limit → explicit exhausted-boundary
      failure, no empty-success report or credit for undelivered final page. X; fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case total-budget, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Literal instruction requests repeated valid pages until cumulative
      evidence limit → explicit exhausted-boundary failure, no empty-success report or credit for undelivered final
      page. X; fault.'
    capture: evidence/coverage-boundary.query.total-budget
  timeout_minutes: 4
  requires:
  - baseline.scale
- id: query.total-budget.restore
  phase: exercise
  action: Archive query.total-budget result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.scale
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: query.total-budget.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive query.total-budget result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.scale exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive query.total-budget result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.scale exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive query.total-budget result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.scale exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.query.total-budget.restore
  timeout_minutes: 4
  requires:
  - baseline.scale
  recovery:
    baseline: baseline.scale
    detection:
    - query.total-budget
    instruction: Archive query.total-budget result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.scale exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.excluded
  phase: exercise
  action: Select provider boundary fixture case excluded, using the exact executable selection and real MCP initialization
    recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Request candidate
    for delivered, verified eligible RTL point with reason excluded and concrete evidence → `not_approved`, ready_for_human_review
    only.
  checks:
  - id: candidate.excluded
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case excluded, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request candidate for delivered, verified eligible RTL point with reason excluded and concrete evidence →
      `not_approved`, ready_for_human_review only.
    expected: Select provider boundary fixture case excluded, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request candidate for delivered, verified eligible RTL point with reason excluded and concrete evidence →
      `not_approved`, ready_for_human_review only.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case excluded, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Request candidate for delivered, verified eligible RTL point with
      reason excluded and concrete evidence → `not_approved`, ready_for_human_review only.'
    capture: evidence/coverage-boundary.candidate.excluded
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.excluded.restore
  phase: exercise
  action: Archive candidate.excluded result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: candidate.excluded.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.excluded result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.excluded result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.excluded result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.excluded.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.excluded
    instruction: Archive candidate.excluded result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.unreachable-proof
  phase: exercise
  action: Select provider boundary fixture case unreachable-proof, using the exact executable selection and real
    MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Request unreachable candidate for unhit delivered RTL point with predeclared proof reference → at most ready_for_human_review
    and always not_approved.
  checks:
  - id: candidate.unreachable-proof
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case unreachable-proof, using the exact executable selection and
      real MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses
      and audit. Request unreachable candidate for unhit delivered RTL point with predeclared proof reference →
      at most ready_for_human_review and always not_approved.
    expected: Select provider boundary fixture case unreachable-proof, using the exact executable selection and
      real MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses
      and audit. Request unreachable candidate for unhit delivered RTL point with predeclared proof reference →
      at most ready_for_human_review and always not_approved.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case unreachable-proof,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. Request unreachable candidate for unhit delivered RTL point
      with predeclared proof reference → at most ready_for_human_review and always not_approved.'
    capture: evidence/coverage-boundary.candidate.unreachable-proof
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.unreachable-proof.restore
  phase: exercise
  action: Archive candidate.unreachable-proof result even if detection failed. Reverse only its owned injection
    using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
    Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
    use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: candidate.unreachable-proof.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.unreachable-proof result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.unreachable-proof result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.unreachable-proof result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.unreachable-proof.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.unreachable-proof
    instruction: Archive candidate.unreachable-proof result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.missing-proof
  phase: exercise
  action: Select provider boundary fixture case missing-proof, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Request unreachable candidate without proof reference → investigate.
  checks:
  - id: candidate.missing-proof
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case missing-proof, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request unreachable candidate without proof reference → investigate.
    expected: Select provider boundary fixture case missing-proof, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request unreachable candidate without proof reference → investigate.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case missing-proof, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Request unreachable candidate without proof reference → investigate.'
    capture: evidence/coverage-boundary.candidate.missing-proof
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.missing-proof.restore
  phase: exercise
  action: Archive candidate.missing-proof result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: candidate.missing-proof.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.missing-proof result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.missing-proof result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.missing-proof result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.missing-proof.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.missing-proof
    instruction: Archive candidate.missing-proof result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.observed-hit
  phase: exercise
  action: Select provider boundary fixture case observed-hit, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Request unreachable candidate contradicted by retained hits → investigate, no invented unreachability.
  checks:
  - id: candidate.observed-hit
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case observed-hit, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request unreachable candidate contradicted by retained hits → investigate, no invented unreachability.
    expected: Select provider boundary fixture case observed-hit, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Request unreachable candidate contradicted by retained hits → investigate, no invented unreachability.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case observed-hit, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Request unreachable candidate contradicted by retained hits → investigate,
      no invented unreachability.'
    capture: evidence/coverage-boundary.candidate.observed-hit
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.observed-hit.restore
  phase: exercise
  action: Archive candidate.observed-hit result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: candidate.observed-hit.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.observed-hit result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.observed-hit result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.observed-hit result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.observed-hit.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.observed-hit
    instruction: Archive candidate.observed-hit result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.missing-source
  phase: exercise
  action: Select provider boundary fixture case missing-source, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Same otherwise valid candidate in report-only analysis → investigate.
  checks:
  - id: candidate.missing-source
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case missing-source, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Same otherwise valid candidate in report-only analysis → investigate.
    expected: Select provider boundary fixture case missing-source, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Same otherwise valid candidate in report-only analysis → investigate.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case missing-source,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. Same otherwise valid candidate in report-only analysis →
      investigate.'
    capture: evidence/coverage-boundary.candidate.missing-source
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.missing-source.restore
  phase: exercise
  action: Archive candidate.missing-source result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: candidate.missing-source.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.missing-source result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.missing-source result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.missing-source result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.missing-source.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.missing-source
    instruction: Archive candidate.missing-source result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.missing-evidence
  phase: exercise
  action: Select provider boundary fixture case missing-evidence, using the exact executable selection and real
    MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Otherwise valid candidate without evidence → investigate.
  checks:
  - id: candidate.missing-evidence
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case missing-evidence, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Otherwise valid candidate without evidence → investigate.
    expected: Select provider boundary fixture case missing-evidence, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Otherwise valid candidate without evidence → investigate.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case missing-evidence,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. Otherwise valid candidate without evidence → investigate.'
    capture: evidence/coverage-boundary.candidate.missing-evidence
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.missing-evidence.restore
  phase: exercise
  action: Archive candidate.missing-evidence result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: candidate.missing-evidence.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.missing-evidence result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.missing-evidence result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.missing-evidence result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.missing-evidence.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.missing-evidence
    instruction: Archive candidate.missing-evidence result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.non-rtl
  phase: exercise
  action: Select provider boundary fixture case non-rtl, using the exact executable selection and real MCP initialization
    recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate points
    to delivered TB/foreign record → forbidden.
  checks:
  - id: candidate.non-rtl
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case non-rtl, using the exact executable selection and real MCP initialization
      recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate points
      to delivered TB/foreign record → forbidden.
    expected: Select provider boundary fixture case non-rtl, using the exact executable selection and real MCP initialization
      recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate points
      to delivered TB/foreign record → forbidden.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case non-rtl, using the
      exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Candidate points to delivered TB/foreign record → forbidden.'
    capture: evidence/coverage-boundary.candidate.non-rtl
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.non-rtl.restore
  phase: exercise
  action: Archive candidate.non-rtl result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: candidate.non-rtl.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.non-rtl result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.non-rtl result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.non-rtl result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.non-rtl.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.non-rtl
    instruction: Archive candidate.non-rtl result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.unscored
  phase: exercise
  action: Select provider boundary fixture case unscored, using the exact executable selection and real MCP initialization
    recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate points
    to delivered unscored record → forbidden.
  checks:
  - id: candidate.unscored
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case unscored, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Candidate points to delivered unscored record → forbidden.
    expected: Select provider boundary fixture case unscored, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Candidate points to delivered unscored record → forbidden.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case unscored, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Candidate points to delivered unscored record → forbidden.'
    capture: evidence/coverage-boundary.candidate.unscored
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.unscored.restore
  phase: exercise
  action: Archive candidate.unscored result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: candidate.unscored.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.unscored result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.unscored result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.unscored result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.unscored.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.unscored
    instruction: Archive candidate.unscored result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.waived
  phase: exercise
  action: Select provider boundary fixture case waived, using the exact executable selection and real MCP initialization
    recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate points
    to already-waived record → forbidden, no duplicate approval.
  checks:
  - id: candidate.waived
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case waived, using the exact executable selection and real MCP initialization
      recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate points
      to already-waived record → forbidden, no duplicate approval.
    expected: Select provider boundary fixture case waived, using the exact executable selection and real MCP initialization
      recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate points
      to already-waived record → forbidden, no duplicate approval.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case waived, using the
      exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Candidate points to already-waived record → forbidden, no duplicate
      approval.'
    capture: evidence/coverage-boundary.candidate.waived
  timeout_minutes: 4
  requires:
  - baseline.waiver
- id: candidate.waived.restore
  phase: exercise
  action: Archive candidate.waived result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.waiver
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: candidate.waived.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.waived result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.waiver
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    expected: Archive candidate.waived result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.waiver
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.waived result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.waiver exact inputs and rerun its public valid operation in this
      run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower thresholds
      or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.waived.restore
  timeout_minutes: 4
  requires:
  - baseline.waiver
  recovery:
    baseline: baseline.waiver
    detection:
    - candidate.waived
    instruction: Archive candidate.waived result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.waiver exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.duplicate
  phase: exercise
  action: Select provider boundary fixture case duplicate, using the exact executable selection and real MCP initialization
    recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Two candidates
    for same exact point → duplicate forbidden.
  checks:
  - id: candidate.duplicate
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case duplicate, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Two candidates for same exact point → duplicate forbidden.
    expected: Select provider boundary fixture case duplicate, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Two candidates for same exact point → duplicate forbidden.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case duplicate, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Two candidates for same exact point → duplicate forbidden.'
    capture: evidence/coverage-boundary.candidate.duplicate
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.duplicate.restore
  phase: exercise
  action: Archive candidate.duplicate result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: candidate.duplicate.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.duplicate result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.duplicate result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.duplicate result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.duplicate.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.duplicate
    instruction: Archive candidate.duplicate result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.invalid-reason
  phase: exercise
  action: Select provider boundary fixture case invalid-reason, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Candidate reason outside excluded/unreachable → forbidden.
  checks:
  - id: candidate.invalid-reason
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case invalid-reason, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Candidate reason outside excluded/unreachable → forbidden.
    expected: Select provider boundary fixture case invalid-reason, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Candidate reason outside excluded/unreachable → forbidden.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case invalid-reason,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. Candidate reason outside excluded/unreachable → forbidden.'
    capture: evidence/coverage-boundary.candidate.invalid-reason
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.invalid-reason.restore
  phase: exercise
  action: Archive candidate.invalid-reason result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: candidate.invalid-reason.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.invalid-reason result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.invalid-reason result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.invalid-reason result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.invalid-reason.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.invalid-reason
    instruction: Archive candidate.invalid-reason result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: candidate.unknown
  phase: exercise
  action: Select provider boundary fixture case unknown, using the exact executable selection and real MCP initialization
    recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate cites
    unknown/undelivered reference → no approved or ready candidate; explicit invalid-reference limitation or validation
    failure according to the public report contract.
  checks:
  - id: candidate.unknown
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Select provider boundary fixture case unknown, using the exact executable selection and real MCP initialization
      recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate cites
      unknown/undelivered reference → no approved or ready candidate; explicit invalid-reference limitation or validation
      failure according to the public report contract.
    expected: Select provider boundary fixture case unknown, using the exact executable selection and real MCP initialization
      recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Candidate cites
      unknown/undelivered reference → no approved or ready candidate; explicit invalid-reference limitation or validation
      failure according to the public report contract.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case unknown, using the
      exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Candidate cites unknown/undelivered reference → no approved or ready
      candidate; explicit invalid-reference limitation or validation failure according to the public report contract.'
    capture: evidence/coverage-boundary.candidate.unknown
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: candidate.unknown.restore
  phase: exercise
  action: Archive candidate.unknown result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: candidate.unknown.restore
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Archive candidate.unknown result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive candidate.unknown result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive candidate.unknown result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.candidate.unknown.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - candidate.unknown
    instruction: Archive candidate.unknown result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: model.malformed-json
  phase: exercise
  action: Select provider boundary fixture case malformed-json, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    External-provider fixture returns malformed JSON → execution error, no successful advisory report. Fault.
  checks:
  - id: model.malformed-json
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case malformed-json, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      External-provider fixture returns malformed JSON → execution error, no successful advisory report. Fault.
    expected: Select provider boundary fixture case malformed-json, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      External-provider fixture returns malformed JSON → execution error, no successful advisory report. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case malformed-json,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. External-provider fixture returns malformed JSON → execution
      error, no successful advisory report. Fault.'
    capture: evidence/coverage-boundary.model.malformed-json
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: model.malformed-json.restore
  phase: exercise
  action: Archive model.malformed-json result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: model.malformed-json.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive model.malformed-json result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive model.malformed-json result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive model.malformed-json result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.model.malformed-json.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - model.malformed-json
    instruction: Archive model.malformed-json result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: model.missing-fields
  phase: exercise
  action: Select provider boundary fixture case missing-fields, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Fixture returns missing required output array → execution error. Fault.
  checks:
  - id: model.missing-fields
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case missing-fields, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Fixture returns missing required output array → execution error. Fault.
    expected: Select provider boundary fixture case missing-fields, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Fixture returns missing required output array → execution error. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case missing-fields,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. Fixture returns missing required output array → execution
      error. Fault.'
    capture: evidence/coverage-boundary.model.missing-fields
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: model.missing-fields.restore
  phase: exercise
  action: Archive model.missing-fields result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: model.missing-fields.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive model.missing-fields result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive model.missing-fields result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive model.missing-fields result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.model.missing-fields.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - model.missing-fields
    instruction: Archive model.missing-fields result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: model.incomplete
  phase: exercise
  action: Select provider boundary fixture case incomplete, using the exact executable selection and real MCP initialization
    recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit. Fixture ends with
    incomplete model result → execution error. Fault.
  checks:
  - id: model.incomplete
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case incomplete, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Fixture ends with incomplete model result → execution error. Fault.
    expected: Select provider boundary fixture case incomplete, using the exact executable selection and real MCP
      initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Fixture ends with incomplete model result → execution error. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case incomplete, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Fixture ends with incomplete model result → execution error. Fault.'
    capture: evidence/coverage-boundary.model.incomplete
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: model.incomplete.restore
  phase: exercise
  action: Archive model.incomplete result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: model.incomplete.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive model.incomplete result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    expected: Archive model.incomplete result even if detection failed. Reverse only its owned injection using RUNBOOK.md
      reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
      exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
      when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
      fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive model.incomplete result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.model.incomplete.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - model.incomplete
    instruction: Archive model.incomplete result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: model.invented-reference
  phase: exercise
  action: Select provider boundary fixture case invented-reference, using the exact executable selection and real
    MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Fixture cites handle never delivered by real bound tool → no fabricated exact point binding; limitation/validation
    result preserved. Fault.
  checks:
  - id: model.invented-reference
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case invented-reference, using the exact executable selection and
      real MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses
      and audit. Fixture cites handle never delivered by real bound tool → no fabricated exact point binding; limitation/validation
      result preserved. Fault.
    expected: Select provider boundary fixture case invented-reference, using the exact executable selection and
      real MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses
      and audit. Fixture cites handle never delivered by real bound tool → no fabricated exact point binding; limitation/validation
      result preserved. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case invented-reference,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. Fixture cites handle never delivered by real bound tool →
      no fabricated exact point binding; limitation/validation result preserved. Fault.'
    capture: evidence/coverage-boundary.model.invented-reference
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: model.invented-reference.restore
  phase: exercise
  action: Archive model.invented-reference result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: model.invented-reference.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive model.invented-reference result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive model.invented-reference result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive model.invented-reference result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.model.invented-reference.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - model.invented-reference
    instruction: Archive model.invented-reference result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: model.context-exhausted
  phase: exercise
  action: Select provider boundary fixture case context-exhausted, using the exact executable selection and real
    MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    External-provider fixture returns the configured provider's context-exhaustion error → actionable failed report
    identifying boundary; no empty-success fallback. Fault.
  checks:
  - id: model.context-exhausted
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case context-exhausted, using the exact executable selection and
      real MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses
      and audit. External-provider fixture returns the configured provider's context-exhaustion error → actionable
      failed report identifying boundary; no empty-success fallback. Fault.
    expected: Select provider boundary fixture case context-exhausted, using the exact executable selection and
      real MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses
      and audit. External-provider fixture returns the configured provider's context-exhaustion error → actionable
      failed report identifying boundary; no empty-success fallback. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case context-exhausted,
      using the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success
      credit; archive actual tool responses and audit. External-provider fixture returns the configured provider''s
      context-exhaustion error → actionable failed report identifying boundary; no empty-success fallback. Fault.'
    capture: evidence/coverage-boundary.model.context-exhausted
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: model.context-exhausted.restore
  phase: exercise
  action: Archive model.context-exhausted result even if detection failed. Reverse only its owned injection using
    RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
    baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
    configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
    Mark any unreversed fault release-failed.
  checks:
  - id: model.context-exhausted.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive model.context-exhausted result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive model.context-exhausted result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive model.context-exhausted result even if detection
      failed. Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions,
      executable and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation
      in this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never
      lower thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.model.context-exhausted.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - model.context-exhausted
    instruction: Archive model.context-exhausted result even if detection failed. Reverse only its owned injection
      using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection.
      Restore baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence;
      use real configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: model.failure-preserves
  phase: exercise
  action: Observe the exact failed model.malformed-json call; launch no new provider call. Compare its captured
    before/after Criteria, source, approvals, Campaign and process ledger. During one previous failed Analyst call
    compare source, Campaign, Criteria and approved-waiver bytes → unchanged; no surviving child/tool process. Separate
    observation, not a substitute for each failure record.
  checks:
  - id: model.failure-preserves
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Observe the exact failed model.malformed-json call; launch no new provider call. Compare its captured
      before/after Criteria, source, approvals, Campaign and process ledger. During one previous failed Analyst
      call compare source, Campaign, Criteria and approved-waiver bytes → unchanged; no surviving child/tool process.
      Separate observation, not a substitute for each failure record.
    expected: Observe the exact failed model.malformed-json call; launch no new provider call. Compare its captured
      before/after Criteria, source, approvals, Campaign and process ledger. During one previous failed Analyst
      call compare source, Campaign, Criteria and approved-waiver bytes → unchanged; no surviving child/tool process.
      Separate observation, not a substitute for each failure record.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Observe the exact failed model.malformed-json call; launch
      no new provider call. Compare its captured before/after Criteria, source, approvals, Campaign and process
      ledger. During one previous failed Analyst call compare source, Campaign, Criteria and approved-waiver bytes
      → unchanged; no surviving child/tool process. Separate observation, not a substitute for each failure record.'
    capture: evidence/coverage-boundary.model.failure-preserves
  timeout_minutes: 4
  requires:
  - baseline.analysis
  - model.malformed-json
- id: query.invalid-limit
  phase: exercise
  action: Select provider boundary fixture case invalid-limit, using the exact executable selection and real MCP
    initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
    Send limit=100000 through the external provider boundary; public schema rejects it. This earns invalid-input
    credit only. Fault.
  checks:
  - id: query.invalid-limit
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Select provider boundary fixture case invalid-limit, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Send limit=100000 through the external provider boundary; public schema rejects it. This earns invalid-input
      credit only. Fault.
    expected: Select provider boundary fixture case invalid-limit, using the exact executable selection and real
      MCP initialization recipe in RUNBOOK.md. No live-model success credit; archive actual tool responses and audit.
      Send limit=100000 through the external provider boundary; public schema rejects it. This earns invalid-input
      credit only. Fault.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Select provider boundary fixture case invalid-limit, using
      the exact executable selection and real MCP initialization recipe in RUNBOOK.md. No live-model success credit;
      archive actual tool responses and audit. Send limit=100000 through the external provider boundary; public
      schema rejects it. This earns invalid-input credit only. Fault.'
    capture: evidence/coverage-boundary.query.invalid-limit
  timeout_minutes: 4
  requires:
  - baseline.analysis
- id: query.invalid-limit.restore
  phase: exercise
  action: Archive query.invalid-limit result even if detection failed. Reverse only its owned injection using RUNBOOK.md
    reversal recipe; compare original hashes, permissions, executable and environment selection. Restore baseline.analysis
    exact inputs and rerun its public valid operation in this run with fresh evidence; use real configured Analyst
    when the failed operation was analysis. Never lower thresholds or fabricate approval/audit. Mark any unreversed
    fault release-failed.
  checks:
  - id: query.invalid-limit.restore
    capabilities:
    - COVERAGE-ANALYST-EVIDENCE-BOUNDARY
    stimulus: Archive query.invalid-limit result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    expected: Archive query.invalid-limit result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/internals/MCP-TOOLS.md#report-driven-coverage-analyst
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Archive query.invalid-limit result even if detection failed.
      Reverse only its owned injection using RUNBOOK.md reversal recipe; compare original hashes, permissions, executable
      and environment selection. Restore baseline.analysis exact inputs and rerun its public valid operation in
      this run with fresh evidence; use real configured Analyst when the failed operation was analysis. Never lower
      thresholds or fabricate approval/audit. Mark any unreversed fault release-failed.'
    capture: evidence/coverage-boundary.query.invalid-limit.restore
  timeout_minutes: 4
  requires:
  - baseline.analysis
  recovery:
    baseline: baseline.analysis
    detection:
    - query.invalid-limit
    instruction: Archive query.invalid-limit result even if detection failed. Reverse only its owned injection using
      RUNBOOK.md reversal recipe; compare original hashes, permissions, executable and environment selection. Restore
      baseline.analysis exact inputs and rerun its public valid operation in this run with fresh evidence; use real
      configured Analyst when the failed operation was analysis. Never lower thresholds or fabricate approval/audit.
      Mark any unreversed fault release-failed.
- id: final.regression
  phase: final
  action: With all injections disabled rerun native numeric half and four-property half cases, current accepted
    Ticket suite where applicable; compare fixed hit sets and independent verdicts. Preserve earlier failures.
  checks:
  - id: final.regression
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: With all injections disabled rerun native numeric half and four-property half cases, current accepted
      Ticket suite where applicable; compare fixed hit sets and independent verdicts. Preserve earlier failures.
    expected: With all injections disabled rerun native numeric half and four-property half cases, current accepted
      Ticket suite where applicable; compare fixed hit sets and independent verdicts. Preserve earlier failures.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: With all injections disabled rerun native numeric half
      and four-property half cases, current accepted Ticket suite where applicable; compare fixed hit sets and independent
      verdicts. Preserve earlier failures.'
    capture: evidence/coverage-boundary.final.regression
  timeout_minutes: 15
  requires:
  - setup.ready
- id: final.analysis
  phase: final
  action: Run the real configured Analyst on the exact fresh final.regression Campaign. Verify valid advisory report,
    correct closure and byte-identical source, Criteria and approvals.
  checks:
  - id: final.analysis
    capabilities:
    - SPECIALIST-COVERAGE-ANALYSIS
    stimulus: Run the real configured Analyst on the exact fresh final.regression Campaign. Verify valid advisory
      report, correct closure and byte-identical source, Criteria and approvals.
    expected: Run the real configured Analyst on the exact fresh final.regression Campaign. Verify valid advisory
      report, correct closure and byte-identical source, Criteria and approvals.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Run the real configured Analyst on the exact fresh final.regression
      Campaign. Verify valid advisory report, correct closure and byte-identical source, Criteria and approvals.'
    capture: evidence/coverage-boundary.final.analysis
  timeout_minutes: 4
  requires:
  - final.regression
- id: final.archive
  phase: final
  action: Verify every selected Check has immutable, hash-indexed evidence outside mutable/pruned state; record
    failures, blocked work and restoration separately.
  checks:
  - id: final.archive
    capabilities:
    - SIMULATION-NATIVE-COVERAGE
    stimulus: Verify every selected Check has immutable, hash-indexed evidence outside mutable/pruned state; record
      failures, blocked work and restoration separately.
    expected: Verify every selected Check has immutable, hash-indexed evidence outside mutable/pruned state; record
      failures, blocked work and restoration separately.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/docs/user/FLOW_REFERENCE.md#native-coverage-campaigns
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Verify every selected Check has immutable, hash-indexed
      evidence outside mutable/pruned state; record failures, blocked work and restoration separately.'
    capture: evidence/coverage-boundary.final.archive
  timeout_minutes: 5
- id: cleanup.processes
  phase: cleanup
  action: Stop and reap owned model clients, MCP servers, simulators and controller process groups; prove no descendants
    or fault handles remain.
  checks:
  - id: cleanup.processes
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Stop and reap owned model clients, MCP servers, simulators and controller process groups; prove no
      descendants or fault handles remain.
    expected: Stop and reap owned model clients, MCP servers, simulators and controller process groups; prove no
      descendants or fault handles remain.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Stop and reap owned model clients, MCP servers, simulators
      and controller process groups; prove no descendants or fault handles remain.'
    capture: evidence/coverage-boundary.cleanup.processes
  timeout_minutes: 5
- id: cleanup.runtime
  phase: cleanup
  action: Release owned Session Runtimes, attachments and Project inventory entries through product routes; preserve
    borrowed entries.
  checks:
  - id: cleanup.runtime
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Release owned Session Runtimes, attachments and Project inventory entries through product routes;
      preserve borrowed entries.
    expected: Release owned Session Runtimes, attachments and Project inventory entries through product routes;
      preserve borrowed entries.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Release owned Session Runtimes, attachments and Project
      inventory entries through product routes; preserve borrowed entries.'
    capture: evidence/coverage-boundary.cleanup.runtime
  timeout_minutes: 5
- id: cleanup.faults
  phase: cleanup
  action: Remove owned temporary executable wrappers, redirection variables, library injection, altered permissions
    and mounts; borrowed executable/credential bytes unchanged.
  checks:
  - id: cleanup.faults
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Remove owned temporary executable wrappers, redirection variables, library injection, altered permissions
      and mounts; borrowed executable/credential bytes unchanged.
    expected: Remove owned temporary executable wrappers, redirection variables, library injection, altered permissions
      and mounts; borrowed executable/credential bytes unchanged.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Remove owned temporary executable wrappers, redirection
      variables, library injection, altered permissions and mounts; borrowed executable/credential bytes unchanged.'
    capture: evidence/coverage-boundary.cleanup.faults
  timeout_minutes: 5
- id: cleanup.git
  phase: cleanup
  action: Release or retain only eligible inert owned Project/Ticket/worktree/branch state with exact identity,
    review owner, bounded location and deletion instructions.
  checks:
  - id: cleanup.git
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Release or retain only eligible inert owned Project/Ticket/worktree/branch state with exact identity,
      review owner, bounded location and deletion instructions.
    expected: Release or retain only eligible inert owned Project/Ticket/worktree/branch state with exact identity,
      review owner, bounded location and deletion instructions.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Release or retain only eligible inert owned Project/Ticket/worktree/branch
      state with exact identity, review owner, bounded location and deletion instructions.'
    capture: evidence/coverage-boundary.cleanup.git
  timeout_minutes: 5
- id: cleanup.storage
  phase: cleanup
  action: Release owned ephemeral volumes and scarce resources; preserve shared images/caches and archive records;
    eligible inert review state has an explicit ledger disposition.
  checks:
  - id: cleanup.storage
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Release owned ephemeral volumes and scarce resources; preserve shared images/caches and archive records;
      eligible inert review state has an explicit ledger disposition.
    expected: Release owned ephemeral volumes and scarce resources; preserve shared images/caches and archive records;
      eligible inert review state has an explicit ledger disposition.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Release owned ephemeral volumes and scarce resources;
      preserve shared images/caches and archive records; eligible inert review state has an explicit ledger disposition.'
    capture: evidence/coverage-boundary.cleanup.storage
  timeout_minutes: 5
- id: cleanup.records
  phase: cleanup
  action: Record every resource disposition and every selected Check result. Required cleanup failure prevents pass;
    unchanged remotes, no external publication. Deadline means incomplete missing work, never inferred success.
  checks:
  - id: cleanup.records
    capabilities:
    - SESSION-RUNTIME-LIFECYCLE
    - SECURITY-RUNTIME-ISOLATION
    stimulus: Record every resource disposition and every selected Check result. Required cleanup failure prevents
      pass; unchanged remotes, no external publication. Deadline means incomplete missing work, never inferred success.
    expected: Record every resource disposition and every selected Check result. Required cleanup failure prevents
      pass; unchanged remotes, no external publication. Deadline means incomplete missing work, never inferred success.
    authority_ref: https://github.com/boldaxolotl/booley/blob/main/qa/doc/FINISH.md
    evidence: 'Exact CLI argv/MCP request, exit/envelope, timestamps, Target/tests and source/build/runtime/model
      identities; hashes of native per-run data, Campaign/Simulation and relevant Criteria snapshots. For Analyst
      retain actual transcript, bounded tool responses and audit. Independent oracle compares raw native data with
      normalized points and fixed expected operands; source rollups only for line/branch/expression/toggle, overall
      rollups for cover_property. For faults preserve the concrete mutation or consumed syscall/provider handshake
      before restoration. Specific result to demonstrate: Record every resource disposition and every selected Check
      result. Required cleanup failure prevents pass; unchanged remotes, no external publication. Deadline means
      incomplete missing work, never inferred success.'
    capture: evidence/coverage-boundary.cleanup.records
  timeout_minutes: 5
